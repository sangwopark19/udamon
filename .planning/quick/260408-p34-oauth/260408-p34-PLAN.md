---
phase: quick-260408-p34
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/contexts/AuthContext.tsx
autonomous: true
must_haves:
  truths:
    - "Android Kakao OAuth callback 후 메인화면으로 정상 진입한다"
    - "public.users 행이 없는 OAuth 사용자도 자동으로 프로필이 생성된다"
    - "기존 테스트 계정 및 이메일 로그인 동작이 영향받지 않는다"
  artifacts:
    - path: "app/src/contexts/AuthContext.tsx"
      provides: "ensureUserProfile fallback + enhanced OAuth logging"
  key_links:
    - from: "onAuthStateChange"
      to: "ensureUserProfile"
      via: "fetchUserProfile null 시 upsert fallback"
      pattern: "ensureUserProfile"
---

<objective>
Android 카카오 로그인 OAuth 콜백 후 메인화면 미진입 버그 수정

Root cause: OAuth 인증 성공 후 onAuthStateChange에서 fetchUserProfile(session.user)가 public.users 행을 찾지 못해 null 반환 -> setUser(null) -> isAuthenticated=false -> 로그인 화면 유지. handle_new_user 트리거와의 race condition 또는 트리거 실패가 원인.

Purpose: OAuth 로그인 후 public.users 행이 없으면 클라이언트에서 user_metadata로 자동 생성(upsert)하여 안정적으로 프로필을 확보
Output: 수정된 AuthContext.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/src/contexts/AuthContext.tsx
@supabase/migrations/011_users_table.sql

Key interfaces already in AuthContext.tsx:
```typescript
export interface UserProfile {
  id: string;
  email: string | null;
  username: string | null;
  display_name: string | null;
  avatar_url: string | null;
  bio: string | null;
  is_photographer: boolean;
  is_admin: boolean;
  admin_role: AdminRole | null;
  ticket_balance: number;
  my_team_id: string | null;
  created_at: string;
}
```

DB trigger (011_users_table.sql) inserts columns: id, email, nickname, username, display_name, avatar_url, role.
App's UserProfile expects: id, email, username, display_name, avatar_url, bio, is_photographer, is_admin, admin_role, ticket_balance, my_team_id, created_at.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ensureUserProfile fallback and enhance OAuth logging</name>
  <files>app/src/contexts/AuthContext.tsx</files>
  <action>
In AuthContext.tsx, make these changes:

1. Create a new helper function `ensureUserProfile` right after `fetchUserProfile`:

```typescript
const ensureUserProfile = async (authUser: User): Promise<UserProfile | null> => {
  // 1차 시도: 기존 프로필 조회
  const existing = await fetchUserProfile(authUser);
  if (existing) return existing;

  console.log('[Auth] Profile not found, creating from user_metadata:', authUser.id);

  // 2차: user_metadata에서 프로필 upsert
  const meta = authUser.user_metadata ?? {};
  const { error: upsertError } = await supabase
    .from('users')
    .upsert({
      id: authUser.id,
      email: authUser.email ?? null,
      nickname: meta.preferred_username ?? meta.user_name ?? meta.name ?? (authUser.email ? authUser.email.split('@')[0] : 'user_' + authUser.id.replace(/-/g, '').slice(0, 8)),
      username: meta.preferred_username ?? meta.user_name ?? (authUser.email ? authUser.email.split('@')[0] : null),
      display_name: meta.full_name ?? meta.name ?? (authUser.email ? authUser.email.split('@')[0] : null),
      avatar_url: meta.avatar_url ?? null,
      role: 'user',
    }, { onConflict: 'id' });

  if (upsertError) {
    console.error('[Auth] ensureUserProfile upsert error:', upsertError);
    return null;
  }

  // 3차: upsert 성공 후 재조회 (DB default 값 포함된 완전한 행)
  const profile = await fetchUserProfile(authUser);
  if (!profile) {
    console.error('[Auth] Profile still null after upsert for user:', authUser.id);
  }
  return profile;
};
```

2. In the `init` useEffect (line ~192-199), replace `fetchUserProfile` call with `ensureUserProfile`:
   - Before: `const profile = await fetchUserProfile(session.user);`
   - After: `const profile = await ensureUserProfile(session.user);`

3. In the `onAuthStateChange` callback (line ~207-210), replace `fetchUserProfile` call with `ensureUserProfile`:
   - Before: `const profile = await fetchUserProfile(session.user);`
   - After: `const profile = await ensureUserProfile(session.user);`
   - Add log right before: `console.log('[Auth] onAuthStateChange event:', _event, 'user:', session.user.id);`

4. In `extractAndSetSession`, add a log after successful setSession/exchangeCode (lines ~156-165):
   - After code exchange success: `console.log('[OAuth] Code exchange success, waiting for onAuthStateChange');`
   - After setSession success: `console.log('[OAuth] setSession success, waiting for onAuthStateChange');`

5. In `refreshUser` callback (line ~349-352), also replace with `ensureUserProfile`:
   - Before: `const profile = await fetchUserProfile(session.user);`
   - After: `const profile = await ensureUserProfile(session.user);`

IMPORTANT: Do NOT change the `fetchUserProfile` function itself -- it stays as the pure SELECT query. The new `ensureUserProfile` wraps it with fallback logic.

IMPORTANT: The upsert columns (nickname, role) must match what the DB trigger uses in migration 011. The UserProfile interface includes fields like bio, is_photographer, is_admin, ticket_balance, my_team_id, created_at which have DB defaults -- don't include them in the upsert, let DB defaults apply.

IMPORTANT: Keep all existing test account logic, loginProvider logic, and guestMode logic untouched.
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/workspace/udamon/app && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - ensureUserProfile 함수가 AuthContext.tsx에 존재하며, fetchUserProfile null 시 supabase upsert -> 재조회 fallback 수행
    - onAuthStateChange, init, refreshUser 모두 ensureUserProfile 사용
    - OAuth 흐름에 진단 로그 추가 ([Auth], [OAuth] prefix)
    - TypeScript 컴파일 에러 없음
    - 기존 테스트 계정/이메일 로그인 동작 불변
  </done>
</task>

</tasks>

<verification>
1. `cd app && npx tsc --noEmit` -- TypeScript 컴파일 에러 없음
2. grep으로 ensureUserProfile 호출 확인: onAuthStateChange, init, refreshUser 3곳
3. grep으로 fetchUserProfile 직접 호출이 ensureUserProfile 내부에서만 존재 확인
</verification>

<success_criteria>
- Android 카카오 OAuth 로그인 후 public.users 행이 없어도 클라이언트가 자동 생성하여 메인화면 진입
- 기존 handle_new_user 트리거가 정상 작동하는 경우 upsert는 onConflict로 무해하게 처리
- 모든 OAuth/Auth 흐름에 진단 로그 추가로 향후 디버깅 용이
</success_criteria>

<output>
After completion, create `.planning/quick/260408-p34-oauth/260408-p34-SUMMARY.md`
</output>
