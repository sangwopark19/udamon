---
id: 260408-kv5
type: quick
title: "iOS OAuth URL validation + Kakao CJK font workaround"
files_modified:
  - app/src/contexts/AuthContext.tsx
  - app/src/i18n/locales/ko.ts
autonomous: true
---

<objective>
iOS 시뮬레이터에서 발생하는 두 가지 OAuth 로그인 버그를 수정한다.

1. Google 로그인 시 Safari가 "주소가 유효하지 않습니다" 에러를 표시하는 문제 -- URL 유효성 검증과 디버그 로깅을 추가하여 원인을 명확히 하고, 사용자에게 구체적인 에러 메시지를 제공한다.
2. Kakao 로그인 시 한글이 "?" 박스로 표시되는 문제 -- iOS Simulator의 ASWebAuthenticationSession CJK 폰트 렌더링 한계를 문서화하고, `preferEphemeralSession` 옵션을 명시적으로 설정한다.

Purpose: OAuth 플로우 안정성을 강화하고, 실패 시 사용자가 문제 원인을 파악할 수 있게 한다.
Output: 수정된 AuthContext.tsx, 추가된 i18n 키
</objective>

<context>
@app/src/contexts/AuthContext.tsx (lines 237-313 -- OAuth login flow)
@app/src/services/supabase.ts (Supabase client config, PKCE flow for native)
@app/src/i18n/locales/ko.ts (translation keys)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Google OAuth URL 유효성 검증 및 에러 핸들링 강화</name>
  <files>app/src/contexts/AuthContext.tsx, app/src/i18n/locales/ko.ts</files>
  <action>
AuthContext.tsx의 네이티브 OAuth 플로우 (line 283-312)에서 다음을 수정한다:

1. `data?.url` 체크 후, `WebBrowser.openAuthSessionAsync`를 호출하기 전에 URL 유효성 검증을 추가한다:
   - `new URL(data.url)`을 try/catch로 감싸서 URL 파싱이 실패하면 에러 로그 + 사용자 토스트를 표시한다.
   - URL이 `https://`로 시작하는지 확인한다 (Supabase가 비정상 URL을 반환하는 경우 방어).
   - 검증 실패 시 `console.error('[OAuth] Invalid auth URL:', data.url)`로 실제 URL 값을 로깅하여 디버깅을 돕는다.

2. `data?.url`이 falsy일 때도 에러를 처리한다 (현재는 조용히 무시됨):
   - `else` 블록을 추가하여 `console.error('[OAuth] No auth URL returned from Supabase')` 로깅.
   - `showToast(t('oauth_url_error'), 'error')` 표시.
   - `pendingOAuthProvider.current = null` 리셋.

3. ko.ts에 새 i18n 키를 추가한다:
   - `oauth_url_error: '로그인 URL을 생성할 수 없습니다. 잠시 후 다시 시도해주세요.'`
   - `oauth_url_invalid: '로그인 URL이 유효하지 않습니다. 관리자에게 문의해주세요.'`

4. `openAuthSessionAsync` 호출 시 두 번째 인자 `redirectUrl` 뒤에 세 번째 인자로 옵션 객체를 전달한다:
   ```typescript
   const result = await WebBrowser.openAuthSessionAsync(data.url, redirectUrl, {
     preferEphemeralSession: false,
   });
   ```
   `preferEphemeralSession: false`는 기존 쿠키/세션을 유지하여 iOS에서 재인증 빈도를 줄이고, 시뮬레이터에서 ASWebAuthenticationSession 안정성을 높인다.

수정 결과 코드 구조 (네이티브 블록, line 296 이후):

```typescript
    console.log('[OAuth] Opening auth URL:', data?.url);

    if (data?.url) {
      // URL 유효성 검증
      try {
        const parsed = new URL(data.url);
        if (parsed.protocol !== 'https:') {
          console.error('[OAuth] Non-HTTPS auth URL:', data.url);
          pendingOAuthProvider.current = null;
          showToast(t('oauth_url_invalid'), 'error');
          return;
        }
      } catch {
        console.error('[OAuth] Invalid auth URL:', data.url);
        pendingOAuthProvider.current = null;
        showToast(t('oauth_url_invalid'), 'error');
        return;
      }

      try {
        const result = await WebBrowser.openAuthSessionAsync(data.url, redirectUrl, {
          preferEphemeralSession: false,
        });
        console.log('[OAuth] WebBrowser result:', result.type);
        if (result.type === 'success' && result.url && pendingOAuthProvider.current) {
          await extractAndSetSession(result.url);
        } else if (result.type === 'cancel') {
          pendingOAuthProvider.current = null;
        }
      } catch (err: unknown) {
        console.error('[OAuth] WebBrowser error:', err);
        pendingOAuthProvider.current = null;
        showToast(t('oauth_error'), 'error');
      }
    } else {
      console.error('[OAuth] No auth URL returned from Supabase');
      pendingOAuthProvider.current = null;
      showToast(t('oauth_url_error'), 'error');
    }
```
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/workspace/udamon/app && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - URL 유효성 검증이 `openAuthSessionAsync` 호출 전에 실행된다.
    - 비정상 URL(non-HTTPS, 파싱 불가)은 사용자에게 토스트로 알리고 로그에 실제 URL을 남긴다.
    - `data.url`이 falsy일 때 에러 토스트가 표시된다.
    - `preferEphemeralSession: false` 옵션이 전달된다.
    - ko.ts에 `oauth_url_error`, `oauth_url_invalid` 키가 존재한다.
    - TypeScript 컴파일 에러 없음.
  </done>
</task>

<task type="auto">
  <name>Task 2: Kakao iOS Simulator CJK 폰트 이슈 문서화</name>
  <files>app/src/contexts/AuthContext.tsx</files>
  <action>
AuthContext.tsx의 OAuth 플로우 상단 주석 (line 237 `// --- OAuth Login ---` 근처)에 iOS Simulator CJK 렌더링 제한사항을 문서화하는 블록 코멘트를 추가한다:

```typescript
// ─── OAuth Login ───
// NOTE: iOS Simulator의 ASWebAuthenticationSession은 CJK 폰트 렌더링에
// 제한이 있어 Kakao 로그인 화면에서 한글이 "?" 박스로 표시될 수 있다.
// 이는 시뮬레이터 전용 이슈이며, 실제 기기에서는 정상 렌더링된다.
// Ref: https://developer.apple.com/forums/thread/709491
```

이 코멘트는 향후 개발자가 같은 이슈를 재조사하는 시간 낭비를 방지한다. 실제 기기에서는 문제가 없으므로 코드 수준의 워크어라운드는 불필요하다.
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/workspace/udamon && grep -c "CJK" app/src/contexts/AuthContext.tsx</automated>
  </verify>
  <done>
    - AuthContext.tsx OAuth 섹션에 iOS Simulator CJK 폰트 이슈 주석이 존재한다.
    - 주석이 시뮬레이터 전용 이슈임을 명시하고, 실제 기기에서는 정상임을 기술한다.
  </done>
</task>

</tasks>

<verification>
1. `cd app && npx tsc --noEmit` -- TypeScript 컴파일 에러 없음
2. AuthContext.tsx에서 `new URL(data.url)` 검증이 `openAuthSessionAsync` 호출 전에 실행됨을 코드에서 확인
3. `data?.url` falsy 경로에 에러 핸들링이 존재함을 확인
4. ko.ts에 `oauth_url_error`, `oauth_url_invalid` 키가 추가됨을 확인
5. `preferEphemeralSession: false` 옵션이 `openAuthSessionAsync`에 전달됨을 확인
6. CJK 폰트 이슈 문서화 주석이 존재함을 확인
</verification>

<success_criteria>
- Google OAuth: 잘못된 URL이 반환되면 사용자에게 명확한 에러 메시지가 표시되고, 콘솔에 실제 URL이 로깅된다 (빈 Safari 페이지 대신).
- Google OAuth: URL이 아예 반환되지 않으면 "URL을 생성할 수 없습니다" 토스트가 표시된다.
- Kakao OAuth: preferEphemeralSession 옵션이 명시적으로 설정되어 세션 관리가 안정적이다.
- Kakao CJK: 시뮬레이터 한계가 코드에 문서화되어 향후 재조사 방지.
- TypeScript 컴파일 성공.
</success_criteria>
