---
phase: 01-database-foundation-security
plan: 03
type: execute
wave: 2
depends_on:
  - 01
  - 02
files_modified:
  - app/src/contexts/AuthContext.tsx
  - app/src/services/supabase.ts
  - app/.env.example
  - admin/src/contexts/AuthContext.tsx
  - admin/.env.example
  - supabase/functions/get-upload-url/index.ts
autonomous: false
requirements:
  - SEC-01
  - SEC-02
  - SEC-03
  - SEC-04
  - SEC-05

must_haves:
  truths:
    - "코드베이스에 test@udamon.com, test2@udamon.com, admin@udamon.com 하드코딩이 없다"
    - "코드베이스에 DUMMY_URL, DUMMY_KEY, isSupabaseConfigured가 없다"
    - "앱이 .env 없이 실행 시 명확한 에러 메시지를 표시한다"
    - "admin/src에 하드코딩된 비밀번호('admin1234')가 없다"
    - "app/src/contexts/AuthContext.tsx에 console.log가 없다"
    - "Edge Function에 Access-Control-Allow-Origin: * 가 없다"
  artifacts:
    - path: "app/src/contexts/AuthContext.tsx"
      provides: "테스트 계정 제거 + console.log 제거된 AuthContext"
      min_lines: 200
    - path: "app/src/services/supabase.ts"
      provides: "환경변수 필수 검증 + 더미 키 제거된 supabase 클라이언트"
      min_lines: 10
    - path: "app/.env.example"
      provides: "필수 환경변수 목록 문서화"
      contains: "EXPO_PUBLIC_SUPABASE_URL"
    - path: "admin/src/contexts/AuthContext.tsx"
      provides: "하드코딩 비밀번호 제거된 어드민 AuthContext"
      min_lines: 50
    - path: "admin/.env.example"
      provides: "어드민 필수 환경변수 목록"
      contains: "VITE_ADMIN_PASSWORD"
    - path: "supabase/functions/get-upload-url/index.ts"
      provides: "CORS origin 제한된 Edge Function"
      min_lines: 100
  key_links:
    - from: "app/src/services/supabase.ts"
      to: "process.env"
      via: "필수 환경변수 검증 후 throw"
      pattern: "throw new Error.*Missing required"
    - from: "admin/src/contexts/AuthContext.tsx"
      to: "import.meta.env"
      via: "VITE_ADMIN_PASSWORD 환경변수 참조"
      pattern: "import.meta.env.VITE_ADMIN_PASSWORD"
    - from: "supabase/functions/get-upload-url/index.ts"
      to: "ALLOWED_ORIGINS"
      via: "조건부 CORS origin 검증"
      pattern: "ALLOWED_ORIGINS"
---

<objective>
프로덕션 보안 취약점 제거: 테스트 계정 완전 삭제, console.log 제거, 환경변수 정리, 어드민 하드코딩 비밀번호 제거, CORS origin 제한.

Purpose: 코드베이스에서 하드코딩된 인증정보와 디버그 출력을 제거하여 프로덕션 배포 준비를 완성한다.
Output: 수정된 앱 코드 파일 6개
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-database-foundation-security/01-CONTEXT.md
@.planning/phases/01-database-foundation-security/01-RESEARCH.md

<interfaces>
<!-- 현재 파일 구조 -- 수정 대상 -->

From app/src/contexts/AuthContext.tsx:
```typescript
// lines 55-112: TEST_ACCOUNTS 객체 (3개 테스트 계정 하드코딩)
const TEST_ACCOUNTS: Record<string, { password: string; profile: UserProfile; isPhotographer: boolean }> = {
  'test@udamon.com': { password: 'test1234', ... },
  'test2@udamon.com': { password: 'test1234', ... },
  'admin@udamon.com': { password: 'admin1234', ... },
};

// line 141: console.log('[OAuth] extractAndSetSession URL:', url);
// line 153: console.log('[OAuth] code:', code, ...);
// line 224: console.log('[Deep Link] URL received:', url, ...);

// line 55: const TEST_ACCOUNT_KEY = 'udamon_test_account';
// lines 176-188: init()에서 AsyncStorage TEST_ACCOUNT_KEY 복원
// lines 277-288: loginWithEmail에서 TEST_ACCOUNTS 바이패스
// lines 311-312: logout에서 TEST_ACCOUNT_KEY 제거
// lines 321: updateUserProfile에서 test account 분기
// lines 213-218: onAuthStateChange에서 test account 분기
```

From app/src/services/supabase.ts:
```typescript
export const isSupabaseConfigured = Boolean(SUPABASE_URL && SUPABASE_ANON_KEY);
const DUMMY_URL = 'https://localhost.invalid';
const DUMMY_KEY = 'eyJhbGci...';
```

From admin/src/contexts/AuthContext.tsx:
```typescript
const ADMIN_ACCOUNTS: Record<string, { password: string; user: AdminUser }> = {
  'admin@udamon.com': {
    password: 'admin1234',
    ...
  },
};
```

From supabase/functions/get-upload-url/index.ts:
```typescript
const corsHeaders = {
  "Access-Control-Allow-Origin": "*",
  ...
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 테스트 계정 제거 + console.log 제거 + 환경변수 정리 (SEC-01, SEC-02, SEC-04)</name>
  <files>
    app/src/contexts/AuthContext.tsx,
    app/src/services/supabase.ts,
    app/.env.example
  </files>
  <read_first>
    - app/src/contexts/AuthContext.tsx (전체 파일 -- 테스트 계정 참조 위치 파악)
    - app/src/services/supabase.ts (전체 파일 -- 더미 키 구조 파악)
  </read_first>
  <action>
**Part A: app/src/services/supabase.ts 완전 교체 (per D-06, SEC-02)**

기존 파일을 다음으로 교체한다:

```typescript
import { createClient } from '@supabase/supabase-js';

const SUPABASE_URL = process.env.EXPO_PUBLIC_SUPABASE_URL;
const SUPABASE_ANON_KEY = process.env.EXPO_PUBLIC_SUPABASE_ANON_KEY;

if (!SUPABASE_URL || !SUPABASE_ANON_KEY) {
  throw new Error(
    'Missing required environment variables: EXPO_PUBLIC_SUPABASE_URL and EXPO_PUBLIC_SUPABASE_ANON_KEY. ' +
    'Copy app/.env.example to app/.env and fill in your Supabase project credentials.'
  );
}

export const supabase = createClient(SUPABASE_URL, SUPABASE_ANON_KEY, {
  auth: {
    autoRefreshToken: true,
    persistSession: true,
    detectSessionInUrl: false,
  },
});
```

제거 대상:
- `isSupabaseConfigured` export 제거
- `DUMMY_URL`, `DUMMY_KEY` 상수 제거
- `createClient`의 조건부 URL/키 로직 제거

**Part B: app/src/contexts/AuthContext.tsx에서 테스트 계정 완전 제거 (per D-04, SEC-01)**

다음 코드 블록을 제거한다:

1. **line 55**: `const TEST_ACCOUNT_KEY = 'udamon_test_account';` 삭제
2. **lines 57-112**: `const TEST_ACCOUNTS: Record<...> = { ... };` 전체 객체 삭제
3. **lines 176-188 (init 함수 내)**: `const savedEmail = await AsyncStorage.getItem(TEST_ACCOUNT_KEY);` 블록 전체 삭제 (try~catch 내부의 test account 복원 로직)
4. **lines 277-288 (loginWithEmail 함수 내)**: `const testAcct = TEST_ACCOUNTS[email];` 분기 블록 전체 삭제
5. **line 312**: `await AsyncStorage.removeItem(TEST_ACCOUNT_KEY).catch(() => {});` 삭제
6. **lines 213-218 (onAuthStateChange 콜백 내)**: test account 확인하는 `const savedEmail = await AsyncStorage.getItem(TEST_ACCOUNT_KEY).catch(...)` 블록을 단순화:
   ```typescript
   } else {
     setUser(null);
     setIsPhotographer(false);
     setPhotographerId(null);
   }
   ```
7. **lines 320-322 (updateUserProfile 함수 내)**: `const isTestAccount = user.id.startsWith('test-user-');` 분기 제거. 항상 Supabase 호출:
   ```typescript
   const updateUserProfile = useCallback(async (updates: Partial<UserProfile>) => {
     if (!user) return;
     const { error } = await supabase.from('users').update(updates).eq('id', user.id);
     if (error) { console.error('updateUserProfile error:', error); return; }
     setUser((prev) => (prev ? { ...prev, ...updates } : prev));
   }, [user]);
   ```

`AsyncStorage` import는 유지한다 (Supabase auth가 사용할 수 있음). `TEST_ACCOUNT_KEY` 관련 import가 유일한 용도였다면, AsyncStorage가 다른 곳에서도 사용되는지 확인 후 유지/제거 결정.

**Part C: console.log 제거 (per D-05, SEC-04)**

`app/src/contexts/AuthContext.tsx`에서 console.log 3곳을 제거한다:

1. **line 141**: `console.log('[OAuth] extractAndSetSession URL:', url);` 삭제
2. **line 153**: `console.log('[OAuth] code:', code, 'accessToken:', !!accessToken, 'refreshToken:', !!refreshToken);` 삭제
3. **line 224**: `console.log('[Deep Link] URL received:', url, 'pendingOAuth:', pendingOAuthProvider.current);` 삭제

console.error, console.warn은 유지한다 (에러 핸들링 용도). 단, OAuth URL/토큰 정보를 출력하는 부분은 보안 관점에서 민감하므로 console.log만 제거하는 것으로 충분. D-05에서 "console.log 전량 제거"로 명시.

**Part D: app/.env.example 생성**

```
# Supabase Project Credentials
# Get these from: https://supabase.com/dashboard/project/_/settings/api
EXPO_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
```

**Part E: isSupabaseConfigured 참조 정리**

코드베이스에서 `isSupabaseConfigured`를 import하는 파일이 있는지 grep으로 확인한다. 참조가 있으면 해당 import를 제거하고 조건 분기를 단순화한다 (Supabase가 항상 설정되어 있다고 가정).
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/workspace/udamon && echo "=== SEC-01: test accounts ===" && grep -c "test@udamon\|test2@udamon\|TEST_ACCOUNTS\|TEST_ACCOUNT_KEY\|test-user-" app/src/contexts/AuthContext.tsx || echo "0 (CLEAN)" && echo "=== SEC-02: dummy keys ===" && grep -c "DUMMY_URL\|DUMMY_KEY\|isSupabaseConfigured" app/src/services/supabase.ts || echo "0 (CLEAN)" && echo "=== SEC-04: console.log ===" && grep -c "console\.log" app/src/contexts/AuthContext.tsx || echo "0 (CLEAN)" && echo "=== .env.example ===" && test -f app/.env.example && echo "EXISTS" || echo "MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "test@udamon" app/src/` 결과가 0건이다
    - `grep -r "TEST_ACCOUNTS" app/src/` 결과가 0건이다
    - `grep -r "TEST_ACCOUNT_KEY" app/src/` 결과가 0건이다
    - `grep -r "test-user-" app/src/` 결과가 0건이다
    - `grep "DUMMY_URL\|DUMMY_KEY" app/src/services/supabase.ts` 결과가 0건이다
    - `grep "isSupabaseConfigured" app/src/services/supabase.ts` 결과가 0건이다
    - `grep "console\.log" app/src/contexts/AuthContext.tsx` 결과가 0건이다
    - app/src/services/supabase.ts에 `throw new Error` 포함 (환경변수 미설정 시)
    - app/src/services/supabase.ts에 `EXPO_PUBLIC_SUPABASE_URL` 포함
    - app/.env.example 파일이 존재하고 `EXPO_PUBLIC_SUPABASE_URL` 포함
    - AuthContext.tsx가 여전히 `AuthProvider`, `useAuth` export 유지
    - AuthContext.tsx의 loginWithEmail에서 `supabase.auth.signInWithPassword` 직접 호출
  </acceptance_criteria>
  <done>테스트 계정 3개 완전 삭제, console.log 3곳 제거, 더미 키/fallback 제거, 환경변수 필수 검증 적용, .env.example 생성 완료</done>
</task>

<task type="auto">
  <name>Task 2: 어드민 하드코딩 비밀번호 제거 + CORS origin 제한 (SEC-03, SEC-05)</name>
  <files>
    admin/src/contexts/AuthContext.tsx,
    admin/.env.example,
    supabase/functions/get-upload-url/index.ts
  </files>
  <read_first>
    - admin/src/contexts/AuthContext.tsx (전체 파일 -- 하드코딩 비밀번호 위치)
    - supabase/functions/get-upload-url/index.ts (전체 파일 -- CORS 헤더 위치)
    - .planning/phases/01-database-foundation-security/01-RESEARCH.md (Pattern 4: CORS origin 제한, Pattern 5: 환경변수 검증)
  </read_first>
  <action>
**Part A: admin/src/contexts/AuthContext.tsx 수정 (per SEC-03)**

하드코딩된 비밀번호를 환경변수로 이동한다. Phase 5(ADM-01)에서 Supabase Auth로 완전 전환될 때까지 환경변수 기반 인증을 유지한다.

기존 코드:
```typescript
const ADMIN_ACCOUNTS: Record<string, { password: string; user: AdminUser }> = {
  'admin@udamon.com': {
    password: 'admin1234',
    user: { id: 'admin-001', email: 'admin@udamon.com', displayName: '관리자', role: 'super_admin' },
  },
};
```

수정 코드:
```typescript
const ADMIN_EMAIL = import.meta.env.VITE_ADMIN_EMAIL || '';
const ADMIN_PASSWORD = import.meta.env.VITE_ADMIN_PASSWORD || '';

if (!ADMIN_EMAIL || !ADMIN_PASSWORD) {
  console.error(
    'Missing admin credentials. Set VITE_ADMIN_EMAIL and VITE_ADMIN_PASSWORD in admin/.env'
  );
}

const ADMIN_ACCOUNTS: Record<string, { password: string; user: AdminUser }> = {
  [ADMIN_EMAIL]: {
    password: ADMIN_PASSWORD,
    user: {
      id: 'admin-001',
      email: ADMIN_EMAIL,
      displayName: '관리자',
      role: 'super_admin',
    },
  },
};
```

나머지 로직(login, logout, useAuth 등)은 그대로 유지한다. ADMIN_ACCOUNTS 구조를 유지하면서 비밀번호만 환경변수로 이동하므로 기존 코드 호환성이 보장된다.

**Part B: admin/.env.example 생성**

```
# Admin Credentials (Phase 5에서 Supabase Auth로 전환 예정)
VITE_ADMIN_EMAIL=admin@udamon.com
VITE_ADMIN_PASSWORD=change-me-to-a-strong-password
```

**Part C: supabase/functions/get-upload-url/index.ts CORS 수정 (per SEC-05)**

기존 와일드카드 CORS:
```typescript
const corsHeaders = {
  "Access-Control-Allow-Origin": "*",
  ...
};
```

조건부 CORS로 교체 (Research Pattern 4 기반):

```typescript
// TODO: 도메인 구매 후 실제 도메인으로 교체 (STATE.md blocker)
const ALLOWED_ORIGINS = [
  'https://udamonfan.com',
  'https://www.udamonfan.com',
  'https://admin.udamonfan.com',
];

function getCorsHeaders(req: Request): Record<string, string> {
  const origin = req.headers.get('origin') ?? '';
  // 네이티브 앱은 origin 헤더가 없음 -- origin이 빈 문자열이면 허용
  // 브라우저 요청은 origin이 있으므로 ALLOWED_ORIGINS와 대조
  const isNativeApp = origin === '';
  const isAllowedOrigin = ALLOWED_ORIGINS.includes(origin);
  const allowedOrigin = isNativeApp ? '*' : (isAllowedOrigin ? origin : ALLOWED_ORIGINS[0]);

  return {
    'Access-Control-Allow-Origin': allowedOrigin,
    'Access-Control-Allow-Methods': 'POST, OPTIONS',
    'Access-Control-Allow-Headers': 'Authorization, Content-Type, apikey, x-client-info',
  };
}
```

그리고 기존 `corsHeaders` 상수 참조를 모두 `getCorsHeaders(req)` 함수 호출로 교체한다:

1. OPTIONS 핸들러: `return new Response(null, { status: 204, headers: getCorsHeaders(req) });`
2. `jsonResponse` 함수에 `req` 매개변수 추가:
   ```typescript
   function jsonResponse(body: unknown, status = 200, req: Request): Response {
     return new Response(JSON.stringify(body), {
       status,
       headers: { ...getCorsHeaders(req), "Content-Type": "application/json" },
     });
   }
   ```
3. `errorResponse` 함수에 `req` 매개변수 추가:
   ```typescript
   function errorResponse(message: string, status: number, req: Request): Response {
     return jsonResponse({ error: message }, status, req);
   }
   ```
4. `Deno.serve` 핸들러 내 모든 `jsonResponse(...)`, `errorResponse(...)` 호출에 `req` 인자 추가

주의: 네이티브 앱(React Native)에서 fetch 요청 시 origin 헤더가 전송되지 않을 수 있다. origin이 빈 문자열인 경우(네이티브 앱)에는 `*`를 반환하여 호환성을 유지한다. 브라우저 요청만 origin 제한을 적용한다.
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/workspace/udamon && echo "=== SEC-03: hardcoded password ===" && grep -c "admin1234" admin/src/contexts/AuthContext.tsx || echo "0 (CLEAN)" && echo "=== SEC-03: env var ===" && grep -c "VITE_ADMIN_PASSWORD" admin/src/contexts/AuthContext.tsx && echo "=== SEC-05: CORS wildcard ===" && grep -c '"Access-Control-Allow-Origin": "\*"' supabase/functions/get-upload-url/index.ts || echo "0 (CLEAN)" && echo "=== SEC-05: ALLOWED_ORIGINS ===" && grep -c "ALLOWED_ORIGINS" supabase/functions/get-upload-url/index.ts && echo "=== admin .env.example ===" && test -f admin/.env.example && echo "EXISTS" || echo "MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "admin1234" admin/src/contexts/AuthContext.tsx` 결과가 0건이다
    - `grep "VITE_ADMIN_PASSWORD" admin/src/contexts/AuthContext.tsx` 결과가 1건 이상이다
    - `grep "VITE_ADMIN_EMAIL" admin/src/contexts/AuthContext.tsx` 결과가 1건 이상이다
    - admin/.env.example 파일이 존재하고 `VITE_ADMIN_PASSWORD` 포함
    - admin/src/contexts/AuthContext.tsx가 여전히 `AuthProvider`, `useAuth` export 유지
    - admin/src/contexts/AuthContext.tsx의 login 함수가 여전히 동작 (환경변수 기반)
    - `grep '"Access-Control-Allow-Origin": "\\*"' supabase/functions/get-upload-url/index.ts` 결과가 0건이다
    - `grep "ALLOWED_ORIGINS" supabase/functions/get-upload-url/index.ts` 결과가 1건 이상이다
    - `grep "getCorsHeaders" supabase/functions/get-upload-url/index.ts` 결과가 1건 이상이다
    - supabase/functions/get-upload-url/index.ts에 `udamonfan.com` 포함
    - supabase/functions/get-upload-url/index.ts에 네이티브 앱 origin 빈 문자열 허용 로직 포함
  </acceptance_criteria>
  <done>어드민 비밀번호가 환경변수로 이동, CORS origin이 도메인 기반 제한으로 변경, .env.example 생성 완료</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: DB 스키마 푸시 + 보안 정리 검증</name>
  <files>supabase/migrations/011_users.sql</files>
  <action>
[BLOCKING] 모든 마이그레이션 파일과 보안 코드 정리가 완료된 후, 사용자가 supabase db push를 실행하여 원격 DB에 스키마를 적용한다. 이 태스크 전에 executor가 먼저 `npx supabase db push`를 시도하되, 인증 에러 발생 시 사용자에게 안내한다.
  </action>
  <what-built>
    - Plan 01-02: 12개 마이그레이션 파일 생성 (011~022)
    - Plan 03: 보안 코드 정리 (테스트 계정, console.log, 환경변수, CORS)
  </what-built>
  <how-to-verify>
    1. **[BLOCKING] DB 스키마 푸시**: `cd /Users/sangwopark19/workspace/udamon && npx supabase db push` 실행
       - 모든 마이그레이션이 순서대로 적용되는지 확인
       - 에러가 있으면 해당 마이그레이션 파일 수정 필요
       - 참고: SUPABASE_ACCESS_TOKEN 환경변수가 설정되어 있어야 비대화형 실행 가능. 없으면 `supabase login` 후 실행

    2. **Supabase Dashboard 검증** (https://supabase.com/dashboard):
       - Table Editor에서 public.users, notifications, announcements, inquiries, photographer_applications, cheerleaders, audit_logs, site_settings 테이블 존재 확인
       - cheerleaders 테이블에 10개 구단 시드 데이터 존재 확인
       - photo_posts에 status, rejection_reason, cheerleader_id 컬럼 존재 확인
       - Authentication -> Users에서 테스트 사용자 생성 후 public.users에 자동으로 행이 생성되는지 확인 (DB-02 트리거)

    3. **보안 정리 검증**:
       ```bash
       grep -r "test@udamon\|admin1234\|DUMMY_URL\|DUMMY_KEY\|isSupabaseConfigured" app/src/ admin/src/
       grep "console\.log" app/src/contexts/AuthContext.tsx
       grep '"Access-Control-Allow-Origin": "\*"' supabase/functions/get-upload-url/index.ts
       ```
       위 명령어 결과가 모두 0건이어야 한다.

    4. **.env.example 확인**:
       - `cat app/.env.example` -- EXPO_PUBLIC_SUPABASE_URL, EXPO_PUBLIC_SUPABASE_ANON_KEY 포함
       - `cat admin/.env.example` -- VITE_ADMIN_EMAIL, VITE_ADMIN_PASSWORD 포함
  </how-to-verify>
  <verify>
    <automated>cd /Users/sangwopark19/workspace/udamon && grep -r "test@udamon\|admin1234\|DUMMY_URL\|DUMMY_KEY" app/src/ admin/src/ 2>/dev/null; echo "exit:$?"</automated>
  </verify>
  <done>supabase db push 성공, Dashboard에서 테이블/트리거/RLS 확인, 보안 grep 결과 0건</done>
  <resume-signal>"approved" 입력하거나 발견된 문제를 설명해주세요</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client code -> credentials | 하드코딩된 비밀번호/키가 소스 코드에 존재 (제거 대상) |
| browser -> Edge Function | CORS origin이 와일드카드로 열려 있음 (제한 대상) |
| app startup -> environment | 환경변수 미설정 시 더미 값으로 실행됨 (차단 대상) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | TEST_ACCOUNTS 하드코딩 | mitigate | 코드에서 완전 삭제 (per D-04). grep으로 잔존 확인 |
| T-03-02 | Information Disclosure | console.log OAuth URL/토큰 | mitigate | console.log 3곳 제거 (per D-05). console.error/warn은 에러 핸들링용으로 유지 |
| T-03-03 | Information Disclosure | admin 하드코딩 비밀번호 | mitigate | 환경변수(VITE_ADMIN_PASSWORD)로 이동. .env는 gitignore. Phase 5에서 Supabase Auth로 완전 전환 |
| T-03-04 | Spoofing | CORS 와일드카드 | mitigate | ALLOWED_ORIGINS 기반 조건부 검증. 네이티브 앱(origin 없음)은 허용, 브라우저만 제한 |
| T-03-05 | Denial of Service | 환경변수 미설정 | mitigate | throw Error로 앱 시작 차단 (per D-06). 명확한 에러 메시지로 개발자 가이드 |
| T-03-06 | Information Disclosure | DUMMY_KEY JWT 토큰 | mitigate | DUMMY_URL/DUMMY_KEY 완전 삭제. createClient에 빈 문자열 대신 검증된 값만 전달 |
</threat_model>

<verification>
1. grep으로 하드코딩된 인증정보 잔존 여부 확인 (0건이어야 함)
2. grep으로 console.log 잔존 여부 확인 (0건이어야 함)
3. .env.example 파일 존재 및 내용 확인
4. Edge Function CORS에 와일드카드 없음 확인
5. supabase db push 성공 (checkpoint에서 사용자가 확인)
</verification>

<success_criteria>
- 코드베이스에 test@udamon.com, admin1234, DUMMY_URL, DUMMY_KEY가 0건
- app/src/contexts/AuthContext.tsx에 console.log가 0건
- app/src/services/supabase.ts에 환경변수 필수 검증 throw 존재
- admin 비밀번호가 VITE_ADMIN_PASSWORD 환경변수 기반
- Edge Function CORS가 ALLOWED_ORIGINS 기반 조건부 검증
- .env.example 파일 2개 생성됨 (app, admin)
- supabase db push가 모든 마이그레이션을 성공적으로 적용 (checkpoint)
</success_criteria>

<output>
After completion, create `.planning/phases/01-database-foundation-security/01-03-SUMMARY.md`
</output>
