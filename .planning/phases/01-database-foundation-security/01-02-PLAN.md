---
phase: 01-database-foundation-security
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/012_rls_helpers.sql
  - supabase/migrations/021_drop_spam_and_cleanup.sql
autonomous: true
requirements:
  - DB-12
  - DB-13
  - DB-14

must_haves:
  truths:
    - "is_admin()과 is_owner() 헬퍼 함수가 존재하고 security definer set search_path = '' 를 사용한다"
    - "모든 신규 테이블에 인증 필수 RLS 정책이 적용된다 (비인증 사용자 차단)"
    - "기존 posts_anon_read, comments_anon_read 정책이 DROP 되어 비인증 조회가 불가능하다"
    - "community_reports의 auto_blind_on_report 트리거가 제거된다"
    - "spam_filter_words 테이블이 DROP 된다"
  artifacts:
    - path: "supabase/migrations/012_rls_helpers.sql"
      provides: "is_admin(), is_owner() 헬퍼 함수 + 모든 신규 테이블 RLS 정책"
      contains: "create or replace function public.is_admin"
    - path: "supabase/migrations/021_drop_spam_and_cleanup.sql"
      provides: "spam_filter DROP, 자동 블라인드 트리거 제거, anon 정책 제거"
      contains: "DROP TABLE.*spam_filter_words"
  key_links:
    - from: "supabase/migrations/012_rls_helpers.sql"
      to: "public.users"
      via: "is_admin() queries users.role"
      pattern: "from public.users.*role = 'admin'"
    - from: "supabase/migrations/021_drop_spam_and_cleanup.sql"
      to: "community_reports"
      via: "DROP TRIGGER trg_auto_blind"
      pattern: "drop trigger.*trg_auto_blind"
---

<objective>
RLS 헬퍼 함수 생성, 모든 신규 테이블에 RLS 정책 적용, 기존 anon 정책 제거, 자동 블라인드 트리거 제거, spam_filter_words 테이블 DROP.

Purpose: 비인증 사용자의 데이터 접근을 완전 차단하고(per D-11), 어드민 RLS를 중앙화된 is_admin() 함수로 관리하며(per D-10), out-of-scope 기능을 정리한다.
Output: 마이그레이션 파일 2개 (012, 021)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-database-foundation-security/01-CONTEXT.md
@.planning/phases/01-database-foundation-security/01-RESEARCH.md

<interfaces>
<!-- 기존 RLS 정책 패턴 참조 -->

From supabase/migrations/005_rls_policies.sql:
```sql
-- 기존 anon 정책 (DROP 대상):
CREATE POLICY "posts_anon_read" ON community_posts
  FOR SELECT TO anon USING (is_blinded = FALSE);

CREATE POLICY "comments_anon_read" ON community_comments
  FOR SELECT TO anon USING (TRUE);

-- 기존 인증 사용자 패턴 (참조용):
CREATE POLICY "posts_public_read" ON community_posts
  FOR SELECT USING (
    is_blinded = FALSE
    AND user_id NOT IN (
      SELECT blocked_id FROM user_blocks WHERE blocker_id = auth.uid()
    )
  );
```

From supabase/migrations/002_community.sql:
```sql
-- DROP 대상 트리거/함수:
CREATE OR REPLACE FUNCTION auto_blind_on_report() ...
CREATE TRIGGER trg_auto_blind
  AFTER INSERT ON community_reports
  FOR EACH ROW EXECUTE FUNCTION auto_blind_on_report();
```

From supabase/migrations/004_spam_filter.sql:
```sql
-- DROP 대상 테이블:
CREATE TABLE spam_filter_words (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  word TEXT NOT NULL,
  category TEXT NOT NULL,
  ...
);
```

<!-- Plan 01에서 생성되는 테이블 목록 (RLS 정책 대상) -->
신규 테이블: public.users, public.notifications, public.announcements,
public.inquiries, public.photographer_applications, public.cheerleaders,
public.audit_logs, public.site_settings
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RLS 헬퍼 함수 + 모든 신규 테이블 RLS 정책 (DB-13, DB-14)</name>
  <files>supabase/migrations/012_rls_helpers.sql</files>
  <read_first>
    - supabase/migrations/005_rls_policies.sql (기존 RLS 패턴 참조)
    - supabase/migrations/008_photographer_rls.sql (photographer RLS 패턴 참조)
    - .planning/phases/01-database-foundation-security/01-RESEARCH.md (Pattern 2: is_admin, Pattern 3: (select auth.uid()) 래핑)
  </read_first>
  <action>
`supabase/migrations/012_rls_helpers.sql` 파일을 생성한다.

**Part A: RLS 헬퍼 함수**

```sql
-- is_admin: 관리자 여부 확인 (per D-10: public.users.role 직접 확인)
CREATE OR REPLACE FUNCTION public.is_admin()
RETURNS BOOLEAN
LANGUAGE plpgsql
SECURITY DEFINER SET search_path = ''
AS $$
BEGIN
  RETURN EXISTS (
    SELECT 1 FROM public.users
    WHERE id = (SELECT auth.uid())
    AND role = 'admin'
    AND is_deleted = FALSE
  );
END;
$$;

-- is_owner: 리소스 소유자 여부 확인
CREATE OR REPLACE FUNCTION public.is_owner(resource_user_id UUID)
RETURNS BOOLEAN
LANGUAGE plpgsql
SECURITY DEFINER SET search_path = ''
AS $$
BEGIN
  RETURN (SELECT auth.uid()) = resource_user_id;
END;
$$;
```

**Part B: public.users RLS 정책**

```sql
-- users: 인증 사용자만 조회, 본인만 수정, 어드민은 전체 관리
CREATE POLICY "users_read_authenticated" ON public.users
  FOR SELECT TO authenticated
  USING (is_deleted = FALSE);

CREATE POLICY "users_update_own" ON public.users
  FOR UPDATE TO authenticated
  USING ((SELECT auth.uid()) = id);

CREATE POLICY "users_admin_all" ON public.users
  FOR ALL TO authenticated
  USING (public.is_admin());
```

**Part C: notifications RLS 정책**

```sql
CREATE POLICY "notifications_read_own" ON public.notifications
  FOR SELECT TO authenticated
  USING ((SELECT auth.uid()) = user_id);

CREATE POLICY "notifications_update_own" ON public.notifications
  FOR UPDATE TO authenticated
  USING ((SELECT auth.uid()) = user_id);

CREATE POLICY "notifications_delete_own" ON public.notifications
  FOR DELETE TO authenticated
  USING ((SELECT auth.uid()) = user_id);

CREATE POLICY "notifications_admin_insert" ON public.notifications
  FOR INSERT TO authenticated
  WITH CHECK (public.is_admin());
```

**Part D: announcements RLS 정책**

```sql
-- 인증 사용자 전체 조회, 어드민만 생성/수정/삭제
CREATE POLICY "announcements_read_authenticated" ON public.announcements
  FOR SELECT TO authenticated
  USING (TRUE);

CREATE POLICY "announcements_admin_manage" ON public.announcements
  FOR ALL TO authenticated
  USING (public.is_admin());
```

**Part E: inquiries RLS 정책**

```sql
-- 본인 문의만 조회/생성, 어드민은 전체 관리
CREATE POLICY "inquiries_read_own" ON public.inquiries
  FOR SELECT TO authenticated
  USING ((SELECT auth.uid()) = user_id OR public.is_admin());

CREATE POLICY "inquiries_insert_own" ON public.inquiries
  FOR INSERT TO authenticated
  WITH CHECK ((SELECT auth.uid()) = user_id);

CREATE POLICY "inquiries_update_admin" ON public.inquiries
  FOR UPDATE TO authenticated
  USING (public.is_admin());
```

**Part F: photographer_applications RLS 정책**

```sql
-- 본인 신청만 조회/생성, 어드민은 전체 관리
CREATE POLICY "photographer_apps_read" ON public.photographer_applications
  FOR SELECT TO authenticated
  USING ((SELECT auth.uid()) = user_id OR public.is_admin());

CREATE POLICY "photographer_apps_insert_own" ON public.photographer_applications
  FOR INSERT TO authenticated
  WITH CHECK ((SELECT auth.uid()) = user_id);

CREATE POLICY "photographer_apps_update_admin" ON public.photographer_applications
  FOR UPDATE TO authenticated
  USING (public.is_admin());
```

**Part G: cheerleaders RLS 정책**

```sql
-- 인증 사용자 전체 조회, 어드민만 관리
CREATE POLICY "cheerleaders_read_authenticated" ON public.cheerleaders
  FOR SELECT TO authenticated
  USING (TRUE);

CREATE POLICY "cheerleaders_admin_manage" ON public.cheerleaders
  FOR ALL TO authenticated
  USING (public.is_admin());
```

**Part H: audit_logs RLS 정책**

```sql
-- 어드민만 조회/삽입
CREATE POLICY "audit_logs_admin_read" ON public.audit_logs
  FOR SELECT TO authenticated
  USING (public.is_admin());

CREATE POLICY "audit_logs_admin_insert" ON public.audit_logs
  FOR INSERT TO authenticated
  WITH CHECK (public.is_admin());
```

**Part I: site_settings RLS 정책**

```sql
-- 인증 사용자 조회 가능, 어드민만 수정
CREATE POLICY "site_settings_read_authenticated" ON public.site_settings
  FOR SELECT TO authenticated
  USING (TRUE);

CREATE POLICY "site_settings_admin_manage" ON public.site_settings
  FOR ALL TO authenticated
  USING (public.is_admin());
```

**핵심 패턴:**
- 모든 정책에 `TO authenticated` 지정 (per D-11: 비인증 완전 차단)
- auth.uid() 호출은 항상 `(SELECT auth.uid())`로 래핑 (성능 최적화)
- 어드민 정책은 `public.is_admin()` 헬퍼 사용 (per D-10)
- is_deleted 사용자는 users 조회에서 제외
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/workspace/udamon && grep -c "is_admin()" supabase/migrations/012_rls_helpers.sql && grep -c "is_owner" supabase/migrations/012_rls_helpers.sql && grep -c "TO authenticated" supabase/migrations/012_rls_helpers.sql && grep -c "security definer set search_path" supabase/migrations/012_rls_helpers.sql</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/012_rls_helpers.sql 파일이 존재한다
    - `CREATE OR REPLACE FUNCTION public.is_admin()` 함수가 정의된다
    - `CREATE OR REPLACE FUNCTION public.is_owner(resource_user_id UUID)` 함수가 정의된다
    - 두 함수 모두 `SECURITY DEFINER SET search_path = ''` 포함
    - is_admin()에서 `public.users` 테이블의 `role = 'admin'`과 `is_deleted = FALSE` 확인
    - 8개 테이블(users, notifications, announcements, inquiries, photographer_applications, cheerleaders, audit_logs, site_settings) 모두에 대한 RLS 정책 존재
    - 모든 정책에 `TO authenticated` 지정 (anon 정책 없음)
    - auth.uid()가 `(SELECT auth.uid())`로 래핑됨 (최소 5곳 이상)
    - audit_logs는 어드민 전용 (is_admin() 사용)
    - notifications는 본인만 조회 (user_id = auth.uid())
  </acceptance_criteria>
  <done>is_admin(), is_owner() 헬퍼 함수와 8개 신규 테이블의 RLS 정책이 모두 012_rls_helpers.sql에 정의됨. 비인증 사용자는 모든 테이블에서 차단됨.</done>
</task>

<task type="auto">
  <name>Task 2: spam_filter DROP + 자동 블라인드 트리거 제거 + anon 정책 제거 (DB-12, D-08, D-11, D-12)</name>
  <files>supabase/migrations/021_drop_spam_and_cleanup.sql</files>
  <read_first>
    - supabase/migrations/004_spam_filter.sql (DROP 대상 테이블 구조 확인)
    - supabase/migrations/002_community.sql (auto_blind_on_report 트리거/함수 확인)
    - supabase/migrations/005_rls_policies.sql (posts_anon_read, comments_anon_read 정책 확인)
  </read_first>
  <action>
`supabase/migrations/021_drop_spam_and_cleanup.sql` 파일을 생성한다.

이 마이그레이션은 3가지 정리 작업을 수행한다:

**Part A: spam_filter_words 테이블 DROP (per D-08)**
```sql
-- Out of Scope 확정: 금칙어/스팸 필터 기능 제거
-- 004_spam_filter.sql에서 생성된 spam_filter_words만 DROP
-- user_restrictions, user_blocks, recent_searches는 유지 (다른 기능에서 사용)

-- 먼저 spam_filter_words의 RLS 정책 제거 (005에서 ENABLE만 되어 있고 정책은 없음)
ALTER TABLE spam_filter_words DISABLE ROW LEVEL SECURITY;
DROP TABLE IF EXISTS spam_filter_words;
```

**Part B: 자동 블라인드 트리거 제거 (per D-12)**
```sql
-- 002_community.sql에서 생성된 자동 블라인드 로직 제거
-- 관리자 수동 처리로 전환 (REQUIREMENTS.md -- Out of Scope: "자동 블라인드 (5건 누적)" 제거 확정)
DROP TRIGGER IF EXISTS trg_auto_blind ON community_reports;
DROP FUNCTION IF EXISTS auto_blind_on_report();
```

**Part C: 비인증 사용자 조회 정책 제거 (per D-11)**
```sql
-- 005_rls_policies.sql에서 생성된 anon 정책 제거
-- D-11: 비인증 사용자 완전 차단
DROP POLICY IF EXISTS "posts_anon_read" ON community_posts;
DROP POLICY IF EXISTS "comments_anon_read" ON community_comments;
```

주의사항:
- `spam_filter_words`만 DROP. `user_restrictions`, `user_blocks`, `recent_searches`는 유지 (004_spam_filter.sql에 같이 정의되어 있지만 다른 기능에서 사용)
- `check_self_report()` 함수와 `trg_prevent_self_report` 트리거는 유지 (신고 기능은 관리자 수동 처리로 계속 사용)
- `posts_public_read`, `comments_public_read` 정책은 유지 (인증 사용자용 -- anon이 아님)
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/workspace/udamon && grep "DROP TABLE" supabase/migrations/021_drop_spam_and_cleanup.sql && grep "trg_auto_blind" supabase/migrations/021_drop_spam_and_cleanup.sql && grep "posts_anon_read" supabase/migrations/021_drop_spam_and_cleanup.sql && grep "comments_anon_read" supabase/migrations/021_drop_spam_and_cleanup.sql</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/021_drop_spam_and_cleanup.sql 파일이 존재한다
    - `DROP TABLE IF EXISTS spam_filter_words` 포함
    - `DROP TRIGGER IF EXISTS trg_auto_blind ON community_reports` 포함
    - `DROP FUNCTION IF EXISTS auto_blind_on_report()` 포함
    - `DROP POLICY IF EXISTS "posts_anon_read" ON community_posts` 포함
    - `DROP POLICY IF EXISTS "comments_anon_read" ON community_comments` 포함
    - `user_restrictions`, `user_blocks`, `recent_searches`에 대한 DROP 문이 없음
    - `check_self_report`, `trg_prevent_self_report`에 대한 DROP 문이 없음
  </acceptance_criteria>
  <done>spam_filter_words DROP, auto_blind 트리거/함수 제거, anon RLS 정책 2개 제거가 단일 마이그레이션에 정의됨</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon -> authenticated | 비인증 사용자의 데이터 접근 차단 (D-11) |
| authenticated -> admin | is_admin() 함수로 어드민 전용 리소스 보호 |
| client SDK -> RLS layer | 모든 테이블 접근이 RLS를 통과해야 함 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | is_admin() search_path | mitigate | `SECURITY DEFINER SET search_path = ''` 적용, `public.users` 스키마 명시 |
| T-02-02 | Spoofing | 비인증 데이터 접근 | mitigate | 모든 정책에 `TO authenticated` 지정, `posts_anon_read`/`comments_anon_read` DROP |
| T-02-03 | Tampering | soft-deleted users | mitigate | users_read_authenticated에 `is_deleted = FALSE` 조건 포함, is_admin()에서도 `is_deleted = FALSE` 확인 |
| T-02-04 | Information Disclosure | audit_logs 노출 | mitigate | audit_logs는 `is_admin()` 전용 -- 일반 사용자 조회 불가 |
| T-02-05 | Spoofing | auto_blind 우회 | accept | 트리거 제거됨 (D-12). 관리자 수동 처리로 전환. 공격 벡터 자체가 제거됨 |
</threat_model>

<verification>
1. 012_rls_helpers.sql에 is_admin(), is_owner() 함수 존재 확인
2. 8개 신규 테이블의 RLS 정책이 모두 `TO authenticated`로 지정 확인
3. 021에서 spam_filter_words DROP, trg_auto_blind DROP, anon 정책 2개 DROP 확인
4. user_restrictions, user_blocks, recent_searches가 DROP 되지 않았음 확인
</verification>

<success_criteria>
- is_admin(), is_owner() 헬퍼 함수가 security definer set search_path = '' 로 정의됨
- 8개 신규 테이블 모두에 인증 필수 RLS 정책 적용됨
- 어드민 전용 테이블(audit_logs, site_settings 관리)에 is_admin() 사용됨
- 기존 anon RLS 정책 2개가 제거됨
- auto_blind 트리거/함수가 제거됨
- spam_filter_words 테이블이 DROP 됨
</success_criteria>

<output>
After completion, create `.planning/phases/01-database-foundation-security/01-02-SUMMARY.md`
</output>
