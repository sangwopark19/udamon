---
phase: 02-authentication
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified: []
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - AUTH-07
  - AUTH-08
  - AUTH-09
  - AUTH-10

user_setup:
  - service: supabase-auth-providers
    why: "OAuth provider 설정이 Supabase Dashboard에서 필요"
    env_vars: []
    dashboard_config:
      - task: "Google OAuth provider 활성화"
        location: "Supabase Dashboard > Auth > Providers > Google"
      - task: "Kakao OAuth provider 활성화"
        location: "Supabase Dashboard > Auth > Providers > Kakao"
      - task: "Naver Custom OAuth Provider 설정"
        location: "Supabase Dashboard > Auth > Providers > New Provider > OAuth2"
  - service: supabase-edge-functions
    why: "delete-account Edge Function 배포"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard > Account > Access Tokens"

must_haves:
  truths:
    - "supabase db push가 성공하여 DB 스키마가 최신 상태이다"
    - "delete-account Edge Function이 배포되어 호출 가능하다"
    - "타입 체크(tsc --noEmit)가 에러 없이 통과한다"
    - "모든 Plan(01~03)의 코드 변경이 정상 빌드된다"
  artifacts: []
  key_links: []
---

<objective>
Phase 2 통합 검증 -- DB 스키마 Push, Edge Function 배포, 타입 체크, 전체 빌드 확인을 수행한다.

Purpose: Plan 01~03의 코드 변경이 실제 Supabase 환경과 통합되어 정상 동작하는지 확인한다. DB 스키마가 push되지 않으면 타입 체크가 통과해도 런타임에서 실패한다.
Output: DB 스키마 동기화, Edge Function 배포, 빌드 검증 완료
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-authentication/02-01-SUMMARY.md
@.planning/phases/02-authentication/02-02-SUMMARY.md
@.planning/phases/02-authentication/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB 스키마 Push + Edge Function 배포 + 타입 체크</name>
  <files></files>
  <read_first>
    - supabase/functions/delete-account/index.ts (배포 대상 Edge Function)
    - supabase/config.toml (Supabase 프로젝트 설정 확인, 없으면 supabase init 필요)
    - app/tsconfig.json (TypeScript 설정)
  </read_first>
  <action>
**1. DB 스키마 Push:**
```bash
# SUPABASE_ACCESS_TOKEN이 환경변수로 설정되어 있어야 함 (non-TTY 환경)
supabase db push
```
- 실패 시: 마이그레이션 충돌 확인, `supabase db push --include-all` 시도
- Phase 1 마이그레이션이 이미 push되었다면 이 단계는 no-op

**2. Edge Function 배포:**
```bash
supabase functions deploy delete-account --no-verify-jwt
```
- `--no-verify-jwt`: JWT 검증은 함수 내부에서 직접 수행 (get-upload-url 패턴과 동일)
- 배포 후 확인: `supabase functions list`에서 delete-account 존재 확인

**3. 타입 체크:**
```bash
cd app && npx tsc --noEmit
```
- 에러가 있으면 해당 파일 수정 (import 경로, 타입 불일치 등)

**4. Expo 빌드 확인:**
```bash
cd app && npx expo export --platform web --output-dir /tmp/expo-check 2>&1 | tail -20
```
- 번들링 에러가 없는지 확인 (완전한 빌드가 아닌 번들링 가능 여부 체크)

모든 단계가 성공하면 Phase 2 코드 변경이 통합 검증 완료.
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/workspace/udamon/app && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` 또는 동등한 스키마 동기화 명령이 에러 없이 완료 (또는 이미 최신 상태)
    - `supabase functions list` 출력에 `delete-account`가 포함
    - `npx tsc --noEmit` 종료 코드 0 (타입 에러 없음)
    - Expo 번들링 체크에서 치명적 에러 없음
  </acceptance_criteria>
  <done>DB 스키마가 동기화되고, Edge Function이 배포되며, 전체 타입 체크와 빌드가 통과한다</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 2 전체 흐름 사용자 검증</name>
  <files></files>
  <action>
사용자가 직접 앱을 실행하여 Phase 2에서 구현한 모든 기능을 검증한다.
검증 항목: 소셜 로그인 UI 순서, Apple 비활성화 표시, OAuth 로그인, 프로필 온보딩, 차단 관리.
  </action>
  <what-built>
Phase 2 Authentication 전체 구현:
1. Supabase Auth 기반 OAuth 4종 (Google/Kakao/Naver + Apple 비활성화) + 이메일 인증
2. 테스트 계정 완전 제거, AuthContext Supabase 전환
3. ProfileSetupScreen (닉네임 + 구단 선택 온보딩)
4. 회원 탈퇴 (Edge Function soft delete)
5. 사용자 차단 (user_blocks 테이블 연동)
  </what-built>
  <how-to-verify>
**사전 조건:** Supabase Dashboard에서 최소 Google 또는 Kakao OAuth provider가 활성화되어 있어야 합니다.

1. **소셜 로그인 UI 확인:**
   - `cd app && npx expo start` 실행
   - 로그인 화면에서 버튼 순서 확인: 카카오 -> 네이버 -> Google -> Apple
   - Apple 버튼이 회색 비활성화 + '준비 중' 텍스트 표시 확인
   - 활성화된 OAuth provider(Google 또는 Kakao)로 로그인 시도

2. **온보딩 확인:**
   - 최초 로그인 후 ProfileSetupScreen이 표시되는지 확인
   - 닉네임 입력 시 실시간 중복 체크 동작 확인 (500ms 후 결과 표시)
   - 구단 선택 3열 그리드 표시 확인
   - 완료 후 홈 화면으로 이동 확인

3. **차단 기능 확인:**
   - 설정 > 차단 관리 화면 접근 확인

4. **빌드 확인:**
   - 타입 에러 없이 앱이 실행되는지 확인
  </how-to-verify>
  <verify>사용자가 "approved" 입력 또는 이슈를 설명</verify>
  <done>사용자가 전체 Phase 2 인증 흐름을 검증하고 승인</done>
  <resume-signal>"approved" 입력 또는 발견된 이슈를 설명해주세요</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI/CD -> Supabase | DB push와 Edge Function 배포가 인증된 상태에서만 수행 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-16 | Elevation of Privilege | Edge Function deployment | mitigate | SUPABASE_ACCESS_TOKEN으로 인증된 배포만 허용. --no-verify-jwt는 함수 내부에서 JWT를 직접 검증하므로 안전 |
</threat_model>

<verification>
1. `supabase db push` 성공
2. `supabase functions list` 에 delete-account 존재
3. `npx tsc --noEmit` 에러 0
4. 사용자가 전체 흐름 승인
</verification>

<success_criteria>
- DB 스키마가 Supabase 프로젝트와 동기화됨
- delete-account Edge Function이 배포되어 호출 가능
- 타입 체크와 빌드가 에러 없이 통과
- 사용자가 로그인 -> 온보딩 -> 홈 흐름을 승인
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication/02-04-SUMMARY.md`
</output>
