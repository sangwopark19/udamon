---
phase: 02-authentication
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - app/package.json
  - app/jest.config.ts
  - app/src/__tests__/mocks/supabase.ts
  - app/src/__tests__/auth/authContext.test.ts
  - app/src/__tests__/auth/nickname.test.ts
  - app/src/__tests__/auth/block.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-03
  - AUTH-05
  - AUTH-06
  - AUTH-07
  - AUTH-08
  - AUTH-10

must_haves:
  truths:
    - "jest --testPathPattern=auth 명령이 실행 가능하고 테스트 파일을 감지한다"
    - "supabase mock 모듈이 import 가능하고 auth/from 메서드가 mock된다"
    - "모든 Wave 0 테스트가 RED 상태 (아직 구현 전이므로 실패해야 정상)"
  artifacts:
    - path: "app/jest.config.ts"
      provides: "Jest 설정 (jest-expo 프리셋)"
      contains: "jest-expo"
    - path: "app/src/__tests__/mocks/supabase.ts"
      provides: "Supabase client mock 유틸리티"
      exports: ["supabase", "mockAuth", "mockFrom"]
    - path: "app/src/__tests__/auth/authContext.test.ts"
      provides: "AuthContext 테스트 (TEST_ACCOUNTS 제거, OAuth 호출 검증)"
      contains: "TEST_ACCOUNTS"
    - path: "app/src/__tests__/auth/nickname.test.ts"
      provides: "닉네임 유효성 검사 테스트 (2~12자, 한글/영문/숫자, 30일 제한)"
      contains: "nickname"
    - path: "app/src/__tests__/auth/block.test.ts"
      provides: "차단 기능 테스트 (user_blocks INSERT/DELETE)"
      contains: "user_blocks"
  key_links:
    - from: "app/src/__tests__/mocks/supabase.ts"
      to: "app/src/services/supabase.ts"
      via: "jest.mock path mapping"
      pattern: "jest\\.mock.*supabase"
---

<objective>
Wave 0 테스트 인프라 구축 -- Jest/jest-expo를 설치하고, Supabase client mock과 Phase 2 요구사항별 테스트 스캐폴드를 생성한다. 모든 테스트는 RED 상태(구현 전 실패)로 시작하여, 후속 Plan 01~03에서 구현 시 GREEN으로 전환된다.

Purpose: VALIDATION.md의 Wave 0 요구사항을 충족하고, Plan 01~03의 <automated> 검증이 실제 jest 명령으로 동작하도록 테스트 인프라를 선행 구축한다.
Output: Jest 설정, Supabase mock, auth 테스트 스캐폴드
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authentication/02-CONTEXT.md
@.planning/phases/02-authentication/02-RESEARCH.md
@.planning/phases/02-authentication/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Jest/jest-expo 설치 + 설정 파일 생성</name>
  <files>app/package.json, app/jest.config.ts</files>
  <read_first>
    - app/package.json (현재 dependencies, scripts 확인)
    - app/tsconfig.json (TypeScript 설정 확인)
  </read_first>
  <action>
**1. 테스트 의존성 설치:**
```bash
cd app && npm install --save-dev jest jest-expo @testing-library/react-native @testing-library/jest-native @types/jest ts-jest
```

**2. app/jest.config.ts 생성:**
```typescript
import type { Config } from 'jest';

const config: Config = {
  preset: 'jest-expo',
  transformIgnorePatterns: [
    'node_modules/(?!((jest-)?react-native|@react-native(-community)?)|expo(nent)?|@expo(nent)?/.*|@expo-google-fonts/.*|react-navigation|@react-navigation/.*|@supabase/.*|@react-native-async-storage/.*)',
  ],
  setupFilesAfterSetup: [],
  moduleNameMapper: {
    '^../../services/supabase$': '<rootDir>/src/__tests__/mocks/supabase',
    '^../services/supabase$': '<rootDir>/src/__tests__/mocks/supabase',
  },
  testPathDirs: ['<rootDir>/src/__tests__'],
  collectCoverageFrom: [
    'src/contexts/**/*.{ts,tsx}',
    'src/screens/auth/**/*.{ts,tsx}',
    'src/screens/onboarding/**/*.{ts,tsx}',
    '!src/**/*.d.ts',
  ],
};

export default config;
```

**3. package.json에 test 스크립트 추가:**
- `"test"` 스크립트가 없으면 추가: `"test": "jest"`
- `"test:auth"` 스크립트 추가: `"test:auth": "jest --testPathPattern=auth --no-coverage"`
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/workspace/udamon/app && npx jest --version 2>&1 | head -1 && test -f jest.config.ts && echo "jest.config.ts exists"</automated>
  </verify>
  <done>Jest가 설치되고 jest-expo 프리셋으로 설정이 완료되어 `npx jest` 명령이 실행 가능하다</done>
</task>

<task type="auto">
  <name>Task 2: Supabase mock + Auth 테스트 스캐폴드 생성</name>
  <files>app/src/__tests__/mocks/supabase.ts, app/src/__tests__/auth/authContext.test.ts, app/src/__tests__/auth/nickname.test.ts, app/src/__tests__/auth/block.test.ts</files>
  <read_first>
    - app/src/services/supabase.ts (mock 대상 인터페이스 확인)
    - app/src/contexts/AuthContext.tsx (테스트 대상 함수 확인)
    - app/src/contexts/BlockContext.tsx (차단 함수 확인)
  </read_first>
  <action>
**1. app/src/__tests__/mocks/supabase.ts 생성:**
Supabase client를 mock하는 유틸리티. auth, from 메서드를 jest.fn()으로 제공.
```typescript
export const mockAuth = {
  signInWithOAuth: jest.fn(),
  signInWithPassword: jest.fn(),
  signUp: jest.fn(),
  signOut: jest.fn(),
  getUser: jest.fn(),
  onAuthStateChange: jest.fn(() => ({ data: { subscription: { unsubscribe: jest.fn() } } })),
  startAutoRefresh: jest.fn(),
  stopAutoRefresh: jest.fn(),
  resetPasswordForEmail: jest.fn(),
};

const mockQueryBuilder = {
  select: jest.fn().mockReturnThis(),
  insert: jest.fn().mockReturnThis(),
  update: jest.fn().mockReturnThis(),
  delete: jest.fn().mockReturnThis(),
  eq: jest.fn().mockReturnThis(),
  in: jest.fn().mockReturnThis(),
  maybeSingle: jest.fn(),
  single: jest.fn(),
  order: jest.fn().mockReturnThis(),
};

export const mockFrom = jest.fn(() => mockQueryBuilder);

export const supabase = {
  auth: mockAuth,
  from: mockFrom,
  functions: {
    invoke: jest.fn(),
  },
};

export const isSupabaseConfigured = true;
```

**2. app/src/__tests__/auth/authContext.test.ts 생성:**
RED 테스트 -- AuthContext가 TEST_ACCOUNTS 없이 동작하고 OAuth가 올바른 provider로 호출되는지 검증.
```typescript
describe('AuthContext', () => {
  test('TEST_ACCOUNTS가 코드에서 완전히 제거되었다 (AUTH-07)', () => {
    // AuthContext 소스를 읽어서 TEST_ACCOUNTS 문자열이 없는지 확인
  });

  test('Google OAuth 호출 시 signInWithOAuth에 google provider가 전달된다 (AUTH-01)', () => {
    // login('google') 호출 -> supabase.auth.signInWithOAuth({ provider: 'google' })
  });

  test('Kakao OAuth 호출 시 signInWithOAuth에 kakao provider가 전달된다 (AUTH-03)', () => {
    // login('kakao') 호출 -> supabase.auth.signInWithOAuth({ provider: 'kakao' })
  });

  test('이메일 로그인 시 signInWithPassword가 호출된다 (AUTH-05)', () => {
    // loginWithEmail(email, password) -> supabase.auth.signInWithPassword
  });

  test('resetPassword 호출 시 resetPasswordForEmail이 호출된다 (AUTH-06)', () => {
    // resetPassword(email) -> supabase.auth.resetPasswordForEmail
  });
});
```

**3. app/src/__tests__/auth/nickname.test.ts 생성:**
RED 테스트 -- 닉네임 유효성 검사 로직 검증.
```typescript
describe('Nickname Validation (AUTH-08)', () => {
  test('2~12자 한글/영문/숫자만 허용한다', () => {
    // 정규식 /^[가-힣a-zA-Z0-9]{2,12}$/ 검증
  });

  test('특수문자가 포함되면 거부한다', () => {
    // '닉네임!' -> false
  });

  test('1자 이하면 거부한다', () => {
    // '가' -> false
  });

  test('13자 이상이면 거부한다', () => {
    // '가나다라마바사아자차카타파' -> false
  });

  test('닉네임 변경 30일 미경과 시 변경이 차단된다', () => {
    // nickname_changed_at이 29일 전이면 에러
  });

  test('닉네임 변경 30일 경과 시 변경이 허용된다', () => {
    // nickname_changed_at이 31일 전이면 성공
  });
});
```

**4. app/src/__tests__/auth/block.test.ts 생성:**
RED 테스트 -- 차단 기능 Supabase 연동 검증.
```typescript
describe('Block Feature (AUTH-10)', () => {
  test('blockUser 호출 시 user_blocks에 INSERT된다', () => {
    // blockUser(targetId) -> supabase.from('user_blocks').insert(...)
  });

  test('unblockUser 호출 시 user_blocks에서 DELETE된다', () => {
    // unblockUser(targetId) -> supabase.from('user_blocks').delete(...)
  });

  test('이미 차단된 사용자를 다시 차단하면 에러 메시지 표시', () => {
    // 23505 에러 코드 -> 토스트 메시지
  });

  test('초기 로드 시 차단 목록을 user_blocks에서 조회한다', () => {
    // useEffect -> supabase.from('user_blocks').select('blocked_id')
  });
});
```

모든 테스트는 의도적으로 RED 상태로 작성한다. Plan 01~03 실행 시 GREEN으로 전환되어야 한다.
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/workspace/udamon/app && test -f src/__tests__/mocks/supabase.ts && test -f src/__tests__/auth/authContext.test.ts && test -f src/__tests__/auth/nickname.test.ts && test -f src/__tests__/auth/block.test.ts && echo "All test files exist"</automated>
  </verify>
  <done>Supabase mock 모듈과 AUTH 요구사항별 테스트 스캐폴드가 생성되어 후속 Plan의 자동화 검증에 사용 가능하다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test -> Mock | 테스트가 실제 Supabase가 아닌 mock을 호출해야 함 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-00-01 | Information Disclosure | test fixtures | accept | 테스트 데이터에 실제 비밀번호/토큰 포함 금지. mock 데이터만 사용 |
</threat_model>

<verification>
1. `npx jest --version` 성공
2. `jest.config.ts` 존재
3. `src/__tests__/mocks/supabase.ts` 존재
4. `src/__tests__/auth/` 내 3개 테스트 파일 존재
5. `npx jest --testPathPattern=auth --no-coverage` 실행 가능 (실패 OK -- RED 상태)
</verification>

<success_criteria>
- Jest + jest-expo가 설치되고 설정이 완료됨
- Supabase client mock 모듈이 import 가능
- auth 테스트 3개 파일(authContext, nickname, block)이 RED 상태로 존재
- `npx jest --testPathPattern=auth` 명령이 테스트 파일을 감지하고 실행됨
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication/02-00-SUMMARY.md`
</output>
