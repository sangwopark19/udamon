---
phase: 02-authentication
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - supabase/functions/delete-account/index.ts
  - app/src/screens/settings/AccountManagementScreen.tsx
  - app/src/contexts/BlockContext.tsx
  - app/src/screens/settings/BlockedUsersScreen.tsx
  - app/src/i18n/locales/ko.ts
autonomous: true
requirements:
  - AUTH-09
  - AUTH-10

must_haves:
  truths:
    - "사용자가 설정 > 계정 관리에서 탈퇴하면 public.users.is_deleted=true가 되고 auth.users도 soft delete된다"
    - "탈퇴 시 2단계 확인 절차가 있다 (경고 문구 + 확인 버튼)"
    - "탈퇴 후 세션이 종료되고 로그인 화면으로 이동한다"
    - "사용자가 다른 사용자를 차단하면 user_blocks 테이블에 INSERT된다"
    - "차단한 사용자의 게시글이 피드에서 숨겨진다 (BlockContext isUserBlocked 활용)"
    - "설정 > 차단 관리에서 차단 목록을 보고 해제할 수 있다"
  artifacts:
    - path: "supabase/functions/delete-account/index.ts"
      provides: "회원 탈퇴 Edge Function (service_role 사용)"
      contains: "auth.admin.deleteUser"
    - path: "app/src/screens/settings/AccountManagementScreen.tsx"
      provides: "계정 관리 화면 (soft delete 연동)"
      contains: "delete-account"
    - path: "app/src/contexts/BlockContext.tsx"
      provides: "Supabase user_blocks 연동 차단 Context"
      contains: "supabase"
    - path: "app/src/screens/settings/BlockedUsersScreen.tsx"
      provides: "차단 관리 화면 (Supabase 연동)"
      contains: "supabase"
  key_links:
    - from: "app/src/screens/settings/AccountManagementScreen.tsx"
      to: "supabase/functions/delete-account"
      via: "supabase.functions.invoke('delete-account')"
      pattern: "functions\\.invoke.*delete-account"
    - from: "app/src/contexts/BlockContext.tsx"
      to: "supabase user_blocks table"
      via: "supabase.from('user_blocks')"
      pattern: "supabase.*from.*user_blocks"
---

<objective>
회원 탈퇴(soft delete) + 사용자 차단 기능 구현 -- Edge Function을 통한 안전한 회원 탈퇴와 user_blocks 테이블 기반 차단 시스템을 Supabase와 완전 연동한다.

Purpose: D-08~D-10에 따라 사용자가 계정을 탈퇴하고, 다른 사용자를 차단/해제할 수 있어야 한다. 탈퇴는 service_role 키가 필요하므로 Edge Function을 통해 처리한다.
Output: delete-account Edge Function, AccountManagement 탈퇴 연동, BlockContext Supabase 연동
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authentication/02-CONTEXT.md
@.planning/phases/02-authentication/02-RESEARCH.md
@.planning/phases/02-authentication/02-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from existing codebase -->

From supabase/functions/get-upload-url/index.ts (Edge Function 패턴 참고):
```typescript
// Deno runtime, npm: prefix for packages
import { createClient } from "npm:@supabase/supabase-js@2";
// CORS headers pattern
const corsHeaders = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Methods": "POST, OPTIONS",
  "Access-Control-Allow-Headers": "Authorization, Content-Type, apikey, x-client-info",
};
// JWT auth pattern: req.headers.get("Authorization")?.replace("Bearer ", "")
```

From supabase/migrations/004_spam_filter.sql (user_blocks 테이블):
```sql
CREATE TABLE user_blocks (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  blocker_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  blocked_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE(blocker_id, blocked_id),
  CHECK(blocker_id != blocked_id)
);
```

From supabase/migrations/005_rls_policies.sql (user_blocks RLS):
```sql
ALTER TABLE user_blocks ENABLE ROW LEVEL SECURITY;
CREATE POLICY "blocks_read_own" ON user_blocks FOR SELECT USING (blocker_id = auth.uid());
CREATE POLICY "blocks_insert_own" ON user_blocks FOR INSERT WITH CHECK (blocker_id = auth.uid());
CREATE POLICY "blocks_delete_own" ON user_blocks FOR DELETE USING (blocker_id = auth.uid());
```

From app/src/contexts/BlockContext.tsx:
```typescript
interface BlockContextValue {
  blockedUserIds: Set<string>;
  blockUser: (userId: string) => void;
  unblockUser: (userId: string) => void;
  isUserBlocked: (userId: string) => boolean;
}
```

From app/src/screens/settings/AccountManagementScreen.tsx:
```typescript
// 현재: logout()만 호출, 실제 soft delete 미구현
const handleConfirmDelete = useCallback(async () => {
  setShowDeleteAccount(false);
  await logout();
  showToast(t('account_delete_done'));
}, [logout, showToast]);
```

From app/src/contexts/AuthContext.tsx (Plan 01 이후):
```typescript
// logout, user 상태 접근 가능
const { user, logout } = useAuth();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 회원 탈퇴 Edge Function + AccountManagementScreen 연동 (AUTH-09)</name>
  <files>supabase/functions/delete-account/index.ts, app/src/screens/settings/AccountManagementScreen.tsx, app/src/i18n/locales/ko.ts</files>
  <read_first>
    - supabase/functions/get-upload-url/index.ts (Edge Function 패턴, CORS, JWT 인증 방식 전체 확인)
    - app/src/screens/settings/AccountManagementScreen.tsx (전체 파일 -- 현재 탈퇴 UI, handleConfirmDelete)
    - app/src/contexts/AuthContext.tsx (logout 함수 구현 확인)
    - app/src/i18n/locales/ko.ts (기존 번역 키)
    - app/src/services/supabase.ts (supabase 클라이언트 import 경로)
  </read_first>
  <action>
**1. supabase/functions/delete-account/index.ts 생성:**
```typescript
import { createClient } from "npm:@supabase/supabase-js@2";

const corsHeaders = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Methods": "POST, OPTIONS",
  "Access-Control-Allow-Headers": "Authorization, Content-Type, apikey, x-client-info",
};

function jsonResponse(body: unknown, status = 200): Response {
  return new Response(JSON.stringify(body), {
    status,
    headers: { ...corsHeaders, "Content-Type": "application/json" },
  });
}

Deno.serve(async (req: Request) => {
  if (req.method === "OPTIONS") {
    return new Response("ok", { headers: corsHeaders });
  }

  if (req.method !== "POST") {
    return jsonResponse({ error: "Method not allowed" }, 405);
  }

  try {
    // JWT에서 사용자 인증
    const token = req.headers.get("Authorization")?.replace("Bearer ", "");
    if (!token) {
      return jsonResponse({ error: "Missing authorization token" }, 401);
    }

    const supabaseAdmin = createClient(
      Deno.env.get("SUPABASE_URL")!,
      Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!
    );

    const { data: { user }, error: authError } = await supabaseAdmin.auth.getUser(token);
    if (authError || !user) {
      return jsonResponse({ error: "Unauthorized" }, 401);
    }

    // 1. public.users soft delete
    const { error: updateError } = await supabaseAdmin
      .from("users")
      .update({
        is_deleted: true,
        deleted_at: new Date().toISOString(),
        nickname: null,  // 닉네임 해제 (재사용 가능)
      })
      .eq("id", user.id);

    if (updateError) {
      return jsonResponse({ error: "Failed to update user record" }, 500);
    }

    // 2. auth.users soft delete (service_role 필요)
    const { error: deleteError } = await supabaseAdmin.auth.admin.deleteUser(
      user.id,
      true  // shouldSoftDelete = true
    );

    if (deleteError) {
      return jsonResponse({ error: "Failed to delete auth user" }, 500);
    }

    return jsonResponse({ success: true });
  } catch (err) {
    return jsonResponse({ error: "Internal server error" }, 500);
  }
});
```

**2. AccountManagementScreen.tsx 탈퇴 연동 (D-08):**
- `import { supabase } from '../../services/supabase';` 추가
- `handleConfirmDelete` 함수를 실제 Edge Function 호출로 변경:
  ```typescript
  const handleConfirmDelete = useCallback(async () => {
    setDeleteLoading(true);
    try {
      const { data, error } = await supabase.functions.invoke('delete-account', {
        method: 'POST',
      });
      
      if (error) {
        showToast(t('account_delete_error'), 'error');
        setDeleteLoading(false);
        return;
      }
      
      setShowDeleteAccount(false);
      await logout();
      showToast(t('account_delete_done'));
    } catch {
      showToast(t('account_delete_error'), 'error');
    } finally {
      setDeleteLoading(false);
    }
  }, [logout, showToast, t]);
  ```
- `deleteLoading` 상태 추가: `const [deleteLoading, setDeleteLoading] = useState(false);`
- 탈퇴 확인 모달의 경고 문구 D-08 적용:
  - "탈퇴 시 작성한 게시글과 댓글은 유지되며, '탈퇴한 사용자'로 표시됩니다."
  - "탈퇴 후 동일 계정으로 재가입해도 이전 데이터를 복구할 수 없습니다."
  - 확인 버튼: "탈퇴하기" (deleteLoading 시 ActivityIndicator)
- 기존 `deleteConfirmText` 입력 패턴을 2단계 확인으로 변경하되, 확인 모달 UI 유지

**3. i18n 키 추가 (ko.ts):**
- `account_delete_warning_1: '탈퇴 시 작성한 게시글과 댓글은 유지되며, \'탈퇴한 사용자\'로 표시됩니다.'`
- `account_delete_warning_2: '탈퇴 후 동일 계정으로 재가입해도 이전 데이터를 복구할 수 없습니다.'`
- `account_delete_confirm_btn: '탈퇴하기'`
- `account_delete_error: '탈퇴 처리에 실패했습니다. 다시 시도해주세요.'`
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/workspace/udamon && test -f supabase/functions/delete-account/index.ts && grep -n "auth.admin.deleteUser" supabase/functions/delete-account/index.ts && grep -n "functions.invoke.*delete-account" app/src/screens/settings/AccountManagementScreen.tsx && grep -n "account_delete_warning_1" app/src/i18n/locales/ko.ts</automated>
  </verify>
  <acceptance_criteria>
    - supabase/functions/delete-account/index.ts 파일이 존재
    - Edge Function에 `auth.admin.deleteUser` 호출이 존재
    - Edge Function에 `is_deleted: true` 업데이트가 존재
    - Edge Function에 `SUPABASE_SERVICE_ROLE_KEY` 환경변수 사용
    - Edge Function에 CORS 헤더가 존재
    - Edge Function에 JWT 토큰 검증 로직이 존재
    - AccountManagementScreen.tsx에 `supabase.functions.invoke('delete-account')` 호출이 존재
    - AccountManagementScreen.tsx에 deleteLoading 상태가 존재
    - ko.ts에 `account_delete_warning_1` 키가 존재
    - ko.ts에 `account_delete_error` 키가 존재
  </acceptance_criteria>
  <done>회원 탈퇴가 Edge Function을 통해 public.users soft delete + auth.users soft delete로 처리되고, AccountManagementScreen에서 2단계 확인 후 탈퇴 요청이 가능하다</done>
</task>

<task type="auto">
  <name>Task 2: BlockContext Supabase 연동 + BlockedUsersScreen 업데이트 (AUTH-10)</name>
  <files>app/src/contexts/BlockContext.tsx, app/src/screens/settings/BlockedUsersScreen.tsx, app/src/i18n/locales/ko.ts</files>
  <read_first>
    - app/src/contexts/BlockContext.tsx (전체 파일 -- 현재 인메모리 차단 로직)
    - app/src/screens/settings/BlockedUsersScreen.tsx (전체 파일 -- 현재 차단 목록 UI)
    - app/src/contexts/AuthContext.tsx (useAuth에서 user.id 접근)
    - supabase/migrations/004_spam_filter.sql (user_blocks 테이블 구조)
    - supabase/migrations/005_rls_policies.sql (user_blocks RLS 정책)
    - app/src/services/supabase.ts (supabase 클라이언트)
    - app/src/i18n/locales/ko.ts (기존 번역 키)
  </read_first>
  <action>
**1. BlockContext.tsx Supabase user_blocks 연동 (D-09, D-10):**
- `import { supabase } from '../services/supabase';` 추가
- `import { useAuth } from './AuthContext';` 추가
- `import { useTranslation } from 'react-i18next';` 추가

- BlockProvider 내부:
  - `const { user } = useAuth();` 로 현재 사용자 ID 접근
  - `const { t } = useTranslation();`

- 초기 로드: useEffect에서 인증된 사용자의 차단 목록을 로드
  ```typescript
  useEffect(() => {
    if (!user?.id) {
      setBlockedUserIds(new Set());
      return;
    }
    
    const loadBlocks = async () => {
      const { data, error } = await supabase
        .from('user_blocks')
        .select('blocked_id')
        .eq('blocker_id', user.id);
      
      if (!error && data) {
        setBlockedUserIds(new Set(data.map((b) => b.blocked_id)));
      }
    };
    
    loadBlocks();
  }, [user?.id]);
  ```

- `blockUser` 함수를 async로 변경:
  ```typescript
  const blockUser = useCallback(async (blockedId: string) => {
    if (!user?.id) return;
    
    const { error } = await supabase
      .from('user_blocks')
      .insert({ blocker_id: user.id, blocked_id: blockedId });
    
    if (error) {
      if (error.code === '23505') {
        // 이미 차단된 사용자 (UNIQUE 제약 위반)
        showToast(t('block_already'), 'error');
        return;
      }
      showToast(t('block_error'), 'error');
      return;
    }
    
    setBlockedUserIds((prev) => new Set(prev).add(blockedId));
    showToast(t('block_success'), 'success');
  }, [user?.id, showToast, t]);
  ```

- `unblockUser` 함수를 async로 변경:
  ```typescript
  const unblockUser = useCallback(async (blockedId: string) => {
    if (!user?.id) return;
    
    const { error } = await supabase
      .from('user_blocks')
      .delete()
      .eq('blocker_id', user.id)
      .eq('blocked_id', blockedId);
    
    if (error) {
      showToast(t('unblock_error'), 'error');
      return;
    }
    
    setBlockedUserIds((prev) => {
      const next = new Set(prev);
      next.delete(blockedId);
      return next;
    });
    showToast(t('unblock_success'), 'success');
  }, [user?.id, showToast, t]);
  ```

- BlockContextValue 인터페이스 업데이트:
  ```typescript
  interface BlockContextValue {
    blockedUserIds: Set<string>;
    blockUser: (userId: string) => Promise<void>;  // void -> Promise<void>
    unblockUser: (userId: string) => Promise<void>; // void -> Promise<void>
    isUserBlocked: (userId: string) => boolean;
  }
  ```

**2. BlockedUsersScreen.tsx 업데이트:**
- 차단 목록에 사용자 정보(닉네임, 아바타) 표시를 위해 차단된 사용자 프로필 로드:
  ```typescript
  const [blockedProfiles, setBlockedProfiles] = useState<Array<{ id: string; nickname: string | null; avatar_url: string | null }>>([]);
  
  useEffect(() => {
    if (blockedList.length === 0) {
      setBlockedProfiles([]);
      return;
    }
    
    const loadProfiles = async () => {
      const { data } = await supabase
        .from('users')
        .select('id, nickname, avatar_url')
        .in('id', blockedList);
      
      if (data) setBlockedProfiles(data);
    };
    
    loadProfiles();
  }, [blockedList]);
  ```
- `import { supabase } from '../../services/supabase';` 추가
- 리스트 아이템에 닉네임 표시 (현재는 userId만 표시)
- handleUnblock을 async로 변경: `onPress: async () => await unblockUser(userId)`

**3. i18n 키 추가 (ko.ts):**
- `block_already: '이미 차단한 사용자입니다'`
- `block_error: '차단에 실패했습니다'`
- `block_success: '차단되었습니다'`
- `unblock_error: '차단 해제에 실패했습니다'`
- `unblock_success: '차단이 해제되었습니다'`
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/workspace/udamon/app && grep -n "supabase" src/contexts/BlockContext.tsx | head -5 && grep -n "user_blocks" src/contexts/BlockContext.tsx && grep -n "supabase" src/screens/settings/BlockedUsersScreen.tsx | head -3 && grep -n "block_already\|block_error\|unblock_success" src/i18n/locales/ko.ts</automated>
  </verify>
  <acceptance_criteria>
    - BlockContext.tsx에 `supabase` import가 존재
    - BlockContext.tsx에 `user_blocks` 문자열이 존재 (테이블명)
    - BlockContext.tsx의 blockUser 함수가 `supabase.from('user_blocks').insert` 호출
    - BlockContext.tsx의 unblockUser 함수가 `supabase.from('user_blocks').delete` 호출
    - BlockContext.tsx에 useEffect로 초기 차단 목록 로드 로직이 존재
    - BlockContext.tsx의 BlockContextValue에서 blockUser 반환 타입이 `Promise<void>`
    - BlockedUsersScreen.tsx에 `supabase` import가 존재
    - BlockedUsersScreen.tsx에 차단된 사용자 프로필 로드 로직이 존재
    - ko.ts에 `block_already` 키가 존재
    - ko.ts에 `unblock_success` 키가 존재
  </acceptance_criteria>
  <done>BlockContext가 Supabase user_blocks 테이블과 완전 연동되어 차단/해제가 DB에 저장되고, BlockedUsersScreen에서 차단된 사용자의 닉네임이 표시된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Edge Function | 탈퇴 요청이 JWT를 통해 인증되어야 함 |
| Edge Function -> auth.admin | service_role 키로 auth.users 삭제 (클라이언트에 노출 불가) |
| Client -> user_blocks RLS | 차단 INSERT/DELETE가 blocker_id = auth.uid() 제약을 통과해야 함 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Elevation of Privilege | delete-account Edge Function | mitigate | JWT에서 user.id를 추출하여 본인 계정만 삭제 가능. service_role 키는 Edge Function 환경변수로만 접근, 클라이언트 코드에 절대 포함 불가 |
| T-02-12 | Tampering | user_blocks insert | mitigate | RLS policy `blocks_insert_own`이 `blocker_id = auth.uid()` 강제. 타인의 blocker_id로 INSERT 불가 |
| T-02-13 | Tampering | user_blocks delete | mitigate | RLS policy `blocks_delete_own`이 `blocker_id = auth.uid()` 강제. 본인의 차단 목록만 삭제 가능 |
| T-02-14 | Information Disclosure | soft deleted user data | accept | is_deleted=true인 사용자의 게시글/댓글은 유지되나 "탈퇴한 사용자"로 표시. Phase 3에서 표시 로직 구현 |
| T-02-15 | Spoofing | block bypass via API | mitigate | RLS 정책이 DB 레벨에서 차단 관계를 강제. community_posts/comments의 SELECT 정책에서 user_blocks 테이블 참조 |
</threat_model>

<verification>
1. `test -f supabase/functions/delete-account/index.ts` 성공
2. Edge Function에 `auth.admin.deleteUser` 존재
3. AccountManagementScreen에 `functions.invoke('delete-account')` 존재
4. BlockContext에 `supabase.from('user_blocks')` 존재
5. BlockedUsersScreen에 사용자 프로필 로드 로직 존재
6. `npx tsc --noEmit` 타입 체크 통과
</verification>

<success_criteria>
- delete-account Edge Function이 JWT 인증 후 soft delete 처리
- AccountManagementScreen에서 2단계 확인 후 탈퇴 요청 가능
- BlockContext가 인메모리에서 Supabase user_blocks 연동으로 전환
- BlockedUsersScreen에서 차단된 사용자 닉네임 표시 + 해제 가능
- 모든 차단/해제가 DB에 영구 저장
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication/02-03-SUMMARY.md`
</output>
