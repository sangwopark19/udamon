---
phase: 04-photographer
plan: 07
type: execute
wave: 5
depends_on: [04-06]
files_modified:
  - docs/phase4-qa-matrix.md
  - docs/dev-environment-setup.md
  - .planning/phases/04-photographer/04-HUMAN-UAT.md
autonomous: false
gap_closure: true
requirements: [PHOT-03, PHOT-04]
user_setup:
  - service: eas-build
    why: "expo-video 3.0.16 native module 을 포함한 dev client 재생성 — managed workflow (CNG) 에서 새 native 의존성 추가 후 필수 단계"
    env_vars: []
    dashboard_config:
      - task: "EAS development 빌드 실행 (Android + iOS)"
        location: "cd app && eas build --profile development --platform android (+ --platform ios)"
      - task: "시뮬레이터/실기기 install"
        location: "eas build:run --platform android 또는 EAS dashboard APK 다운로드 후 drag-drop"
      - task: "새 dev client 로 연결"
        location: "npx expo start --dev-client"

must_haves:
  truths:
    - "EAS development build 가 Android + iOS 양쪽에 대해 ACTIVE 상태로 생성되고 산출물 (.apk / .ipa) 이 다운로드 가능하다"
    - "Android 시뮬레이터 또는 실기기에 새 dev client 가 install 되어 expo-video native module 이 포함된다"
    - "npx expo start --dev-client 로 새 dev client 와 연결하면 앱이 RED screen 없이 정상 부팅된다 (Cannot find native module ExpoVideo 에러 해소)"
    - "UAT Test 1 (Cold Start Smoke Test) 재실행 시 PASS 처리되어 Tests 2~24 진행이 unblock 된다"
    - "docs/phase4-qa-matrix.md 상단에 사전 준비 체크리스트가 추가되어 향후 새 native 모듈 추가 시 동일 운영 단계가 누락되지 않도록 명시된다"
    - "docs/dev-environment-setup.md 에 native-module-rebuild 프로토콜이 추가되어 Phase 5/6 이후에도 재사용 가능하다"
  artifacts:
    - path: "docs/phase4-qa-matrix.md"
      provides: "사전 준비 체크리스트 (EAS dev build 필수) 상단 섹션 추가"
      contains: "사전 준비"
    - path: "docs/dev-environment-setup.md"
      provides: "native-module-rebuild 프로토콜 문서 (신규 생성)"
      contains: "EAS development build"
    - path: ".planning/phases/04-photographer/04-HUMAN-UAT.md"
      provides: "사전 준비 체크리스트 연결 (qa-matrix 참조 추가)"
      contains: "사전 준비"
  key_links:
    - from: "docs/phase4-qa-matrix.md"
      to: "docs/dev-environment-setup.md"
      via: "사전 준비 섹션 내 참조 링크"
      pattern: "dev-environment-setup"
    - from: ".planning/phases/04-photographer/04-HUMAN-UAT.md"
      to: "docs/phase4-qa-matrix.md"
      via: "사전 준비 체크리스트 먼저 확인 안내"
      pattern: "phase4-qa-matrix"
---

<objective>
UAT Test 1 (Cold Start Smoke Test) 을 막고 있는 blocker 해소 — "Cannot find native module 'ExpoVideo'" 크래시는 **코드 버그가 아닌 운영 단계 누락** (Plan 01 에서 `expo-video ~3.0.16` 을 추가했지만 EAS dev build 재생성 없이 기존 dev client / Expo Go 로 실행). 본 Plan 은 (a) 사용자 CLI 를 통한 EAS dev build 재생성 + 시뮬레이터 install + 정상 부팅 검증 체크포인트, (b) 향후 재발 방지를 위한 docs 보강 2가지를 수행한다.

Purpose: UAT Test 1 을 PASS 처리하여 blocked 된 Tests 2~24 (23건) 을 unblock 하고, Phase 4 UAT 를 실질적으로 재개 가능한 상태로 만든다. 동시에 docs 에 native-module-rebuild 프로토콜을 추가하여 Phase 5/6 에서 새 native 모듈 (firebase, sentry 등) 추가 시 동일한 blocker 가 반복되지 않도록 방지한다.

Output:
- EAS development build (Android + iOS) 정상 생성 및 시뮬레이터/실기기 설치 완료
- 앱 fresh start 정상 (ExpoVideo native module 등록 확인)
- `docs/phase4-qa-matrix.md` 상단에 사전 준비 섹션 추가
- `docs/dev-environment-setup.md` 신규 생성 (native-module-rebuild 프로토콜)
- `04-HUMAN-UAT.md` 상단에 사전 준비 참조 추가
- 코드 변경 0건 (debugger 가 5개 파일 검증 완료)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-photographer/04-UAT.md
@.planning/debug/expo-video-native-missing.md
@.planning/phases/04-photographer/04-01-SUMMARY.md
@.planning/phases/04-photographer/04-04-SUMMARY.md
@.planning/phases/04-photographer/04-05-SUMMARY.md
@.planning/phases/04-photographer/04-HUMAN-UAT.md
@docs/phase4-qa-matrix.md
@app/eas.json
@app/package.json
@app/app.json

<interfaces>
<!-- 본 Plan 은 코드 인터페이스 변경 없음. 운영 단계 체크포인트 + docs 보강만 수행. -->
<!-- 사용자 CLI 명령 reference (Plan 01 Task 6 및 debug session 에서 확정된 값) -->

EAS CLI 설정 (app/eas.json):
```json
{
  "cli": { "version": ">= 12.0.0" },
  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "internal",
      "ios": { "simulator": true }
    }
  }
}
```
- development 프로파일: internal distribution, iOS simulator 타겟, developmentClient=true
- 사용자는 `eas login` 선행 필요 (EAS project ID: bdc10dd6-5555-4594-a592-8c1e1ba17e8c, CLAUDE.md Platform Requirements 참조)

expo-video 설치 상태 (Plan 01 Task 6 commit 973a721):
- package.json:31 → "expo-video": "~3.0.16"
- app.json plugins 배열에 "expo-video" 등록됨
- package-lock.json:6608 → "node_modules/expo-video": "3.0.16" 잠금

디버그 세션 root cause (expo-video-native-missing.md):
- 코드/config 5개 검증 모두 정상 (package.json, app.json, VideoPlayer.tsx, eas.json, package-lock.json)
- 원인: app/ios/, app/android/ 부재 → managed workflow (CNG) 확정 → 새 native 모듈은 EAS dev build 재생성 필수
- Expo Go 는 expo-video 3.x 미번들 → EAS dev build 외 대안 없음

UAT blocker (04-UAT.md Gaps):
- truth: "Expo 앱 fresh start 시 크래시 없이 로드되고 Supabase 피드 데이터가 표시됨"
- status: failed
- missing: EAS dev build 재생성, 시뮬레이터 install, dev-client 연결, UAT Test 1 재실행
</interfaces>

</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: EAS development build 재생성 + 시뮬레이터 install + fresh start 검증 (사용자 CLI 작업)</name>
  <files>app/ (no file changes — EAS build 산출물은 EAS 서버 저장)</files>

  <what-built>
    Plan 01 Task 6 에서 `expo-video ~3.0.16` 을 `app/package.json` + `app.json` plugins 에 추가 (commit 973a721) 했으나 — 이 프로젝트는 Expo managed workflow (CNG, `app/ios/`·`app/android/` 부재) 이므로 새 native 의존성 추가 시 EAS development build 를 재생성하여 시뮬레이터/실기기에 재설치해야 native registry 에 ExpoVideo 모듈이 등록됨. 사용자는 UAT 시점에 이 단계를 건너뛰고 기존 (Plan 01 이전 시점) dev client 또는 Expo Go (expo-video 미번들) 로 실행 → 부팅 시 App.tsx → UploadPostScreen → VideoPlayer → `requireNativeModule('ExpoVideo')` throw → [runtime not ready] RED screen 크래시.

    코드/config 5개 검증 (package.json, app.json plugins, VideoPlayer.tsx import/API, eas.json development profile, package-lock.json lock) 은 debug session 에서 모두 정상 확인됨. 본 체크포인트는 순수 운영 단계 수행 체크포인트로, Claude 자동화 불가 (EAS build 는 사용자 계정 인증 + 약 15~30분 대기 + 시뮬레이터 조작 필요).
  </what-built>

  <action>
    **사용자 작업 (Claude 자동화 불가)** — Plan 01 에서 추가한 expo-video 3.0.16 native 모듈을 포함한 EAS development build 를 재생성하고 시뮬레이터에 설치한 뒤 fresh start 를 검증한다. CLI 명령 전체 시퀀스는 <how-to-verify> 참조.

    핵심 작업 5단계:
    1. EAS 계정 로그인 확인 (eas whoami / eas login)
    2. Android + iOS development build 생성 (eas build --profile development, 병렬 15~25분)
    3. 시뮬레이터/실기기 install (eas build:run --profile development)
    4. 새 dev client 로 연결 (npx expo start --dev-client) 후 RED screen 없이 부팅 확인
    5. UAT Test 1 → pass 전환 + Tests 2~24 blocked_by 해제

    코드 변경은 없음 — debug 세션에서 package.json / app.json / VideoPlayer.tsx / eas.json / package-lock.json 5개 파일 모두 정상으로 확인됨. 본 체크포인트는 운영 단계 누락 해소.
  </action>

  <how-to-verify>
    아래 단계를 순서대로 CLI 에서 실행하고 각 결과를 확인해주세요.

    **사전 확인:**
    1. `eas --version` 출력 확인 → `>= 12.0.0` 인지 검증 (app/eas.json cli.version 요구사항)
    2. `eas whoami` 실행 → EAS 계정 로그인 상태 확인. 미로그인 시 `eas login` 수행

    **Step A — Android dev build 생성:**
    3. `cd app && eas build --profile development --platform android --non-interactive`
       - 예상 소요: 15~25분 (Cloud 큐 + 빌드 시간)
       - 빌드 완료 시 EAS dashboard URL 출력 + `.apk` 아티팩트 URL 제공
       - 에러 발생 시 (e.g. expo-video 호환성 경고) 로그 전체를 공유해주세요

    **Step B — iOS simulator dev build 생성 (병렬 가능):**
    4. `cd app && eas build --profile development --platform ios --non-interactive`
       - eas.json 이 `ios.simulator: true` 로 설정되어 있어 Apple Developer 계정 없이 simulator 빌드 가능
       - 예상 소요: 15~25분
       - 빌드 완료 시 `.tar.gz` (simulator app bundle) URL 제공

    **Step C — 시뮬레이터/실기기 install:**
    5. Android:
       - 옵션 A (권장): `eas build:run --platform android --profile development` → 자동 다운로드 + 시뮬레이터 install
       - 옵션 B (수동): EAS dashboard 에서 `.apk` 다운로드 → `adb install <path>` 또는 시뮬레이터 창에 drag-drop
    6. iOS:
       - 옵션 A (권장): `eas build:run --platform ios --profile development` → 자동 다운로드 + simulator 설치
       - 옵션 B (수동): `.tar.gz` 다운로드 + 압축 해제 후 iOS simulator 창에 drag-drop

    **Step D — 새 dev client 연결 + fresh start 검증:**
    7. `cd app && npx expo start --dev-client`
       - QR 코드 또는 URL 표시됨
       - 시뮬레이터의 새 dev client 앱을 열고 해당 URL 로 연결
    8. 앱 로드 결과 확인:
       - **PASS 기준:** RED screen 없이 로그인/홈 화면 진입 (Supabase 피드 데이터 표시)
       - **FAIL 기준:** "Cannot find native module 'ExpoVideo'" 에러 재현 → 즉시 로그 공유 + 이 플랜 중단 후 debugger 재실행

    **Step E — UAT Test 1 공식 재실행:**
    9. `.planning/phases/04-photographer/04-UAT.md` Test 1 결과를 `issue` → `pass` 로 업데이트
    10. blocker 해소 확인 후 Tests 2~24 를 `blocked` → `pending` 으로 일괄 전환 (UAT 재개 준비)

    **결과 보고 양식 (resume-signal 에 포함):**
    ```
    Android build: {SUCCESS|FAIL} — {URL or error excerpt}
    iOS build: {SUCCESS|FAIL} — {URL or error excerpt}
    시뮬레이터 install: {SUCCESS|FAIL}
    Fresh start: {PASS|FAIL} — {에러 있으면 전문}
    UAT Test 1: {pass|issue}
    ```
  </how-to-verify>

  <resume-signal>
    위 Step A~E 결과를 "결과 보고 양식" 대로 보고해주세요. Fresh start PASS 확인되면 "approved" 라고 입력 → Plan 진행. FAIL 시 에러 로그 전문 공유 후 Claude 가 재진단 결정.
  </resume-signal>

  <verify>
    <automated>MISSING — 본 task 는 사용자 CLI 작업 + 실기기 검증이 필수이므로 자동화 불가. 대신 Task 3 에서 UAT Test 1 상태 전환 (issue → pass) 및 04-HUMAN-UAT.md 사전 준비 체크리스트 참조 존재를 자동 grep 으로 검증한다.</automated>
  </verify>

  <done>
    - Android + iOS EAS development build 둘 다 ACTIVE 상태 (EAS dashboard 에서 확인 가능)
    - 시뮬레이터 또는 실기기에 새 dev client 가 install 되어 아이콘이 보임
    - `npx expo start --dev-client` 로 연결 시 RED screen 없이 앱 부팅 성공
    - 04-UAT.md Test 1 결과가 `issue` → `pass` 로 갱신됨
    - Tests 2~24 의 `blocked_by: release-build` 가 해제되어 UAT 재개 준비 완료
    - 코드 변경 0건 (package.json / app.json / VideoPlayer.tsx 수정 없음)
  </done>
</task>

<task type="auto">
  <name>Task 2: docs/dev-environment-setup.md 신규 생성 — native-module-rebuild 프로토콜</name>
  <files>docs/dev-environment-setup.md</files>

  <action>
    `docs/dev-environment-setup.md` 파일을 신규 생성한다. 목적: 향후 Phase 5/6 또는 Quick task 에서 새 native 모듈 (예: `@react-native-firebase/*`, `@sentry/react-native`, `react-native-reanimated` 등) 을 추가할 때 동일한 "Cannot find native module" blocker 가 재발하지 않도록 공식 프로토콜을 문서화한다.

    문서 구조 (한국어 작성, CLAUDE.md Communication 섹션 따름):

    ```markdown
    # Dev Environment Setup — UDAMON 앱

    UDAMON 은 Expo managed workflow (CNG — Continuous Native Generation) 을 사용한다.
    `app/ios/` 와 `app/android/` 디렉토리가 없으며, native code 는 EAS Build 시 동적으로 생성된다.
    이 문서는 신규 native 의존성 추가 시 필수 운영 단계를 정의한다.

    ## Workflow 타입 확인

    ```bash
    ls app/ios app/android 2>/dev/null
    ```

    - 둘 다 부재 → Managed workflow (CNG). native 변경 시 EAS dev build 재생성 필수
    - 둘 다 존재 → Bare workflow. `npx expo run:ios` / `npx expo run:android` 로 로컬 native 빌드 가능

    ## 새 Native 모듈 추가 시 필수 단계

    ### 1. 의존성 설치 + config 등록
    - `cd app && npx expo install <package>` (SDK 호환 버전 자동 선택)
    - `app.json` 의 `plugins` 배열에 플러그인명 추가 (필요한 경우)
    - `package.json` + `package-lock.json` 커밋

    ### 2. EAS development build 재생성 (CRITICAL)
    **이 단계를 건너뛰면 앱 부팅 시 "Cannot find native module 'X'" RED screen 발생한다.**

    ```bash
    cd app
    eas build --profile development --platform android --non-interactive
    eas build --profile development --platform ios --non-interactive
    ```

    - 소요: 15~25분 per 플랫폼 (병렬 실행 권장)
    - 빌드 산출물: `.apk` (Android), `.tar.gz` simulator app bundle (iOS, eas.json simulator=true 설정 시)

    ### 3. 시뮬레이터/실기기 install
    ```bash
    eas build:run --platform android --profile development
    eas build:run --platform ios --profile development
    ```

    또는 EAS dashboard (https://expo.dev) 에서 수동 다운로드 후 drag-drop

    ### 4. Dev client 연결 + fresh start 검증
    ```bash
    cd app && npx expo start --dev-client
    ```

    - 시뮬레이터에서 새 dev client 앱을 열어 URL 로 연결
    - 로그인/홈 화면까지 RED screen 없이 도달하면 통과

    ## Expo Go 사용 불가 케이스

    Expo Go (AppStore 앱) 는 SDK 출시 시점에 번들된 native 모듈만 포함한다. 아래 경우는 EAS dev build 만 사용:

    - SDK 출시 이후 추가된 native 모듈 (expo-video 3.x 는 SDK 54 Expo Go 에 미번들)
    - 커스텀 native 모듈 (3rd party 패키지)
    - Firebase / Sentry 등 통합 SDK

    ## 트러블슈팅

    | 증상 | 원인 | 해결 |
    |------|------|------|
    | `Cannot find native module 'X'` RED screen | 기존 dev client 에 native 모듈 부재 | EAS dev build 재생성 + 시뮬레이터 재설치 |
    | `CocoaPods could not find compatible versions` iOS 빌드 실패 | 의존성 버전 충돌 | `npx expo install --check` 로 SDK 호환 버전 확인 |
    | `Expo Go 에서 앱 크래시` | Expo Go 는 새 native 모듈 미번들 | EAS dev build 사용 |
    | `eas build` 중 authentication 실패 | 세션 만료 | `eas login` 재실행 |

    ## 참고

    - EAS project ID: `bdc10dd6-5555-4594-a592-8c1e1ba17e8c`
    - Build profiles: `app/eas.json` — development (internal + simulator), preview (APK), production (auto-increment)
    - 최근 적용 사례: Phase 4 Plan 01 — expo-video 3.0.16 추가 (commit 973a721)

    ## 관련 문서

    - `docs/phase4-qa-matrix.md` — Phase 4 QA 체크리스트 (사전 준비 포함)
    - `.planning/debug/expo-video-native-missing.md` — 2026-04-15 디버그 세션 (본 프로토콜 출발점)
    ```

    작성 규칙:
    - 한국어 주 본문, 코드블록은 영어 유지
    - 헤더는 `##` 2단계 + `###` 3단계 사용
    - 테이블 형식 유지 (트러블슈팅 섹션)
    - 전체 크기: ~100~120줄 예상
  </action>

  <verify>
    <automated>test -f docs/dev-environment-setup.md && grep -c "EAS development build" docs/dev-environment-setup.md | awk '$1 >= 2 {exit 0} {exit 1}' && grep -q "managed workflow" docs/dev-environment-setup.md && grep -q "Cannot find native module" docs/dev-environment-setup.md && grep -q "expo-video 3.0.16" docs/dev-environment-setup.md</automated>
  </verify>

  <done>
    - `docs/dev-environment-setup.md` 파일이 존재한다
    - 파일 내 "EAS development build" 표현이 2회 이상 등장 (생성 방법 + 트러블슈팅)
    - "managed workflow", "Cannot find native module", "expo-video 3.0.16" 키워드 존재 — Plan 01 실사례 반영 확인
    - 트러블슈팅 테이블이 최소 4개 행 포함 (RED screen, CocoaPods, Expo Go, eas login)
    - CLAUDE.md Communication 규약에 따라 주 본문 한국어 작성
  </done>
</task>

<task type="auto">
  <name>Task 3: phase4-qa-matrix.md + 04-HUMAN-UAT.md 상단에 사전 준비 체크리스트 추가</name>
  <files>docs/phase4-qa-matrix.md, .planning/phases/04-photographer/04-HUMAN-UAT.md</files>

  <action>
    **Step 3-A — `docs/phase4-qa-matrix.md` 상단 수정:**

    현재 파일 (docs/phase4-qa-matrix.md:1~6) 은 다음으로 시작한다:
    ```
    # Phase 4 — Manual QA Matrix

    > Wave 3 완료 전 반드시 통과해야 하는 실기기 검증.
    > 작성: Plan 04-01 (2026-04-15)
    > Target: EAS development 빌드 (iOS + Android)

    ## A. 영상 업로드 (PHOT-03, T-4-02/06)
    ```

    위 `> Target:` 라인 (line 5) 과 `## A.` 헤더 (line 7) 사이에 아래 "사전 준비" 섹션을 삽입한다 (6번 라인 빈 줄은 유지):

    ```markdown
    > Target: EAS development 빌드 (iOS + Android)

    ## 사전 준비 (QA 세션 시작 전 필수)

    > 2026-04-15 UAT Test 1 blocker 재발 방지 — Phase 4 Plan 01 에서 `expo-video ~3.0.16` native 의존성 추가 이후 EAS dev build 재생성 없이 Expo Go / 구 dev client 로 실행하여 "Cannot find native module 'ExpoVideo'" RED screen 크래시 발생.

    - [ ] `ls app/ios app/android` 실행 → 둘 다 부재 확인 (managed workflow / CNG 사용 중)
    - [ ] `eas whoami` → EAS 계정 로그인 확인, 미로그인 시 `eas login`
    - [ ] **신규 native 모듈이 이번 Phase 에 추가되었는지 확인** (아래 중 하나라도 해당):
      - `app/package.json` 의 `expo-*` / `react-native-*` / `@react-native-*` / 네이티브 SDK 패키지가 신규 추가됨
      - `app/app.json` 의 `plugins` 배열에 신규 플러그인 등록됨
      - 이전 빌드와 다른 환경 설정 (env vars 아닌 app config) 변경됨
    - [ ] 위 중 하나라도 해당 → `cd app && eas build --profile development --platform android --non-interactive` 실행 (15~25분)
    - [ ] 병렬: `cd app && eas build --profile development --platform ios --non-interactive`
    - [ ] 빌드 완료 후 `eas build:run --platform android --profile development` + `--platform ios` 로 시뮬레이터 install
    - [ ] `cd app && npx expo start --dev-client` 로 연결 → 앱이 RED screen 없이 부팅하면 QA 매트릭스 시작 가능
    - [ ] 프로토콜 상세: `docs/dev-environment-setup.md` 참조

    ## A. 영상 업로드 (PHOT-03, T-4-02/06)
    ```

    Edit tool 로 line 5~7 의 해당 지점에 삽입한다. 기존 `## A. ~ ## I.` 섹션은 건드리지 않는다.

    **Step 3-B — `.planning/phases/04-photographer/04-HUMAN-UAT.md` 상단 수정:**

    현재 파일 (04-HUMAN-UAT.md:14~17) 은 다음으로 시작한다:
    ```
    ## Tests

    본 UAT는 Phase 4 자동 검증 완료 후 남은 **실기기 QA 매트릭스 9개 섹션**입니다.
    상세 체크리스트: `docs/phase4-qa-matrix.md` (Section A~I)
    ```

    `상세 체크리스트:` 라인 (line 17) 뒤에 아래 1줄을 추가한다:

    ```markdown
    상세 체크리스트: `docs/phase4-qa-matrix.md` (Section A~I)
    **시작 전 필수**: `docs/phase4-qa-matrix.md` 의 "사전 준비" 섹션 전수 확인 (EAS dev build 재생성 누락 시 전 Test 가 blocked 됨 — 2026-04-15 UAT blocker 실사례)
    ```

    작성 규칙:
    - Markdown 포맷 유지 (bold 강조, bullet indentation)
    - 기존 섹션 (Tests 1~9, Summary, Gaps) 건드리지 않음
    - 한국어 본문 + 영어 명령/파일명
  </action>

  <verify>
    <automated>grep -q "사전 준비 (QA 세션 시작 전 필수)" docs/phase4-qa-matrix.md && grep -q "Cannot find native module 'ExpoVideo'" docs/phase4-qa-matrix.md && grep -q "dev-environment-setup.md" docs/phase4-qa-matrix.md && grep -q "시작 전 필수" .planning/phases/04-photographer/04-HUMAN-UAT.md && grep -q "사전 준비" .planning/phases/04-photographer/04-HUMAN-UAT.md</automated>
  </verify>

  <done>
    - `docs/phase4-qa-matrix.md` 에 "사전 준비 (QA 세션 시작 전 필수)" 섹션이 `## A.` 섹션 앞에 추가됨
    - 사전 준비 섹션에 EAS build 명령 3건 (android build / ios build / build:run) 이 체크박스로 명시됨
    - `docs/dev-environment-setup.md` 참조 링크가 사전 준비 섹션 하단에 포함됨
    - `.planning/phases/04-photographer/04-HUMAN-UAT.md` 의 "Tests" 섹션 최상단에 "시작 전 필수" 강조 라인이 추가됨
    - 기존 섹션 (A~I, Tests 1~9, Summary, Gaps) 은 수정되지 않음
    - 사용자는 이후 QA 세션 시작 시 사전 준비 체크리스트를 먼저 확인하게 됨
  </done>
</task>

</tasks>

<verification>

**Gap closure verification — 본 Plan 완료 시점에 아래 모두 TRUE:**

1. **Blocker 해소 검증** (Task 1 체크포인트 통과 조건):
   - EAS dashboard 에서 Android + iOS development build 둘 다 ACTIVE 상태
   - 시뮬레이터에 새 dev client 설치 완료 (앱 아이콘 존재)
   - `npx expo start --dev-client` 로 연결 시 RED screen 없이 로그인/홈 화면 진입
   - 04-UAT.md Test 1 `result: issue` → `result: pass` 로 갱신
   - 04-UAT.md Tests 2~24 의 `blocked_by: release-build` 제거 → UAT 재개 가능

2. **Docs 보강 검증** (Tasks 2~3 완료):
   - `docs/dev-environment-setup.md` 신규 생성됨 (100+ 줄, 5개 섹션)
   - `docs/phase4-qa-matrix.md` 에 사전 준비 섹션 추가 (기존 A~I 섹션 보존)
   - `04-HUMAN-UAT.md` 에 사전 준비 참조 라인 추가
   - 모든 grep 검증 통과:
     - `grep -q "사전 준비" docs/phase4-qa-matrix.md` → 매치
     - `grep -q "EAS development build" docs/dev-environment-setup.md` → 2회 이상 매치
     - `grep -q "dev-environment-setup.md" docs/phase4-qa-matrix.md` → 매치 (cross-link)
     - `grep -q "시작 전 필수" .planning/phases/04-photographer/04-HUMAN-UAT.md` → 매치

3. **코드 변경 없음 검증** (debugger 가 이미 검증):
   - `git diff --name-only HEAD~1 HEAD` 에 `app/` 디렉토리 파일 없음 (`app/package.json`, `app/app.json`, `app/src/**/*` 모두 수정 X)
   - 본 Plan 의 커밋은 `docs/*.md` + `.planning/phases/04-photographer/04-HUMAN-UAT.md` 만 포함

4. **트리거 예방 검증**:
   - 향후 Phase 5/6 에서 새 native 모듈 (Firebase, Sentry 등) 추가 시 `docs/dev-environment-setup.md` 프로토콜 참조 가능
   - Plan 06 이후 Phase 4 gap closure 가 완결됨 → ROADMAP 의 Phase 4 `Plans: 6 plans (5 initial + 1 gap closure)` → `7 plans (5 initial + 2 gap closure)` 로 업데이트

</verification>

<success_criteria>

**Plan 07 완료 조건 (모두 TRUE):**

- [ ] Task 1 체크포인트 통과 — 사용자가 "approved" 응답 + Fresh start PASS 확인
- [ ] EAS Android development build ACTIVE 상태 (eas build list 로 확인 가능)
- [ ] EAS iOS development build ACTIVE 상태
- [ ] 새 dev client 에서 앱 부팅 시 "Cannot find native module 'ExpoVideo'" 에러 재현 불가
- [ ] 04-UAT.md Test 1 상태 `pass` 로 전환
- [ ] 04-UAT.md Tests 2~24 `blocked_by: release-build` 해제 (UAT 재개 준비)
- [ ] `docs/dev-environment-setup.md` 신규 생성 (자동 grep 4건 통과)
- [ ] `docs/phase4-qa-matrix.md` 사전 준비 섹션 추가 (grep 3건 통과)
- [ ] `.planning/phases/04-photographer/04-HUMAN-UAT.md` 사전 준비 참조 추가 (grep 2건 통과)
- [ ] 코드 변경 0건 — `app/` 디렉토리 수정 없음 (debugger 확인한 5개 파일 검증 유지)
- [ ] 3개 커밋 생성 (Task 1 은 checkpoint 성격이라 commit 없음, Tasks 2~3 각 1커밋씩 + gap closure 종합 커밋 가능)

**실패 조건 (아래 중 하나라도 해당 시 Plan 재진단 필요):**

- Fresh start 에서 여전히 ExpoVideo RED screen → debugger 재실행 (코드/설정 재검토)
- EAS build 가 다른 에러 (CocoaPods, gradle, JS bundle) 로 실패 → 에러 로그 기반 디버깅
- docs 수정 중 tsc / lint 에러 발생 → .md 파일은 lint 대상이 아니므로 무시 (만약 lint 가 걸리면 pre-commit hook 재검증)

</success_criteria>

<output>
After completion, create `.planning/phases/04-photographer/04-07-SUMMARY.md` with:
- Fresh start blocker 해소 증명 (EAS build URL + install 스크린샷 reference)
- UAT Test 1 → pass 전환 타임스탬프
- Tests 2~24 unblock 확인
- Docs 3 파일 변경 diff 요약
- Phase 4 gap closure 완결 선언 (Plan 06 + Plan 07 합산 — HI-01/02/03 + native-build blocker)
- requirements-completed: [PHOT-03, PHOT-04] (video native module 이 정상 로드되는 것이 두 requirement 의 실기기 검증 전제)
- Next phase readiness: Phase 4 UAT 완전 재개 가능 → 23개 테스트 실행
</output>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User CLI → EAS cloud | 사용자 EAS 계정 credentials 로 빌드 트리거 — 인증/권한은 EAS 플랫폼이 관리 |
| EAS cloud → 시뮬레이터/기기 | 빌드 산출물 (.apk/.tar.gz) 을 로컬로 다운로드 + install — 네트워크 경로는 HTTPS |
| Dev client ↔ Metro bundler | `npx expo start --dev-client` 의 JS bundle 로컬 통신 — 개발 환경만 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-07-01 | I (Information Disclosure) | EAS build artifacts | accept | "internal distribution" 설정 (eas.json development profile) — EAS 대시보드 로그인 계정만 다운로드 가능 |
| T-4-07-02 | T (Tampering) | docs/*.md 수정 | accept | Markdown docs 는 CI/runtime 영향 없음, 의도적 변경이며 git 히스토리로 추적 |
| T-4-07-03 | R (Repudiation) | UAT 결과 조작 | accept | 04-UAT.md 변경은 git 커밋으로 기록됨, 동일 세션 내 연속 작업이므로 저위험 |
| T-4-07-04 | D (DoS) | EAS build 큐 대기 | accept | 사용자 1명이 2개 빌드 트리거 — EAS 무료 티어 quota 내 (free tier 30 builds/month 기준 저위험) |
| T-4-07-05 | E (Elevation of Privilege) | dev client install | accept | developmentClient + internal distribution — 일반 사용자 배포 아님, 개발자 기기만 해당 |

보안 관점에서 본 Plan 은 "순수 운영 단계 + 문서 보강" 이므로 신규 공격 표면 0. 기존 Phase 4 STRIDE (T-4-01~T-4-08) 에 영향 없음.
</threat_model>