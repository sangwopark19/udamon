---
phase: 04-photographer
plan: 02
type: execute
wave: 1
depends_on:
  - "04-01"
files_modified:
  - app/src/types/photographer.ts
  - app/src/types/cheerleader.ts
  - app/src/types/photographerApplication.ts
  - app/src/services/photographerApi.ts
  - app/src/services/r2Upload.ts
  - app/src/utils/photographerGrade.ts
  - app/src/utils/__tests__/photographerGrade.test.ts
autonomous: true
requirements:
  - PHOT-01
  - PHOT-02
  - PHOT-03
  - PHOT-05
  - PHOT-06
  - PHOT-07
  - PHOT-08
threats: []
user_setup: []

must_haves:
  truths:
    - "app/src/types/photographer.ts 의 PhotoPost 타입이 videos: string[], thumbnail_urls: string[], grade: number 필드를 포함"
    - "app/src/types/cheerleader.ts 가 DB schema(name_ko/name_en/position/status/image_url/team_id) 기준으로 재정의됨 — 기존 name/description 필드 제거"
    - "app/src/types/photographerApplication.ts 신규 파일 — PhotographerApplication 타입 정의 (id, user_id, team_id, activity_links, activity_plan, portfolio_url, bio, status, rejection_reason, reviewed_at, created_at)"
    - "app/src/services/photographerApi.ts 에 submitPhotographerApplication, fetchMyPhotographerApplication, fetchCheerleaders, fetchCollectionPosts 4개 함수가 신규 export 됨"
    - "fetchPhotoPosts 가 { teamSlug?, page?, pageSize? } 파라미터를 받고 .range(from, to) 로 20개씩 반환 (D-23)"
    - "createPhotoPost 시그니처에 videos: string[] 파라미터 추가됨 (D-05)"
    - "mapPhotographer 가 grade 필드를 계산해서 반환 (post_count + floor(follower_count/10))"
    - "mapPhotoPost 가 videos / thumbnail_urls 를 row 에서 매핑하여 반환"
    - "mapCheerleader 신규 — DB row → Cheerleader 타입 매핑 (name_ko → displayName, name_en → displayNameEn, team_id UUID → slug)"
    - "mapApplication 신규 — DB row → PhotographerApplication"
    - "모든 mapRow* 함수가 Row 인터페이스 기반으로 타입 안전 (any 제거)"
    - "photographerGrade 유틸 — calculateGrade(post, follower), gradeToBadge(grade) 2개 함수"
    - "r2Upload.ts 의 uploadPostVideos 가 asset 별 contentType 을 동적으로 받아 처리 (ADJ-02 — mp4/quicktime 둘 다)"
    - "photographerGrade.test.ts 가 green (0/5/20/50 임계값 + edge cases)"
    - "cd app && npx tsc --noEmit 에서 Plan 02 수정 대상 파일 (photographerApi.ts, r2Upload.ts, types/photographer.ts, types/cheerleader.ts, types/photographerApplication.ts, utils/photographerGrade.ts) 자체 에러 0 — 단, Task 1 의 Cheerleader 타입 재정의 (name → name_ko, description 제거) 로 인한 외부 사용처 (UploadPostScreen, PhotographerCard 등) 의 typecheck 오류는 Plan 04 에서 해소되므로 본 Plan 검증 범위 밖"
  artifacts:
    - path: "app/src/services/photographerApi.ts"
      provides: "확장된 photographer 서비스 레이어 — 심사, 치어리더, 페이지네이션, 영상, 썸네일 지원"
      min_lines: 600
      exports:
        - "submitPhotographerApplication"
        - "fetchMyPhotographerApplication"
        - "fetchCheerleaders"
        - "fetchCollectionPosts"
        - "fetchPhotoPosts"
        - "createPhotoPost"
        - "togglePhotoLike"
        - "toggleFollow"
    - path: "app/src/utils/photographerGrade.ts"
      provides: "등급 계산 + 배지 정보"
      min_lines: 40
      exports:
        - "calculateGrade"
        - "gradeToBadge"
        - "GradeInfo"
    - path: "app/src/utils/__tests__/photographerGrade.test.ts"
      provides: "grade util 단위 테스트"
      min_lines: 50
    - path: "app/src/types/cheerleader.ts"
      provides: "DB schema 기준 Cheerleader 타입"
      contains: "name_ko"
    - path: "app/src/types/photographerApplication.ts"
      provides: "PhotographerApplication 타입"
      contains: "activity_links"
  key_links:
    - from: "app/src/services/photographerApi.ts"
      to: "app/src/utils/photographerGrade.ts"
      via: "import { calculateGrade } — mapPhotographer 에서 grade 필드 계산"
      pattern: "calculateGrade"
    - from: "app/src/services/photographerApi.ts"
      to: "app/src/types/photographerApplication.ts"
      via: "import type { PhotographerApplication }"
      pattern: "PhotographerApplication"
---

<objective>
Wave 1 — Wave 0 가 배포한 DB 스키마 / Edge Function 을 소비하는 **서비스 레이어 + 타입 + 유틸** 을 구축한다. 이 Plan 은 **순수 코드 레벨 (Context/화면 미수정)** 이므로 병렬 실행 가능하나 Plan 01 의 schema 없이는 런타임 쿼리 실패 → depends_on: ["04-01"].

Purpose:
- Plan 03 (PhotographerContext 전환) 이 import 할 새 함수/타입 을 먼저 확정
- Plan 04/05 (화면) 이 사용할 GradeBadge/VideoPlayer 전에 `calculateGrade` 유틸 분리
- 현재 `photographerApi.ts` 의 `(row: any)` any 사용을 Row 인터페이스로 교체 (CLAUDE.md strict typing)
- D-23 페이지네이션 파라미터를 서비스 레이어에 도입 (FlatList onEndReached 대응)
- ADJ-02 반영: uploadPostVideos 가 mp4/quicktime asset 의 contentType 을 동적으로 전달 받도록 시그니처 수정
- D-20 반영: Cheerleader 타입을 DB schema 기준으로 재정의 (기존 name/description 제거)

Output:
- 3개 타입 파일 (photographer.ts 확장, cheerleader.ts 재정의, photographerApplication.ts 신규)
- photographerApi.ts 대대적 확장
- r2Upload.ts uploadPostVideos 시그니처 수정
- photographerGrade util + unit test

Addresses: PHOT-01 (서비스 레이어 mock 의존 제거 준비), PHOT-02 (submit/fetchMyApplication API), PHOT-03 (videos 파라미터), PHOT-05 (thumbnail_urls 매핑), PHOT-06 (grade util), PHOT-07 (cheerleaders DB fetch), PHOT-08 (fetchCollectionPosts).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-photographer/04-CONTEXT.md
@.planning/phases/04-photographer/04-RESEARCH.md
@.planning/phases/04-photographer/04-VALIDATION.md
@.planning/phases/04-photographer/04-UI-SPEC.md
@.planning/phases/04-photographer/04-01-SUMMARY.md
@CLAUDE.md

# 기존 코드 (수정 대상 + 참조)
@app/src/services/photographerApi.ts
@app/src/services/r2Upload.ts
@app/src/types/photographer.ts
@app/src/types/cheerleader.ts
@app/src/services/supabase.ts

# 패턴 참조 (Phase 3)
@app/src/services/communityApi.ts

<interfaces>
<!-- Plan 03 이 import 할 새 API 시그니처 (Wave 0 스키마 가정) -->

```ts
// app/src/types/photographer.ts (확장)
export interface PhotoPost {
  // 기존 필드 유지 + 추가:
  videos: string[];           // D-05
  thumbnail_urls: string[];   // D-12~D-15
  // Photographer 측 grade 는 별도 — PhotoPost 에는 photographer.grade 또는 임베드된 photographer 조인 활용
}

export interface Photographer {
  // 기존 유지 + 추가:
  grade: number;              // D-17: post_count + floor(follower_count/10)
}
```

```ts
// app/src/types/cheerleader.ts (재정의 — D-20, RESEARCH §Pitfall 6)
export interface Cheerleader {
  id: string;
  team_id: string;        // slug (teamUuidToSlug 변환 후)
  name_ko: string;
  name_en: string | null;
  position: 'leader' | 'member';
  status: 'active' | 'retired';
  image_url: string | null;
}
```

```ts
// app/src/types/photographerApplication.ts (신규)
export type ApplicationStatus = 'pending' | 'approved' | 'rejected';

export interface PhotographerApplication {
  id: string;
  user_id: string;
  team_id: string | null;          // slug
  portfolio_url: string | null;
  bio: string;
  activity_links: string[];
  activity_plan: string;
  status: ApplicationStatus;
  rejection_reason: string | null;
  reviewed_by: string | null;
  reviewed_at: string | null;
  created_at: string;
  updated_at: string;
}
```

```ts
// app/src/services/photographerApi.ts (신규 export)
export async function submitPhotographerApplication(params: {
  user_id: string;
  team_slug: string | null;
  activity_links: string[];
  activity_plan: string;
  portfolio_url?: string | null;
  bio?: string;
}): Promise<ApiResult<PhotographerApplication>>;

export async function fetchMyPhotographerApplication(
  userId: string,
): Promise<ApiResult<PhotographerApplication | null>>;

export async function fetchCheerleaders(): Promise<ApiResult<Cheerleader[]>>;

export async function fetchCollectionPosts(collectionId: string): Promise<ApiResult<PhotoPost[]>>;

export async function fetchPhotoPosts(params: {
  teamSlug?: string;
  page?: number;
  pageSize?: number;
}): Promise<ApiResult<PhotoPost[]>>;

export async function createPhotoPost(params: {
  photographerId: string;
  teamSlug: string;
  playerId: string | null;
  cheerleaderId: string | null;
  title: string;
  description: string;
  images: string[];
  videos: string[];       // D-05
}): Promise<ApiResult<PhotoPost>>;
```

```ts
// app/src/services/r2Upload.ts — uploadPostVideos 시그니처 (ADJ-02)
export async function uploadPostVideos(
  userId: string,
  localUris: string[],
  accessToken: string,
  contentTypes: string[],   // asset 별 mime (mp4/quicktime) — localUris 와 1:1
): Promise<UploadResult>;
```

```ts
// app/src/utils/photographerGrade.ts
export interface GradeInfo {
  grade: number;
  tier: 'bronze' | 'silver' | 'gold' | 'diamond';
  label: string;
  iconColor: string;
  iconName: 'medal-outline' | 'medal' | 'diamond';
}
export function calculateGrade(postCount: number, followerCount: number): number;
export function gradeToBadge(grade: number): GradeInfo;
```
</interfaces>
</context>

<threat_model>
## STRIDE Threat Register

Wave 1 은 순수 클라이언트 서비스 레이어 — 서버 권한 우회 경로 없음. Wave 0 에서 배포된 RLS (pg_insert_own, posts_insert_own, photographer_apps_insert_own) 가 모든 쿼리 경계를 방어. 본 plan 은 기존 RLS 를 무시하지 않는 쿼리만 생성 (모두 `supabase` 싱글톤 사용, service role 키 미사용).

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (no new threats) | — | — | accept | Wave 0 RLS + T-4-04/07 가 이미 mitigation 제공. 본 plan 의 fetch/create 함수는 모두 사용자 토큰 으로 동작 |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 타입 재정의 + 신규 타입 파일</name>
  <read_first>
    - app/src/types/photographer.ts (현재 PhotoPost / Photographer 정의)
    - app/src/types/cheerleader.ts (현재 name/description — 교체 대상)
    - supabase/migrations/017_cheerleaders.sql (Phase 1 DB schema)
    - supabase/migrations/016_photographer_apps.sql + 031 (team_id/activity_links/activity_plan)
    - .planning/phases/04-photographer/04-RESEARCH.md §Pitfall 6 (Cheerleader 타입 불일치 근거)
    - .planning/phases/04-photographer/04-UI-SPEC.md §GradeBadge (grade 필드 위치)
    - app/src/screens/photographer/UploadPostScreen.tsx (cheerleader.name 사용처 — Plan 04 에서 name_ko 로 변경될 예정, 본 plan 은 타입만 정리)
  </read_first>
  <behavior>
    - Test 1: PhotoPost 타입이 videos / thumbnail_urls string[] 필드 소유
    - Test 2: Photographer 타입이 grade: number 필드 소유
    - Test 3: Cheerleader 타입이 name_ko / name_en / position / status / team_id 필드 소유하고 name / description 필드 부재
    - Test 4: PhotographerApplication 타입이 activity_links string[] / activity_plan string / status ApplicationStatus 필드 소유
  </behavior>
  <files>app/src/types/photographer.ts, app/src/types/cheerleader.ts, app/src/types/photographerApplication.ts</files>
  <action>
**Step 1-A — app/src/types/photographer.ts 수정:**

기존 파일에서 `PhotoPost` 인터페이스에 아래 2 필드 추가 + `Photographer` 에 grade 추가:

```typescript
// 기존 PhotoPost 인터페이스에 추가:
videos: string[];                // D-01 / D-05: R2 public URL 배열, max 3
thumbnail_urls: string[];        // D-12~D-15: generate-thumbnails EF 가 채움. 빈 경우 images[0] fallback

// cheerleader 조인 필드 — DB 에 name_ko 추가 매핑 (D-20)
cheerleader?: {
  name_ko: string;               // 기존 name → name_ko
} | null;
```

`Photographer` 인터페이스에 추가:

```typescript
grade: number;                   // D-17: post_count + floor(follower_count / 10) — mapPhotographer 가 계산
```

`HomeFeedItem` discriminated union 유지 — 수정 없음.

**Step 1-B — app/src/types/cheerleader.ts 전면 교체:**

기존 파일 내용 전체를 아래로 교체 (D-20 / RESEARCH Pitfall 6):

```typescript
// ─── Cheerleader (DB schema 기준, 017_cheerleaders.sql) ─────────
export type CheerleaderPosition = 'leader' | 'member';
export type CheerleaderStatus = 'active' | 'retired';

export interface Cheerleader {
  id: string;
  team_id: string;                // slug (UUID → slug 변환 후)
  name_ko: string;
  name_en: string | null;
  position: CheerleaderPosition;
  status: CheerleaderStatus;
  image_url: string | null;
}
```

**주의:** 기존 `name` / `description` 필드는 제거. 이들을 참조하는 모든 사용처는 Plan 04 (화면) 에서 `name_ko` 로 일괄 변경. 본 plan 에서는 typecheck 오류 가 발생해도 현 시점은 OK — Plan 04 의 선행 의존성.

**Step 1-C — app/src/types/photographerApplication.ts 신규 생성:**

```typescript
// ─── Photographer Application (D-07 Wave 0 스키마 기준) ─────────
export type ApplicationStatus = 'pending' | 'approved' | 'rejected';

export interface PhotographerApplication {
  id: string;
  user_id: string;
  team_id: string | null;          // slug (mapApplication 이 UUID → slug 변환)
  portfolio_url: string | null;
  bio: string;
  activity_links: string[];
  activity_plan: string;
  status: ApplicationStatus;
  rejection_reason: string | null;
  reviewed_by: string | null;
  reviewed_at: string | null;
  created_at: string;
  updated_at: string;
}
```

**Step 1-D — mapPhotoPost.cheerleader 의 기존 `name` 필드는 UI 수정까지 남겨둠:** Plan 04 (화면) 에서 `cheerleader?.name_ko` 로 사용처 변경 완료 시 이 중간 상태 제거. Task 2 의 mapPhotoPost 는 `cheerleader: row.cheerleader ? { name_ko: row.cheerleader.name_ko } : null` 로 변경 (기존 `name` → `name_ko`).
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit 2>&1 | grep -E "(photographer.ts|cheerleader.ts|photographerApplication.ts)" | wc -l 이 0 (파일 내부 오류 없음 — 사용처 오류는 Plan 04 에서 해결되므로 검증 대상 외). grep -q "videos:" app/src/types/photographer.ts + grep -q "thumbnail_urls:" + grep -q "grade:" 모두 match. grep -q "name_ko:" app/src/types/cheerleader.ts + ! grep -q "description:" (파일 자체에는 description 없음). ls app/src/types/photographerApplication.ts 존재</automated>
  </verify>
  <done>
    - PhotoPost 에 videos + thumbnail_urls string[] 필드 추가
    - Photographer 에 grade: number 필드 추가
    - Cheerleader 타입 재정의 (name_ko/name_en/position/status/team_id, name/description 제거)
    - PhotographerApplication 타입 신규 생성
    - photographer.ts 자체 내 typecheck 오류 없음 (외부 사용처 오류는 Plan 04 에서 해결)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: photographerGrade util + unit test (D-17, D-18, PHOT-06)</name>
  <read_first>
    - .planning/phases/04-photographer/04-RESEARCH.md §Code Example 3 (calculateGrade / gradeToBadge 시그니처)
    - .planning/phases/04-photographer/04-UI-SPEC.md §Color §GradeBadge (임계값 0/5/20/50 + tier 색상)
    - .planning/phases/04-photographer/04-CONTEXT.md §D-17, §D-18
    - app/package.json (jest 29.7 + jest-expo preset 사용 — Phase 3 검증됨)
  </read_first>
  <behavior>
    - Test 1: calculateGrade(0, 0) === 0
    - Test 2: calculateGrade(5, 0) === 5
    - Test 3: calculateGrade(0, 50) === 5 (follower/10 기여)
    - Test 4: calculateGrade(3, 25) === 3 + 2 = 5
    - Test 5: calculateGrade(-1, -5) === 0 (음수 방어 — Math.max(0, ...))
    - Test 6: calculateGrade(10.7, 25.3) === 10 + 2 = 12 (Math.floor)
    - Test 7: gradeToBadge(0).tier === 'bronze', iconColor === '#A97142', iconName === 'medal-outline'
    - Test 8: gradeToBadge(4).tier === 'bronze'
    - Test 9: gradeToBadge(5).tier === 'silver', iconColor === '#6B7280'
    - Test 10: gradeToBadge(19).tier === 'silver'
    - Test 11: gradeToBadge(20).tier === 'gold', iconColor === '#FACC15', iconName === 'medal'
    - Test 12: gradeToBadge(49).tier === 'gold'
    - Test 13: gradeToBadge(50).tier === 'diamond', iconColor === '#1B2A4A', iconName === 'diamond'
    - Test 14: gradeToBadge(100).tier === 'diamond'
  </behavior>
  <files>app/src/utils/photographerGrade.ts, app/src/utils/__tests__/photographerGrade.test.ts</files>
  <action>
**Step 2-A — app/src/utils/photographerGrade.ts 신규 생성 (RESEARCH §Code Example 3 verbatim):**

```typescript
// ─── Photographer Grade Utilities (D-17, D-18) ─────────────────
// Source: Phase 4 RESEARCH §Code Example 3
// Used by: GradeBadge 컴포넌트 (Plan 04), mapPhotographer (Task 3)

export interface GradeInfo {
  grade: number;
  tier: 'bronze' | 'silver' | 'gold' | 'diamond';
  label: string;
  iconColor: string;
  iconName: 'medal-outline' | 'medal' | 'diamond';
}

/**
 * 등급 스코어 = post_count + floor(follower_count / 10)
 * 음수/소수는 안전하게 0으로 clamp + floor 처리.
 */
export function calculateGrade(postCount: number, followerCount: number): number {
  const safePostCount = Math.max(0, Math.floor(postCount));
  const safeFollowerCount = Math.max(0, Math.floor(followerCount));
  return safePostCount + Math.floor(safeFollowerCount / 10);
}

/**
 * 등급 스코어 → 배지 정보 (UI-SPEC §GradeBadge tier palette).
 * 임계값: 0~4 브론즈 / 5~19 실버 / 20~49 골드 / 50+ 다이아.
 */
export function gradeToBadge(grade: number): GradeInfo {
  if (grade >= 50) {
    return { grade, tier: 'diamond', label: '다이아', iconColor: '#1B2A4A', iconName: 'diamond' };
  }
  if (grade >= 20) {
    return { grade, tier: 'gold', label: '골드', iconColor: '#FACC15', iconName: 'medal' };
  }
  if (grade >= 5) {
    return { grade, tier: 'silver', label: '실버', iconColor: '#6B7280', iconName: 'medal-outline' };
  }
  return { grade, tier: 'bronze', label: '브론즈', iconColor: '#A97142', iconName: 'medal-outline' };
}
```

**Step 2-B — app/src/utils/__tests__/photographerGrade.test.ts 신규 생성:**

```typescript
import { calculateGrade, gradeToBadge } from '../photographerGrade';

describe('calculateGrade', () => {
  it('both zero → 0', () => {
    expect(calculateGrade(0, 0)).toBe(0);
  });
  it('posts only', () => {
    expect(calculateGrade(5, 0)).toBe(5);
  });
  it('followers contribute floor(/10)', () => {
    expect(calculateGrade(0, 50)).toBe(5);
  });
  it('sum of posts + floor(followers/10)', () => {
    expect(calculateGrade(3, 25)).toBe(5);
  });
  it('negative clamps to 0', () => {
    expect(calculateGrade(-1, -5)).toBe(0);
  });
  it('fractional inputs are floored', () => {
    expect(calculateGrade(10.7, 25.3)).toBe(12);
  });
});

describe('gradeToBadge', () => {
  it('grade 0 → bronze', () => {
    const b = gradeToBadge(0);
    expect(b.tier).toBe('bronze');
    expect(b.iconColor).toBe('#A97142');
    expect(b.iconName).toBe('medal-outline');
  });
  it('grade 4 → bronze (boundary)', () => {
    expect(gradeToBadge(4).tier).toBe('bronze');
  });
  it('grade 5 → silver', () => {
    const s = gradeToBadge(5);
    expect(s.tier).toBe('silver');
    expect(s.iconColor).toBe('#6B7280');
  });
  it('grade 19 → silver (boundary)', () => {
    expect(gradeToBadge(19).tier).toBe('silver');
  });
  it('grade 20 → gold', () => {
    const g = gradeToBadge(20);
    expect(g.tier).toBe('gold');
    expect(g.iconColor).toBe('#FACC15');
    expect(g.iconName).toBe('medal');
  });
  it('grade 49 → gold (boundary)', () => {
    expect(gradeToBadge(49).tier).toBe('gold');
  });
  it('grade 50 → diamond', () => {
    const d = gradeToBadge(50);
    expect(d.tier).toBe('diamond');
    expect(d.iconColor).toBe('#1B2A4A');
    expect(d.iconName).toBe('diamond');
  });
  it('grade 100 → diamond', () => {
    expect(gradeToBadge(100).tier).toBe('diamond');
  });
});
```
  </action>
  <verify>
    <automated>cd app && npm test -- --testPathPattern=photographerGrade --no-coverage 가 14 tests passed 결과로 green</automated>
  </verify>
  <done>
    - photographerGrade.ts exports calculateGrade + gradeToBadge + GradeInfo
    - __tests__/photographerGrade.test.ts 14개 테스트 green
    - 임계값 0/5/20/50 정확히 매핑
    - 음수/소수 방어 (Math.max 0 + Math.floor)
    - UI-SPEC §GradeBadge tier palette 일치 (bronze #A97142, silver #6B7280, gold #FACC15, diamond #1B2A4A)
  </done>
</task>

<task type="auto">
  <name>Task 3: photographerApi 확장 + r2Upload.uploadPostVideos 시그니처 (D-05, D-17, D-20, D-23, ADJ-02)</name>
  <read_first>
    - app/src/services/photographerApi.ts (현재 전체 — 수정 대상)
    - app/src/services/r2Upload.ts (uploadPostVideos 기존 contentType 기본값)
    - app/src/services/communityApi.ts (Phase 3 페이지네이션 `.range(from, to)` 패턴 — mirror 대상)
    - .planning/phases/04-photographer/04-RESEARCH.md §Code Example 1/2 (submitPhotographerApplication / fetchMyPhotographerApplication), §Pattern 4 (페이지네이션)
    - .planning/phases/04-photographer/04-RESEARCH.md §Pitfall 6 (Cheerleader 타입 불일치 해결)
    - .planning/phases/04-photographer/04-CONTEXT.md §D-05 (createPhotoPost.videos), §D-17 (mapPhotographer.grade), §D-20 (fetchCheerleaders), §D-23 (fetchPhotoPosts 페이지네이션)
    - supabase/migrations/007_photographer.sql (photo_posts, photo_collection_posts 조인 구조)
    - supabase/migrations/017_cheerleaders.sql (cheerleaders DB 컬럼)
  </read_first>
  <files>app/src/services/photographerApi.ts, app/src/services/r2Upload.ts</files>
  <action>
**Step 3-A — app/src/services/photographerApi.ts 대대적 확장:**

기존 파일에서 아래 변경사항을 적용한다. 파일 구조 (import → types → mappers → API functions) 는 유지.

**(1) Import 확장:**

```typescript
import { supabase } from './supabase';
import type { Photographer, PhotoPost, Player, TimelineEvent } from '../types/photographer';
import type { Cheerleader } from '../types/cheerleader';
import type { PhotographerApplication, ApplicationStatus } from '../types/photographerApplication';
import type { PhotoComment, PgCollection } from '../contexts/PhotographerContext';
import { calculateGrade } from '../utils/photographerGrade';
```

**(2) Row 인터페이스 추가 — any 제거 (CLAUDE.md strict typing):**

```typescript
interface PhotographerRow {
  id: string;
  user_id: string;
  display_name: string;
  bio: string | null;
  avatar_url: string | null;
  cover_url: string | null;
  team_id: string | null;
  follower_count: number | null;
  post_count: number | null;
  is_verified: boolean | null;
  created_at: string;
}

interface PhotoPostRow {
  id: string;
  photographer_id: string;
  team_id: string;
  player_id: string | null;
  cheerleader_id: string | null;
  title: string;
  description: string | null;
  images: string[];
  videos: string[] | null;
  thumbnail_urls: string[] | null;
  like_count: number | null;
  comment_count: number | null;
  view_count: number | null;
  is_featured: boolean | null;
  featured_week: string | null;
  status: string;
  rejection_reason: string | null;
  created_at: string;
  updated_at: string;
  photographer?: { display_name: string; avatar_url: string | null; is_verified: boolean } | null;
  team?: { name_ko: string } | null;
  player?: { name_ko: string; number: number | null } | null;
  cheerleader?: { name_ko: string } | null;
}

interface CheerleaderRow {
  id: string;
  team_id: string;
  name_ko: string;
  name_en: string | null;
  position: 'leader' | 'member';
  status: 'active' | 'retired';
  image_url: string | null;
}

interface ApplicationRow {
  id: string;
  user_id: string;
  team_id: string | null;
  portfolio_url: string | null;
  bio: string | null;
  activity_links: string[] | null;
  activity_plan: string | null;
  status: string;
  rejection_reason: string | null;
  reviewed_by: string | null;
  reviewed_at: string | null;
  created_at: string;
  updated_at: string;
}
```

기존 `mapPhotographer(row: any)` / `mapPhotoPost(row: any)` 등의 시그니처를 `row: PhotographerRow` / `row: PhotoPostRow` 로 교체.

**(3) mapPhotographer — grade 계산 추가 (D-17):**

```typescript
function mapPhotographer(row: PhotographerRow): Photographer {
  const postCount = row.post_count ?? 0;
  const followerCount = row.follower_count ?? 0;
  return {
    id: row.id,
    user_id: row.user_id,
    display_name: row.display_name,
    bio: row.bio ?? '',
    avatar_url: row.avatar_url,
    cover_url: row.cover_url,
    team_id: teamUuidToSlug(row.team_id),
    follower_count: followerCount,
    post_count: postCount,
    is_verified: row.is_verified ?? false,
    grade: calculateGrade(postCount, followerCount),
    created_at: row.created_at,
  };
}
```

**(4) mapPhotoPost — videos / thumbnail_urls 매핑 + cheerleader.name_ko (D-05, D-15, D-20):**

```typescript
function mapPhotoPost(row: PhotoPostRow): PhotoPost {
  const pg = row.photographer;
  const team = row.team;
  const player = row.player;
  return {
    id: row.id,
    photographer_id: row.photographer_id,
    team_id: teamUuidToSlug(row.team_id) ?? row.team_id,
    player_id: row.player_id,
    cheerleader_id: row.cheerleader_id ?? null,
    title: row.title,
    description: row.description ?? '',
    images: row.images ?? [],
    videos: row.videos ?? [],                  // D-05
    thumbnail_urls: row.thumbnail_urls ?? [],  // D-12~D-15
    like_count: row.like_count ?? 0,
    comment_count: row.comment_count ?? 0,
    view_count: row.view_count ?? 0,
    is_featured: row.is_featured ?? false,
    featured_week: row.featured_week,
    status: (row.status ?? 'approved') as PhotoPost['status'],
    rejection_reason: row.rejection_reason ?? null,
    created_at: row.created_at,
    updated_at: row.updated_at,
    photographer: pg
      ? { display_name: pg.display_name, avatar_url: pg.avatar_url, is_verified: pg.is_verified ?? false }
      : { display_name: '알 수 없음', avatar_url: null, is_verified: false },
    team: team ? { name_ko: team.name_ko } : { name_ko: '' },
    player: player ? { name_ko: player.name_ko, number: player.number ?? 0 } : null,
    cheerleader: row.cheerleader ? { name_ko: row.cheerleader.name_ko } : null,  // D-20
  };
}
```

**(5) mapCheerleader 신규 (D-20):**

```typescript
function mapCheerleader(row: CheerleaderRow): Cheerleader {
  return {
    id: row.id,
    team_id: teamUuidToSlug(row.team_id) ?? row.team_id,
    name_ko: row.name_ko,
    name_en: row.name_en,
    position: row.position,
    status: row.status,
    image_url: row.image_url,
  };
}
```

**(6) mapApplication 신규 (D-07):**

```typescript
function mapApplication(row: ApplicationRow): PhotographerApplication {
  return {
    id: row.id,
    user_id: row.user_id,
    team_id: teamUuidToSlug(row.team_id),
    portfolio_url: row.portfolio_url,
    bio: row.bio ?? '',
    activity_links: row.activity_links ?? [],
    activity_plan: row.activity_plan ?? '',
    status: (row.status ?? 'pending') as ApplicationStatus,
    rejection_reason: row.rejection_reason,
    reviewed_by: row.reviewed_by,
    reviewed_at: row.reviewed_at,
    created_at: row.created_at,
    updated_at: row.updated_at,
  };
}
```

**(7) fetchPhotoPosts — 페이지네이션 (D-23):**

기존 `fetchPhotoPosts()` 를 아래로 교체:

```typescript
export async function fetchPhotoPosts(params: {
  teamSlug?: string;
  page?: number;
  pageSize?: number;
} = {}): Promise<ApiResult<PhotoPost[]>> {
  try {
    await ensureSlugMaps();
    const page = params.page ?? 0;
    const pageSize = params.pageSize ?? 20;
    const from = page * pageSize;
    const to = from + pageSize - 1;

    let query = supabase
      .from('photo_posts')
      .select(`
        *,
        photographer:photographers(display_name, avatar_url, is_verified),
        team:teams(name_ko),
        player:players(name_ko, number),
        cheerleader:cheerleaders(name_ko)
      `)
      .eq('status', 'approved')
      .order('created_at', { ascending: false })
      .range(from, to);

    if (params.teamSlug && params.teamSlug !== 'all') {
      const teamUuid = teamSlugToUuid(params.teamSlug);
      if (teamUuid) query = query.eq('team_id', teamUuid);
    }

    const { data, error } = await query;
    if (error) return { data: null, error: error.message };
    return { data: (data ?? []).map((r) => mapPhotoPost(r as PhotoPostRow)), error: null };
  } catch (e: unknown) {
    const msg = e instanceof Error ? e.message : 'fetchPhotoPosts failed';
    return { data: null, error: msg };
  }
}
```

**(8) createPhotoPost — videos/cheerleaderId 파라미터 (D-05):**

기존 `createPhotoPost` 시그니처 전면 교체:

```typescript
export async function createPhotoPost(params: {
  photographerId: string;
  teamSlug: string;
  playerId: string | null;
  cheerleaderId: string | null;
  title: string;
  description: string;
  images: string[];
  videos: string[];                 // D-05 신규
}): Promise<ApiResult<PhotoPost>> {
  try {
    await ensureSlugMaps();
    const teamUuid = teamSlugToUuid(params.teamSlug);
    if (!teamUuid) return { data: null, error: 'Invalid team slug' };

    const { data, error } = await supabase
      .from('photo_posts')
      .insert({
        photographer_id: params.photographerId,
        team_id: teamUuid,
        player_id: params.playerId || null,
        cheerleader_id: params.cheerleaderId || null,
        title: params.title,
        description: params.description,
        images: params.images,
        videos: params.videos,
      })
      .select(`
        *,
        photographer:photographers(display_name, avatar_url, is_verified),
        team:teams(name_ko),
        player:players(name_ko, number),
        cheerleader:cheerleaders(name_ko)
      `)
      .single();
    if (error) return { data: null, error: error.message };
    return { data: mapPhotoPost(data as PhotoPostRow), error: null };
  } catch (e: unknown) {
    const msg = e instanceof Error ? e.message : 'createPhotoPost failed';
    return { data: null, error: msg };
  }
}
```

**(9) submitPhotographerApplication 신규 (D-08):**

```typescript
export async function submitPhotographerApplication(params: {
  user_id: string;
  team_slug: string | null;
  activity_links: string[];
  activity_plan: string;
  portfolio_url?: string | null;
  bio?: string;
}): Promise<ApiResult<PhotographerApplication>> {
  try {
    await ensureSlugMaps();
    const teamUuid = params.team_slug ? teamSlugToUuid(params.team_slug) : null;

    const { data, error } = await supabase
      .from('photographer_applications')
      .insert({
        user_id: params.user_id,
        team_id: teamUuid,
        activity_links: params.activity_links,
        activity_plan: params.activity_plan,
        portfolio_url: params.portfolio_url ?? null,
        bio: params.bio ?? '',
      })
      .select('*')
      .single();
    if (error) return { data: null, error: error.message };
    return { data: mapApplication(data as ApplicationRow), error: null };
  } catch (e: unknown) {
    const msg = e instanceof Error ? e.message : 'submitPhotographerApplication failed';
    return { data: null, error: msg };
  }
}
```

**(10) fetchMyPhotographerApplication 신규 (D-09):**

```typescript
export async function fetchMyPhotographerApplication(
  userId: string,
): Promise<ApiResult<PhotographerApplication | null>> {
  try {
    await ensureSlugMaps();
    const { data, error } = await supabase
      .from('photographer_applications')
      .select('*')
      .eq('user_id', userId)
      .order('created_at', { ascending: false })
      .limit(1)
      .maybeSingle();
    if (error) return { data: null, error: error.message };
    if (!data) return { data: null, error: null };
    return { data: mapApplication(data as ApplicationRow), error: null };
  } catch (e: unknown) {
    const msg = e instanceof Error ? e.message : 'fetchMyPhotographerApplication failed';
    return { data: null, error: msg };
  }
}
```

**(11) fetchCheerleaders 신규 (D-20):**

```typescript
export async function fetchCheerleaders(): Promise<ApiResult<Cheerleader[]>> {
  try {
    await ensureSlugMaps();
    const { data, error } = await supabase
      .from('cheerleaders')
      .select('*')
      .eq('status', 'active')
      .order('name_ko', { ascending: true });
    if (error) return { data: null, error: error.message };
    return { data: (data ?? []).map((r) => mapCheerleader(r as CheerleaderRow)), error: null };
  } catch (e: unknown) {
    const msg = e instanceof Error ? e.message : 'fetchCheerleaders failed';
    return { data: null, error: msg };
  }
}
```

**(12) fetchCollectionPosts 신규 (D-21 / PHOT-08):**

```typescript
export async function fetchCollectionPosts(collectionId: string): Promise<ApiResult<PhotoPost[]>> {
  try {
    await ensureSlugMaps();
    const { data, error } = await supabase
      .from('photo_collection_posts')
      .select(`
        post_id,
        post:photo_posts(
          *,
          photographer:photographers(display_name, avatar_url, is_verified),
          team:teams(name_ko),
          player:players(name_ko, number),
          cheerleader:cheerleaders(name_ko)
        )
      `)
      .eq('collection_id', collectionId)
      .order('added_at', { ascending: false });
    if (error) return { data: null, error: error.message };
    const posts = (data ?? [])
      .map((r) => r.post as PhotoPostRow | null)
      .filter((p): p is PhotoPostRow => p !== null)
      .map(mapPhotoPost);
    return { data: posts, error: null };
  } catch (e: unknown) {
    const msg = e instanceof Error ? e.message : 'fetchCollectionPosts failed';
    return { data: null, error: msg };
  }
}
```

**(13) uploadPostImages/uploadPostVideos export:**

기존 `export { uploadPostImages } from './r2Upload';` 아래에 `uploadPostVideos` 도 re-export 추가:

```typescript
export { uploadPostImages, uploadPostVideos } from './r2Upload';
```

**(14) 모든 catch 블록의 `e: any` → `e: unknown` 변환:**

파일 전체의 `catch (e: any)` 를 `catch (e: unknown) { const msg = e instanceof Error ? e.message : 'operation failed'; return { data: null, error: msg }; }` 패턴으로 교체 (Phase 3 communityApi.ts 패턴 mirror).

**Step 3-B — app/src/services/r2Upload.ts uploadPostVideos 시그니처 수정 (ADJ-02):**

기존 `uploadPostVideos(userId, localUris, accessToken, contentType = 'video/mp4')` 를 아래로 교체:

```typescript
export async function uploadPostVideos(
  userId: string,
  localUris: string[],
  accessToken: string,
  contentTypes: string[],   // ADJ-02: asset 별 mime (mp4 또는 quicktime), localUris 와 1:1 길이
): Promise<UploadResult> {
  try {
    if (contentTypes.length !== localUris.length) {
      return { data: null, error: 'contentTypes length must match localUris length' };
    }
    const publicUrls: string[] = [];

    // get-upload-url 은 호출당 동일 contentType 가정 — asset 별로 개별 호출
    for (let i = 0; i < localUris.length; i++) {
      const contentType = contentTypes[i];
      if (contentType !== 'video/mp4' && contentType !== 'video/quicktime') {
        return { data: null, error: `Unsupported video mime: ${contentType}` };
      }
      const { uploads } = await getPresignedUrls(accessToken, 'photo-posts', contentType, 1);
      await putToR2(uploads[0].uploadUrl, localUris[i], contentType);
      publicUrls.push(uploads[0].publicUrl);
    }

    return { data: publicUrls, error: null };
  } catch (e: unknown) {
    const message = e instanceof Error ? e.message : 'Upload failed';
    return { data: null, error: message };
  }
}
```

주의:
- `getPresignedUrls` 가 단일 contentType + count 를 받으므로 asset 별로 count=1 호출 (기존 일괄 호출과 달리 mp4+mov 혼합 가능하게).
- 기존 기본값 `contentType = 'video/mp4'` 제거 — 명시적 전달 강제.
- userId 파라미터는 유지 (기존 시그니처 호환). Edge Function 이 token 에서 userId 를 다시 추출하므로 클라이언트가 보낼 필요는 없음, 하지만 시그니처 호환성 + 로그 식별자로 유지.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit 2>&1 | grep "error TS" | grep -E "(src/services/photographerApi|src/services/r2Upload|src/utils/photographerGrade|src/types/photographer\\.ts|src/types/cheerleader\\.ts|src/types/photographerApplication)" | wc -l 이 0 (Plan 02 수정 대상 파일 스코프 내 에러 없음 — Plan 04 가 해결할 외부 사용처 오류는 본 verify 범위 밖). grep -c "e: any" app/src/services/photographerApi.ts 가 0. grep -c "export async function" app/src/services/photographerApi.ts 가 최소 15개 이상. grep -q "calculateGrade" app/src/services/photographerApi.ts. grep -q "contentTypes: string\\[\\]" app/src/services/r2Upload.ts</automated>
  </verify>
  <done>
    - photographerApi.ts 에 submitPhotographerApplication, fetchMyPhotographerApplication, fetchCheerleaders, fetchCollectionPosts 신규 export
    - fetchPhotoPosts 가 { teamSlug, page, pageSize } 파라미터로 변경됨 (D-23)
    - createPhotoPost 에 videos + cheerleaderId 파라미터 추가됨 (D-05)
    - mapPhotographer 가 grade 필드 반환 (D-17)
    - mapPhotoPost 가 videos / thumbnail_urls 매핑 (D-05, D-15)
    - mapCheerleader + mapApplication 신규 함수
    - 모든 `any` → Row 인터페이스 또는 unknown 으로 교체
    - r2Upload.uploadPostVideos 가 contentTypes string[] 배열을 받음 (ADJ-02)
    - cd app && npx tsc --noEmit 에서 Plan 02 수정 대상 파일 (photographerApi.ts, r2Upload.ts, photographerGrade.ts, types/photographer.ts, types/cheerleader.ts, types/photographerApplication.ts) 자체 에러 0. 외부 사용처 (UploadPostScreen, PhotographerCard 등) 의 cheerleader.name → name_ko / isRemote 제거 / videos 파라미터 전달 관련 오류는 Plan 04 가 해소 — 본 Plan 검증 스코프 밖
  </done>
</task>

</tasks>

<verification>
1. `cd app && npx tsc --noEmit` 실행 결과 Plan 02 수정 파일 범위 (photographerApi.ts, r2Upload.ts, utils/photographerGrade.ts, types/photographer.ts, types/cheerleader.ts, types/photographerApplication.ts) 자체 오류 없음. 외부 사용처 오류 (Cheerleader.name → name_ko, isRemote 제거, videos 파라미터 등) 는 Plan 04 에서 해소되므로 verify 스코프 밖.
2. `cd app && npm test -- --testPathPattern=photographerGrade` 14 tests green
3. PhotographerContext 또는 화면 사용처 에서 발생하는 오류는 본 Plan 에서 의도된 것 — Plan 03/04/05 에서 해결
4. `git diff app/src/services/photographerApi.ts` 에 새로운 함수 6개 + 기존 2개 수정 확인
5. ADJ-02 반영: r2Upload.uploadPostVideos 가 동적 contentTypes 수용
6. CLAUDE.md 준수: 모든 catch 가 e instanceof Error 내로잉 + Row 인터페이스로 any 제거
</verification>

<success_criteria>
- [ ] PhotoPost / Photographer / Cheerleader / PhotographerApplication 타입 정의 완성
- [ ] photographerApi.ts 에 Phase 4 필수 신규 함수 4개 + 수정 함수 2개 반영
- [ ] r2Upload.ts uploadPostVideos 가 contentTypes[] 동적 수용
- [ ] photographerGrade util + 14 tests green
- [ ] mapPhotographer 가 grade 계산 포함
- [ ] ADJ-02 (mp4+quicktime) 적용
- [ ] Plan 02 수정 대상 파일 (photographerApi.ts, r2Upload.ts, photographerGrade.ts, types/photographer.ts, types/cheerleader.ts, types/photographerApplication.ts) 자체 typecheck 에러 0
- [ ] 외부 사용처 typecheck 오류 (UploadPostScreen, PhotographerCard 에서 cheerleader.name 접근 / isRemote prop / videos 파라미터 미전달 등) 는 Plan 04 수행 후 해소 — 본 Plan 검증 스코프 밖임을 명시
</success_criteria>

<output>
완료 후 `.planning/phases/04-photographer/04-02-SUMMARY.md` 생성 — 추가된 export 목록 + Row 인터페이스 전환 통계 + 하위 Plan 03 이 import 할 진입점 안내 (특히 submitPhotographerApplication / fetchMyPhotographerApplication / fetchCheerleaders / fetchCollectionPosts / 페이지네이션 fetchPhotoPosts).
</output>
</content>
</invoke>