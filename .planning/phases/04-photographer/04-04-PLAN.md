---
phase: 04-photographer
plan: 04
type: execute
wave: 3
depends_on:
  - "04-03"
files_modified:
  - app/src/components/common/VideoPlayer.tsx
  - app/src/components/photographer/GradeBadge.tsx
  - app/src/components/photographer/PhotographerCard.tsx
  - app/src/screens/photographer/UploadPostScreen.tsx
  - app/src/screens/photographer/__tests__/UploadPostScreen.validateVideoAsset.test.ts
  - app/src/i18n/locales/ko.ts
  - app/src/styles/theme.ts
autonomous: true
requirements:
  - PHOT-03
  - PHOT-04
  - PHOT-05
  - PHOT-06
threats:
  - T-4-02
  - T-4-06
  - T-4-08
user_setup: []

must_haves:
  truths:
    - "app/src/components/common/VideoPlayer.tsx 가 expo-video(useVideoPlayer + VideoView) 기반으로 구현되고 mode: feed/detail/studio prop 을 지원"
    - "app/src/components/photographer/GradeBadge.tsx 가 UI-SPEC tier palette (bronze/silver/gold/diamond) 를 gradeToBadge util 기반으로 렌더"
    - "GradeBadge 는 variant (icon | icon-label) + size (sm | md) 조합을 지원"
    - "UploadPostScreen.handleAddVideo 가 duration>30s / fileSize>50MB / mime 미허용(mp4+quicktime 아님) 세 케이스에서 Alert 로 차단하고 videos[] 에 추가 안 함 (D-03 + ADJ-02)"
    - "UploadPostScreen.doPublish 가 Phase 3 D-09 R2-first 순서: optimizeImage → uploadPostImages → uploadPostVideos(contentTypes) → createPhotoPost(images, videos) → generate-thumbnails 호출(fire-and-forget)"
    - "uploadPostVideos 호출 시 각 video asset 의 contentType 을 asset.mimeType 또는 확장자 fallback 으로 string[] 배열로 전달 (ADJ-02)"
    - "업로드 실패 시 Alert + 폼 유지 (Phase 3 D-18 패턴) — navigation.goBack 미호출"
    - "generate-thumbnails Edge Function 호출은 await 없이 fire-and-forget + .catch"
    - "PhotographerCard 에 GradeBadge variant='icon' 20×20 이 display_name 뒤 + is_verified 아이콘 앞에 렌더"
    - "bronze 색상 #A97142 이 colors 토큰에 추가됨 (theme.ts)"
    - "i18n ko.ts 에 upload_video_* / grade_tier_* / grade_a11y_label / video_unplayable 신규 키 포함"
    - "cheerleader.name 참조 → cheerleader.name_ko 로 변경된 사용처 모두 업데이트 (UploadPostScreen, PhotographerCard)"
    - "UploadPostScreen 에서 isRemote prop 사용 제거"
    - "cd app && npx tsc --noEmit 수정 대상 파일들에서 오류 0 (Plan 05 이관 오류 외)"
  artifacts:
    - path: "app/src/components/common/VideoPlayer.tsx"
      provides: "expo-video 기반 재생 래퍼 + 에러/로딩 상태"
      min_lines: 80
      exports:
        - "VideoPlayer (default)"
        - "VideoPlayerProps (type)"
    - path: "app/src/components/photographer/GradeBadge.tsx"
      provides: "UI-SPEC 기반 등급 배지 컴포넌트"
      min_lines: 70
      exports:
        - "GradeBadge (default)"
        - "GradeBadgeProps (type)"
  key_links:
    - from: "app/src/components/common/VideoPlayer.tsx"
      to: "expo-video"
      via: "useVideoPlayer + VideoView"
      pattern: "useVideoPlayer|VideoView"
    - from: "app/src/components/photographer/GradeBadge.tsx"
      to: "app/src/utils/photographerGrade.ts"
      via: "gradeToBadge(grade)"
      pattern: "gradeToBadge"
    - from: "app/src/screens/photographer/UploadPostScreen.tsx"
      to: "app/src/services/r2Upload.ts::uploadPostVideos"
      via: "contentTypes string[] 배열 전달 (ADJ-02)"
      pattern: "uploadPostVideos"
    - from: "app/src/screens/photographer/UploadPostScreen.tsx"
      to: "supabase/functions/generate-thumbnails"
      via: "fetch fire-and-forget after createPhotoPost"
      pattern: "generate-thumbnails"
---

<objective>
Wave 3a — 공통 컴포넌트 (VideoPlayer, GradeBadge) 를 구축하고 UploadPostScreen 의 영상 업로드 흐름을 완성한다. Plan 03 의 Context 재작성으로 인한 UploadPostScreen/PhotographerCard 의 TypeScript 오류도 본 Plan 에서 정리.

Purpose:
- 새 컴포넌트 2개 (VideoPlayer, GradeBadge) 는 Plan 05 의 Studio/Profile 화면에서 즉시 사용됨 → Wave 3a 에서 먼저 구현
- UploadPostScreen 은 Phase 4 의 사용자 여정 핵심 (영상 업로드 전 검증 + R2 first + 썸네일 트리거) — 단일 화면 내 3개 흐름 (image/video/thumbnail) 통합
- ADJ-02 반영: asset 별 contentType (mp4/quicktime) 동적 전달
- D-14 fire-and-forget: generate-thumbnails 호출은 await 없음
- T-4-08 mitigation 은 기존 (line 60-63 `https://`/`http://` prefix 검증) — Plan 05 PhotographerRegisterScreen 에서 재확인

Output:
- VideoPlayer 컴포넌트 (expo-video 기반, feed/detail/studio 모드)
- GradeBadge 컴포넌트 (UI-SPEC tier palette)
- UploadPostScreen 전면 영상 흐름 구현
- PhotographerCard GradeBadge 통합
- bronze 토큰 추가 + i18n 신규 키

Addresses: PHOT-03 (영상 클라이언트 검증 + R2 업로드), PHOT-04 (VideoPlayer 재생), PHOT-05 (thumbnail_urls 렌더 fallback + generate-thumbnails 호출), PHOT-06 (GradeBadge).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-photographer/04-CONTEXT.md
@.planning/phases/04-photographer/04-RESEARCH.md
@.planning/phases/04-photographer/04-UI-SPEC.md
@.planning/phases/04-photographer/04-VALIDATION.md
@.planning/phases/04-photographer/04-03-SUMMARY.md
@CLAUDE.md

# 수정 대상
@app/src/screens/photographer/UploadPostScreen.tsx
@app/src/styles/theme.ts
@app/src/i18n/locales/ko.ts

# 참조 (서비스 레이어 — Plan 02)
@app/src/services/photographerApi.ts
@app/src/services/r2Upload.ts
@app/src/utils/photographerGrade.ts
@app/src/types/cheerleader.ts

# 참조 (UI 패턴)
@app/src/components/common/ProgressiveImage.tsx
@app/src/components/photographer/PhotographerCard.tsx

<interfaces>
```ts
// VideoPlayer props (UI-SPEC §VideoPlayer)
interface VideoPlayerProps {
  uri: string;
  mode: 'feed' | 'detail' | 'studio';
  width: number;
  height: number;
  isVisible?: boolean;        // feed mode viewport-aware play/pause
  onError?: (e: Error) => void;
}
```

```ts
// GradeBadge props (UI-SPEC §GradeBadge)
interface GradeBadgeProps {
  grade: number;
  variant?: 'icon' | 'icon-label';
  size?: 'sm' | 'md';
}
```

```ts
// UploadPostScreen handleAddVideo 동작
const VIDEO_MAX_DURATION_MS = 30_000;
const VIDEO_MAX_SIZE_BYTES = 50 * 1024 * 1024;
const ALLOWED_VIDEO_MIME = ['video/mp4', 'video/quicktime']; // ADJ-02

// videos state + videoContentTypes state (병렬) — 업로드 시 쌍으로 전달
const [videos, setVideos] = useState<string[]>([]);
const [videoContentTypes, setVideoContentTypes] = useState<string[]>([]);
```
</interfaces>
</context>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-02 | DoS (영상 50MB 초과) | UploadPostScreen.handleAddVideo | mitigate | Task 3: asset.fileSize > 50*1024*1024 시 Alert 차단 (fileSize undefined 시 서버 측 50MB 상향 안전망 — Plan 01 T2) |
| T-4-06 | Tampering (iOS .mov 우회) | UploadPostScreen.handleAddVideo | mitigate | Task 3: ALLOWED_VIDEO_MIME = ['video/mp4','video/quicktime'] 체크, 그 외 거부. 서버 측 get-upload-url ALLOWED_TYPES 가 이중 방어 |
| T-4-08 | XSS (activity_links javascript:) | (Plan 05 PhotographerRegisterScreen) | accept (deferred) | 본 plan 범위 외 — 기존 `https://`/`http://` prefix 검증 (RegisterScreen line 60-63) 유지. Plan 05 에서 테스트 포함 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: VideoPlayer 컴포넌트 + theme.ts bronze 토큰 + i18n 키 (PHOT-04, D-16)</name>
  <read_first>
    - app/src/styles/theme.ts (colors 토큰 구조)
    - app/src/i18n/locales/ko.ts (기존 키 스타일 snake_case)
    - .planning/phases/04-photographer/04-UI-SPEC.md §VideoPlayer §Copywriting Contract
    - .planning/phases/04-photographer/04-RESEARCH.md §Pattern 1 (expo-video + FlatList viewport-aware)
    - app/package.json (expo-video 설치 확인 — Plan 01 Task 6 완료)
  </read_first>
  <files>app/src/components/common/VideoPlayer.tsx, app/src/styles/theme.ts, app/src/i18n/locales/ko.ts</files>
  <action>
**Step 1-A — theme.ts 에 bronze 토큰 추가:**

`app/src/styles/theme.ts` 의 `colors` 객체 안, `star` 또는 `warning` 근처에 추가 (UI-SPEC §Color note):

```typescript
// UI-SPEC §Color: bronze 는 Phase 4 의 유일한 신규 hex (GradeBadge tier 브론즈 시그니파이어)
bronze: '#A97142',
```

**Step 1-B — i18n 키 추가 (app/src/i18n/locales/ko.ts):**

UI-SPEC §Copywriting Contract 테이블 전체를 반영. 기존 `upload_*` / `pg_*` 키와 충돌 안 하도록 확인 후 새 키 추가:

```typescript
// Phase 4 신규 키 (UI-SPEC §Copywriting Contract)
upload_video_label: '동영상 (선택)',
upload_video_hint: '최대 3개 · 각 30초 이하 · 50MB 이하 · MP4',
upload_add_video_prompt: '동영상 추가',
upload_video_too_long_title: '영상 길이 초과',
upload_video_too_long_desc: '30초 이하 영상만 업로드할 수 있어요. 다른 영상을 선택해 주세요.',
upload_video_too_large_title: '파일 크기 초과',
upload_video_too_large_desc: '50MB 이하 파일만 업로드할 수 있어요. 영상 길이를 줄이거나 다른 영상을 선택해 주세요.',
upload_video_unsupported_format_title: '지원하지 않는 형식',
upload_video_unsupported_format_desc: 'MP4 형식 영상만 업로드할 수 있어요.',
upload_video_upload_failed_title: '영상 업로드 실패',
upload_video_upload_failed_desc: '네트워크 상태를 확인하고 다시 시도해 주세요.',
upload_max_videos: '최대 3개까지 업로드 가능',
upload_max_videos_desc: '영상은 최대 {{max}}개까지 첨부할 수 있어요.',
video_unplayable: '재생할 수 없는 영상입니다',

// Plan 05 도 사용하는 UI-SPEC §Studio keys — 여기서 함께 추가
pg_register_submit: '포토그래퍼 신청하기',
pg_register_pending_title: '심사 대기 중입니다',
pg_register_pending_desc: '신청이 접수되었어요. 영업일 기준 2~3일 이내에 결과를 알려드립니다.',
pg_register_pending_go_home: '홈으로 돌아가기',
pg_register_reapply: '재신청하기',
pg_register_fail: '신청에 실패했습니다',
pg_register_fail_desc: '잠시 후 다시 시도해 주세요.',
studio_pending_title: '포토그래퍼 신청 심사 중',
studio_pending_desc: '관리자가 신청을 확인하고 있어요. 영업일 기준 2~3일 이내에 결과를 안내드립니다.',
studio_pending_notification_hint: '결과는 알림 탭에서 확인할 수 있어요',
studio_rejected_title: '신청이 거절되었어요',
studio_rejected_reason_label: '거절 사유',
studio_rejected_default_reason: '제출된 자료가 요구사항을 충족하지 못했습니다. 내용을 보완해 재신청해 주세요.',
studio_signup_cta: '포토그래퍼로 활동하기',
studio_signup_desc: '직관 사진과 영상을 팬들과 나눠보세요.',
pg_feed_empty: '아직 게시물이 없어요',
pg_feed_empty_desc: '첫 번째 사진을 올려 팬들과 공유해 보세요.',
pg_feed_empty_upload: '사진 업로드하기',
pg_feed_loading_failed: '불러오지 못했어요',
pg_feed_loading_retry: '다시 시도',

// GradeBadge (UI-SPEC §Copywriting)
grade_tier_bronze: '브론즈',
grade_tier_silver: '실버',
grade_tier_gold: '골드',
grade_tier_diamond: '다이아',
grade_a11y_label: '{{tier}} 등급',

// Tab labels (UI-SPEC §MainTabNavigator)
tab_photographer: '포토그래퍼',
tab_pending_review: '심사중',
tab_studio: '스튜디오',
```

**Step 1-C — VideoPlayer 컴포넌트 신규 생성 (UI-SPEC §VideoPlayer + RESEARCH §Pattern 1):**

`app/src/components/common/VideoPlayer.tsx`:

```typescript
import React, { useEffect, useState } from 'react';
import { View, StyleSheet, ActivityIndicator } from 'react-native';
import { Ionicons } from '@expo/vector-icons';
import { useTranslation } from 'react-i18next';
import { useVideoPlayer, VideoView } from 'expo-video';
import { colors, fontSize } from '../../styles/theme';

export interface VideoPlayerProps {
  uri: string;
  mode: 'feed' | 'detail' | 'studio';
  width: number;
  height: number;
  isVisible?: boolean;       // feed 모드 viewport-aware play/pause
  onError?: (e: Error) => void;
}

export default function VideoPlayer({ uri, mode, width, height, isVisible = true, onError }: VideoPlayerProps) {
  const { t } = useTranslation();
  const [isLoading, setIsLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);

  const player = useVideoPlayer(uri, (p) => {
    if (mode === 'feed') {
      p.muted = true;
      p.loop = true;
    } else {
      p.muted = false;
      p.loop = false;
    }
  });

  // feed 모드 viewport-aware: isVisible 변경 시 play/pause
  useEffect(() => {
    if (mode !== 'feed') return;
    try {
      if (isVisible) {
        player.play();
      } else {
        player.pause();
      }
    } catch (e) {
      console.warn('[VideoPlayer] play/pause failed', e);
    }
  }, [isVisible, mode, player]);

  // 로딩/에러 이벤트 리스너
  useEffect(() => {
    const statusSub = player.addListener('statusChange', ({ status }) => {
      if (status === 'readyToPlay') setIsLoading(false);
      if (status === 'error') {
        const err = new Error('Video playback failed');
        setError(err);
        setIsLoading(false);
        onError?.(err);
      }
    });
    return () => {
      statusSub.remove();
    };
  }, [player, onError]);

  if (error) {
    return (
      <View style={[styles.placeholder, { width, height }]}>
        <Ionicons name="alert-circle-outline" size={24} color={colors.textTertiary} />
        <ActivityIndicator style={{ display: 'none' }} />
      </View>
    );
  }

  return (
    <View style={{ width, height }}>
      <VideoView
        player={player}
        style={{ width, height }}
        nativeControls={mode === 'detail'}
        allowsPictureInPicture={mode === 'detail'}
        contentFit="cover"
      />
      {isLoading && (
        <View style={[StyleSheet.absoluteFillObject, styles.loadingOverlay]}>
          <ActivityIndicator color={colors.primary} size="small" />
        </View>
      )}
    </View>
  );
}

const styles = StyleSheet.create({
  placeholder: {
    backgroundColor: colors.surface,
    justifyContent: 'center',
    alignItems: 'center',
  },
  loadingOverlay: {
    backgroundColor: colors.surface,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
```

주의사항:
- `expo-video` 의 `addListener('statusChange', ...)` 인터페이스는 SDK 54 버전에 따라 약간 다를 수 있음 — Plan 01 Task 6 에서 설치한 expo-video 패키지의 실제 타입 정의 확인 필요. 시그니처 불일치 시 `player.status` polling 또는 `addListener('statusChange', ({ status }) => {...})` 형태로 조정.
- RESEARCH.md §Pattern 1 Anti-pattern 경고: 단일 player 를 여러 VideoView 에 공유 금지. 본 컴포넌트는 prop `uri` 당 독립 player 인스턴스 생성 → FlatList 내 각 item 이 별도 VideoPlayer → 별도 player → Android 충돌 방지.
- `contentFit="cover"` 는 UI-SPEC 의 width/height 고정 박스 대응.
- `onError` prop 은 UploadPostScreen 의 실패 카운팅 용으로 제공 (현재는 로그만).
  </action>
  <verify>
    <automated>ls app/src/components/common/VideoPlayer.tsx 존재 + 60줄 이상. grep -q "useVideoPlayer" app/src/components/common/VideoPlayer.tsx. grep -q "bronze:" app/src/styles/theme.ts. grep -q "upload_video_too_long_title" app/src/i18n/locales/ko.ts. grep -q "grade_tier_bronze" app/src/i18n/locales/ko.ts. cd app && npx tsc --noEmit 에서 VideoPlayer.tsx 오류 0</automated>
  </verify>
  <done>
    - VideoPlayer.tsx 신규 생성, useVideoPlayer + VideoView 사용
    - mode 별 autoplay/muted/loop/nativeControls 분기 동작
    - loading / error state UI 구현
    - feed 모드 isVisible prop 으로 viewport-aware play/pause
    - theme.ts 에 bronze '#A97142' 추가
    - ko.ts 에 UI-SPEC §Copywriting 33개 키 추가
    - typecheck 자체 오류 없음
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GradeBadge 컴포넌트 + PhotographerCard 통합 (D-18, PHOT-06)</name>
  <read_first>
    - .planning/phases/04-photographer/04-UI-SPEC.md §GradeBadge (props/layout/iconography)
    - .planning/phases/04-photographer/04-RESEARCH.md §Code Example 4 (GradeBadge 구현 예시)
    - app/src/components/photographer/PhotographerCard.tsx (현재 display_name + is_verified 렌더 위치 확인)
    - app/src/utils/photographerGrade.ts (Plan 02 에서 생성)
    - app/src/styles/theme.ts (Plan 04 Task 1 에서 bronze 추가, featuredAccent/featuredAlpha20/primaryAlpha8/surfaceLight 확인)
  </read_first>
  <behavior>
    - Test 1: variant='icon' → Ionicons 20×20 단독 (tier 색상)
    - Test 2: variant='icon-label' size='sm' → padding H8 V4, icon 12, label 11px, fontWeight 700
    - Test 3: variant='icon-label' size='md' → padding H12 V8, icon 16, label 13px
    - Test 4: tier='bronze' → pill bg colors.surfaceLight, label colors.textSecondary
    - Test 5: tier='gold' → pill bg colors.featuredAlpha20, label colors.featuredAccent
    - Test 6: accessibilityLabel = t('grade_a11y_label', { tier }) 형식
  </behavior>
  <files>app/src/components/photographer/GradeBadge.tsx, app/src/components/photographer/PhotographerCard.tsx</files>
  <action>
**Step 2-A — GradeBadge 신규 생성 (UI-SPEC §GradeBadge + RESEARCH §Code Example 4):**

`app/src/components/photographer/GradeBadge.tsx`:

```typescript
import React from 'react';
import { View, Text, StyleSheet } from 'react-native';
import { Ionicons } from '@expo/vector-icons';
import { useTranslation } from 'react-i18next';
import { colors, fontSize, fontWeight, radius, spacing } from '../../styles/theme';
import { gradeToBadge, type GradeInfo } from '../../utils/photographerGrade';

export interface GradeBadgeProps {
  grade: number;
  variant?: 'icon' | 'icon-label';
  size?: 'sm' | 'md';
}

export default function GradeBadge({ grade, variant = 'icon-label', size = 'sm' }: GradeBadgeProps) {
  const { t } = useTranslation();
  const info = gradeToBadge(grade);
  const isMd = size === 'md';

  if (variant === 'icon') {
    return (
      <Ionicons
        name={info.iconName}
        size={20}
        color={info.iconColor}
        accessibilityLabel={t('grade_a11y_label', { tier: info.label })}
      />
    );
  }

  const iconSize = isMd ? 16 : 12;
  const labelSize = isMd ? fontSize.meta : fontSize.badge;

  return (
    <View
      style={[
        styles.pill,
        isMd ? styles.pillMd : styles.pillSm,
        { backgroundColor: gradeBgColor(info.tier) },
      ]}
      accessibilityLabel={t('grade_a11y_label', { tier: info.label })}
    >
      <Ionicons name={info.iconName} size={iconSize} color={info.iconColor} />
      <Text style={[styles.label, { fontSize: labelSize, color: gradeLabelColor(info.tier) }]}>
        {info.label}
      </Text>
    </View>
  );
}

function gradeBgColor(tier: GradeInfo['tier']): string {
  if (tier === 'gold') return colors.featuredAlpha20;
  if (tier === 'diamond') return colors.primaryAlpha8;
  return colors.surfaceLight;
}

function gradeLabelColor(tier: GradeInfo['tier']): string {
  if (tier === 'gold') return colors.featuredAccent;
  if (tier === 'diamond') return colors.primary;
  if (tier === 'bronze') return colors.textSecondary;
  return colors.textPrimary;
}

const styles = StyleSheet.create({
  pill: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: spacing.xs,
    borderRadius: radius.round,
  },
  pillSm: {
    paddingHorizontal: spacing.sm,
    paddingVertical: spacing.xs,
  },
  pillMd: {
    paddingHorizontal: spacing.md,
    paddingVertical: spacing.sm,
  },
  label: {
    fontWeight: fontWeight.name,
  },
});
```

**Step 2-B — PhotographerCard 에 GradeBadge 통합:**

`app/src/components/photographer/PhotographerCard.tsx` 를 읽어서 author 정보 렌더 섹션 (display_name + is_verified 자리)을 찾는다. display_name 텍스트 바로 뒤, is_verified 아이콘 바로 앞에 아래 블록 추가:

```typescript
import GradeBadge from './GradeBadge';
// ...
<View style={styles.authorRow}>
  <Text style={styles.displayName}>{photographer.display_name}</Text>
  <GradeBadge grade={photographer.grade} variant="icon" />
  {photographer.is_verified && (
    <Ionicons name="checkmark-circle" size={16} color={colors.verified} style={{ marginLeft: spacing.sm }} />
  )}
</View>
```

- GradeBadge `variant="icon"` → UI-SPEC §PhotographerCard: "피드 카드는 아이콘만"
- is_verified 와의 간격: `marginLeft: spacing.sm` (8px) — UI-SPEC §GradeBadge Coexistence

PhotographerCard 기존 구조 유지 + GradeBadge 1개 Ionicons 추가만 삽입.

주의사항:
- `photographer.grade` 필드는 Plan 02 mapPhotographer 가 제공 — Context → PhotographerCard prop 으로 전달 가정. 만약 PhotographerCard 가 `photographer: Photographer` prop 을 받지 않고 개별 필드를 받는다면, `grade` prop 도 추가하거나 `photographer` 객체 전체를 받도록 변경.
- `colors.verified` 가 theme 에 없다면 `colors.success` (#22C55E) 로 대체 (UI-SPEC §Color Success).
  </action>
  <verify>
    <automated>ls app/src/components/photographer/GradeBadge.tsx 존재. grep -q "gradeToBadge" app/src/components/photographer/GradeBadge.tsx. grep -q "GradeBadge" app/src/components/photographer/PhotographerCard.tsx. cd app && npx tsc --noEmit 에서 GradeBadge / PhotographerCard 오류 없음</automated>
  </verify>
  <done>
    - GradeBadge.tsx 신규 생성, UI-SPEC §GradeBadge 일치
    - variant (icon|icon-label) + size (sm|md) 조합 동작
    - tier 별 pill bg / label 색상 매핑 (bronze → surfaceLight/textSecondary, silver/diamond/gold 매핑)
    - PhotographerCard 에 variant="icon" 20×20 삽입
    - accessibilityLabel 템플릿 사용
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: UploadPostScreen 영상 흐름 전면 구현 (D-03, D-04, D-14, D-15, ADJ-02, T-4-02, T-4-06)</name>
  <read_first>
    - app/src/screens/photographer/UploadPostScreen.tsx (전체 — 수정 대상, 현재 video 상태 / handleAddVideo / doPublish 위치)
    - app/src/services/photographerApi.ts + r2Upload.ts (Plan 02 시그니처)
    - app/src/utils/image.ts (optimizeImage)
    - app/src/services/supabase.ts (SUPABASE_URL 환경 변수 접근)
    - app/src/components/common/VideoPlayer.tsx (Task 1 에서 생성 — thumbnail 재생 시 사용)
    - .planning/phases/04-photographer/04-UI-SPEC.md §Video section §Video validation flow §Upload sequence
    - .planning/phases/04-photographer/04-RESEARCH.md §Code Example 5 (handleAddVideo 예시), §Pitfall 3 (iOS .mov)
    - .planning/phases/04-photographer/04-CONTEXT.md §D-03/D-04/D-14
    - .planning/phases/04-photographer/04-03-SUMMARY.md (Context 재작성 후 제공 props)
  </read_first>
  <behavior>
    - Test 1: handleAddVideo helper (duration 35000 입력) → Alert 'upload_video_too_long_title' + videos 변경 없음
    - Test 2: handleAddVideo helper (fileSize 60 * 1024 * 1024) → Alert 'upload_video_too_large_title' + videos 변경 없음
    - Test 3: handleAddVideo helper (mimeType 'video/webm') → Alert 'upload_video_unsupported_format_title' + videos 변경 없음
    - Test 4: handleAddVideo helper (mimeType 'video/mp4', duration 25000, fileSize 10MB) → videos + videoContentTypes 에 추가됨 (videos.length=1)
    - Test 5: handleAddVideo helper (mimeType 'video/quicktime' — ADJ-02 iOS .mov) → videos 에 추가 (ALLOWED 확인)
    - Test 6: fileSize undefined 인 Android 케이스에서 'filesize' lowercase key fallback 동작 (RESEARCH Example 5)
  </behavior>
  <files>app/src/screens/photographer/UploadPostScreen.tsx</files>
  <action>
**Step 3-A — UploadPostScreen.tsx 의 상단 상수/타입 추가 (handleAddVideo 위):**

```typescript
// ─── Video 검증 상수 (D-03, ADJ-02) ───
const VIDEO_MAX_DURATION_MS = 30_000;
const VIDEO_MAX_SIZE_BYTES = 50 * 1024 * 1024;
const ALLOWED_VIDEO_MIME = ['video/mp4', 'video/quicktime'] as const;

// handleAddVideo 테스트 가능한 pure helper — Behavior Test 1~6 대상
export interface VideoValidationResult {
  ok: boolean;
  reason?: 'too_long' | 'too_large' | 'unsupported_format';
  contentType?: 'video/mp4' | 'video/quicktime';
}

export function validateVideoAsset(asset: {
  duration?: number | null;
  fileSize?: number | null;
  filesize?: number | null;  // Android lowercase fallback (RESEARCH Pitfall / Example 5)
  mimeType?: string | null;
  uri: string;
}): VideoValidationResult {
  // 1) Duration
  if (typeof asset.duration === 'number' && asset.duration > VIDEO_MAX_DURATION_MS) {
    return { ok: false, reason: 'too_long' };
  }
  // 2) FileSize (mixed casing — Android 일부 기기)
  const size = asset.fileSize ?? asset.filesize ?? 0;
  if (size > VIDEO_MAX_SIZE_BYTES) {
    return { ok: false, reason: 'too_large' };
  }
  // 3) MIME (iOS .mov 자동 변환 대응 — ADJ-02)
  let mime = asset.mimeType ?? null;
  if (!mime) {
    // fallback: 확장자 기반 추정
    if (asset.uri.toLowerCase().endsWith('.mov')) mime = 'video/quicktime';
    else if (asset.uri.toLowerCase().endsWith('.mp4')) mime = 'video/mp4';
  }
  if (!mime || !ALLOWED_VIDEO_MIME.includes(mime as (typeof ALLOWED_VIDEO_MIME)[number])) {
    return { ok: false, reason: 'unsupported_format' };
  }
  return { ok: true, contentType: mime as 'video/mp4' | 'video/quicktime' };
}
```

**Step 3-B — state 추가:**

기존 `const [videos, setVideos] = useState<string[]>([]);` 아래에 병렬 state 추가:

```typescript
const [videoContentTypes, setVideoContentTypes] = useState<string[]>([]);
```

handleRemoveVideo 수정:
```typescript
const handleRemoveVideo = (idx: number) => {
  setVideos((prev) => prev.filter((_, i) => i !== idx));
  setVideoContentTypes((prev) => prev.filter((_, i) => i !== idx));
};
```

**Step 3-C — handleAddVideo 재구현:**

```typescript
const handleAddVideo = async () => {
  if (videos.length >= MAX_VIDEOS) {
    Alert.alert(t('upload_max_videos'), t('upload_max_videos_desc', { max: MAX_VIDEOS }));
    return;
  }

  const result = await ImagePicker.launchImageLibraryAsync({
    mediaTypes: ['videos'],
    allowsMultipleSelection: false,
    quality: 0.7,
  });

  if (result.canceled || result.assets.length === 0) return;
  const asset = result.assets[0];

  const validation = validateVideoAsset({
    duration: asset.duration ?? null,
    fileSize: asset.fileSize ?? null,
    // Android lowercase key fallback
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    filesize: (asset as any).filesize ?? null,
    mimeType: asset.mimeType ?? null,
    uri: asset.uri,
  });

  if (!validation.ok) {
    const titleKey =
      validation.reason === 'too_long' ? 'upload_video_too_long_title' :
      validation.reason === 'too_large' ? 'upload_video_too_large_title' :
      'upload_video_unsupported_format_title';
    const descKey =
      validation.reason === 'too_long' ? 'upload_video_too_long_desc' :
      validation.reason === 'too_large' ? 'upload_video_too_large_desc' :
      'upload_video_unsupported_format_desc';
    Alert.alert(t(titleKey), t(descKey));
    return;
  }

  setVideos((prev) => [...prev, asset.uri].slice(0, MAX_VIDEOS));
  setVideoContentTypes((prev) => [...prev, validation.contentType!].slice(0, MAX_VIDEOS));
};
```

**Step 3-D — doPublish 재구성 (Phase 3 D-09 mirror + 영상 단계 추가):**

기존 doPublish 의 `isRemote` 분기 전체 제거 — Plan 03 Context 에서 isRemote 삭제됨. 단일 Supabase 전용 경로:

```typescript
const doPublish = async () => {
  const team = KBO_TEAMS.find((t) => t.id === selectedTeamId);
  const player = teamPlayers.find((p) => p.id === selectedPlayerId);
  // cheerleader.name → name_ko (Plan 02 타입 변경, Task 4 에서 사용처 정리)
  const cheerleader = teamCheerleaders.find((c) => c.id === selectedCheerleaderId);

  if (!user?.id || !session) {
    Alert.alert(t('pg_register_fail'), t('pg_register_fail_desc'));
    return;
  }

  const pg = getPhotographer(user.id);
  if (!pg) {
    // photographers 없음 = 심사 미승인 상태 (D-09) → StudioScreen 에서 차단되어야 하지만 방어
    Alert.alert(t('pg_register_fail'), t('studio_pending_desc'));
    return;
  }

  setUploading(true);
  try {
    // ─── Step 1: optimize + upload images (Phase 3 D-09) ───
    const optimized = await Promise.all(images.map(optimizeImage));
    const imageUpload = await photographerApi.uploadPostImages(user.id, optimized, session.access_token);
    if (imageUpload.error || !imageUpload.data) {
      Alert.alert(t('upload_video_upload_failed_title'), imageUpload.error ?? t('upload_video_upload_failed_desc'));
      setUploading(false);
      return;
    }

    // ─── Step 2: upload videos (D-04, ADJ-02 contentTypes) ───
    let finalVideos: string[] = [];
    if (videos.length > 0) {
      const videoUpload = await photographerApi.uploadPostVideos(
        user.id,
        videos,
        session.access_token,
        videoContentTypes,
      );
      if (videoUpload.error || !videoUpload.data) {
        Alert.alert(t('upload_video_upload_failed_title'), videoUpload.error ?? t('upload_video_upload_failed_desc'));
        setUploading(false);
        return;
      }
      finalVideos = videoUpload.data;
    }

    // ─── Step 3: createPhotoPost (D-05) ───
    const postResult = await photographerApi.createPhotoPost({
      photographerId: pg.id,
      teamSlug: selectedTeamId!,
      playerId: selectedPlayerId,
      cheerleaderId: selectedCheerleaderId,
      title: title.trim(),
      description: description.trim(),
      images: imageUpload.data,
      videos: finalVideos,
    });
    if (postResult.error || !postResult.data) {
      Alert.alert(t('upload_video_upload_failed_title'), postResult.error ?? t('upload_video_upload_failed_desc'));
      setUploading(false);
      return;
    }

    addPhotoPost(postResult.data);
    if (selectedCollectionId) {
      await addPostToCollection(selectedCollectionId, postResult.data.id);
    }

    // ─── Step 4: generate-thumbnails fire-and-forget (D-14) ───
    const supabaseUrl = process.env.EXPO_PUBLIC_SUPABASE_URL ?? '';
    if (supabaseUrl && imageUpload.data.length > 0) {
      fetch(`${supabaseUrl}/functions/v1/generate-thumbnails`, {
        method: 'POST',
        headers: {
          Authorization: `Bearer ${session.access_token}`,
          'Content-Type': 'application/json',
        },
        body: JSON.stringify({
          postId: postResult.data.id,
          imageUrls: imageUpload.data,
        }),
      }).catch((e) => console.warn('[Thumbnail] fire-and-forget failed', e));
    }

    showToast(t('btn_done'));
    navigation.goBack();
  } catch (e: unknown) {
    const msg = e instanceof Error ? e.message : 'Upload failed';
    Alert.alert(t('upload_video_upload_failed_title'), msg);
  } finally {
    setUploading(false);
  }
};
```

**Step 3-E — cheerleader.name → name_ko 사용처 변경:**

파일 내 `cheerleader.name` 참조를 `cheerleader.name_ko` 로 교체 (grep 으로 2-3곳 확인). 예를 들어:
```typescript
// 기존: cheerleader ? { name: cheerleader.name } : null
// 변경: cheerleader ? { name_ko: cheerleader.name_ko } : null
```

(단, 편집 모드 기존 게시물 업데이트 경로 — isEditing 블록 — 는 Phase 5 에서 admin 연계 시 재정비. 본 plan 에서는 기존 구조 보존하며 cheerleader 타입만 name_ko 로 맞춤.)

**Step 3-F — Context 의 제거된 props 사용 정리:**

- `isRemote` 사용 모두 제거 (Plan 03 에서 Context 에서 삭제됨)
- `deletePhotoPost` 는 Context 가 async 로 변경됨 — isEditing 블록 안에서 `deletePhotoPost(existingPost.id)` 앞에 `await` 추가
- `addPostToCollection` 도 async → await 추가

Video section UI 추가 — UI-SPEC §UploadPostScreen §video section:

```typescript
<View style={styles.section}>
  <Text style={styles.sectionLabel}>{t('upload_video_label')}</Text>
  <Text style={styles.sectionHint}>{t('upload_video_hint')}</Text>
  <ScrollView horizontal showsHorizontalScrollIndicator={false} style={styles.videoList}>
    {videos.map((uri, idx) => (
      <View key={uri + idx} style={styles.videoThumb}>
        <VideoPlayer uri={uri} mode="studio" width={80} height={80} />
        <TouchableOpacity
          onPress={() => handleRemoveVideo(idx)}
          style={styles.videoRemoveBtn}
          activeOpacity={0.7}
          accessibilityLabel={t('btn_delete')}
        >
          <Ionicons name="close" size={14} color={colors.textPrimary} />
        </TouchableOpacity>
      </View>
    ))}
    {videos.length < MAX_VIDEOS && (
      <TouchableOpacity onPress={handleAddVideo} style={styles.videoAddBtn} activeOpacity={0.7}>
        <Ionicons name="add" size={24} color={colors.textTertiary} />
        <Text style={styles.videoAddLabel}>{t('upload_add_video_prompt')}</Text>
      </TouchableOpacity>
    )}
  </ScrollView>
</View>
```

해당 스타일 (videoList, videoThumb, videoRemoveBtn, videoAddBtn, videoAddLabel) 을 StyleSheet 에 추가 — UI-SPEC §Video thumb list 스펙 (80×80, radius.md, dashed border 등).

**Behavior unit test — app/src/screens/photographer/__tests__/UploadPostScreen.validateVideoAsset.test.ts:**

```typescript
import { validateVideoAsset } from '../UploadPostScreen';

describe('validateVideoAsset', () => {
  it('duration 초과 → too_long', () => {
    const r = validateVideoAsset({ duration: 35000, fileSize: 10, mimeType: 'video/mp4', uri: 'x.mp4' });
    expect(r.ok).toBe(false);
    expect(r.reason).toBe('too_long');
  });
  it('fileSize 초과 → too_large', () => {
    const r = validateVideoAsset({ duration: 10000, fileSize: 60 * 1024 * 1024, mimeType: 'video/mp4', uri: 'x.mp4' });
    expect(r.reason).toBe('too_large');
  });
  it('MIME webm 거부 → unsupported_format', () => {
    const r = validateVideoAsset({ duration: 10000, fileSize: 10, mimeType: 'video/webm', uri: 'x.webm' });
    expect(r.reason).toBe('unsupported_format');
  });
  it('MP4 정상 허용', () => {
    const r = validateVideoAsset({ duration: 25000, fileSize: 10 * 1024 * 1024, mimeType: 'video/mp4', uri: 'x.mp4' });
    expect(r.ok).toBe(true);
    expect(r.contentType).toBe('video/mp4');
  });
  it('iOS MOV (video/quicktime) 허용 — ADJ-02', () => {
    const r = validateVideoAsset({ duration: 25000, fileSize: 10 * 1024 * 1024, mimeType: 'video/quicktime', uri: 'x.mov' });
    expect(r.ok).toBe(true);
    expect(r.contentType).toBe('video/quicktime');
  });
  it('Android filesize lowercase fallback', () => {
    const r = validateVideoAsset({ duration: 10000, fileSize: null, filesize: 60 * 1024 * 1024, mimeType: 'video/mp4', uri: 'x.mp4' });
    expect(r.reason).toBe('too_large');
  });
  it('mimeType undefined + .mov 확장자 → quicktime fallback', () => {
    const r = validateVideoAsset({ duration: 10000, fileSize: 10, mimeType: null, uri: 'file://path/clip.mov' });
    expect(r.ok).toBe(true);
    expect(r.contentType).toBe('video/quicktime');
  });
});
```
  </action>
  <verify>
    <automated>cd app && npm test -- --testPathPattern=UploadPostScreen.validateVideoAsset --no-coverage 가 7 tests passed. grep -q "validateVideoAsset" app/src/screens/photographer/UploadPostScreen.tsx. grep -q "ALLOWED_VIDEO_MIME" app/src/screens/photographer/UploadPostScreen.tsx. grep -q "uploadPostVideos" app/src/screens/photographer/UploadPostScreen.tsx. grep -q "generate-thumbnails" app/src/screens/photographer/UploadPostScreen.tsx. grep -q "isRemote" app/src/screens/photographer/UploadPostScreen.tsx 결과 0 (제거 확인). cd app && npx tsc --noEmit 에서 UploadPostScreen.tsx 관련 오류 0</automated>
  </verify>
  <done>
    - validateVideoAsset pure helper export 됨, 7 cases green
    - handleAddVideo 가 validateVideoAsset 사용 + Alert 3종 분기
    - ADJ-02: video/mp4 + video/quicktime 둘 다 허용, uri 확장자 fallback 포함
    - doPublish 순서: optimize → uploadPostImages → uploadPostVideos(contentTypes) → createPhotoPost(images, videos) → generate-thumbnails fire-and-forget
    - 실패 시 Alert + 폼 유지 (setUploading(false), navigation.goBack 없음)
    - cheerleader.name → name_ko 변경
    - isRemote 사용 제거
    - Video section UI (thumb list + add button) UI-SPEC 일치
    - T-4-02 mitigated (50MB client check), T-4-06 mitigated (mp4+quicktime whitelist)
  </done>
</task>

</tasks>

<verification>
1. Plan 01 Task 6 설치한 expo-video 를 Plan 04 Task 1 에서 정상 import 및 `useVideoPlayer` 사용
2. Plan 02 의 서비스 레이어 새 시그니처 (uploadPostVideos contentTypes[], createPhotoPost videos) 를 Task 3 이 호출
3. Plan 03 의 Context 재작성 (isRemote 제거, addPostToCollection async) 을 Task 3 이 소비
4. pgTAP + Plan 01 BLOCKING checkpoint 에서 배포된 generate-thumbnails EF 에 Task 3 이 fetch
5. cd app && npx tsc --noEmit 에서 VideoPlayer.tsx / GradeBadge.tsx / PhotographerCard.tsx / UploadPostScreen.tsx 오류 0 (Plan 05 담당 화면의 오류는 존재 가능)
6. cd app && npm test 에서 photographerGrade.test.ts (14) + UploadPostScreen.validateVideoAsset.test.ts (7) 모두 green
</verification>

<success_criteria>
- [ ] VideoPlayer 컴포넌트 구현 완료
- [ ] GradeBadge 컴포넌트 구현 완료 + PhotographerCard 에 icon variant 삽입
- [ ] bronze 토큰 + 33개 i18n 키 추가
- [ ] UploadPostScreen validateVideoAsset 7 tests green
- [ ] UploadPostScreen doPublish Phase 3 D-09 + 영상 + 썸네일 3단계 흐름
- [ ] T-4-02, T-4-06 mitigate 검증 (unit test)
- [ ] cheerleader.name → name_ko 사용처 모두 수정 (UploadPostScreen + PhotographerCard 범위)
- [ ] isRemote 제거 (본 plan 범위)
</success_criteria>

<output>
완료 후 `.planning/phases/04-photographer/04-04-SUMMARY.md` 생성 — VideoPlayer/GradeBadge API 문서 + UploadPostScreen 흐름 다이어그램 (Mermaid optional) + Plan 05 가 사용할 컴포넌트 진입점 + 잔여 typecheck 오류 (StudioScreen, PhotographerRegisterScreen, CollectionDetailScreen, MainTabNavigator — Plan 05 해결).
</output>
</content>
</invoke>