---
phase: 04-photographer
plan: 08
type: execute
wave: 6
depends_on: [04-07]
files_modified:
  - app/src/contexts/PhotographerContext.tsx
  - app/src/screens/photographer/PhotographerRegisterScreen.tsx
  - app/src/navigation/MainTabNavigator.tsx
  - app/src/screens/photographer/StudioScreen.tsx
autonomous: true
gap_closure: true
requirements: [PHOT-02]

must_haves:
  truths:
    - "PhotographerContext 가 myApplication 상태를 단일 source-of-truth 로 소유하며 submitPhotographerApplication / refreshMyApplication 함수를 export 한다"
    - "PhotographerRegisterScreen 이 photographerApi.submitPhotographerApplication 직접 호출을 제거하고 usePhotographer().submitPhotographerApplication 을 사용한다"
    - "MainTabNavigator 가 자체 state/useEffect/photographerApi 호출을 제거하고 usePhotographer().myApplication + user.is_photographer 를 derive 하여 studioState 를 계산한다"
    - "StudioScreen 이 자체 fetchMyPhotographerApplication 호출을 제거하고 Context 의 myApplication 을 사용한다 (override photographerId 경로는 기존 그대로 유지)"
    - "미신청 사용자가 PhotographerRegister Step 3 에서 '포토그래퍼 신청하기' 제출 → Step 4 pending hero 로 전환된 직후 Studio 탭으로 돌아가면 label='심사중' + icon=time-outline 으로 실시간 반영된다 (앱 재시작 불필요)"
    - "동일 API (fetchMyPhotographerApplication) 호출이 앱 부팅 + 탭 네비 당 1회 이하로 수렴한다 (중복 호출 제거)"
  artifacts:
    - path: "app/src/contexts/PhotographerContext.tsx"
      provides: "myApplication state + submitPhotographerApplication + refreshMyApplication — Context-as-store 통합"
      contains: "myApplication"
    - path: "app/src/screens/photographer/PhotographerRegisterScreen.tsx"
      provides: "Context 기반 submit — 성공 시 Context state 자동 업데이트"
      contains: "usePhotographer"
    - path: "app/src/navigation/MainTabNavigator.tsx"
      provides: "derive-only studioState (local state 제거)"
      contains: "usePhotographer"
    - path: "app/src/screens/photographer/StudioScreen.tsx"
      provides: "Context 기반 state — 중복 fetch 제거"
      contains: "myApplication"
  key_links:
    - from: "app/src/screens/photographer/PhotographerRegisterScreen.tsx"
      to: "app/src/contexts/PhotographerContext.tsx"
      via: "usePhotographer().submitPhotographerApplication(...) 호출"
      pattern: "submitPhotographerApplication"
    - from: "app/src/navigation/MainTabNavigator.tsx"
      to: "app/src/contexts/PhotographerContext.tsx"
      via: "usePhotographer().myApplication 구독 → studioState derive"
      pattern: "usePhotographer"
    - from: "app/src/screens/photographer/StudioScreen.tsx"
      to: "app/src/contexts/PhotographerContext.tsx"
      via: "usePhotographer().myApplication + refreshMyApplication"
      pattern: "myApplication"
---

<objective>
UAT Test 4 gap 해소 — "Studio 탭 state 실시간 전환 누락". application 상태를 공유하는 shared channel 이 없어 submit 후 MainTabNavigator 가 재fetch 되지 않아 앱 재시작 전까지 탭 label/icon 이 no_app 그대로 유지되는 문제 수정.

Purpose: Context-as-store 패턴 (PhotographerContext 가 photographers/photoPosts 관리하듯 myApplication 도 관리) 로 통합하여 submit → Context state 업데이트 → MainTabNavigator 자동 리렌더 체인을 완성한다. 동시에 MainTabNavigator 와 StudioScreen 이 각자 독립 호출하던 fetchMyPhotographerApplication 중복을 제거한다.

Output: 신청 제출 직후 Studio 탭 label/icon 이 실시간 전환되고, fetchMyPhotographerApplication 호출이 cold-start 1회로 수렴.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-photographer/04-UAT.md
@app/src/contexts/PhotographerContext.tsx
@app/src/navigation/MainTabNavigator.tsx
@app/src/screens/photographer/PhotographerRegisterScreen.tsx
@app/src/screens/photographer/StudioScreen.tsx
@app/src/screens/photographer/studioState.ts
@app/src/types/photographerApplication.ts

<interfaces>
<!-- Executor reference — do NOT rediscover via grep. -->

From app/src/types/photographerApplication.ts:
```typescript
export type ApplicationStatus = 'pending' | 'approved' | 'rejected';
export interface PhotographerApplication {
  id: string;
  user_id: string;
  team_id: string | null;
  portfolio_url: string | null;
  bio: string;
  activity_links: string[];
  activity_plan: string;
  status: ApplicationStatus;
  rejection_reason: string | null;
  reviewed_by: string | null;
  reviewed_at: string | null;
  created_at: string;
  updated_at: string;
}
```

From app/src/services/photographerApi.ts (existing — reuse as-is, no modification):
```typescript
export async function submitPhotographerApplication(params: {
  user_id: string;
  team_slug: string | null;
  activity_links: string[];
  activity_plan: string;
  portfolio_url?: string | null;
  bio?: string;
}): Promise<ApiResult<PhotographerApplication>>;

export async function fetchMyPhotographerApplication(
  userId: string,
): Promise<ApiResult<PhotographerApplication | null>>;

export async function fetchPhotographerByUserId(
  userId: string,
): Promise<ApiResult<Photographer>>;
```

From app/src/contexts/AuthContext.tsx (existing):
```typescript
// user.is_photographer: boolean — public.users 에서 로드. 승인 trigger 후에만 true.
```

From app/src/screens/photographer/studioState.ts (existing — StudioScreen 이 approved 분기에서 이미 사용 중; 본 plan 에서 변경 없음):
```typescript
export type StudioState =
  | { kind: 'loading' }
  | { kind: 'no_application' }
  | { kind: 'pending'; application: PhotographerApplication }
  | { kind: 'approved'; photographer: Photographer }
  | { kind: 'rejected'; application: PhotographerApplication };
```

Context wiring pattern (mirror — PhotographerContext 는 이미 아래 패턴으로 photoPosts 를 관리 중):
```typescript
const [photoPosts, setPhotoPosts] = useState<PhotoPost[]>([]);
useEffect(() => { void refreshData(); }, [refreshData]);
// submit → api call → if ok: setState → consumers 자동 리렌더
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PhotographerContext 에 myApplication state + submit/refresh 함수 추가</name>
  <files>app/src/contexts/PhotographerContext.tsx</files>
  <action>
PhotographerContext 에 application 상태 관리 3종을 추가한다. Context-as-store 원칙 유지 — 기존 photographers/photoPosts 관리 패턴과 동일한 모양.

1. Import 보강:
   - `import type { PhotographerApplication } from '../types/photographerApplication';`

2. `PhotographerContextValue` interface 에 필드 추가 (// ─── Likes 섹션 앞에 배치):
   ```typescript
   // Application (PHOT-02 — Context-as-store 통합, Plan 04-08 gap closure)
   myApplication: PhotographerApplication | null;
   applicationLoading: boolean;
   submitPhotographerApplication: (params: {
     user_id: string;
     team_slug: string | null;
     activity_links: string[];
     activity_plan: string;
     portfolio_url?: string | null;
     bio?: string;
   }) => Promise<{ data: PhotographerApplication | null; error: string | null }>;
   refreshMyApplication: () => Promise<void>;
   ```

3. Provider 내부 — Core state 섹션에 추가:
   ```typescript
   const [myApplication, setMyApplication] = useState<PhotographerApplication | null>(null);
   const [applicationLoading, setApplicationLoading] = useState(false);
   ```

4. `refreshMyApplication` useCallback 추가 (refreshData 아래에 새 섹션으로):
   ```typescript
   const refreshMyApplication = useCallback(async () => {
     if (!userId) {
       setMyApplication(null);
       return;
     }
     setApplicationLoading(true);
     try {
       const res = await photographerApi.fetchMyPhotographerApplication(userId);
       if (res.error) {
         console.warn('[PhotographerContext] refreshMyApplication error', res.error);
         // error 시 기존 state 유지 (network blip 대응). 명시적 fail-closed 는 하지 않음.
         return;
       }
       setMyApplication(res.data);
     } finally {
       setApplicationLoading(false);
     }
   }, [userId]);
   ```

5. userId 변경 시 myApplication 자동 재fetch — 기존 `useEffect(() => { void refreshData(); }, [refreshData])` 바로 아래에 추가:
   ```typescript
   useEffect(() => {
     void refreshMyApplication();
   }, [refreshMyApplication]);
   ```

6. `submitPhotographerApplication` wrapper 추가 (refreshMyApplication 바로 아래):
   ```typescript
   const submitApplication = useCallback(
     async (params: {
       user_id: string;
       team_slug: string | null;
       activity_links: string[];
       activity_plan: string;
       portfolio_url?: string | null;
       bio?: string;
     }) => {
       const res = await photographerApi.submitPhotographerApplication(params);
       if (res.data) {
         // 성공 — Context state 즉시 업데이트 → 구독 중인 MainTabNavigator / StudioScreen 자동 리렌더
         setMyApplication(res.data);
       }
       return res;
     },
     [],
   );
   ```

7. `value` useMemo 에 새 필드 4종 포함 + deps 배열에도 추가:
   ```typescript
   myApplication,
   applicationLoading,
   submitPhotographerApplication: submitApplication,
   refreshMyApplication,
   ```

Context value 최종 노출 이름은 `submitPhotographerApplication` (api 와 동일 시그니처 유지). 내부 변수명만 `submitApplication` 으로 충돌 회피.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit 2>&1 | tee /tmp/tsc-task1.log && grep -q "myApplication" src/contexts/PhotographerContext.tsx && grep -q "submitPhotographerApplication" src/contexts/PhotographerContext.tsx && grep -q "refreshMyApplication" src/contexts/PhotographerContext.tsx</automated>
  </verify>
  <done>PhotographerContext 에 myApplication state + submitPhotographerApplication wrapper + refreshMyApplication 3종이 추가되고 TypeScript 타입체크 통과. userId 변경 시 자동 refresh 가 동작.</done>
</task>

<task type="auto">
  <name>Task 2: PhotographerRegisterScreen 을 Context 기반으로 전환</name>
  <files>app/src/screens/photographer/PhotographerRegisterScreen.tsx</files>
  <action>
PhotographerRegisterScreen.tsx 의 `handleNext` Step 3 분기에서 `photographerApi.submitPhotographerApplication` 직접 호출을 제거하고 `usePhotographer().submitPhotographerApplication` 을 사용하도록 전환. Plan 04-08 gap closure 의 핵심 wiring.

1. Import 변경:
   - 제거: `import * as photographerApi from '../../services/photographerApi';`
   - 추가: `import { usePhotographer } from '../../contexts/PhotographerContext';`

2. 함수 본문 상단 (useAuth 호출 근처, useState 선언 전):
   ```typescript
   const { submitPhotographerApplication } = usePhotographer();
   ```

3. `handleNext` 의 `else if (step === 3)` 블록 — 기존 `photographerApi.submitPhotographerApplication(...)` 호출을 `submitPhotographerApplication(...)` 으로 교체 (인자 동일):
   ```typescript
   } else if (step === 3) {
     if (!copyrightConfirmed || !selectedTeamId || !user) return;
     setLoading(true);
     const res = await submitPhotographerApplication({
       user_id: user.id,
       team_slug: selectedTeamId,
       activity_links: activityLinks.map((s) => s.trim()).filter(Boolean),
       activity_plan: activityPlan.trim(),
       portfolio_url: null,
       bio: '',
     });
     setLoading(false);
     if (res.error) {
       Alert.alert(t('pg_register_fail'), t('pg_register_fail_desc'));
       setError(res.error);
       return;
     }
     setStep(4);
   }
   ```

4. 나머지 파일은 변경 없음. `photographerApi` import 제거 후 unused import 경고가 없어야 한다.

참고 — 이 전환으로 성공 시 Context 내부 setMyApplication 이 호출되고, 같은 Context 를 구독하는 MainTabNavigator 가 자동 리렌더 → studioLabel/studioIcon 이 즉시 pending 으로 업데이트된다.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit 2>&1 | tee /tmp/tsc-task2.log && ! grep -q "photographerApi.submitPhotographerApplication" src/screens/photographer/PhotographerRegisterScreen.tsx && grep -q "usePhotographer" src/screens/photographer/PhotographerRegisterScreen.tsx</automated>
  </verify>
  <done>PhotographerRegisterScreen 이 photographerApi 직접 호출을 제거하고 usePhotographer().submitPhotographerApplication 을 사용. Step 3 제출 로직은 성공/실패 동작이 기존과 동일하되 성공 시 Context state 가 갱신된다.</done>
</task>

<task type="auto">
  <name>Task 3: MainTabNavigator + StudioScreen 을 Context 구독으로 전환</name>
  <files>app/src/navigation/MainTabNavigator.tsx, app/src/screens/photographer/StudioScreen.tsx</files>
  <action>
두 화면에서 자체 local state + photographerApi 직접 호출을 제거하고 Context 의 myApplication 을 구독하도록 전환. 중복 fetch 가 사라지고 submit 후 실시간 전환이 완성된다.

### File 1: `app/src/navigation/MainTabNavigator.tsx`

기존 useEffect + local studioState 를 전면 대체. `user.is_photographer` + `myApplication.status` 로 derive.

1. Import 교체:
   - 제거: `import React, { useEffect, useState } from 'react';` → `import React from 'react';`
   - 제거: `import * as photographerApi from '../services/photographerApi';`
   - 추가: `import { usePhotographer } from '../contexts/PhotographerContext';`

2. 컴포넌트 본문 전면 교체 — 기존 `const [studioState, setStudioState] = ...` 부터 `useEffect(() => { ... }, [user?.id, user?.is_photographer])` 까지 블록을 삭제하고 다음 derive 로 대체:
   ```typescript
   const { t } = useTranslation();
   const { user } = useAuth();
   const { myApplication } = usePhotographer();

   // HI-02 유지: user.is_photographer 가 synchronous baseline. application 상태가
   // 더 최신이면 우선 반영. derive-only → submit → Context update → 자동 리렌더 체인.
   const studioState: StudioTabState = !user?.id
     ? 'no_app'
     : user.is_photographer
       ? 'approved'
       : myApplication?.status === 'pending'
         ? 'pending'
         : myApplication?.status === 'rejected'
           ? 'rejected'
           : 'no_app';
   ```

3. `StudioTabState` type 은 기존 그대로 유지. 아래 `studioLabel`/`studioIconInactive`/`studioIconActive` 파생 로직도 그대로 (rejected 상태에 대해 기존처럼 no_app icon 을 쓰는지 확인 — 현재 코드는 rejected 를 다루지 않으므로 `'rejected'` 는 `camera-outline` 이 fallback 되도록 기존 조건을 그대로 둔다).

4. `import` 섹션 위쪽에 `type StudioTabState = 'no_app' | 'pending' | 'approved' | 'rejected';` 선언은 그대로 유지.

### File 2: `app/src/screens/photographer/StudioScreen.tsx`

자체 `useEffect` 에서 fetchMyPhotographerApplication 을 호출하던 로직을 Context 의 myApplication 을 구독하는 방식으로 교체. `override photographerId` (다른 포토그래퍼 스튜디오 조회) 경로는 기존 로직 유지.

1. Import 추가:
   - 기존 `import { usePhotographer } from '../../contexts/PhotographerContext';` 에서 `myApplication`, `applicationLoading`, `refreshMyApplication` 함께 구조분해.

2. 컴포넌트 상단 — `getPhotographer, getPhotoPostsByPhotographer` destructure 라인을 다음으로 교체:
   ```typescript
   const {
     getPhotographer,
     getPhotoPostsByPhotographer,
     myApplication,
     applicationLoading,
     refreshMyApplication,
   } = usePhotographer();
   ```

3. 기존 `useEffect(() => { ... }, [user?.id, overridePhotographerId, getPhotographer]);` 블록을 다음 두 useEffect 로 분할:

   a. override 경로 (변경 없음, 그대로 유지):
   ```typescript
   useEffect(() => {
     if (!overridePhotographerId) return;
     const pg = getPhotographer(overridePhotographerId);
     setState(pg ? { kind: 'approved', photographer: pg } : { kind: 'no_application' });
   }, [overridePhotographerId, getPhotographer]);
   ```

   b. 본인 스튜디오 — Context 구독 (fetchMyPhotographerApplication 직접 호출 제거):
   ```typescript
   useEffect(() => {
     if (overridePhotographerId) return;
     if (!user?.id) {
       setState({ kind: 'no_application' });
       return;
     }
     if (applicationLoading) {
       setState({ kind: 'loading' });
       return;
     }

     let cancelled = false;
     (async () => {
       // approved 상태만 photographer row 조회 필요 (Context 에 없는 추가 데이터)
       const needsPhotographer =
         myApplication?.status === 'approved'
           ? await photographerApi.fetchPhotographerByUserId(user.id)
           : null;
       if (cancelled) return;
       setState(
         determineStudioState({
           appResult: { data: myApplication, error: null },
           photographerResult: needsPhotographer,
         }),
       );
     })();
     return () => {
       cancelled = true;
     };
   }, [user?.id, overridePhotographerId, myApplication, applicationLoading]);
   ```

4. 첫 useEffect 에서 `photographerApi.fetchMyPhotographerApplication` 호출 라인은 완전 제거된 상태여야 한다. `photographerApi` import 는 `fetchPhotographerByUserId` 용으로 남겨둔다 (기존 그대로).

5. Context 에서 `refreshMyApplication` 을 가져와 두되, 현재 수동 refresh 트리거 경로가 별도 없으므로 호출은 선택적. (후속 pull-to-refresh 추가 시 사용 예정 — 본 plan 에서는 state 전환만 보장하면 충분.)

### 검증 — 전환 완료 후 관찰되어야 할 동작:
- 앱 cold-start: PhotographerContext 가 userId 변경에 반응해 refreshMyApplication 1회 호출 → MainTabNavigator 의 studioState derive → 올바른 label/icon 표시
- Step 3 제출: submitPhotographerApplication → Context setMyApplication(pendingApp) → MainTabNavigator + StudioScreen 동시 리렌더 → label='심사중', icon='time-outline', StudioScreen 진입 시 pending hero
- 중복 호출 제거: MainTabNavigator + StudioScreen + PhotographerRegisterScreen 3개가 공통 Context 를 바라봄 → fetchMyPhotographerApplication 호출은 Context refreshMyApplication 1곳에서만 발생
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit 2>&1 | tee /tmp/tsc-task3.log && ! grep -q "photographerApi.fetchMyPhotographerApplication" src/navigation/MainTabNavigator.tsx && ! grep -q "photographerApi.fetchMyPhotographerApplication" src/screens/photographer/StudioScreen.tsx && grep -q "myApplication" src/navigation/MainTabNavigator.tsx && grep -q "myApplication" src/screens/photographer/StudioScreen.tsx</automated>
  </verify>
  <done>MainTabNavigator 와 StudioScreen 모두 자체 fetchMyPhotographerApplication 호출을 제거하고 Context 의 myApplication 을 구독한다. TypeScript 타입체크 통과. 수동 검증에서 submit 제출 후 탭으로 복귀하면 label/icon 이 즉시 '심사중'/time-outline 으로 반영됨 (user 는 별도 실기기 QA 로 확인 예정).</done>
</task>

</tasks>

<verification>
### Code-level 자동 검증 (실행 시 전체 통과 필수)
- `cd app && npx tsc --noEmit` — 타입 에러 0
- `grep -rn "photographerApi.submitPhotographerApplication\|photographerApi.fetchMyPhotographerApplication" app/src/screens app/src/navigation` — **0 results** (Context 외부 호출 완전 제거 확인). PhotographerContext.tsx 내부의 호출 1건은 허용.
- `grep -q "myApplication" app/src/contexts/PhotographerContext.tsx app/src/navigation/MainTabNavigator.tsx app/src/screens/photographer/StudioScreen.tsx` — 3개 파일 모두 매치

### Behavior-level 검증 (실기기 — UAT Test 4 재실행 시)
- 미신청 계정 로그인: Studio 탭 label='포토그래퍼', icon='camera-outline'
- PhotographerRegister Step 3 '포토그래퍼 신청하기' 탭 → Step 4 pending hero 전환
- Step 4 '홈으로 돌아가기' → Studio 탭 탭: label='심사중', icon='time-outline' (앱 재시작 없이 실시간 전환)
- Studio 탭 진입: pending hero UI 표시
- 앱 kill-and-restart 후 재로그인: cold-start 에도 동일 상태 유지
</verification>

<success_criteria>
1. UAT Test 4 gap 이 `status: resolved` 로 flip 가능 — 신청 제출 후 앱 재시작 없이 탭 label/icon 이 실시간 변경됨
2. PhotographerContext 가 application 상태의 단일 source-of-truth — MainTabNavigator / StudioScreen / PhotographerRegisterScreen 이 모두 이를 구독
3. fetchMyPhotographerApplication 호출 지점이 Context 내부 1곳으로 통합 (기존 3곳 중복 제거)
4. TypeScript strict 모드 통과 (no any)
5. 기존 5가지 StudioState (loading / no_application / pending / approved / rejected) 모두 정상 작동
</success_criteria>

<output>
After completion, create `.planning/phases/04-photographer/04-08-SUMMARY.md`
</output>
