---
phase: 04-photographer
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/029_photo_posts_videos.sql
  - supabase/migrations/030_photographer_approval_trigger.sql
  - supabase/migrations/031_photographer_apps_extend.sql
  - supabase/migrations/032_photo_posts_thumbnails.sql
  - supabase/functions/get-upload-url/index.ts
  - supabase/functions/generate-thumbnails/index.ts
  - supabase/functions/generate-thumbnails/deno.json
  - supabase/functions/generate-thumbnails/magick.wasm
  - supabase/config.toml
  - supabase/tests/photographer-approval-trigger.sql
  - supabase/tests/photo-posts-videos-check.sql
  - supabase/tests/photo-posts-images-1-7-check.sql
  - supabase/tests/photographer-apps-extend.sql
  - supabase/tests/photo-posts-thumbnails.sql
  - docs/phase4-qa-matrix.md
  - app/package.json
  - app/package-lock.json
autonomous: false
requirements:
  - PHOT-02
  - PHOT-03
  - PHOT-05
  - PHOT-07
threats:
  - T-4-01
  - T-4-02
  - T-4-03
  - T-4-04
  - T-4-05
  - T-4-06
  - T-4-07
user_setup: []

must_haves:
  truths:
    - "supabase db push가 성공하여 029~032 마이그레이션이 원격 DB에 반영됨"
    - "photo_posts.videos TEXT[] NOT NULL DEFAULT '{}' 컬럼이 존재하고 CHECK (array_length(videos,1) IS NULL OR BETWEEN 1 AND 3) 제약이 적용됨"
    - "photo_posts.images CHECK 제약이 BETWEEN 1 AND 7로 변경됨 (기존 1~10)"
    - "photo_posts.thumbnail_urls TEXT[] NOT NULL DEFAULT '{}' 컬럼이 존재함"
    - "photographer_applications에 team_id UUID REFERENCES teams(id) NULL, activity_links TEXT[] NOT NULL DEFAULT '{}' CHECK length ≤ 3, activity_plan TEXT NOT NULL DEFAULT '' 컬럼이 존재함"
    - "handle_photographer_application_decision() 함수가 SECURITY DEFINER SET search_path = '' 로 정의되고 trg_photographer_application_decision이 AFTER UPDATE OF status WHEN (OLD.status IS DISTINCT FROM NEW.status) 로 등록됨"
    - "승인 시 photographers INSERT (ON CONFLICT DO NOTHING) + users.is_photographer=TRUE UPDATE + notifications INSERT(photographer_approved) 가 단일 트랜잭션으로 실행됨 (ADJ-01: users.role UPDATE 안 함)"
    - "거절 시 photographers INSERT 없이 notifications INSERT(photographer_rejected) 만 실행됨"
    - "pgTAP 테스트 5개 파일 모두 supabase test db 에서 green"
    - "generate-thumbnails Edge Function이 supabase functions deploy 성공하고 curl 샘플 호출 시 400×400 JPEG 생성 + photo_posts.thumbnail_urls UPDATE 동작함"
    - "get-upload-url 의 SIZE_LIMITS['photo-posts']=50*1024*1024 로 상향되고 재배포됨"
    - "expo-video 가 app/package.json dependencies 에 포함되고 npm install 완료"
  artifacts:
    - path: "supabase/migrations/029_photo_posts_videos.sql"
      provides: "videos TEXT[] 컬럼 추가 + images CHECK 1~7 변경"
      contains: "ADD COLUMN videos TEXT[] NOT NULL DEFAULT '{}'"
    - path: "supabase/migrations/030_photographer_approval_trigger.sql"
      provides: "승인/거절 트리거 함수 + notifications/photographers/is_photographer 원자적 처리"
      contains: "handle_photographer_application_decision"
    - path: "supabase/migrations/031_photographer_apps_extend.sql"
      provides: "photographer_applications team_id/activity_links/activity_plan 컬럼 추가"
      contains: "activity_links TEXT[]"
    - path: "supabase/migrations/032_photo_posts_thumbnails.sql"
      provides: "photo_posts.thumbnail_urls 컬럼 추가"
      contains: "thumbnail_urls TEXT[] NOT NULL DEFAULT"
    - path: "supabase/functions/generate-thumbnails/index.ts"
      provides: "Edge Function — R2 download → magick-wasm 400×400 cover crop JPEG → R2 upload → photo_posts.thumbnail_urls UPDATE"
      min_lines: 100
    - path: "supabase/tests/photographer-approval-trigger.sql"
      provides: "pgTAP 승인/거절/재승인 8 케이스 검증"
      contains: "SELECT plan(8)"
    - path: "docs/phase4-qa-matrix.md"
      provides: "Wave 3 manual QA 매트릭스 (영상 업로드 iOS/Android, VideoPlayer, Studio state, GradeBadge, Cheerleader, CollectionDetail)"
      min_lines: 60
  key_links:
    - from: "supabase/migrations/030_photographer_approval_trigger.sql"
      to: "public.users.is_photographer"
      via: "UPDATE public.users SET is_photographer = TRUE (ADJ-01)"
      pattern: "is_photographer\\s*=\\s*TRUE"
    - from: "supabase/migrations/030_photographer_approval_trigger.sql"
      to: "public.notifications"
      via: "INSERT INTO public.notifications photographer_approved/photographer_rejected"
      pattern: "photographer_(approved|rejected)"
    - from: "supabase/functions/generate-thumbnails/index.ts"
      to: "photo_posts.thumbnail_urls"
      via: "supabase.from('photo_posts').update({ thumbnail_urls })"
      pattern: "thumbnail_urls"
    - from: "supabase/functions/get-upload-url/index.ts"
      to: "SIZE_LIMITS"
      via: "SIZE_LIMITS['photo-posts'] = 50 * 1024 * 1024"
      pattern: "50 \\* 1024 \\* 1024"
---

<objective>
Wave 0 — Phase 4 구현이 기대는 **모든 서버측 기반**을 준비한다. DB 스키마 변경 (029~032), 심사 트리거 + 알림, 썸네일 Edge Function 신규, get-upload-url 영상 50MB 상향 + MIME whitelist 확장, pgTAP 검증, expo-video 설치, 그리고 원격 환경에 실제 반영하는 `supabase db push` + `supabase functions deploy` 를 단일 책임으로 수행한다.

Purpose:
- Wave 1~3 의 모든 클라이언트 작업(`photographerApi`, `PhotographerContext`, 화면/컴포넌트)은 본 Plan 이 배포한 스키마/Edge Function 을 전제로 한다
- D-06 트리거는 시스템 핵심 — pgTAP 8개 케이스로 승인/거절/재승인/ON CONFLICT 를 격리 검증
- CONTEXT.md ADJ-01 반영: `users.role='photographer'` UPDATE 금지, `users.is_photographer=TRUE` 만 사용 (Phase 1 011_users.sql:14 CHECK (role IN ('user','admin')) 유지)
- CONTEXT.md ADJ-02 반영: get-upload-url 이 `video/mp4`+`video/quicktime` 허용 유지 + SIZE_LIMIT 50MB 상향 (기존 30MB)
- [BLOCKING] `supabase db push` + `supabase functions deploy generate-thumbnails` + `supabase functions deploy get-upload-url` 를 명시적으로 실행 — 이 없이는 이후 wave 가 staging/prod 에서 실패함

Output:
- 4개 마이그레이션 파일 + 5개 pgTAP 테스트 + 신규 Edge Function `generate-thumbnails` + `get-upload-url` 수정 + QA 매트릭스 문서 + expo-video 설치 + 원격 배포 완료

Addresses: PHOT-02 (트리거), PHOT-03 (영상 스키마 + EF MIME/SIZE), PHOT-05 (썸네일 컬럼 + EF), PHOT-07 (cheerleaders 는 이미 Phase 1, 본 plan 은 schema 변경 없음 — types/fetch 는 Plan 02).

Threat mitigation:
- **T-4-02 (DoS 영상 비용)**: get-upload-url SIZE_LIMITS.photo-posts = 50MB + ALLOWED_TYPES 이미 video/mp4+quicktime 제한
- **T-4-03 (트리거 ROLLBACK)**: SECURITY DEFINER SET search_path + pgTAP 8 cases 전수 검증
- **T-4-05 (임의 thumbnail 호출 R2 비용)**: generate-thumbnails EF 내부에서 postId 의 photographers.user_id 와 토큰 user.id 일치 검증
- **T-4-06 (MIME 우회)**: get-upload-url 서버측 contentType whitelist (video/mp4 + video/quicktime 만)
- **T-4-07 (pending 사용자 INSERT 우회)**: 008_photographer_rls.sql `posts_insert_own` 이 photographers EXISTS 체크 — 트리거가 photographers 미생성 보장 → 기본 적용됨 (검증만 필요)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-photographer/04-CONTEXT.md
@.planning/phases/04-photographer/04-RESEARCH.md
@.planning/phases/04-photographer/04-VALIDATION.md
@.planning/phases/04-photographer/04-UI-SPEC.md
@CLAUDE.md

# 기존 스키마 / Edge Function / 코드 참조
@supabase/migrations/007_photographer.sql
@supabase/migrations/008_photographer_rls.sql
@supabase/migrations/011_users.sql
@supabase/migrations/016_photographer_apps.sql
@supabase/migrations/020_photo_posts_alter.sql
@supabase/migrations/023_rls_policies_remaining.sql
@supabase/functions/get-upload-url/index.ts
@app/package.json

<interfaces>
<!-- Wave 0 는 서버 측 변경이 중심이므로 SQL/Edge Function DDL 만 노출. 클라이언트 타입은 Plan 02 에서 갱신 -->

기존 photo_posts (007_photographer.sql):
```sql
CREATE TABLE photo_posts (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  photographer_id UUID NOT NULL REFERENCES photographers(id) ON DELETE CASCADE,
  team_id UUID NOT NULL REFERENCES teams(id),
  player_id UUID REFERENCES players(id),
  title TEXT NOT NULL,
  description TEXT DEFAULT '',
  images TEXT[] NOT NULL CHECK (array_length(images, 1) BETWEEN 1 AND 10), -- 1~7 로 변경됨
  like_count INTEGER DEFAULT 0,
  ...
);
-- 020_photo_posts_alter.sql 에서 status, rejection_reason, cheerleader_id 추가됨
```

기존 photographer_applications (016_photographer_apps.sql):
```sql
CREATE TABLE public.photographer_applications (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES public.users(id) ON DELETE CASCADE,
  portfolio_url TEXT,
  bio TEXT DEFAULT '',
  status TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('pending', 'approved', 'rejected')),
  reviewed_by UUID REFERENCES public.users(id),
  reviewed_at TIMESTAMPTZ,
  rejection_reason TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
-- 031 에서 team_id UUID, activity_links TEXT[], activity_plan TEXT 추가
```

기존 public.users (011_users.sql):
```sql
-- role CHECK IN ('user','admin') -- 변경 금지 (ADJ-01)
-- is_photographer BOOLEAN DEFAULT FALSE -- 이미 존재, 트리거에서 TRUE 로 UPDATE
```

기존 get-upload-url ALLOWED_TYPES (이미 video/mp4 + video/quicktime 포함):
```ts
const ALLOWED_TYPES: Record<string, string[]> = {
  "photo-posts": ["image/jpeg", "image/png", "image/webp", "video/mp4", "video/quicktime", "video/webm"],
  ...
};
const SIZE_LIMITS: Record<string, number> = {
  "photo-posts": 30 * 1024 * 1024,  // → 50MB 로 상향
  ...
};
```

기존 notifications RLS (023_rls_policies_remaining.sql):
- `notifications_admin_insert` 정책: `WITH CHECK (public.is_admin())` — 트리거 함수는 SECURITY DEFINER 로 RLS 우회 필요
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Edge Function | 사용자 토큰(Bearer) 이 get-upload-url / generate-thumbnails 경계를 넘을 때 검증 필수 |
| Edge Function→R2 | AWS SDK v3 SigV4 + R2 credentials (server-only) |
| admin UI→photographer_applications | 어드민 토큰 UPDATE → AFTER UPDATE 트리거 경계 |
| 트리거→notifications/photographers/users | DB 내부 단일 트랜잭션, SECURITY DEFINER 로 RLS 우회 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Tampering | get-upload-url key prefix | mitigate | 기존 EF 가 token userId 로 `${prefix}/${userId}/...` 강제 — 본 plan 은 변경 없음, 유지 검증만 |
| T-4-02 | DoS | get-upload-url SIZE_LIMITS | mitigate | Task 2: SIZE_LIMITS['photo-posts']=50*1024*1024 로 상향 + R2 bucket lifecycle 은 v1 범위 밖 (CONTEXT.md deferred) |
| T-4-03 | DoS | 030 trigger ROLLBACK → 승인 영구 차단 | mitigate | Task 3: SECURITY DEFINER SET search_path='' + ON CONFLICT DO NOTHING on photographers + Task 6: pgTAP 8 cases |
| T-4-04 | Info disclosure | photographer_applications 타인 조회 | mitigate (existing) | 023 `photographer_apps_read` `auth.uid() = user_id OR public.is_admin()` — 변경 없음 |
| T-4-05 | DoS | generate-thumbnails 임의 사용자 호출 → R2 비용 | mitigate | Task 5: EF 내부 `supabase.auth.getUser(token)` + postId owner 검증 (`SELECT photographers.user_id WHERE photo_posts.id=postId` 와 token user.id 일치) |
| T-4-06 | Tampering | iOS .mov 클라이언트 우회 | mitigate | Task 2: get-upload-url contentType whitelist `video/mp4` + `video/quicktime` 이미 적용 — 재배포만 필요 (SIZE_LIMIT 변경과 함께) |
| T-4-07 | Bypass | pending 사용자 photo_posts INSERT 시도 | mitigate (existing) | 008_photographer_rls.sql `posts_insert_own` photographers EXISTS — 트리거가 photographers 미생성 시 자동 차단 — Task 3 pgTAP 검증 포함 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: 029_photo_posts_videos.sql — videos 컬럼 추가 + images CHECK 1~7 변경</name>
  <read_first>
    - supabase/migrations/007_photographer.sql (기존 photo_posts 정의, images CHECK 1~10)
    - supabase/migrations/020_photo_posts_alter.sql (기존 status/rejection_reason/cheerleader_id ALTER)
    - .planning/phases/04-photographer/04-CONTEXT.md §D-01 (videos 컬럼 스펙)
    - .planning/phases/04-photographer/04-RESEARCH.md §Runtime State Inventory (기존 데이터 검증)
  </read_first>
  <files>supabase/migrations/029_photo_posts_videos.sql</files>
  <action>
`supabase/migrations/029_photo_posts_videos.sql` 파일을 신규 생성하고 아래 SQL 을 그대로 작성한다 (D-01 verbatim):

```sql
-- ============================================================
-- 029_photo_posts_videos.sql
-- photo_posts: videos TEXT[] 컬럼 추가 + images CHECK 1~7 로 변경 (D-01)
-- ============================================================

-- 방어: 기존 데이터가 7장 초과인지 확인 (RESEARCH.md §Runtime State Inventory)
-- 초과 row 가 있으면 이 마이그레이션은 ALTER 실패 — 수동 정리 필요.
DO $$
DECLARE
  v_max_images INTEGER;
BEGIN
  SELECT COALESCE(MAX(array_length(images, 1)), 0) INTO v_max_images FROM public.photo_posts;
  IF v_max_images > 7 THEN
    RAISE EXCEPTION 'photo_posts 에 images 8장 이상 row 존재 (max=%). 1~7 CHECK 변경 전에 수동 정리 필요.', v_max_images;
  END IF;
END $$;

-- images CHECK 제약 변경: 1~10 → 1~7
ALTER TABLE public.photo_posts DROP CONSTRAINT photo_posts_images_check;
ALTER TABLE public.photo_posts
  ADD CONSTRAINT photo_posts_images_check
  CHECK (array_length(images, 1) BETWEEN 1 AND 7);

-- videos TEXT[] 컬럼 추가: NULL 허용 x, DEFAULT '{}', 1~3 CHECK
ALTER TABLE public.photo_posts
  ADD COLUMN videos TEXT[] NOT NULL DEFAULT '{}'
  CHECK (array_length(videos, 1) IS NULL OR array_length(videos, 1) BETWEEN 1 AND 3);

-- 인덱스: videos 가 있는 post 만 필터링 가능하도록 partial index
CREATE INDEX idx_photo_posts_has_videos ON public.photo_posts((array_length(videos, 1) > 0))
  WHERE array_length(videos, 1) > 0;
```

주의사항:
- Phase 1 에서 `photo_posts_images_check` 가 자동 생성 제약명인지 확인 필요 — 명명되지 않았다면 `ALTER TABLE ... DROP CONSTRAINT ...` 은 `photo_posts_images_check` 가 맞음 (PostgreSQL 관례: `{table}_{column}_check`). 만약 `supabase db push` 중 제약명 불일치 오류 시 `\d public.photo_posts` 로 실제 이름 확인 후 수정.
- 기존 CHECK 는 `BETWEEN 1 AND 10` — 025_fix_like_count_trigger.sql ~ 028 까지 읽어 image CHECK 를 건드리는 다른 마이그레이션이 없는지 확인.
- DEFAULT '{}' 은 빈 배열 (not NULL). `NOT NULL` 컬럼이므로 INSERT 시 생략 가능.
  </action>
  <verify>
    <automated>supabase db reset 또는 supabase db push 후 psql 에서 `SELECT column_name, is_nullable, column_default FROM information_schema.columns WHERE table_name='photo_posts' AND column_name='videos';` 결과가 `videos | NO | '{}'::text[]` 이고, `\d public.photo_posts` 의 CHECK 섹션에 `array_length(images, 1) BETWEEN 1 AND 7` 가 포함되어야 한다</automated>
  </verify>
  <done>
    - supabase/migrations/029_photo_posts_videos.sql 파일 존재
    - `supabase db push` 성공 후 photo_posts.videos TEXT[] NOT NULL DEFAULT '{}' 컬럼 존재
    - images CHECK 제약이 1~7 로 변경됨
    - pgTAP (Task 6) 에서 videos/images CHECK 검증 green
    - T-4-02 완화: 영상 array 최대 3개 DB 수준 강제
  </done>
</task>

<task type="auto">
  <name>Task 2: get-upload-url Edge Function — SIZE_LIMITS 50MB 상향 (T-4-02, T-4-06)</name>
  <read_first>
    - supabase/functions/get-upload-url/index.ts (현재 SIZE_LIMITS.photo-posts = 30MB, ALLOWED_TYPES 이미 video/mp4+quicktime 포함)
    - .planning/phases/04-photographer/04-RESEARCH.md §Pitfall 2 (SIZE_LIMIT 상향 근거)
    - .planning/phases/04-photographer/04-CONTEXT.md §D-02 (contentType 검증만 확장, prefix 추가 없음)
  </read_first>
  <files>supabase/functions/get-upload-url/index.ts</files>
  <action>
`supabase/functions/get-upload-url/index.ts` 의 19줄을 수정한다:

```ts
// 기존:
const SIZE_LIMITS: Record<string, number> = {
  "photo-posts": 30 * 1024 * 1024,
  "community-posts": 10 * 1024 * 1024,
  avatars: 5 * 1024 * 1024,
};

// 변경:
const SIZE_LIMITS: Record<string, number> = {
  "photo-posts": 50 * 1024 * 1024,  // v1: 이미지 30MB / 영상 50MB 허용 (영상 최대값 기준)
  "community-posts": 10 * 1024 * 1024,
  avatars: 5 * 1024 * 1024,
};
```

추가 변경 없음:
- ALLOWED_TYPES['photo-posts'] 은 이미 `["image/jpeg","image/png","image/webp","video/mp4","video/quicktime","video/webm"]` — 수정 불필요 (ADJ-02: mp4+quicktime 둘 다 허용 이미 반영됨)
- getExtension 맵도 이미 quicktime→mov, mp4→mp4 지원 — 수정 불필요
- CORS / auth / presigned URL 로직 전부 유지

**주의:** RESEARCH.md §Pitfall 2 의 "SIZE_LIMITS 는 참고용 상수일 뿐 실제 검증 안 함"(현재 line 60-61 주석 참조) — 즉 ContentLength 바인딩을 presigned URL 에 포함하지 않는 것이 R2 호환성 이유였음. SIZE_LIMITS 상향은 "클라이언트 검증 가이드 + R2 bucket lifecycle 의 기준점" 역할. 추가 enforcement 는 v1 범위 밖 (CONTEXT.md deferred). 따라서 Task 2 는 상수값만 변경하고 PutObjectCommand 본문은 그대로 둔다 — 변경 시 SignatureDoesNotMatch 재발 (Phase 3 03-04 결정 참고).

Wave 0 T7 에서 `supabase functions deploy get-upload-url` 로 재배포 된다.
  </action>
  <verify>
    <automated>grep -n "SIZE_LIMITS" supabase/functions/get-upload-url/index.ts | grep "50 \* 1024 \* 1024" 가 1개 이상 match 되어야 한다 (photo-posts 라인)</automated>
  </verify>
  <done>
    - SIZE_LIMITS['photo-posts'] = 50 * 1024 * 1024 로 변경됨
    - ALLOWED_TYPES/CORS/auth 로직은 변경 없음 (diff 라인 수 < 5)
    - Task 7 에서 재배포 완료 시 staging curl 테스트로 영상 mp4/quicktime/size별 4개 케이스 통과
    - T-4-02 mitigated: 서버 SIZE_LIMIT = 50MB (클라이언트 D-03 과 정렬)
    - T-4-06 mitigated: ALLOWED_TYPES 기존 유지 (mp4+quicktime 만 허용, .mov 우회 안 됨)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 030_photographer_approval_trigger.sql — 승인/거절 트리거 (ADJ-01, D-06, D-11, T-4-03)</name>
  <read_first>
    - supabase/migrations/016_photographer_apps.sql (photographer_applications 기존 스키마)
    - supabase/migrations/011_users.sql (users.role CHECK 'user','admin' 만 허용 + is_photographer BOOLEAN 이미 존재)
    - supabase/migrations/013_notifications.sql (notifications 스키마)
    - supabase/migrations/023_rls_policies_remaining.sql:25-27 (notifications_admin_insert RLS — SECURITY DEFINER 로 우회 필요)
    - supabase/migrations/007_photographer.sql:23-37 (photographers UNIQUE(user_id), ON CONFLICT 근거)
    - .planning/phases/04-photographer/04-RESEARCH.md §Pattern 2 (트리거 코드), §Pitfall 1 (ADJ-01 근거), §Pitfall 5 (SECURITY DEFINER)
  </read_first>
  <behavior>
    - Test 1: pending 상태에서는 photographers 행 미생성 (트리거 미실행)
    - Test 2: status pending → approved 시 photographers 행 1개 생성 (user_id, team_id, display_name 자동 채움)
    - Test 3: photographers.team_id 가 applications.team_id 또는 users.my_team_id fallback 으로 채워짐
    - Test 4: users.is_photographer = TRUE 로 UPDATE 됨 (users.role 은 변경 안 함 — ADJ-01)
    - Test 5: notifications type='photographer_approved' 1건 INSERT
    - Test 6: pending → rejected 시 photographers 미생성 + notifications type='photographer_rejected' INSERT + rejection_reason body 에 반영
    - Test 7: 재신청 후 다시 approved 시 ON CONFLICT (user_id) DO NOTHING 로 photographers 중복 생성 안 됨
    - Test 8: notifications RLS 위반 없이 트리거 성공 (SECURITY DEFINER 동작 확인)
  </behavior>
  <files>supabase/migrations/030_photographer_approval_trigger.sql</files>
  <action>
`supabase/migrations/030_photographer_approval_trigger.sql` 파일을 신규 생성한다. **CONTEXT.md ADJ-01: `users.role` UPDATE 금지. `users.is_photographer = TRUE` 만 사용.** RESEARCH.md §Pattern 2 예시를 ADJ-01 반영으로 수정한 아래 내용을 verbatim 으로 작성:

```sql
-- ============================================================
-- 030_photographer_approval_trigger.sql
-- photographer_applications 승인/거절 트리거 (D-06, D-11, ADJ-01)
--
-- ADJ-01: users.role CHECK 는 ('user','admin') 만 허용됨 (011_users.sql:14).
--         users.role UPDATE 금지 — is_photographer BOOLEAN 컬럼 사용.
-- ============================================================

-- ─── 트리거 함수 ─────────────────────────────────────────
-- SECURITY DEFINER + SET search_path = '' (Phase 1 handle_new_user 패턴):
--   - notifications RLS (notifications_admin_insert: WITH CHECK is_admin()) 우회
--   - Supabase Security Advisor 요구사항

CREATE OR REPLACE FUNCTION public.handle_photographer_application_decision()
RETURNS TRIGGER
LANGUAGE plpgsql
SECURITY DEFINER SET search_path = ''
AS $$
DECLARE
  v_team_id UUID;
  v_display_name TEXT;
BEGIN
  -- 거절 처리: photographers 행 미생성, 알림만 INSERT
  IF NEW.status = 'rejected' AND OLD.status IS DISTINCT FROM NEW.status THEN
    INSERT INTO public.notifications (user_id, type, title, body, data)
    VALUES (
      NEW.user_id,
      'photographer_rejected',
      '포토그래퍼 신청이 거절되었습니다',
      COALESCE(NEW.rejection_reason, '제출된 자료가 요구사항을 충족하지 못했습니다. 내용을 보완해 재신청해 주세요.'),
      jsonb_build_object('application_id', NEW.id)
    );
    RETURN NEW;
  END IF;

  -- 승인 처리: photographers INSERT + users.is_photographer UPDATE + 알림
  IF NEW.status = 'approved' AND OLD.status IS DISTINCT FROM NEW.status THEN
    -- 1) team_id / display_name 결정: application.team_id 우선, fallback users.my_team_id
    SELECT
      COALESCE(NEW.team_id, u.my_team_id),
      COALESCE(NULLIF(u.nickname, ''), 'Photographer')
    INTO v_team_id, v_display_name
    FROM public.users u
    WHERE u.id = NEW.user_id;

    -- 2) photographers 행 자동 생성 (ON CONFLICT DO NOTHING: 재신청 후 재승인 안전)
    INSERT INTO public.photographers (
      user_id, display_name, team_id, is_verified, bio, follower_count, post_count
    ) VALUES (
      NEW.user_id,
      v_display_name,
      v_team_id,
      FALSE,
      COALESCE(NEW.bio, ''),
      0,
      0
    )
    ON CONFLICT (user_id) DO NOTHING;

    -- 3) users.is_photographer = TRUE (ADJ-01: role UPDATE 금지)
    UPDATE public.users
       SET is_photographer = TRUE
     WHERE id = NEW.user_id;

    -- 4) 승인 알림
    INSERT INTO public.notifications (user_id, type, title, body, data)
    VALUES (
      NEW.user_id,
      'photographer_approved',
      '포토그래퍼 신청이 승인되었습니다',
      '지금부터 사진과 영상을 업로드할 수 있어요.',
      jsonb_build_object('application_id', NEW.id)
    );
  END IF;

  RETURN NEW;
END;
$$;

-- 함수 소유권을 postgres 로 (SECURITY DEFINER 기본 동작 확인)
ALTER FUNCTION public.handle_photographer_application_decision() OWNER TO postgres;

-- ─── 트리거 등록 ─────────────────────────────────────────

DROP TRIGGER IF EXISTS trg_photographer_application_decision ON public.photographer_applications;

CREATE TRIGGER trg_photographer_application_decision
  AFTER UPDATE OF status ON public.photographer_applications
  FOR EACH ROW
  WHEN (OLD.status IS DISTINCT FROM NEW.status)
  EXECUTE FUNCTION public.handle_photographer_application_decision();
```

주의사항:
- `OLD.status IS DISTINCT FROM NEW.status` 을 함수 내부 + 트리거 WHEN 양쪽에 둠 — WHEN 조건으로 함수 호출 자체를 필터, 함수 내부 IF 는 이중 방어.
- `DROP TRIGGER IF EXISTS` 로 재실행 안전.
- `ALTER FUNCTION ... OWNER TO postgres` — SECURITY DEFINER 는 함수 소유자 권한으로 실행되므로 postgres 권한 필수. Supabase remote DB 는 기본 owner 가 postgres 이지만 명시.
- notifications 테이블이 `SELECT user_id AS user_id FROM public.users` FK 를 사용 — notifications.user_id 도 auth.users 또는 public.users FK 인지 013 확인.
- Task 6 에서 pgTAP 8 cases 로 전수 검증. **트리거 실패 시 어드민 승인이 영구 차단 (T-4-03)** — 반드시 pgTAP 통과 확인 후 배포.
  </action>
  <verify>
    <automated>supabase db push 후 psql 에서 `SELECT proname, prosecdef FROM pg_proc WHERE proname='handle_photographer_application_decision';` 결과 prosecdef=t 이고, `SELECT tgname FROM pg_trigger WHERE tgrelid='public.photographer_applications'::regclass;` 에 trg_photographer_application_decision 포함. Task 6 의 pgTAP 8 cases 모두 green</automated>
  </verify>
  <done>
    - 030_photographer_approval_trigger.sql 파일 존재
    - handle_photographer_application_decision 함수 SECURITY DEFINER + search_path='' 설정됨
    - 트리거 WHEN (OLD.status IS DISTINCT FROM NEW.status) 등록됨
    - ADJ-01 준수: 함수 본문에 `UPDATE public.users SET is_photographer = TRUE` 만 존재, `role = 'photographer'` UPDATE 없음 (grep 확인: `grep -c "role.*=.*'photographer'" supabase/migrations/030_photographer_approval_trigger.sql` = 0)
    - ON CONFLICT (user_id) DO NOTHING 절 존재
    - Task 6 pgTAP 8 cases 모두 ok
    - T-4-03 mitigated: SECURITY DEFINER + pgTAP 전수 검증
    - T-4-07 mitigated: photographers 미생성 → 008_photographer_rls.sql posts_insert_own EXISTS 자동 차단 (pgTAP Test 9 로 추가 검증)
  </done>
</task>

<task type="auto">
  <name>Task 4: 031_photographer_apps_extend.sql — team_id/activity_links/activity_plan 컬럼 추가 (D-07)</name>
  <read_first>
    - supabase/migrations/016_photographer_apps.sql (기존 photographer_applications 스키마)
    - supabase/migrations/007_photographer.sql:6-20 (teams.slug 매핑)
    - .planning/phases/04-photographer/04-CONTEXT.md §D-07 (컬럼 스펙)
    - app/src/screens/photographer/PhotographerRegisterScreen.tsx (기존 activityLinks / activityPlan state 참조 — Plan 05 에서 사용)
  </read_first>
  <files>supabase/migrations/031_photographer_apps_extend.sql</files>
  <action>
`supabase/migrations/031_photographer_apps_extend.sql` 파일을 신규 생성:

```sql
-- ============================================================
-- 031_photographer_apps_extend.sql
-- photographer_applications 컬럼 추가 (D-07)
-- - team_id: 신청자 대표 팀 (우선 승인 트리거 가 사용)
-- - activity_links: 외부 활동 URL 최대 3개
-- - activity_plan: 활동 계획 자유 텍스트
-- ============================================================

ALTER TABLE public.photographer_applications
  ADD COLUMN team_id UUID REFERENCES public.teams(id),
  ADD COLUMN activity_links TEXT[] NOT NULL DEFAULT '{}'
    CHECK (array_length(activity_links, 1) IS NULL OR array_length(activity_links, 1) <= 3),
  ADD COLUMN activity_plan TEXT NOT NULL DEFAULT '';

CREATE INDEX idx_photographer_apps_team ON public.photographer_applications(team_id)
  WHERE team_id IS NOT NULL;

COMMENT ON COLUMN public.photographer_applications.team_id IS
  '신청자가 대표 팀으로 지정한 팀 UUID (optional, 승인 트리거에서 photographers.team_id fallback 기준)';
COMMENT ON COLUMN public.photographer_applications.activity_links IS
  '외부 활동 URL 배열 (Instagram/X/YouTube 등), 최대 3개';
COMMENT ON COLUMN public.photographer_applications.activity_plan IS
  '신청자의 활동 계획 자유 텍스트';
```

주의사항:
- `activity_links CHECK array_length <= 3` — NULL 허용 대신 NOT NULL + DEFAULT '{}' + CHECK. CONTEXT.md D-07 "MAX 3" 을 DB level 에서 보장.
- `activity_plan NOT NULL DEFAULT ''` — 신청자가 빈 문자열 허용 (어드민 심사 시 판단). 033 이후 마이그레이션 에서 정책 강화 가능.
- `team_id UUID REFERENCES public.teams(id)` — NULL 허용 (users.my_team_id fallback 이 030 트리거에 있음).
- 기존 row 가 있으면 DEFAULT 로 ''/[] 자동 채움.
  </action>
  <verify>
    <automated>supabase db push 후 `SELECT column_name, data_type, is_nullable, column_default FROM information_schema.columns WHERE table_name='photographer_applications' AND column_name IN ('team_id','activity_links','activity_plan') ORDER BY column_name;` 결과 3개 행 반환 (activity_links ARRAY NO '{}'::text[], activity_plan text NO '', team_id uuid YES null)</automated>
  </verify>
  <done>
    - 031_photographer_apps_extend.sql 파일 존재
    - photographer_applications 에 team_id UUID NULL, activity_links TEXT[] NOT NULL DEFAULT '{}', activity_plan TEXT NOT NULL DEFAULT '' 컬럼 존재
    - activity_links CHECK ≤3 적용됨
    - Task 6 pgTAP photographer-apps-extend.sql 통과
    - idx_photographer_apps_team 인덱스 존재
  </done>
</task>

<task type="auto">
  <name>Task 5: 032_photo_posts_thumbnails.sql + generate-thumbnails Edge Function (D-12~D-15, T-4-05)</name>
  <read_first>
    - supabase/migrations/007_photographer.sql (photo_posts)
    - supabase/functions/get-upload-url/index.ts (Edge Function CORS/auth/R2 클라이언트 패턴 — Phase 3 03-04 결정으로 --no-verify-jwt + supabase.auth.getUser(token))
    - .planning/phases/04-photographer/04-CONTEXT.md §D-12~D-15 (thumbnail 컬럼 + EF 플로우)
    - .planning/phases/04-photographer/04-RESEARCH.md §Pattern 3 (magick-wasm Edge Function 코드), §Open Question 4 (verify_jwt 모드), §Security Domain (T-4-05 postId owner 검증)
  </read_first>
  <files>supabase/migrations/032_photo_posts_thumbnails.sql, supabase/functions/generate-thumbnails/index.ts, supabase/functions/generate-thumbnails/deno.json, supabase/functions/generate-thumbnails/magick.wasm, supabase/config.toml</files>
  <action>
**Step 5-A — 마이그레이션 032_photo_posts_thumbnails.sql 생성:**

```sql
-- ============================================================
-- 032_photo_posts_thumbnails.sql
-- photo_posts.thumbnail_urls 컬럼 추가 (D-12, D-15)
-- 기존 데이터는 '{}' 로 채워지고, Phase 4 신규 post 만 generate-thumbnails EF 로 채워짐.
-- 렌더 fallback: thumbnail_urls.length > 0 ? thumbnail_urls[0] : images[0]
-- ============================================================

ALTER TABLE public.photo_posts
  ADD COLUMN thumbnail_urls TEXT[] NOT NULL DEFAULT '{}';
```

**Step 5-B — Edge Function 코드 `supabase/functions/generate-thumbnails/index.ts` 생성 (RESEARCH.md §Pattern 3 + T-4-05 owner 검증 강화):**

```typescript
// ============================================================
// generate-thumbnails
// R2 에서 photo_posts.images 를 다운로드 → magick-wasm 로 400×400 cover crop JPEG →
// R2 업로드 (${key}_thumb.jpg) → photo_posts.thumbnail_urls UPDATE
//
// 호출: UploadPostScreen 이 createPhotoPost 성공 직후 fire-and-forget.
// Security: T-4-05 — postId owner 검증 (토큰 user.id 와 photographers.user_id 일치)
// ============================================================

import {
  initialize,
  ImageMagick,
  MagickFormat,
  MagickGeometry,
  Gravity,
} from 'npm:@imagemagick/magick-wasm@0.0.39';
import { createClient } from 'npm:@supabase/supabase-js@2';
import {
  S3Client,
  PutObjectCommand,
} from 'npm:@aws-sdk/client-s3@3';

// WASM 1회 초기화 (cold start 시 1.5~3초)
const wasmBytes = await Deno.readFile(new URL('./magick.wasm', import.meta.url));
await initialize(wasmBytes);

const SUPABASE_URL = Deno.env.get('SUPABASE_URL')!;
const SERVICE_ROLE_KEY = Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!;

const R2_ACCOUNT_ID = Deno.env.get('R2_ACCOUNT_ID')!;
const R2_ACCESS_KEY_ID = Deno.env.get('R2_ACCESS_KEY_ID')!;
const R2_SECRET_ACCESS_KEY = Deno.env.get('R2_SECRET_ACCESS_KEY')!;
const R2_BUCKET_NAME = Deno.env.get('R2_BUCKET_NAME')!;
const R2_PUBLIC_URL = Deno.env.get('R2_PUBLIC_URL')!;

const s3 = new S3Client({
  region: 'auto',
  endpoint: `https://${R2_ACCOUNT_ID}.r2.cloudflarestorage.com`,
  credentials: {
    accessKeyId: R2_ACCESS_KEY_ID,
    secretAccessKey: R2_SECRET_ACCESS_KEY,
  },
  requestChecksumCalculation: 'WHEN_REQUIRED',
  responseChecksumValidation: 'WHEN_REQUIRED',
});

// CORS (get-upload-url 패턴)
const ALLOWED_ORIGINS = [
  'https://udamonfan.com',
  'https://www.udamonfan.com',
  'https://admin.udamonfan.com',
];

function getCorsHeaders(req: Request): Record<string, string> {
  const origin = req.headers.get('origin') ?? '';
  const isNativeApp = origin === '';
  const isAllowedOrigin = ALLOWED_ORIGINS.includes(origin);
  const allowedOrigin = isNativeApp ? '*' : isAllowedOrigin ? origin : ALLOWED_ORIGINS[0];
  return {
    'Access-Control-Allow-Origin': allowedOrigin,
    'Access-Control-Allow-Methods': 'POST, OPTIONS',
    'Access-Control-Allow-Headers': 'Authorization, Content-Type, apikey, x-client-info',
  };
}

function jsonResponse(body: unknown, status = 200, req: Request): Response {
  return new Response(JSON.stringify(body), {
    status,
    headers: { ...getCorsHeaders(req), 'Content-Type': 'application/json' },
  });
}

interface ThumbnailRequest {
  postId: string;
  imageUrls: string[];
}

Deno.serve(async (req: Request) => {
  if (req.method === 'OPTIONS') {
    return new Response(null, { status: 204, headers: getCorsHeaders(req) });
  }
  if (req.method !== 'POST') {
    return jsonResponse({ error: 'Method not allowed' }, 405, req);
  }

  // ─── Auth (Phase 3 03-04 --no-verify-jwt 패턴) ───
  const authHeader = req.headers.get('Authorization');
  if (!authHeader?.startsWith('Bearer ')) {
    return jsonResponse({ error: 'Missing Authorization' }, 401, req);
  }
  const token = authHeader.replace('Bearer ', '');
  const supabase = createClient(SUPABASE_URL, SERVICE_ROLE_KEY);
  const { data: userData, error: authError } = await supabase.auth.getUser(token);
  if (authError || !userData.user) {
    return jsonResponse({ error: 'Unauthorized' }, 401, req);
  }
  const userId = userData.user.id;

  // ─── Parse body ───
  let body: ThumbnailRequest;
  try {
    body = await req.json();
  } catch {
    return jsonResponse({ error: 'Invalid JSON' }, 400, req);
  }
  const { postId, imageUrls } = body;
  if (!postId || !Array.isArray(imageUrls) || imageUrls.length === 0) {
    return jsonResponse({ error: 'Bad request' }, 400, req);
  }

  // ─── T-4-05: postId owner 검증 ───
  // photo_posts → photographers → user_id 조인으로 본인 게시물만 허용
  const { data: ownerRow, error: ownerErr } = await supabase
    .from('photo_posts')
    .select('id, photographers!inner(user_id)')
    .eq('id', postId)
    .maybeSingle();
  if (ownerErr || !ownerRow) {
    return jsonResponse({ error: 'Post not found' }, 404, req);
  }
  // deno-lint-ignore no-explicit-any
  const ownerUserId = (ownerRow.photographers as any)?.user_id;
  if (ownerUserId !== userId) {
    return jsonResponse({ error: 'Forbidden: not post owner' }, 403, req);
  }

  // ─── 썸네일 생성 (이미지당 순차 처리 — Open Question 5 메모리 안전) ───
  const thumbnailUrls: string[] = [];
  const failures: string[] = [];

  for (const imageUrl of imageUrls) {
    try {
      const res = await fetch(imageUrl);
      if (!res.ok) throw new Error(`download ${res.status}`);
      const buf = new Uint8Array(await res.arrayBuffer());

      let outBytes: Uint8Array | null = null;
      ImageMagick.read(buf, (image) => {
        // Cover crop 400×400: fillArea=true resize → center crop
        const fitGeometry = new MagickGeometry(400, 400);
        fitGeometry.fillArea = true;
        image.resize(fitGeometry);
        const cropGeometry = new MagickGeometry(400, 400);
        image.crop(cropGeometry, Gravity.Center);
        image.format = MagickFormat.Jpeg;
        image.quality = 80;
        image.write((data) => {
          outBytes = data;
        });
      });

      if (!outBytes) throw new Error('encode failed');

      // R2 업로드: original key + _thumb.jpg
      const key = new URL(imageUrl).pathname.slice(1);
      const thumbKey = key.replace(/\.[^.]+$/, '_thumb.jpg');

      await s3.send(
        new PutObjectCommand({
          Bucket: R2_BUCKET_NAME,
          Key: thumbKey,
          Body: outBytes,
          ContentType: 'image/jpeg',
          CacheControl: 'public, max-age=31536000, immutable',
        }),
      );

      thumbnailUrls.push(`${R2_PUBLIC_URL}/${thumbKey}`);
    } catch (e) {
      console.error('[Thumbnail] failed', imageUrl, e);
      failures.push(imageUrl);
    }
  }

  // ─── photo_posts.thumbnail_urls UPDATE (service role) ───
  if (thumbnailUrls.length > 0) {
    const { error: updateErr } = await supabase
      .from('photo_posts')
      .update({ thumbnail_urls: thumbnailUrls })
      .eq('id', postId);
    if (updateErr) {
      console.error('[Thumbnail] DB update failed', updateErr);
    }
  }

  return jsonResponse({ thumbnailUrls, failures }, 200, req);
});
```

**Step 5-C — supabase/functions/generate-thumbnails/deno.json 생성:**

```json
{
  "imports": {
    "@imagemagick/magick-wasm": "npm:@imagemagick/magick-wasm@0.0.39",
    "@supabase/supabase-js": "npm:@supabase/supabase-js@2",
    "@aws-sdk/client-s3": "npm:@aws-sdk/client-s3@3"
  }
}
```

**Step 5-D — magick.wasm 파일 복사:**

```bash
# npm install 후 지역 복사
cd app && npm install @imagemagick/magick-wasm@0.0.39 --save-dev
cp app/node_modules/@imagemagick/magick-wasm/dist/magick.wasm supabase/functions/generate-thumbnails/magick.wasm
```

**Step 5-E — supabase/config.toml 수정 (WASM 정적 파일 등록):**

기존 파일에 아래 블록 추가 (Supabase docs §Wasm modules 패턴):

```toml
[functions.generate-thumbnails]
verify_jwt = false
# Phase 3 get-upload-url 과 동일 — Supabase gateway 가 ES256 rotation 후 HS256 만 검증 →
# internal 에서 supabase.auth.getUser(token) 로 재검증.

[[functions.generate-thumbnails.static_files]]
files = ['./functions/generate-thumbnails/magick.wasm']
```

주의사항:
- Step 5-D 의 magick-wasm 패키지는 **Edge Function deploy 용 WASM 파일 복사를 위한 전용 devDependency** — app 런타임 에서 import 하지 않음. Wave 0 에서만 필요, 빌드/실행 번들 사이즈에 영향 없음.
- `verify_jwt = false` — Phase 3 03-04 결정에 따라 Supabase platform gateway 가 ES256 rotation 후 user token 을 거부하는 이슈 우회. 내부에서 `supabase.auth.getUser(token)` 이 정확히 검증.
- T-4-05 owner 검증: token user.id 와 post → photographers → user_id 조회 결과 비교. 불일치 403.
- magick-wasm 콜드 스타트 1.5~3초 예상 — 첫 호출 응답 지연 가능.
- Open Question 5 (메모리): 이미지당 순차 처리 — 병렬 처리 시 OOM 가능 (7장 × 10MB + WASM 10MB).
  </action>
  <verify>
    <automated>ls supabase/functions/generate-thumbnails/index.ts supabase/functions/generate-thumbnails/deno.json supabase/functions/generate-thumbnails/magick.wasm 이 모두 존재하고, grep -n "verify_jwt = false" supabase/config.toml 에 generate-thumbnails 섹션 포함. Task 7 배포 후 `curl` POST staging EF 에 샘플 800×600 PNG URL 전달 → 응답 thumbnailUrls 배열 1개 + 해당 URL 을 curl -sI 로 GET 하여 Content-Type: image/jpeg + dimensions 400×400 확인</automated>
  </verify>
  <done>
    - 032_photo_posts_thumbnails.sql 파일 존재, thumbnail_urls TEXT[] NOT NULL DEFAULT '{}' 컬럼 추가됨
    - supabase/functions/generate-thumbnails/index.ts + deno.json + magick.wasm 존재
    - supabase/config.toml 에 generate-thumbnails 섹션 + static_files 등록됨
    - Task 7 배포 후 curl 샘플 호출로 400×400 JPEG 생성 + photo_posts.thumbnail_urls UPDATE 확인
    - T-4-05 mitigated: postId owner 검증 로직 포함 (grep "not post owner" 1개 match)
  </done>
</task>

<task type="auto">
  <name>Task 6: pgTAP 테스트 5종 + QA 매트릭스 문서 + expo-video 설치</name>
  <read_first>
    - .planning/phases/04-photographer/04-RESEARCH.md §Code Examples Example 6 (pgTAP 트리거 테스트 예시)
    - .planning/phases/04-photographer/04-VALIDATION.md §Wave 0 Requirements (pgTAP 파일 목록)
    - supabase/migrations/009_seed_photographer.sql (teams slug 참조)
    - Tasks 1, 3, 4, 5 (본 plan 의 4개 마이그레이션 파일 — DDL 검증 대상)
    - app/package.json (expo-video 미설치 확인)
  </read_first>
  <files>supabase/tests/photographer-approval-trigger.sql, supabase/tests/photo-posts-videos-check.sql, supabase/tests/photo-posts-images-1-7-check.sql, supabase/tests/photographer-apps-extend.sql, supabase/tests/photo-posts-thumbnails.sql, docs/phase4-qa-matrix.md, app/package.json, app/package-lock.json</files>
  <action>
**Step 6-A — supabase/tests/photographer-approval-trigger.sql (pgTAP 8 케이스):**

RESEARCH.md §Example 6 의 8 케이스를 **ADJ-01 반영** 하여 생성 (users.role 검증 → users.is_photographer 검증 으로 교체):

```sql
BEGIN;
SELECT plan(9);

-- Setup (auth.users + public.users 자동 생성 via handle_new_user)
INSERT INTO auth.users (id, email) VALUES ('11111111-1111-1111-1111-111111111111', 'pg-test@example.com');
INSERT INTO public.photographer_applications (user_id, team_id, status, activity_plan)
  VALUES (
    '11111111-1111-1111-1111-111111111111',
    (SELECT id FROM public.teams WHERE slug = 'ssg'),
    'pending',
    'test plan'
  );

-- Test 1: pending 상태에서 photographers 행 없음
SELECT is(
  (SELECT count(*) FROM public.photographers WHERE user_id = '11111111-1111-1111-1111-111111111111'),
  0::bigint,
  'T1: pending 에서는 photographers 미생성'
);

-- Test 2: approved UPDATE 시 photographers INSERT
UPDATE public.photographer_applications
   SET status = 'approved'
 WHERE user_id = '11111111-1111-1111-1111-111111111111';

SELECT is(
  (SELECT count(*) FROM public.photographers WHERE user_id = '11111111-1111-1111-1111-111111111111'),
  1::bigint,
  'T2: approved 트리거가 photographers INSERT'
);

-- Test 3: photographers.team_id = application.team_id
SELECT is(
  (SELECT team_id FROM public.photographers WHERE user_id = '11111111-1111-1111-1111-111111111111'),
  (SELECT id FROM public.teams WHERE slug = 'ssg'),
  'T3: photographers.team_id 매칭'
);

-- Test 4: users.is_photographer = TRUE (ADJ-01: role UPDATE 금지)
SELECT is(
  (SELECT is_photographer FROM public.users WHERE id = '11111111-1111-1111-1111-111111111111'),
  TRUE,
  'T4: users.is_photographer = TRUE'
);

-- Test 4b: ADJ-01 방어 — users.role 은 그대로 'user'
SELECT is(
  (SELECT role FROM public.users WHERE id = '11111111-1111-1111-1111-111111111111'),
  'user',
  'T4b (ADJ-01): users.role 변경 없음, user 유지'
);

-- Test 5: photographer_approved notification INSERT
SELECT is(
  (SELECT count(*) FROM public.notifications
   WHERE user_id = '11111111-1111-1111-1111-111111111111' AND type = 'photographer_approved'),
  1::bigint,
  'T5: approved 알림 1건 INSERT'
);

-- Test 6: 거절 케이스 — 신규 user
INSERT INTO auth.users (id, email) VALUES ('22222222-2222-2222-2222-222222222222', 'pg-test2@example.com');
INSERT INTO public.photographer_applications (user_id, team_id, status, rejection_reason, activity_plan)
  VALUES (
    '22222222-2222-2222-2222-222222222222',
    (SELECT id FROM public.teams WHERE slug = 'lg'),
    'pending',
    '자료 부족',
    'test'
  );

UPDATE public.photographer_applications
   SET status = 'rejected', rejection_reason = '자료 부족'
 WHERE user_id = '22222222-2222-2222-2222-222222222222';

SELECT is(
  (SELECT count(*) FROM public.photographers WHERE user_id = '22222222-2222-2222-2222-222222222222'),
  0::bigint,
  'T6: rejected 에서 photographers 미생성'
);

SELECT is(
  (SELECT count(*) FROM public.notifications
   WHERE user_id = '22222222-2222-2222-2222-222222222222' AND type = 'photographer_rejected'),
  1::bigint,
  'T7: rejected 알림 INSERT'
);

-- Test 8: 재승인 — ON CONFLICT DO NOTHING
UPDATE public.photographer_applications
   SET status = 'approved'
 WHERE user_id = '22222222-2222-2222-2222-222222222222';

SELECT is(
  (SELECT count(*) FROM public.photographers WHERE user_id = '22222222-2222-2222-2222-222222222222'),
  1::bigint,
  'T8: ON CONFLICT 로 중복 INSERT 방지, 1개만'
);

-- Test 9 (T-4-07 방어): pending 사용자는 photo_posts INSERT 불가능 (RLS)
-- pending user (위의 11111111 은 이미 approved) — 새 pending user 생성
INSERT INTO auth.users (id, email) VALUES ('33333333-3333-3333-3333-333333333333', 'pg-pending@example.com');
INSERT INTO public.photographer_applications (user_id, team_id, status, activity_plan)
  VALUES (
    '33333333-3333-3333-3333-333333333333',
    (SELECT id FROM public.teams WHERE slug = 'doosan'),
    'pending',
    'pending test'
  );
-- 3번째 user 는 photographers 에 row 없음 (pending) → photo_posts INSERT 시 posts_insert_own 차단
SELECT throws_ok(
  $$INSERT INTO public.photo_posts (photographer_id, team_id, title, images)
    VALUES (gen_random_uuid(), (SELECT id FROM public.teams WHERE slug='doosan'), 'x', ARRAY['x']::TEXT[])$$,
  '42501',
  NULL,
  'T9 (T-4-07): pending 사용자는 photo_posts INSERT 차단 (RLS)'
);

SELECT * FROM finish();
ROLLBACK;
```

**Step 6-B — supabase/tests/photo-posts-videos-check.sql (videos CHECK):**

```sql
BEGIN;
SELECT plan(3);

-- videos 0개 (DEFAULT '{}') INSERT 허용
SELECT lives_ok(
  $$INSERT INTO public.photo_posts (photographer_id, team_id, title, images)
    SELECT gen_random_uuid(), (SELECT id FROM public.teams WHERE slug='ssg'), 'vt1', ARRAY['u']::TEXT[]$$,
  'videos DEFAULT {} INSERT 허용'
);

-- videos 3개 CHECK 허용
SELECT lives_ok(
  $$INSERT INTO public.photo_posts (photographer_id, team_id, title, images, videos)
    SELECT gen_random_uuid(), (SELECT id FROM public.teams WHERE slug='ssg'), 'vt2', ARRAY['u']::TEXT[],
           ARRAY['v1','v2','v3']::TEXT[]$$,
  'videos 3개 허용'
);

-- videos 4개 CHECK 실패
SELECT throws_ok(
  $$INSERT INTO public.photo_posts (photographer_id, team_id, title, images, videos)
    SELECT gen_random_uuid(), (SELECT id FROM public.teams WHERE slug='ssg'), 'vt3', ARRAY['u']::TEXT[],
           ARRAY['v1','v2','v3','v4']::TEXT[]$$,
  '23514',
  NULL,
  'videos 4개 CHECK 실패'
);

SELECT * FROM finish();
ROLLBACK;
```

**Step 6-C — supabase/tests/photo-posts-images-1-7-check.sql:**

```sql
BEGIN;
SELECT plan(2);

SELECT lives_ok(
  $$INSERT INTO public.photo_posts (photographer_id, team_id, title, images)
    SELECT gen_random_uuid(), (SELECT id FROM public.teams WHERE slug='ssg'), 'it1',
           ARRAY['u1','u2','u3','u4','u5','u6','u7']::TEXT[]$$,
  'images 7장 허용'
);

SELECT throws_ok(
  $$INSERT INTO public.photo_posts (photographer_id, team_id, title, images)
    SELECT gen_random_uuid(), (SELECT id FROM public.teams WHERE slug='ssg'), 'it2',
           ARRAY['u1','u2','u3','u4','u5','u6','u7','u8']::TEXT[]$$,
  '23514',
  NULL,
  'images 8장 CHECK 실패'
);

SELECT * FROM finish();
ROLLBACK;
```

**Step 6-D — supabase/tests/photographer-apps-extend.sql:**

```sql
BEGIN;
SELECT plan(5);

SELECT has_column('public', 'photographer_applications', 'team_id', 'team_id 컬럼 존재');
SELECT has_column('public', 'photographer_applications', 'activity_links', 'activity_links 컬럼 존재');
SELECT has_column('public', 'photographer_applications', 'activity_plan', 'activity_plan 컬럼 존재');

-- activity_links CHECK ≤ 3
INSERT INTO auth.users (id, email) VALUES ('44444444-4444-4444-4444-444444444444', 'apps@e.com');
SELECT lives_ok(
  $$INSERT INTO public.photographer_applications (user_id, activity_links, activity_plan)
    VALUES ('44444444-4444-4444-4444-444444444444', ARRAY['a','b','c']::TEXT[], 'plan')$$,
  'activity_links 3개 허용'
);

SELECT throws_ok(
  $$INSERT INTO public.photographer_applications (user_id, activity_links, activity_plan)
    VALUES ('44444444-4444-4444-4444-444444444444', ARRAY['a','b','c','d']::TEXT[], 'plan')$$,
  '23514',
  NULL,
  'activity_links 4개 CHECK 실패'
);

SELECT * FROM finish();
ROLLBACK;
```

**Step 6-E — supabase/tests/photo-posts-thumbnails.sql:**

```sql
BEGIN;
SELECT plan(2);

SELECT has_column('public', 'photo_posts', 'thumbnail_urls', 'thumbnail_urls 컬럼 존재');

SELECT col_default_is(
  'public', 'photo_posts', 'thumbnail_urls', '{}'::TEXT[], 'DEFAULT {} 이어야 함'
);

SELECT * FROM finish();
ROLLBACK;
```

**Step 6-F — docs/phase4-qa-matrix.md (Wave 3 실기기 QA 매트릭스 — Markdown 체크리스트 형식):**

```markdown
# Phase 4 — Manual QA Matrix

> Wave 3 완료 전 반드시 통과해야 하는 실기기 검증.

## A. 영상 업로드 (PHOT-03, T-4-02/06)
- [ ] iOS 16+: 갤러리에서 25초 H.264 MOV 영상 선택 → Alert 없음 → 업로드 성공 → 피드 썸네일 + 재생 OK
- [ ] iOS 16+: 45초 영상 선택 → "영상 길이 초과" Alert 표시, 업로드 차단
- [ ] iOS 16+: 60MB 영상 선택 → "파일 크기 초과" Alert, 업로드 차단
- [ ] Android API 30+: 25초 MP4 영상 업로드 성공
- [ ] Android API 30+: 3개 업로드 후 4번째 시도 → "최대 3개" Alert
- [ ] 네트워크 끊김 상황: 업로드 실패 Alert + 폼 유지

## B. VideoPlayer (PHOT-04)
- [ ] iOS: feed 모드 자동 muted 재생, 탭 시 상세 화면 이동
- [ ] iOS: detail 모드 탭 → native controls 노출, 정지/재생/seek OK
- [ ] iOS: PiP 지원 (detail 모드)
- [ ] Android: feed scroll 중 viewport 벗어난 아이템 pause 확인
- [ ] Android: Back 버튼 누를 시 player cleanup 됨 (메모리 leak 없음)

## C. Studio state machine (PHOT-02, D-09)
- [ ] No application: Studio 탭 진입 → "포토그래퍼로 활동하기" CTA 표시 → Register 화면 이동
- [ ] Pending: Studio 탭 → "심사 대기 중" 화면 + 예상 시간 안내
- [ ] Approved: Studio 탭 → 기존 스튜디오 UI + 상단 GradeBadge 노출
- [ ] Rejected: Studio 탭 → "거절됨" 화면 + rejection_reason + 재신청 버튼

## D. GradeBadge (PHOT-06)
- [ ] 0 posts, 0 followers → 브론즈 (#A97142)
- [ ] 10 posts, 0 followers → 실버
- [ ] 30 posts, 0 followers → 골드 (#FACC15)
- [ ] 50 posts, 0 followers → 다이아 (#1B2A4A)
- [ ] PhotographerProfileScreen header 에 `icon-label` `md`, PhotographerCard 에 `icon` 20px

## E. Cheerleader selector (PHOT-07)
- [ ] my_team=SSG 로 UploadPostScreen 진입 → 치어리더 selector 에 '박기량' 등 SSG 치어리더만 표시
- [ ] my_team=두산 으로 전환 후 selector 리로드 → 두산 치어리더만 표시
- [ ] 선택한 cheerleader_id 가 photo_posts.cheerleader_id 에 저장됨

## F. CollectionDetail (PHOT-08)
- [ ] 컬렉션 생성 → await 이후 state 반영 (optimistic 없음)
- [ ] CollectionDetailScreen 진입 → photo_collection_posts JOIN 으로 posts 표시

## G. 어드민 승인 → 사용자 반영 (PHOT-02, 트리거)
- [ ] 스테이징 DB 에서 `UPDATE photographer_applications SET status='approved' WHERE user_id=X` 수동 실행
- [ ] 테스트 사용자 앱 재실행 → Studio 탭 진입 시 approved 상태 반영 (AuthContext.user.is_photographer)

## H. Range Request 성능 (Open Question 3)
- [ ] iOS: 50MB MP4 업로드 후 feed 재생 → 첫 프레임까지 5초 이내 OK 여부 기록
- [ ] 5초 초과 시 → Phase 6 또는 v2 에 Cloudflare Worker Range 보정 이슈 생성

---
*Wave 3 QA 실기기 세션 일자:* _____
*통과율:* __ / __
*담당자:* _____
```

**Step 6-G — expo-video 설치:**

```bash
cd app && npx expo install expo-video
```

실행 후 `app/package.json` 에 `"expo-video"` 가 dependencies 에 추가되고 `app/package-lock.json` 이 업데이트됨.

`npx expo-doctor` 실행해서 `expo-video` 와 Expo SDK 54 호환성 확인 (경고 없음).
  </action>
  <verify>
    <automated>supabase test db --file photographer-approval-trigger.sql + --file photo-posts-videos-check.sql + --file photo-posts-images-1-7-check.sql + --file photographer-apps-extend.sql + --file photo-posts-thumbnails.sql — 5개 파일 모두 ok. grep -q "expo-video" app/package.json (dependencies 에 존재). ls docs/phase4-qa-matrix.md 존재 + 100줄 이상</automated>
  </verify>
  <done>
    - supabase/tests/*.sql 5개 파일 존재
    - photographer-approval-trigger.sql 에 plan(9) + Test 9 (T-4-07) 포함
    - pgTAP 전부 green (supabase test db)
    - docs/phase4-qa-matrix.md 존재, 8개 섹션(A~H) 포함
    - app/package.json 에 expo-video dependencies 추가됨
    - npx expo-doctor 경고 없음
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: [BLOCKING] supabase db push + functions deploy — 원격 배포 검증</name>
  <what-built>
    Wave 0 의 모든 서버측 변경을 원격 Supabase 프로젝트에 반영:
    - supabase db push — 029~032 마이그레이션 적용
    - supabase functions deploy generate-thumbnails — WASM static_files 포함 신규 배포
    - supabase functions deploy get-upload-url — SIZE_LIMITS 상향 재배포
    - pgTAP 테스트 5개 파일 원격 실행 검증
    - expo-video native module (Plan 04 에서 사용) — EAS development 빌드 재생성 안내
  </what-built>
  <how-to-verify>
    **자동 실행 단계 (실행자가 먼저 시도):**
    
    1. 환경 변수 확인:
       ```bash
       echo "SUPABASE_ACCESS_TOKEN: ${SUPABASE_ACCESS_TOKEN:+set}"
       supabase --version
       ```
       - `SUPABASE_ACCESS_TOKEN` 이 set 이어야 함. 미설정 시 `supabase login` 필요 (대화형 — 사용자 개입).
    
    2. DB push:
       ```bash
       supabase db push
       ```
       - 029, 030, 031, 032 마이그레이션이 순차 적용됨을 확인.
    
    3. Functions deploy:
       ```bash
       supabase functions deploy generate-thumbnails
       supabase functions deploy get-upload-url
       ```
       - generate-thumbnails 배포 시 magick.wasm static_files 포함 확인 (로그에 file count 2).
       - verify_jwt=false 설정이 config.toml 에서 정상 적용됨 확인.
    
    4. 원격 pgTAP 실행:
       ```bash
       supabase test db
       ```
       - 5개 파일 모두 ok (plan 수만큼 테스트 green).
    
    5. Edge Function 샘플 호출:
       ```bash
       # staging Supabase project_ref / user token 필요
       curl -X POST "${SUPABASE_URL}/functions/v1/generate-thumbnails" \
         -H "Authorization: Bearer ${TEST_USER_TOKEN}" \
         -H "Content-Type: application/json" \
         -d '{"postId":"<staging_post_id>","imageUrls":["<staging_image_url>"]}'
       ```
       - 응답 `{ thumbnailUrls: [...], failures: [] }` 확인.
       - 응답 URL curl -sI 로 HEAD 하여 `Content-Type: image/jpeg` 확인.
    
    6. get-upload-url 영상 MIME/SIZE 검증:
       ```bash
       curl -X POST "${SUPABASE_URL}/functions/v1/get-upload-url" \
         -H "Authorization: Bearer ${TEST_USER_TOKEN}" \
         -H "Content-Type: application/json" \
         -d '{"prefix":"photo-posts","contentType":"video/quicktime","count":1}'
       ```
       - 200 응답 + uploads[0].uploadUrl 발급 확인.
    
    **사용자 검증 단계:**
    
    - [ ] Supabase Dashboard → Database → Migrations 에 029~032 4개 표시됨
    - [ ] Supabase Dashboard → Edge Functions 에 `generate-thumbnails` 추가됨 + `get-upload-url` 갱신됨
    - [ ] pgTAP 결과 모두 ok (red/fail 없음)
    - [ ] Sample Edge Function 호출 응답 정상
    - [ ] 만약 `supabase login` 대화형 프롬프트 발생 → 사용자가 직접 브라우저 인증 완료
    - [ ] 만약 `Permission denied` 또는 `Invalid project ref` → `supabase link --project-ref <ref>` 실행 후 재시도
  </how-to-verify>
  <files>Deployment operations — no file changes; verifies supabase CLI apply + deploy</files>
  <action>
실행자는 아래 bash 시퀀스를 실제로 실행하여 Wave 0 결과물을 원격 Supabase 에 반영한다. 환경 변수 SUPABASE_ACCESS_TOKEN 미설정 시 supabase login 대화형 프롬프트가 뜨므로 사용자에게 이양. 각 단계는 실패 가능성이 있으므로 순차 실행 + 실패 지점에서 즉시 중단:

```bash
# 0) 환경 변수/로그인 확인
echo "SUPABASE_ACCESS_TOKEN: ${SUPABASE_ACCESS_TOKEN:+set}"
supabase --version

# 1) DB push — 029~032 마이그레이션 적용
supabase db push

# 2) Edge Functions 배포
supabase functions deploy generate-thumbnails
supabase functions deploy get-upload-url

# 3) 원격 pgTAP 실행
supabase test db

# 4) Edge Function 샘플 호출 (사용자에게 staging URL/token 요청)
curl -X POST "${SUPABASE_URL}/functions/v1/generate-thumbnails" \
  -H "Authorization: Bearer ${TEST_USER_TOKEN}" \
  -H "Content-Type: application/json" \
  -d "{\"postId\":\"<staging_post_id>\",\"imageUrls\":[\"<staging_image_url>\"]}"

# 5) get-upload-url video MIME 검증
curl -X POST "${SUPABASE_URL}/functions/v1/get-upload-url" \
  -H "Authorization: Bearer ${TEST_USER_TOKEN}" \
  -H "Content-Type: application/json" \
  -d "{\"prefix\":\"photo-posts\",\"contentType\":\"video/quicktime\",\"count\":1}"
```

만약 `supabase login` 대화형 프롬프트 발생 또는 `Permission denied`/`Invalid project ref` 가 나타나면 checkpoint 로 사용자에게 이양. 사용자가 브라우저 인증 완료 또는 `supabase link --project-ref <ref>` 실행 후 재시도하도록 안내. 자동 실행이 모두 성공하면 사용자에게 Supabase Dashboard 검증 체크리스트 (Migrations 029~032, Edge Functions 2개, pgTAP 결과) 를 제시하고 resume-signal 대기.
  </action>
  <verify>
    <automated>supabase db remote list 에 029/030/031/032 4개가 applied 상태로 나타나야 하고, supabase functions list 에 generate-thumbnails + get-upload-url 포함, supabase test db 가 5 파일 ok</automated>
  </verify>
  <done>
    - supabase db push 성공
    - generate-thumbnails Edge Function 신규 배포 완료
    - get-upload-url Edge Function 재배포 완료 (SIZE_LIMITS 50MB 반영)
    - supabase test db 5개 파일 모두 ok
    - 사용자 approved — migrations pushed, edge functions deployed, pgTAP green 신호 수신
  </done>
  <resume-signal>
    "approved — migrations pushed, edge functions deployed, pgTAP green" 또는 실패 원인 + 수정 지침 명시
  </resume-signal>
</task>

</tasks>

<verification>
Wave 0 완료 후 Wave 1+ 진입 전 필수:

1. `supabase db push` 성공 (029~032)
2. `supabase functions deploy generate-thumbnails get-upload-url` 성공
3. `supabase test db` 5개 파일 모두 ok
4. Edge Function staging 샘플 호출 정상 (400×400 JPEG 반환 + photo_posts.thumbnail_urls UPDATE)
5. get-upload-url video/quicktime + 50MB 상향 재배포 확인
6. app/package.json expo-video 추가 + npx expo-doctor 경고 없음
7. docs/phase4-qa-matrix.md 8개 섹션 존재
8. ADJ-01 컴플라이언스: 030 마이그레이션 본문에 `role.*=.*'photographer'` 패턴 0건, `is_photographer.*=.*TRUE` 패턴 1건

Threat 검증:
- T-4-02: Task 2 SIZE_LIMITS 50MB 확인
- T-4-03: Task 6 pgTAP 9 cases green
- T-4-05: Task 5 EF owner 검증 로직 존재
- T-4-06: Task 2 ALLOWED_TYPES mp4+quicktime 유지 재배포
- T-4-07: Task 6 pgTAP Test 9 green
</verification>

<success_criteria>
- [ ] 029/030/031/032 마이그레이션 4개 파일 생성 + 원격 적용
- [ ] generate-thumbnails Edge Function 신규 배포
- [ ] get-upload-url Edge Function SIZE_LIMITS 50MB 재배포
- [ ] pgTAP 5개 파일 모두 green (총 plan 수 = 9+3+2+5+2 = 21 테스트)
- [ ] expo-video 설치 완료
- [ ] docs/phase4-qa-matrix.md 존재
- [ ] ADJ-01/ADJ-02 반영 검증
- [ ] T-4-02/03/05/06/07 mitigation 배포 반영
- [ ] BLOCKING checkpoint 승인
</success_criteria>

<output>
완료 후 `.planning/phases/04-photographer/04-01-SUMMARY.md` 생성 — 배포된 마이그레이션/EF 목록 + pgTAP 결과 + Edge Function 배포 URL + Wave 1 인계 사항 (특히 is_photographer 컬럼 사용 + video/mp4+quicktime MIME 허용 정책).
</output>
</content>
</invoke>