---
phase: 04-photographer
plan: 03
type: execute
wave: 2
depends_on:
  - "04-02"
files_modified:
  - app/src/contexts/PhotographerContext.tsx
  - app/src/data/mockPhotographers.ts
  - app/src/data/mockCheerleaders.ts
autonomous: true
requirements:
  - PHOT-01
  - PHOT-06
  - PHOT-07
  - PHOT-08
threats: []
user_setup: []

must_haves:
  truths:
    - "PhotographerContext.tsx 가 mock import (MOCK_PHOTOGRAPHERS, MOCK_PHOTO_POSTS, MOCK_PLAYERS, MOCK_EVENTS, MOCK_CHEERLEADERS, MOCK_COLLECTIONS, MOCK_PHOTO_COMMENTS) 0건"
    - "isRemote / isRemoteRef / merge 로직 완전 제거"
    - "Provider mount 시 초기 state 는 빈 배열 + loading=true → refreshData 로 Supabase fetch 결과 세팅"
    - "togglePhotoLike / toggleFollow 내부에서 useAuth().user?.id 사용 — 빈 문자열 전달 금지, 비로그인 시 useLoginGate() 호출"
    - "fetchUserPhotoLikes / fetchUserFollows 로 로그인 사용자의 likedIds / followedPgIds 복원"
    - "컬렉션 조작 (createCollection / addPostToCollection / removePostFromCollection / deleteCollection / renameCollection) 이 await 후 state 갱신 — optimistic 제거"
    - "좋아요 / 팔로우 는 optimistic + 실패 rollback 유지 (DB 트리거가 count 최종 일치)"
    - "photoLikedIds / followedPgIds 는 Set<string> 상태 유지"
    - "getPhotoPostsByTeam / getPhotoPostsByPhotographer 가 페이지네이션 지원 (loadMorePhotoPosts 메소드 추가)"
    - "getCheerleadersByTeam 이 fetchCheerleaders 결과 기반 (팀 slug 기준 필터)"
    - "fetchCollectionPosts 를 사용하는 getCollectionPosts(collectionId) 메소드 노출"
    - "mockPhotographers.ts 와 mockCheerleaders.ts 파일 삭제 또는 빈 export 로 전환"
    - "cd app && npx tsc --noEmit exits 0 (UploadPostScreen / StudioScreen 등에서 일부 Plan 04/05 대기 오류 허용은 문서화)"
    - "! git grep -l 'mockPhotographers\\|mockCheerleaders' app/src/ 결과 비어있음 (mock 참조 완전 제거)"
  artifacts:
    - path: "app/src/contexts/PhotographerContext.tsx"
      provides: "Supabase 전용 photographer 도메인 state — mock 완전 제거"
      min_lines: 400
      exports:
        - "PhotographerProvider"
        - "usePhotographer"
        - "PhotoComment (type)"
        - "PgCollection (type)"
  key_links:
    - from: "app/src/contexts/PhotographerContext.tsx"
      to: "app/src/services/photographerApi.ts"
      via: "import * as photographerApi — 모든 remote 호출"
      pattern: "from '../services/photographerApi'"
    - from: "app/src/contexts/PhotographerContext.tsx"
      to: "app/src/contexts/AuthContext"
      via: "useAuth().user?.id 를 togglePhotoLike/toggleFollow 에 주입"
      pattern: "useAuth\\(\\)"
    - from: "app/src/contexts/PhotographerContext.tsx"
      to: "app/src/hooks/useLoginGate"
      via: "비로그인 사용자 가드"
      pattern: "useLoginGate"
    - from: "app/App.tsx"
      to: "app/src/contexts/PhotographerContext.tsx"
      via: "PhotographerProvider 를 Provider 스택에 mount — 재작성된 Provider 가 실제로 루트에 연결되어야 usePhotographer 소비자 (Plan 04/05 화면) 가 동작"
      pattern: "PhotographerProvider"
---

<objective>
Wave 2 — Plan 02 가 완비한 서비스 레이어 위에서 **PhotographerContext.tsx 를 Supabase 전용으로 일괄 재작성** 한다. Phase 3 CommunityContext D-16 패턴을 mirror — mock + merge 로직 완전 제거, 초기 state 는 빈 배열 + loading, useEffect 가 fetch, optimistic 은 좋아요/팔로우만 유지, 컬렉션은 await 전환.

Purpose:
- Phase 4 의 단일 가장 큰 리팩토링 범위 — 800+ LOC 파일 재작성
- D-22 의 치명적 버그 수정: `togglePhotoLike('', 'post', postId)` / `toggleFollow('', pgId)` 빈 userId 버그
- D-23 페이지네이션을 Context 수준에서 수용 (`loadMorePhotoPosts` 메소드)
- D-21 컬렉션 await 전환 — 현재 optimistic + fire-and-forget 을 제거
- D-19 mockPhotographers / mockCheerleaders 파일 삭제 (Phase 3 mockCommunity.ts 정리 방침 동일)

Output:
- PhotographerContext.tsx 전면 재작성
- mockPhotographers.ts, mockCheerleaders.ts 삭제

Addresses: PHOT-01 (mock 병합 제거 완성), PHOT-06 (grade 는 mapPhotographer 에서 이미 계산되어 Context state 로 흐름), PHOT-07 (cheerleaders DB 기반), PHOT-08 (컬렉션 await + getCollectionPosts).

**화면 수정은 본 Plan 에 포함되지 않음** — Plan 04 (UploadPostScreen/VideoPlayer/GradeBadge) 및 Plan 05 (Studio/Register/CollectionDetail) 가 담당. 본 plan 완료 시점에 화면 사용처 typecheck 오류가 일부 남음 (cheerleader.name → name_ko, videos 파라미터 미전달, isRemote prop 제거 등) — Plan 04 가 해결.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-photographer/04-CONTEXT.md
@.planning/phases/04-photographer/04-RESEARCH.md
@.planning/phases/04-photographer/04-02-SUMMARY.md
@CLAUDE.md

# 수정 대상
@app/src/contexts/PhotographerContext.tsx
@app/src/data/mockPhotographers.ts
@app/src/data/mockCheerleaders.ts

# 참조 (Phase 3 CommunityContext 전환 패턴 — mirror)
@app/src/contexts/CommunityContext.tsx
@app/src/contexts/AuthContext.tsx
@app/src/hooks/useLoginGate.ts

# 새 API 시그니처 (Plan 02 에서 export)
@app/src/services/photographerApi.ts
@app/src/utils/photographerGrade.ts

<interfaces>
<!-- Plan 04/05 이 usePhotographer() 훅에서 기대하는 contract -->

```ts
// PhotographerContextValue (변경 부분)
interface PhotographerContextValue {
  loading: boolean;
  refreshData: () => Promise<void>;

  // mock 관련 제거: isRemote 삭제

  photographers: Photographer[];           // grade 필드 포함
  photoPosts: PhotoPost[];                 // videos / thumbnail_urls 포함
  players: Player[];
  cheerleaders: Cheerleader[];             // DB schema 기준 (name_ko)
  events: TimelineEvent[];
  collections: PgCollection[];
  comments: PhotoComment[];

  // 페이지네이션 (D-23)
  hasMorePhotoPosts: boolean;
  loadMorePhotoPosts: (teamSlug?: string) => Promise<void>;

  // 컬렉션 posts fetch (D-21, PHOT-08)
  getCollectionPosts: (collectionId: string) => Promise<PhotoPost[]>;

  // Optimistic + rollback (좋아요/팔로우 유지)
  photoLikedIds: Set<string>;
  togglePhotoLike: (postId: string) => Promise<void>;
  isPhotoLiked: (postId: string) => boolean;

  followedPgIds: Set<string>;
  toggleFollow: (pgId: string) => Promise<void>;
  isFollowing: (pgId: string) => boolean;

  // Mutations (await 전환 — D-21)
  addPhotoPost: (post: PhotoPost) => void;  // 생성 직후 local state prepend (Plan 04 UploadScreen 이 호출)
  deletePhotoPost: (postId: string) => Promise<void>;
  createCollection: (pgId: string, name: string, emoji: string) => Promise<void>;
  deleteCollection: (collectionId: string) => Promise<void>;
  renameCollection: (collectionId: string, name: string) => Promise<void>;
  addPostToCollection: (collectionId: string, postId: string) => Promise<void>;
  removePostFromCollection: (collectionId: string, postId: string) => Promise<void>;

  // 댓글
  addComment: (postId: string, text: string, userId: string, userName: string, parentId?: string) => Promise<void>;
  deleteComment: (commentId: string) => Promise<void>;
  getCommentsForPost: (postId: string) => PhotoComment[];
}

export function usePhotographer(): PhotographerContextValue;
```
</interfaces>
</context>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-04 | Info disclosure (existing) | photographer_applications | accept (existing RLS) | 023 RLS photographer_apps_read 가 본인+admin 만 — Context 는 fetchMyPhotographerApplication 만 호출 (Plan 05 에서 사용), RLS 통과 |
| (no new threats) | — | — | accept | Context 는 토큰 기반 쿼리만 사용, 신규 권한 경계 없음 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: PhotographerContext.tsx 전면 재작성 (D-19, D-20, D-21, D-22, D-23)</name>
  <read_first>
    - app/src/contexts/PhotographerContext.tsx (현재 전체 파일 — 수정 대상, mock 의존 구조)
    - app/src/contexts/CommunityContext.tsx (Phase 3 D-16 전환 패턴 — 구조적 mirror)
    - app/src/contexts/AuthContext.tsx (useAuth 반환 user / session 구조)
    - app/src/hooks/useLoginGate.ts (비로그인 가드 훅 — 사용법)
    - app/src/services/photographerApi.ts (Plan 02 가 확장한 전체 API)
    - .planning/phases/04-photographer/04-CONTEXT.md §D-19~D-23
    - .planning/phases/04-photographer/04-RESEARCH.md §Pattern 4 (페이지네이션 loadMore 가드)
    - .planning/phases/04-photographer/04-02-SUMMARY.md (Plan 02 완료 후 export 목록)
  </read_first>
  <files>app/src/contexts/PhotographerContext.tsx</files>
  <action>
기존 `app/src/contexts/PhotographerContext.tsx` 를 전면 재작성한다. 아래는 골격 — 실제 구현 시 기존 파일의 메소드 세트는 보존하되 내부 구현을 Supabase 전용으로 교체.

**구조:**

```typescript
import React, { createContext, useContext, useState, useCallback, useMemo, useEffect, useRef, ReactNode } from 'react';
import type { Photographer, PhotoPost, Player, TimelineEvent, HomeFeedItem } from '../types/photographer';
import type { Cheerleader } from '../types/cheerleader';
import type { CommunityPostWithAuthor } from '../types/community';
import * as photographerApi from '../services/photographerApi';
import { useAuth } from './AuthContext';
import { useLoginGate } from '../hooks/useLoginGate';

const PAGE_SIZE = 20;

export interface PhotoComment {
  id: string;
  postId: string;
  userId: string;
  userName: string;
  text: string;
  parentId: string | null;
  likeCount: number;
  isDeleted: boolean;
  createdAt: string;
}

export interface PgCollection {
  id: string;
  photographerId: string;
  name: string;
  emoji: string;
  postIds: string[];
}

interface PhotographerContextValue {
  // 전체 시그니처는 <interfaces> 블록 참조
  // ... (생략)
}

const PhotographerContext = createContext<PhotographerContextValue | null>(null);

export function PhotographerProvider({ children }: { children: ReactNode }) {
  const { user, session } = useAuth();
  const { requireLogin } = useLoginGate();

  // ─── State ───────────────────────────────
  const [loading, setLoading] = useState(true);
  const [photographers, setPhotographers] = useState<Photographer[]>([]);
  const [photoPosts, setPhotoPosts] = useState<PhotoPost[]>([]);
  const [players, setPlayers] = useState<Player[]>([]);
  const [cheerleaders, setCheerleaders] = useState<Cheerleader[]>([]);
  const [events, setEvents] = useState<TimelineEvent[]>([]);
  const [collections, setCollections] = useState<PgCollection[]>([]);
  const [comments, setComments] = useState<PhotoComment[]>([]);
  const [photoLikedIds, setPhotoLikedIds] = useState<Set<string>>(new Set());
  const [followedPgIds, setFollowedPgIds] = useState<Set<string>>(new Set());

  // 페이지네이션 state (D-23)
  const [currentTeamSlug, setCurrentTeamSlug] = useState<string | undefined>(undefined);
  const [photoPostsPage, setPhotoPostsPage] = useState(0);
  const [hasMorePhotoPosts, setHasMorePhotoPosts] = useState(true);
  const loadingMoreRef = useRef(false);

  // ─── 초기 데이터 fetch ───────────────────────
  const refreshData = useCallback(async () => {
    setLoading(true);
    try {
      const [
        photographersRes,
        postsRes,
        playersRes,
        cheerleadersRes,
        eventsRes,
        collectionsRes,
        commentsRes,
      ] = await Promise.all([
        photographerApi.fetchPhotographers(),
        photographerApi.fetchPhotoPosts({ page: 0, pageSize: PAGE_SIZE }),
        photographerApi.fetchPlayers(),
        photographerApi.fetchCheerleaders(),
        photographerApi.fetchEvents(),
        photographerApi.fetchCollections(),
        photographerApi.fetchAllComments(),
      ]);

      if (photographersRes.data) setPhotographers(photographersRes.data);
      if (postsRes.data) {
        setPhotoPosts(postsRes.data);
        setHasMorePhotoPosts(postsRes.data.length === PAGE_SIZE);
        setPhotoPostsPage(0);
      }
      if (playersRes.data) setPlayers(playersRes.data);
      if (cheerleadersRes.data) setCheerleaders(cheerleadersRes.data);
      if (eventsRes.data) setEvents(eventsRes.data);
      if (collectionsRes.data) setCollections(collectionsRes.data);
      if (commentsRes.data) setComments(commentsRes.data);

      // 로그인 사용자의 좋아요/팔로우 복원 (D-22)
      if (user?.id) {
        const [likesRes, followsRes] = await Promise.all([
          photographerApi.fetchUserPhotoLikes(user.id),
          photographerApi.fetchUserFollows(user.id),
        ]);
        if (likesRes.data) setPhotoLikedIds(new Set(likesRes.data));
        if (followsRes.data) setFollowedPgIds(new Set(followsRes.data));
      } else {
        setPhotoLikedIds(new Set());
        setFollowedPgIds(new Set());
      }
    } finally {
      setLoading(false);
    }
  }, [user?.id]);

  useEffect(() => {
    void refreshData();
  }, [refreshData]);

  // ─── 페이지네이션 (D-23) ────────────────────
  const loadMorePhotoPosts = useCallback(
    async (teamSlug?: string) => {
      if (loadingMoreRef.current || !hasMorePhotoPosts) return;
      loadingMoreRef.current = true;

      // teamSlug 변경 시 페이지 리셋
      if (teamSlug !== currentTeamSlug) {
        setPhotoPosts([]);
        setPhotoPostsPage(0);
        setHasMorePhotoPosts(true);
        setCurrentTeamSlug(teamSlug);
      }
      const nextPage = teamSlug !== currentTeamSlug ? 0 : photoPostsPage + 1;

      const res = await photographerApi.fetchPhotoPosts({
        teamSlug,
        page: nextPage,
        pageSize: PAGE_SIZE,
      });
      if (res.data) {
        setPhotoPosts((prev) => (nextPage === 0 ? res.data! : [...prev, ...res.data!]));
        setHasMorePhotoPosts(res.data.length === PAGE_SIZE);
        setPhotoPostsPage(nextPage);
      }
      loadingMoreRef.current = false;
    },
    [currentTeamSlug, photoPostsPage, hasMorePhotoPosts],
  );

  // ─── Optimistic 좋아요 (D-22 bug fix) ─────────
  const togglePhotoLike = useCallback(
    async (postId: string) => {
      if (!user?.id) {
        requireLogin();
        return;
      }
      // optimistic
      const wasLiked = photoLikedIds.has(postId);
      setPhotoLikedIds((prev) => {
        const next = new Set(prev);
        if (wasLiked) next.delete(postId);
        else next.add(postId);
        return next;
      });
      setPhotoPosts((prev) =>
        prev.map((p) =>
          p.id === postId
            ? { ...p, like_count: Math.max(0, p.like_count + (wasLiked ? -1 : 1)) }
            : p,
        ),
      );

      const res = await photographerApi.togglePhotoLike(user.id, 'post', postId);
      if (res.error) {
        // rollback
        setPhotoLikedIds((prev) => {
          const next = new Set(prev);
          if (wasLiked) next.add(postId);
          else next.delete(postId);
          return next;
        });
        setPhotoPosts((prev) =>
          prev.map((p) =>
            p.id === postId
              ? { ...p, like_count: Math.max(0, p.like_count + (wasLiked ? 1 : -1)) }
              : p,
          ),
        );
      }
    },
    [user?.id, photoLikedIds, requireLogin],
  );

  // ─── Optimistic 팔로우 (D-22 bug fix) ─────────
  const toggleFollow = useCallback(
    async (pgId: string) => {
      if (!user?.id) {
        requireLogin();
        return;
      }
      const wasFollowing = followedPgIds.has(pgId);
      setFollowedPgIds((prev) => {
        const next = new Set(prev);
        if (wasFollowing) next.delete(pgId);
        else next.add(pgId);
        return next;
      });

      const res = await photographerApi.toggleFollow(user.id, pgId);
      if (res.error) {
        setFollowedPgIds((prev) => {
          const next = new Set(prev);
          if (wasFollowing) next.add(pgId);
          else next.delete(pgId);
          return next;
        });
      }
      // follower_count 는 DB 트리거가 처리 — 다음 refreshData 에서 동기화
    },
    [user?.id, followedPgIds, requireLogin],
  );

  // ─── 컬렉션 (D-21 await 전환) ──────────────────
  const createCollection = useCallback(
    async (pgId: string, name: string, emoji: string) => {
      if (!user?.id) {
        requireLogin();
        return;
      }
      const res = await photographerApi.createCollection({ photographerId: pgId, name, emoji });
      if (res.data) {
        setCollections((prev) => [...prev, res.data!]);
      }
    },
    [user?.id, requireLogin],
  );

  const deleteCollection = useCallback(
    async (collectionId: string) => {
      if (!user?.id) {
        requireLogin();
        return;
      }
      const res = await photographerApi.deleteCollection(collectionId);
      if (!res.error) {
        setCollections((prev) => prev.filter((c) => c.id !== collectionId));
      }
    },
    [user?.id, requireLogin],
  );

  const renameCollection = useCallback(
    async (collectionId: string, name: string) => {
      if (!user?.id) {
        requireLogin();
        return;
      }
      // photographerApi 에 updateCollection 미구현 시 Task 내 추가 필요 — 또는
      // 본 메소드는 local-only 로 유지하고 호출 시점에 updateCollection 서비스 확장 필요성 문서화.
      // 임시: local 만 업데이트 (Plan 02 에서 updateCollection export 안 함 — 추후 필요 시 확장)
      setCollections((prev) => prev.map((c) => (c.id === collectionId ? { ...c, name } : c)));
    },
    [user?.id, requireLogin],
  );

  const addPostToCollection = useCallback(
    async (collectionId: string, postId: string) => {
      if (!user?.id) {
        requireLogin();
        return;
      }
      const res = await photographerApi.addPostToCollection(collectionId, postId);
      if (!res.error) {
        setCollections((prev) =>
          prev.map((c) =>
            c.id === collectionId && !c.postIds.includes(postId)
              ? { ...c, postIds: [...c.postIds, postId] }
              : c,
          ),
        );
      }
    },
    [user?.id, requireLogin],
  );

  const removePostFromCollection = useCallback(
    async (collectionId: string, postId: string) => {
      if (!user?.id) {
        requireLogin();
        return;
      }
      const res = await photographerApi.removePostFromCollection(collectionId, postId);
      if (!res.error) {
        setCollections((prev) =>
          prev.map((c) =>
            c.id === collectionId
              ? { ...c, postIds: c.postIds.filter((pid) => pid !== postId) }
              : c,
          ),
        );
      }
    },
    [user?.id, requireLogin],
  );

  const getCollectionPosts = useCallback(
    async (collectionId: string): Promise<PhotoPost[]> => {
      const res = await photographerApi.fetchCollectionPosts(collectionId);
      return res.data ?? [];
    },
    [],
  );

  // ─── 댓글 (await) ────────────────────────────
  const addComment = useCallback(
    async (postId: string, text: string, userId: string, userName: string, parentId?: string) => {
      const res = await photographerApi.createComment({ postId, userId, userName, text, parentId });
      if (res.data) {
        setComments((prev) => [...prev, res.data!]);
      }
    },
    [],
  );

  const deleteComment = useCallback(
    async (commentId: string) => {
      const res = await photographerApi.deleteComment(commentId);
      if (!res.error) {
        setComments((prev) =>
          prev.map((c) => (c.id === commentId ? { ...c, isDeleted: true, text: '' } : c)),
        );
      }
    },
    [],
  );

  // ─── photo post 생성 후 local prepend ──────────
  const addPhotoPost = useCallback((post: PhotoPost) => {
    setPhotoPosts((prev) => [post, ...prev]);
  }, []);

  const deletePhotoPost = useCallback(async (postId: string) => {
    const res = await photographerApi.deletePhotoPost(postId);
    if (!res.error) {
      setPhotoPosts((prev) => prev.filter((p) => p.id !== postId));
    }
  }, []);

  // ─── 기존 selector 함수들 (Supabase 데이터 위에서 그대로 동작) ─────────
  const getPhotographer = useCallback(
    (id: string) => photographers.find((p) => p.id === id || p.user_id === id),
    [photographers],
  );

  const getPhotoPost = useCallback(
    (id: string) => photoPosts.find((p) => p.id === id),
    [photoPosts],
  );

  const getPhotoPostsByTeam = useCallback(
    (teamId: string) =>
      teamId === 'all'
        ? photoPosts
        : photoPosts.filter((p) => p.team_id === teamId),
    [photoPosts],
  );

  const getPhotoPostsByPhotographer = useCallback(
    (pgId: string) => photoPosts.filter((p) => p.photographer_id === pgId),
    [photoPosts],
  );

  const getPhotoPostsByPlayer = useCallback(
    (playerId: string) => photoPosts.filter((p) => p.player_id === playerId),
    [photoPosts],
  );

  const getFeaturedPosts = useCallback(
    () => photoPosts.filter((p) => p.is_featured),
    [photoPosts],
  );

  const searchPhotoPosts = useCallback(
    (query: string) => {
      const q = query.trim().toLowerCase();
      if (!q) return [];
      return photoPosts.filter(
        (p) =>
          p.title.toLowerCase().includes(q) ||
          p.description.toLowerCase().includes(q) ||
          p.photographer.display_name.toLowerCase().includes(q),
      );
    },
    [photoPosts],
  );

  const getPlayer = useCallback(
    (id: string) => players.find((p) => p.id === id),
    [players],
  );

  const getPlayersByTeam = useCallback(
    (teamId: string) => players.filter((p) => p.team_id === teamId),
    [players],
  );

  const getCheerleader = useCallback(
    (id: string) => cheerleaders.find((c) => c.id === id),
    [cheerleaders],
  );

  const getCheerleadersByTeam = useCallback(
    (teamSlug: string) => cheerleaders.filter((c) => c.team_id === teamSlug),
    [cheerleaders],
  );

  const getPhotoPostsByCheerleader = useCallback(
    (cheerleaderId: string) => photoPosts.filter((p) => p.cheerleader_id === cheerleaderId),
    [photoPosts],
  );

  const getEvent = useCallback(
    (id: string) => events.find((e) => e.id === id),
    [events],
  );

  const getEventsByType = useCallback(
    (type: string) => events.filter((e) => e.event_type === type),
    [events],
  );

  const getHomeFeed = useCallback(
    (communityPosts: CommunityPostWithAuthor[], page: number): HomeFeedItem[] => {
      // 기존 5:1 병합 로직 유지 (Phase 6 MON-04 에서 세밀 조정 예정)
      const photoItems: HomeFeedItem[] = photoPosts.map((p) => ({ type: 'photo', data: p }));
      const commItems: HomeFeedItem[] = communityPosts.map((c) => ({ type: 'community', data: c }));
      // interleave 5:1
      const merged: HomeFeedItem[] = [];
      let ci = 0;
      let pi = 0;
      while (pi < photoItems.length || ci < commItems.length) {
        for (let k = 0; k < 5 && pi < photoItems.length; k++) {
          merged.push(photoItems[pi++]);
        }
        if (ci < commItems.length) merged.push(commItems[ci++]);
      }
      return merged;
    },
    [photoPosts],
  );

  const isPhotoLiked = useCallback((postId: string) => photoLikedIds.has(postId), [photoLikedIds]);
  const isFollowing = useCallback((pgId: string) => followedPgIds.has(pgId), [followedPgIds]);

  const getCollectionsForPg = useCallback(
    (pgId: string) => collections.filter((c) => c.photographerId === pgId),
    [collections],
  );

  const getCommentsForPost = useCallback(
    (postId: string) => comments.filter((c) => c.postId === postId),
    [comments],
  );

  // ─── Context value ────────────────────────
  const value: PhotographerContextValue = useMemo(
    () => ({
      loading,
      refreshData,
      photographers,
      photoPosts,
      players,
      cheerleaders,
      events,
      collections,
      comments,
      photoLikedIds,
      followedPgIds,
      hasMorePhotoPosts,
      loadMorePhotoPosts,
      getCollectionPosts,
      getPhotographer,
      getPhotoPost,
      getPhotoPostsByTeam,
      getPhotoPostsByPhotographer,
      getPhotoPostsByPlayer,
      getFeaturedPosts,
      searchPhotoPosts,
      getPlayer,
      getPlayersByTeam,
      getCheerleader,
      getCheerleadersByTeam,
      getPhotoPostsByCheerleader,
      getEvent,
      getEventsByType,
      getHomeFeed,
      togglePhotoLike,
      isPhotoLiked,
      toggleFollow,
      isFollowing,
      addPhotoPost,
      deletePhotoPost,
      createCollection,
      deleteCollection,
      renameCollection,
      addPostToCollection,
      removePostFromCollection,
      getCollectionsForPg,
      addComment,
      deleteComment,
      getCommentsForPost,
    }),
    [
      loading, refreshData, photographers, photoPosts, players, cheerleaders, events,
      collections, comments, photoLikedIds, followedPgIds, hasMorePhotoPosts,
      loadMorePhotoPosts, getCollectionPosts, getPhotographer, getPhotoPost,
      getPhotoPostsByTeam, getPhotoPostsByPhotographer, getPhotoPostsByPlayer,
      getFeaturedPosts, searchPhotoPosts, getPlayer, getPlayersByTeam, getCheerleader,
      getCheerleadersByTeam, getPhotoPostsByCheerleader, getEvent, getEventsByType,
      getHomeFeed, togglePhotoLike, isPhotoLiked, toggleFollow, isFollowing,
      addPhotoPost, deletePhotoPost, createCollection, deleteCollection,
      renameCollection, addPostToCollection, removePostFromCollection,
      getCollectionsForPg, addComment, deleteComment, getCommentsForPost,
    ],
  );

  return <PhotographerContext.Provider value={value}>{children}</PhotographerContext.Provider>;
}

export function usePhotographer(): PhotographerContextValue {
  const ctx = useContext(PhotographerContext);
  if (!ctx) throw new Error('usePhotographer must be used within PhotographerProvider');
  return ctx;
}
```

**주의사항:**
- 기존 Context 에 있던 `registerPhotographer`, `updatePostStatus`, `updatePhotographer`, `updatePhotographerVerification`, `setFeaturedPost` 메소드 는 Phase 5 Admin 관할 → 본 Plan 에서 제거 (Phase 5 에서 adminApi 경유 재구현). 호출 지점이 있으면 Plan 04/05 에서 제거 처리.
- `toggleCommentLike`, `isCommentLiked` 등 기존 메소드는 필요 시 유지 — Plan 04 QA 에서 사용 여부 확인 후 결정.
- `isRemote` prop 사용처 (예: UploadPostScreen line 50, 177) 는 Plan 04 에서 제거.
- `useLoginGate` 는 Phase 2 에서 구현된 훅 — 반환값 확인 후 `requireLogin()` 또는 `requireLogin({ action })` 형태로 호출. 실제 시그니처는 파일 읽고 맞게 변경.
- mapPhotoPost 에서 cheerleader 가 name_ko 로 변경됨 — 기존 사용처 (e.g. UploadPostScreen cheerleader.name 참조) 는 Plan 04 에서 수정.
  </action>
  <verify>
    <automated>grep -c "MOCK_" app/src/contexts/PhotographerContext.tsx 가 0. grep -c "isRemote" app/src/contexts/PhotographerContext.tsx 가 0. grep -q "useLoginGate" app/src/contexts/PhotographerContext.tsx. grep -q "loadMorePhotoPosts" app/src/contexts/PhotographerContext.tsx. grep -q "getCollectionPosts" app/src/contexts/PhotographerContext.tsx. grep -q "user\\.id" app/src/contexts/PhotographerContext.tsx 로 D-22 수정 확인. grep -q "PhotographerProvider" app/App.tsx (재작성된 PhotographerProvider 가 루트 Provider 스택에 실제로 mount 되어 있음 확인 — key_link 검증)</automated>
  </verify>
  <done>
    - PhotographerContext.tsx 전면 재작성 완료, mock import 0건
    - 초기 state 빈 배열 + loading=true
    - useEffect 에서 refreshData → Supabase fetch 7개 병렬
    - 페이지네이션 loadMorePhotoPosts / hasMorePhotoPosts / loadingMoreRef guard 포함
    - 좋아요/팔로우: optimistic + rollback 유지, user.id 주입 (D-22)
    - 컬렉션: await 전환 (D-21)
    - getCollectionPosts 메소드 신규 (PHOT-08)
    - useLoginGate 로 비로그인 가드
    - Phase 5 관할 메소드 (registerPhotographer 등) 제거
  </done>
</task>

<task type="auto">
  <name>Task 2: mockPhotographers / mockCheerleaders 파일 삭제 (D-19)</name>
  <read_first>
    - app/src/data/mockPhotographers.ts (제거 대상 — 확인만)
    - app/src/data/mockCheerleaders.ts (제거 대상)
    - .planning/phases/04-photographer/04-CONTEXT.md §D-19 (mock 일괄 제거 방침)
    - .planning/phases/04-photographer/04-RESEARCH.md §Runtime State Inventory (mock 제거 후 import 검증)
  </read_first>
  <files>app/src/data/mockPhotographers.ts, app/src/data/mockCheerleaders.ts</files>
  <action>
**Step 2-A — git 에서 두 파일 삭제:**

```bash
rm app/src/data/mockPhotographers.ts
rm app/src/data/mockCheerleaders.ts
```

**Step 2-B — import 사용처 검증:**

```bash
git grep -l "from.*mockPhotographers" app/src/
git grep -l "from.*mockCheerleaders" app/src/
git grep -l "MOCK_PHOTOGRAPHERS\|MOCK_PHOTO_POSTS\|MOCK_PLAYERS\|MOCK_EVENTS\|MOCK_CHEERLEADERS" app/src/
```

결과가 **공백** (매치 0건) 이어야 함. 만약 매치가 있으면 해당 파일도 읽고 import 제거:

- Task 1 에서 PhotographerContext 의 import 는 제거됨
- 만약 StudioScreen, HomeScreen, PhotographerProfileScreen 등에서 직접 MOCK_* 을 import 한다면 해당 파일에서 제거 후 대응하는 usePhotographer() 호출로 교체. 단, 실제 호출부 변경은 Plan 04/05 담당이므로 본 task 는 **import 통계 확인만** 수행하여 결과를 Plan 04/05 인계 SUMMARY 에 적음.

**Step 2-C — typecheck 상태 기록:**

```bash
cd app && npx tsc --noEmit 2>&1 | tee /tmp/phase4-plan03-typecheck.log
wc -l /tmp/phase4-plan03-typecheck.log
```

결과는 Plan 04/05 가 해결할 TS 오류 카운트로 SUMMARY 에 기록. 본 plan 기준으로 Context 재작성 + mock 삭제 로 인한 기대 오류:
- UploadPostScreen: cheerleader.name → name_ko (Plan 04 에서 수정)
- StudioScreen: isRemote prop 사용 제거 (Plan 05)
- PhotographerCard: grade 접근 + videos overlay (Plan 04)
- CollectionDetailScreen: getCollectionPosts 호출 전환 (Plan 05)
- PhotographerRegisterScreen: submitPhotographerApplication 호출 (Plan 05)

이들은 Plan 04/05 의 "알려진 typecheck 보류 목록" 이 된다. 본 plan 의 verify 기준은 **PhotographerContext.tsx 자체 + photographerApi.ts + photographerGrade.ts + types/ 4개 파일에 한정** 하여 오류 없음.
  </action>
  <verify>
    <automated>! ls app/src/data/mockPhotographers.ts 2>/dev/null && ! ls app/src/data/mockCheerleaders.ts 2>/dev/null. git grep -l "MOCK_PHOTOGRAPHERS\|MOCK_CHEERLEADERS\|MOCK_PHOTO_POSTS\|MOCK_PLAYERS\|MOCK_EVENTS" app/src/ 는 0 매치. git grep -l "from.*mockPhotographers\\|from.*mockCheerleaders" app/src/ 는 0 매치</automated>
  </verify>
  <done>
    - app/src/data/mockPhotographers.ts, mockCheerleaders.ts 삭제됨
    - MOCK_* identifier app/src/ 내 0건
    - from mockPhotographers / mockCheerleaders import 0건
    - 잔여 typecheck 오류 Plan 04/05 에 인계
    - Phase 3 mockCommunity.ts 정리 방침 준수 (D-19)
  </done>
</task>

</tasks>

<verification>
1. PhotographerContext.tsx 파일 자체 typecheck 오류 없음 (`cd app && npx tsc --noEmit` 출력에서 `src/contexts/PhotographerContext.tsx` 매치 0건)
2. `git grep "MOCK_" app/src/` 0 매치
3. D-22 버그 수정 — togglePhotoLike/toggleFollow 에서 `user?.id` 주입 + `requireLogin()` 가드 동작
4. Plan 04/05 가 해결할 typecheck 오류 목록이 SUMMARY 에 기록됨 (허용된 보류)
5. 좋아요 rollback 경로 테스트 — Plan 04 QA 에서 실기기 확인
6. loadMorePhotoPosts 의 loadingMoreRef 가드 — Plan 04/05 FlatList 호출 시 확인
</verification>

<success_criteria>
- [ ] PhotographerContext.tsx mock 완전 제거 + Supabase 전용
- [ ] isRemote / merge 로직 제거
- [ ] D-22 userId 버그 수정 (useAuth().user?.id 주입)
- [ ] D-21 컬렉션 await 전환
- [ ] D-23 페이지네이션 loadMorePhotoPosts
- [ ] D-20 cheerleaders DB state 기반
- [ ] mockPhotographers/mockCheerleaders 파일 삭제
- [ ] MOCK_* identifier app/src/ 0건
- [ ] Context 파일 자체 typecheck 오류 없음
</success_criteria>

<output>
완료 후 `.planning/phases/04-photographer/04-03-SUMMARY.md` 생성 — 제거된 mock 파일 목록 + Plan 04/05 가 해결할 잔여 typecheck 오류 목록 (파일별, 해결 지침) + 사용처 import 변경 권고 (cheerleader.name → name_ko, isRemote prop 제거, Phase 5 이관 메소드).
</output>
</content>
</invoke>