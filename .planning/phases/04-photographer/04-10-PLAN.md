---
phase: 04-photographer
plan: 10
type: execute
wave: 1
depends_on: [04-04, 04-09]
files_modified:
  - app/src/screens/photographer/UploadPostScreen.tsx
  - app/src/screens/home/HomeScreen.tsx
  - app/src/screens/home/AllPostsScreen.tsx
  - app/src/screens/home/FeaturedAllScreen.tsx
  - app/src/screens/photographer/PhotographerProfileScreen.tsx
  - app/src/screens/archive/ArchiveScreen.tsx
  - app/src/screens/photographer/StudioScreen.tsx
  - app/src/screens/photographer/CollectionDetailScreen.tsx
autonomous: false   # end-of-plan UAT Test 12 재개 checkpoint
requirements: [PHOT-03, PHOT-04]
gap_closure: true
tags: [video, upload, feed, autoplay, uat-gap-closure, wave-7]
user_setup: []

must_haves:
  truths:
    - "영상만 업로드한 경우에도 UploadPostScreen '게시하기' CTA 가 활성화되어 포스트 생성이 완료된다"
    - "게시 후 영상-only 포스트가 photo_posts 테이블에 images=[] + videos=['https://.../post-videos/...'] 로 기록된다"
    - "홈 featured 가로 캐러셀에서 영상-포함 포스트가 viewport 에 들어오면 VideoPlayer(mode='feed') 로 autoplay (muted+loop), 이탈하면 pause"
    - "AllPostsScreen / FeaturedAllScreen / PhotographerProfileScreen / ArchiveScreen 그리드에서 영상-포함 포스트가 viewport 에 들어오면 VideoPlayer(mode='feed') 로 autoplay, 이탈하면 pause (trending 그리드와 동일 동작)"
    - "혼합 포스트 (images+videos 모두 존재) 는 모든 피드 surface 에서 video-first 로 통일 표시 — 이미지 우선 노출 없음"
    - "영상-only 포스트가 StudioScreen / CollectionDetailScreen 그리드에서 회색 빈 placeholder 로 표시되지 않고 videos[0] 기반의 정지 poster 또는 overlay-visible 배경으로 렌더된다"
    - "handleClose dirty-check 가 videos.length > 0 도 감지해 영상-only 입력 중 뒤로가기 시 취소 다이얼로그가 노출된다"
  artifacts:
    - path: "app/src/screens/photographer/UploadPostScreen.tsx"
      provides: "canPublish OR 조건 (images.length > 0 || videos.length > 0), doPublish Step 1 image upload 가드 (images.length > 0 일 때만 실행), handleClose videos.length 포함"
      contains: "images.length > 0 || videos.length > 0"
    - path: "app/src/screens/home/HomeScreen.tsx"
      provides: "featured 섹션 horizontal ScrollView → viewport-aware VideoPlayer 도입. featuredVisibleIds Set state + onScroll 기반 viewport 계산 + hasVideo 분기"
      contains: "VideoPlayer"
    - path: "app/src/screens/home/AllPostsScreen.tsx"
      provides: "FlatList onViewableItemsChanged + viewabilityConfig + visibleIndices → renderPost 에서 hasVideo 시 VideoPlayer(mode='feed', isVisible) 마운트"
      contains: "onViewableItemsChanged"
    - path: "app/src/screens/home/FeaturedAllScreen.tsx"
      provides: "ScrollView → FlatList 전환 + viewport-aware VideoPlayer 분기"
      contains: "onViewableItemsChanged"
    - path: "app/src/screens/photographer/PhotographerProfileScreen.tsx"
      provides: "그리드를 FlatList + onViewableItemsChanged 로 전환, visiblePosts 아이템에 VideoPlayer(mode='feed', isVisible) 분기"
      contains: "onViewableItemsChanged"
    - path: "app/src/screens/archive/ArchiveScreen.tsx"
      provides: "renderPhotoGrid → FlatList + onViewableItemsChanged, gridItem 에 VideoPlayer(mode='feed', isVisible) 분기"
      contains: "onViewableItemsChanged"
    - path: "app/src/screens/photographer/StudioScreen.tsx"
      provides: "previewUrl 이 undefined 인 경우 (영상-only) videos[0] 기반 VideoPlayer(mode='studio') 로 정지 poster 렌더 — play overlay 유지"
      contains: "VideoPlayer"
    - path: "app/src/screens/photographer/CollectionDetailScreen.tsx"
      provides: "previewUrl 이 undefined 인 경우 VideoPlayer(mode='studio') 분기"
      contains: "VideoPlayer"
  key_links:
    - from: "app/src/screens/photographer/UploadPostScreen.tsx"
      to: "app/src/services/photographerApi.ts (createPhotoPost)"
      via: "images=[] 허용 INSERT — DB CHECK 통과 확인됨"
      pattern: "createPhotoPost"
    - from: "app/src/screens/home/HomeScreen.tsx (featured 섹션)"
      to: "app/src/components/common/VideoPlayer.tsx (mode='feed', isVisible)"
      via: "viewport-aware autoplay"
      pattern: "VideoPlayer[\\s\\S]*?mode=['\"]feed['\"]"
    - from: "app/src/screens/home/AllPostsScreen.tsx"
      to: "app/src/components/common/VideoPlayer.tsx"
      via: "FlatList onViewableItemsChanged → isVisible prop"
      pattern: "isVisible=\\{"
    - from: "app/src/screens/archive/ArchiveScreen.tsx"
      to: "app/src/components/common/VideoPlayer.tsx"
      via: "동일 viewport 패턴"
      pattern: "isVisible=\\{"
---

<objective>
UAT Test 12 (VideoPlayer Feed Mode Autoplay) 재개 차단 요인 2 가지를 해결한다.

- **Sub-issue A:** UploadPostScreen 의 `canPublish` 클라이언트 게이팅이 `images.length > 0` 을 AND 로 요구해 영상-only 포스트 작성이 불가능. 서버/DB 는 이미 수용.
- **Sub-issue B:** Plan 04-09 이 HomeScreen **trending 그리드** 와 PostDetailScreen 에만 VideoPlayer 인라인 렌더를 적용했고, 나머지 5 개 피드 surface (HomeScreen featured 섹션 / AllPostsScreen / FeaturedAllScreen / PhotographerProfileScreen / ArchiveScreen) 는 여전히 `Image(previewUri) + static play overlay` 로 남아 혼합 포스트는 이미지 우선·영상-only 는 회색 placeholder 가 된다. StudioScreen / CollectionDetailScreen 은 설계상 정적 썸네일을 유지하지만 영상-only 포스트에서 `previewUrl=undefined` 회색 placeholder 가 발생.

Purpose: Test 12 를 실기기에서 재개할 수 있도록 선행 조건 (영상-only 포스트 생성 가능 + 피드 5곳 viewport autoplay + studio/collection fallback) 을 모두 복구.

Output:
1. 영상-only 포스트 생성 성공 (R2 업로드 → createPhotoPost → addPhotoPost 반영)
2. 5개 피드 surface 에서 trending 그리드와 동일한 `VideoPlayer(mode='feed', isVisible)` + `onViewableItemsChanged` + `viewabilityConfig {itemVisiblePercentThreshold: 60}` 패턴 적용
3. StudioScreen / CollectionDetailScreen 에서 영상-only 포스트 fallback poster 렌더
4. UAT Test 12 재개 checkpoint — 실기기 viewport autoplay 검증
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Source artifacts for this gap closure
@.planning/phases/04-photographer/04-UAT.md
@.planning/debug/video-only-submit-and-feed-thumbnail.md

# v1 decision baseline from orchestrator context:
# - 혼합 포스트 feed 우선순위: 모든 피드 surface 에서 video-first (trending 과 일관)
# - 영상-only 정적 fallback: 클라이언트 expo-video `poster` prop 만 사용 — generate-thumbnails 확장/클라이언트 first-frame 추출 v1 scope 제외
# - 4 screens viewport autoplay: v1 도입. viewabilityConfig itemVisiblePercentThreshold=60, trending 과 동일
# - Studio/CollectionDetail: 정적 썸네일 유지, 영상-only 회색 placeholder 만 수정

# Prior plans (only SUMMARYs genuinely needed)
@.planning/phases/04-photographer/04-04-SUMMARY.md   # VideoPlayer 3-mode API 참조
@.planning/phases/04-photographer/04-09-SUMMARY.md   # trending 그리드 + PostDetail 구현 패턴 참조 (복제 대상)

# Source files — reference pattern (trending grid)
@app/src/screens/home/HomeScreen.tsx
@app/src/components/common/VideoPlayer.tsx

# Source files — sub-issue A
@app/src/screens/photographer/UploadPostScreen.tsx

# Source files — sub-issue B (5 feed surfaces + 2 fallback-only)
@app/src/screens/home/AllPostsScreen.tsx
@app/src/screens/home/FeaturedAllScreen.tsx
@app/src/screens/photographer/PhotographerProfileScreen.tsx
@app/src/screens/archive/ArchiveScreen.tsx
@app/src/screens/photographer/StudioScreen.tsx
@app/src/screens/photographer/CollectionDetailScreen.tsx

<interfaces>
<!-- Executor 는 아래 contract 그대로 사용. 코드베이스 재탐색 금지. -->

### VideoPlayer (재사용 — 절대 수정 금지)
From `app/src/components/common/VideoPlayer.tsx`:
```typescript
export interface VideoPlayerProps {
  uri: string;
  mode: 'feed' | 'detail' | 'studio';
  width: number;
  height: number;
  isVisible?: boolean;   // feed 모드 viewport-aware play/pause (default true)
  onError?: (e: Error) => void;
}
export default function VideoPlayer(props: VideoPlayerProps): JSX.Element;
```
정책:
- `feed`: muted + loop + `isVisible=true` 면 play, `false` 면 pause. nativeControls 없음.
- `detail`: nativeControls 노출, PiP 허용. isVisible 무시.
- `studio`: autoplay 없음, nativeControls 없음, thumbnail poster 용도.

### Reference pattern — HomeScreen trending grid (복제 대상)
From `app/src/screens/home/HomeScreen.tsx`:

state + callbacks (lines 64-73):
```typescript
import type { ViewToken } from 'react-native';
import { useRef } from 'react';
const [visibleIndices, setVisibleIndices] = useState<Set<number>>(new Set());
const onViewableItemsChanged = useRef(({ viewableItems }: { viewableItems: ViewToken[] }) => {
  const indices = new Set<number>();
  viewableItems.forEach((vt) => {
    if (typeof vt.index === 'number') indices.add(vt.index);
  });
  setVisibleIndices(indices);
}).current;
const viewabilityConfig = useRef({ itemVisiblePercentThreshold: 60 }).current;
```

renderItem 분기 (lines 381-406, 핵심만):
```typescript
const previewUri = post.thumbnail_urls?.[0] ?? post.images[0];
const hasVideo = (post.videos?.length ?? 0) > 0;
const videoUri = post.videos?.[0];
const isVisible = visibleIndices.has(index);
{hasVideo && videoUri ? (
  <VideoPlayer
    uri={videoUri}
    mode="feed"
    width={CARD_WIDTH}
    height={CARD_WIDTH * 1.25}
    isVisible={isVisible}
  />
) : previewUri ? (
  <Image source={{ uri: previewUri }} style={styles.postImage} />
) : (
  <View style={[styles.postImage, { backgroundColor: colors.surface }]} />
)}
```

FlatList props (lines 372-380):
```typescript
<FlatList
  data={trendingPosts}
  numColumns={2}
  scrollEnabled={false}      // 부모 ScrollView 사용 시 필요
  onViewableItemsChanged={onViewableItemsChanged}
  viewabilityConfig={viewabilityConfig}
  renderItem={...}
/>
```

### photo_posts 데이터 형태 (수정 없음, 확인 목적)
From `app/src/types/photographer.ts`:
```typescript
interface PhotoPost {
  id: string;
  images: string[];           // 빈 배열 허용 (영상-only)
  videos: string[];           // Plan 04-02 로 도입
  thumbnail_urls?: string[];  // generate-thumbnails 가 채움 (이미지 기반)
  photographer_id: string;
  team_id: string;
  player: { name_ko: string; number: number } | null;
  like_count: number;
  view_count: number;
  comment_count: number;
  title: string;
  created_at: string;
  // ...
}
```

### createPhotoPost (수정 없음, 수용 확인됨)
From `app/src/services/photographerApi.ts`:
```typescript
export async function createPhotoPost(params: {
  photographerId: string;
  teamSlug: string;
  playerId: string | null;
  cheerleaderId: string | null;
  title: string;
  description: string;
  images: string[];   // 빈 배열 허용 (DB CHECK array_length(ARRAY[], 1)=NULL 통과)
  videos: string[];
}): Promise<ApiResult<PhotoPost>>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: UploadPostScreen — canPublish OR 조건 + doPublish image upload 가드 + handleClose videos dirty-check</name>
  <files>app/src/screens/photographer/UploadPostScreen.tsx</files>
  <behavior>
    - 영상-only 입력 (images.length === 0 && videos.length === 1) → canPublish=true → '게시하기' 버튼 활성화
    - 영상-only 로 게시 시 Step 1 (uploadPostImages) 를 skip 하고 finalImages=[] 로 createPhotoPost 호출 → 성공 반환
    - 이미지-only / 혼합 입력은 기존 동작 보전 — uploadPostImages 호출 경로 유지
    - handleClose 가 videos.length > 0 일 때도 '취소' 확인 다이얼로그 노출
    - editing 경로에서 기존 post.videos 가 있고 images=[] 인 경우에도 canPublish=true
    - 유효성 실패: title 공백 / selectedTeamId null / rightsConfirmed false (신규) → canPublish=false 유지
  </behavior>
  <action>
    `app/src/screens/photographer/UploadPostScreen.tsx` 에서 정확히 3 개 지점을 수정한다. 다른 줄은 건드리지 않는다.

    **Edit 1 — canPublish 조건 완화 (lines 93-97):**

    현재:
    ```typescript
    const canPublish =
      title.trim().length > 0 &&
      selectedTeamId !== null &&
      images.length > 0 &&
      (isEditing || rightsConfirmed);
    ```

    다음으로 교체:
    ```typescript
    // Plan 04-10 Sub-issue A: 영상-only 포스트 허용 (UI-SPEC §UploadPostScreen line 157 '동영상 (선택)', line 381 'video-only edge case' 명시적 허용).
    // DB/server 수용 확인됨 (07/029 migrations, createPhotoPost).
    const canPublish =
      title.trim().length > 0 &&
      selectedTeamId !== null &&
      (images.length > 0 || videos.length > 0) &&
      (isEditing || rightsConfirmed);
    ```

    **Edit 2 — doPublish Step 1 image upload 를 조건부로 감싸기 (lines 221-265, 신규 post flow 안쪽):**

    현재 (lines 221-233 + 254-265):
    ```typescript
    setUploading(true);
    try {
      // Step 1: optimize + upload images (Phase 3 D-09)
      const optimized = await Promise.all(images.map(optimizeImage));
      const imageUpload = await photographerApi.uploadPostImages(user.id, optimized, session.access_token);
      if (imageUpload.error || !imageUpload.data) {
        Alert.alert(
          t('upload_video_upload_failed_title'),
          imageUpload.error ?? t('upload_video_upload_failed_desc'),
        );
        setUploading(false);
        return;
      }

      // Step 2: ... (변경 없음)
      ...
      // Step 3: createPhotoPost (D-05)
      const postResult = await photographerApi.createPhotoPost({
        ...
        images: imageUpload.data,
        videos: finalVideos,
      });
    ```

    다음으로 변경:
    ```typescript
    setUploading(true);
    try {
      // Step 1: optimize + upload images (Phase 3 D-09)
      // Plan 04-10 Sub-issue A: images=[] (영상-only) 면 uploadPostImages 스킵 — get-upload-url count=0 400 회피 + finalImages=[] 로 createPhotoPost 에 전달.
      let finalImages: string[] = [];
      if (images.length > 0) {
        const optimized = await Promise.all(images.map(optimizeImage));
        const imageUpload = await photographerApi.uploadPostImages(user.id, optimized, session.access_token);
        if (imageUpload.error || !imageUpload.data) {
          Alert.alert(
            t('upload_video_upload_failed_title'),
            imageUpload.error ?? t('upload_video_upload_failed_desc'),
          );
          setUploading(false);
          return;
        }
        finalImages = imageUpload.data;
      }

      // Step 2: upload videos (D-04, ADJ-02 contentTypes) — 변경 없음
      let finalVideos: string[] = [];
      if (videos.length > 0) {
        const videoUpload = await photographerApi.uploadPostVideos(
          user.id,
          videos,
          session.access_token,
          videoContentTypes,
        );
        if (videoUpload.error || !videoUpload.data) {
          Alert.alert(
            t('upload_video_upload_failed_title'),
            videoUpload.error ?? t('upload_video_upload_failed_desc'),
          );
          setUploading(false);
          return;
        }
        finalVideos = videoUpload.data;
      }

      // Step 3: createPhotoPost (D-05)
      const postResult = await photographerApi.createPhotoPost({
        photographerId: pg.id,
        teamSlug: selectedTeamId!,
        playerId: selectedPlayerId,
        cheerleaderId: selectedCheerleaderId,
        title: title.trim(),
        description: description.trim(),
        images: finalImages,            // ← imageUpload.data → finalImages
        videos: finalVideos,
      });
    ```

    그리고 Step 4 (lines 280-294) 의 generate-thumbnails fire-and-forget 가드를:
    ```typescript
    if (supabaseUrl && imageUpload.data.length > 0) { ...  imageUrls: imageUpload.data ... }
    ```
    → 다음으로 변경:
    ```typescript
    if (supabaseUrl && finalImages.length > 0) {
      fetch(`${supabaseUrl}/functions/v1/generate-thumbnails`, {
        method: 'POST',
        headers: {
          Authorization: `Bearer ${session.access_token}`,
          'Content-Type': 'application/json',
        },
        body: JSON.stringify({
          postId: postResult.data.id,
          imageUrls: finalImages,
        }),
      }).catch((e) => console.warn('[Thumbnail] fire-and-forget failed', e));
    }
    ```

    주의: `imageUpload.data` 변수는 이제 `if (images.length > 0)` 블록 안에서만 존재하므로 밖에서 참조하면 TypeScript 에러 — 반드시 `finalImages` 로 전환.

    **Edit 3 — handleClose videos dirty-check 추가 (line 316-325):**

    현재:
    ```typescript
    const handleClose = () => {
      if (title || description || images.length > 0) {
        Alert.alert(t('upload_cancel'), t('upload_cancel_desc'), [
          { text: t('upload_cancel_continue'), style: 'cancel' },
          { text: t('upload_cancel_leave'), style: 'destructive', onPress: () => navigation.goBack() },
        ]);
      } else {
        navigation.goBack();
      }
    };
    ```

    다음으로 변경:
    ```typescript
    const handleClose = () => {
      // Plan 04-10 Sub-issue A side-fix: videos.length 도 dirty 판정 — 영상-only 입력 중 실수로 뒤로가기 시 작업 손실 방지.
      if (title || description || images.length > 0 || videos.length > 0) {
        Alert.alert(t('upload_cancel'), t('upload_cancel_desc'), [
          { text: t('upload_cancel_continue'), style: 'cancel' },
          { text: t('upload_cancel_leave'), style: 'destructive', onPress: () => navigation.goBack() },
        ]);
      } else {
        navigation.goBack();
      }
    };
    ```

    **수정 금지 영역:**
    - Edit flow (isEditing && existingPost, lines 186-206) — `existingPost.videos` 는 spread (`...existingPost`) 로 자동 보존되므로 편집 경로에 추가 수정 불필요. canPublish 완화만으로 기존 영상-only 포스트 편집이 활성화됨.
    - handleAddVideo / handleRemoveVideo / validateVideoAsset — 변경 없음.
    - photographerApi.ts / r2Upload.ts / get-upload-url edge function — 변경 없음.
    - 기타 hooks / styles — 변경 없음.

    수정 후 TypeScript strict check 를 반드시 실행해 `imageUpload` 참조 잔여가 없는지 확인.
  </action>
  <verify>
    <automated>
      cd app && ./node_modules/.bin/tsc --noEmit 2>&1 | tee /tmp/tsc-upload.txt && test ! -s /tmp/tsc-upload.txt || (grep -E "error" /tmp/tsc-upload.txt && exit 1)
      grep -n "images.length > 0 || videos.length > 0" app/src/screens/photographer/UploadPostScreen.tsx
      grep -n "let finalImages: string\[\]" app/src/screens/photographer/UploadPostScreen.tsx
      grep -n "if (images.length > 0)" app/src/screens/photographer/UploadPostScreen.tsx
      grep -n "imageUrls: finalImages" app/src/screens/photographer/UploadPostScreen.tsx
      grep -n "images.length > 0 || videos.length > 0" app/src/screens/photographer/UploadPostScreen.tsx | wc -l | grep -E "^\s*2\s*$"  # canPublish + handleClose 2곳
      ! grep -n "imageUpload.data" app/src/screens/photographer/UploadPostScreen.tsx   # 잔여 참조 0건 확인
    </automated>
  </verify>
  <done>
    - `tsc --noEmit` 0 errors
    - canPublish 조건에 `images.length > 0 || videos.length > 0` 존재
    - doPublish 에 `let finalImages: string[]` 선언 + `if (images.length > 0)` 가드 존재
    - createPhotoPost 호출에서 `images: finalImages` 사용
    - generate-thumbnails fire-and-forget 가드가 `finalImages.length > 0` 로 변경
    - handleClose dirty-check 가 videos.length > 0 포함
    - `imageUpload.data` 참조 잔여 0 건
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 5개 피드 surface 에 trending 패턴 복제 — VideoPlayer(mode='feed', isVisible) + onViewableItemsChanged</name>
  <files>
    app/src/screens/home/HomeScreen.tsx,
    app/src/screens/home/AllPostsScreen.tsx,
    app/src/screens/home/FeaturedAllScreen.tsx,
    app/src/screens/photographer/PhotographerProfileScreen.tsx,
    app/src/screens/archive/ArchiveScreen.tsx
  </files>
  <behavior>
    - HomeScreen featured 캐러셀: 혼합 포스트는 VideoPlayer 가 이미지 대신 표시됨. 영상-only 포스트 카드 배경이 회색 placeholder 가 아닌 VideoPlayer 로 채워짐. viewport 진입 시 autoplay.
    - AllPostsScreen / FeaturedAllScreen / PhotographerProfileScreen / ArchiveScreen: FlatList 에 onViewableItemsChanged 연동. 영상 카드는 viewport 진입 시 autoplay (muted+loop), 이탈 시 pause. 이미지 포스트는 기존 Image 렌더 그대로.
    - 혼합 포스트 (images.length > 0 && videos.length > 0): 모든 5 surface 에서 video-first (VideoPlayer 우선 마운트, images 무시).
    - 각 스크린의 `<Image>` + static play overlay 패턴은 영상-없는 케이스 fallback 으로만 남김.
    - 기존 팔로우/좋아요/네비게이션 탭 등의 UX 는 보전.
  </behavior>
  <action>
    모든 5 개 스크린에 **동일 패턴** 을 복제한다. 참조 정답 = `app/src/screens/home/HomeScreen.tsx` lines 64-73 (state + callbacks), lines 372-406 (FlatList + renderItem 분기). VideoPlayer.tsx 는 수정 금지.

    ### 공통 Recipe (5 스크린 모두 동일하게 적용)

    1. **imports 보강** (파일 맨 위):
       ```typescript
       import { useRef, useState } from 'react';            // 이미 있으면 skip, 없으면 추가
       import { FlatList } from 'react-native';             // ScrollView 만 사용하던 경우 추가
       import type { ViewToken } from 'react-native';       // type-only, 신규
       import VideoPlayer from '../../components/common/VideoPlayer';  // 경로는 스크린 위치에 맞춰
       ```

    2. **컴포넌트 함수 바디 상단** 에 `visibleIndices` state + `onViewableItemsChanged` + `viewabilityConfig` 추가:
       ```typescript
       // Plan 04-10 Sub-issue B: viewport-aware VideoPlayer autoplay (HomeScreen trending 패턴 재사용, itemVisiblePercentThreshold=60)
       const [visibleIndices, setVisibleIndices] = useState<Set<number>>(new Set());
       const onViewableItemsChanged = useRef(({ viewableItems }: { viewableItems: ViewToken[] }) => {
         const indices = new Set<number>();
         viewableItems.forEach((vt) => {
           if (typeof vt.index === 'number') indices.add(vt.index);
         });
         setVisibleIndices(indices);
       }).current;
       const viewabilityConfig = useRef({ itemVisiblePercentThreshold: 60 }).current;
       ```

    3. **카드 렌더 함수/inline map 안쪽** 에서 기존 `Image + play overlay` 를 다음 분기로 감싼다:
       ```typescript
       const previewUri = post.thumbnail_urls?.[0] ?? post.images[0];
       const hasVideo = (post.videos?.length ?? 0) > 0;
       const videoUri = post.videos?.[0];
       const isVisible = visibleIndices.has(index);     // index 가 FlatList renderItem 인자
       // ...
       {hasVideo && videoUri ? (
         <VideoPlayer
           uri={videoUri}
           mode="feed"
           width={<CARD_WIDTH 상수>}
           height={<CARD_HEIGHT 상수 — 각 스크린 기존 imageWrap aspectRatio 에 맞춰>}
           isVisible={isVisible}
         />
       ) : previewUri ? (
         <Image source={{ uri: previewUri }} style={styles.image} />
       ) : (
         <View style={[styles.image, { backgroundColor: colors.surface }]} />
       )}
       ```
       기존 정적 play overlay (`<View style={styles.videoPlayOverlay}><Ionicons name="play" .../></View>`) 는 **제거** 한다 — VideoPlayer 가 이미 재생/일시정지 비주얼을 제공하므로 중복 아이콘 충돌 방지. (단, 혼합 포스트에서 이미지가 아닌 영상이 표시되므로 play overlay 자체가 의미가 없음.)

    ### 스크린별 구체적 지점

    #### 1. `app/src/screens/home/HomeScreen.tsx` — featured 섹션만 수정 (trending 은 절대 건드리지 말 것)

    - **imports:** 이미 `VideoPlayer`, `useRef`, `ViewToken` import 존재 (trending 용). featured 용 별도 state 이름만 신설 — `visibleIndices` 는 이미 trending 용으로 사용 중이므로 **충돌 회피** 를 위해 다음 이름 사용:
      ```typescript
      // featured 섹션 전용 (가로 ScrollView 기반 — FlatList 로 교체)
      const [featuredVisibleIds, setFeaturedVisibleIds] = useState<Set<string>>(new Set());
      const onFeaturedViewableItemsChanged = useRef(({ viewableItems }: { viewableItems: ViewToken[] }) => {
        const ids = new Set<string>();
        viewableItems.forEach((vt) => {
          if (vt.item && typeof (vt.item as { id?: string }).id === 'string') {
            ids.add((vt.item as { id: string }).id);
          }
        });
        setFeaturedVisibleIds(ids);
      }).current;
      const featuredViewabilityConfig = useRef({ itemVisiblePercentThreshold: 60 }).current;
      ```
      (Featured 는 가로 캐러셀이므로 index 보다 id 기반이 안전. trending 의 `visibleIndices` state 와 이름 분리.)

    - **lines 204-253 featured 섹션** 을 `<ScrollView horizontal>...map(post => ...)</ScrollView>` 에서 `<FlatList horizontal data={featured} keyExtractor ... renderItem={...} onViewableItemsChanged={onFeaturedViewableItemsChanged} viewabilityConfig={featuredViewabilityConfig} showsHorizontalScrollIndicator={false} contentContainerStyle={styles.featuredScroll} />` 으로 교체.

    - renderItem 내부는 다음 구조:
      ```typescript
      renderItem={({ item: post }) => {
        const previewUri = post.thumbnail_urls?.[0] ?? post.images[0];
        const hasVideo = (post.videos?.length ?? 0) > 0;
        const videoUri = post.videos?.[0];
        const isVisible = featuredVisibleIds.has(post.id);
        return (
          <TouchableOpacity
            key={post.id}
            style={styles.featuredCard}
            activeOpacity={0.85}
            onPress={() => navigation.navigate('PostDetail', { postId: post.id })}
          >
            <View style={styles.featuredImageWrap}>
              {hasVideo && videoUri ? (
                <VideoPlayer
                  uri={videoUri}
                  mode="feed"
                  width={FEATURED_CARD_WIDTH}
                  height={FEATURED_CARD_WIDTH * 1.25}   // 기존 aspectRatio 에 맞춰 (기존 styles.featuredImage 확인)
                  isVisible={isVisible}
                />
              ) : previewUri ? (
                <Image source={{ uri: previewUri }} style={styles.featuredImage} />
              ) : (
                <View style={[styles.featuredImage, { backgroundColor: colors.surface }]} />
              )}
              {/* featuredPlayOverlay (<View style={styles.featuredPlayOverlay}><Ionicons name="play" size={16} color="#FFFFFF" /></View>) — 제거. VideoPlayer 분기가 시각적 재생 표현을 담당. */}
              <LinearGradient ... />
              <View style={styles.featuredTag}>...</View>
              <View style={styles.featuredBottom}>...</View>
            </View>
          </TouchableOpacity>
        );
      }}
      ```
      (기존 `<LinearGradient>` / `<View style={styles.featuredTag}>` / `<View style={styles.featuredBottom}>` 은 그대로 유지 — overlay 레이어이므로 VideoPlayer 위에 얹힌다. position: absolute 속성 때문에 동일하게 작동.)

    - **trending 그리드 (lines 372-447) 절대 수정 금지** — `visibleIndices` / `onViewableItemsChanged` / `viewabilityConfig` state 는 trending 전용, Plan 04-09 에서 이미 완성됨.

    - **`styles.featuredPlayOverlay` 스타일 정의는 남겨둬도 무해** (trace left) — 제거 선택은 Claude 재량. 제거 시 dead-code 경고 방지.

    #### 2. `app/src/screens/home/AllPostsScreen.tsx`

    - FlatList 는 이미 존재 (lines 155-163). 다음 속성 추가:
      ```typescript
      onViewableItemsChanged={onViewableItemsChanged}
      viewabilityConfig={viewabilityConfig}
      ```
    - 컴포넌트 상단 (lines 38-40 부근) 에 공통 Recipe 의 state + callback 3줄 삽입.
    - `renderPost` (lines 55-106) 의 signature 를 `({ item: post, index }: { item: PhotoPost; index: number })` 로 변경 (기존은 index 없음). FlatList 는 renderItem 콜백에 `index` 를 전달하므로 타입만 맞추면 됨.
    - `renderPost` 안의 `<Image source={{ uri: previewUri }} style={styles.image} />` + surround View 를 공통 Recipe 의 3-way 분기로 교체:
      ```typescript
      <View style={styles.imageWrap}>
        {hasVideo && videoUri ? (
          <VideoPlayer
            uri={videoUri}
            mode="feed"
            width={CARD_WIDTH}
            height={CARD_WIDTH * 5 / 4}    // aspectRatio 4/5 → height = width * 5/4
            isVisible={isVisible}
          />
        ) : previewUri ? (
          <Image source={{ uri: previewUri }} style={styles.image} />
        ) : (
          <View style={[styles.image, { backgroundColor: colors.surface }]} />
        )}
        {/* 기존 videoPlayOverlay (lines 71-75) 제거 */}
        <LinearGradient ... />
        <View style={styles.tagRow}>...</View>
        <View style={styles.bottomInfo}>...</View>
      </View>
      ```
    - `videoUri` 는 `post.videos?.[0]` 로 신규 선언. `isVisible = visibleIndices.has(index)`.

    #### 3. `app/src/screens/home/FeaturedAllScreen.tsx`

    - 현재 ScrollView + `featured.map((post) => ...)` (lines 50-100) 패턴. **FlatList 로 교체** (horizontal 아님, 2 열 그리드):
      ```typescript
      <FlatList
        data={featured}
        keyExtractor={(item) => item.id}
        numColumns={2}
        renderItem={({ item: post, index }) => ( /* 카드 JSX */ )}
        onViewableItemsChanged={onViewableItemsChanged}
        viewabilityConfig={viewabilityConfig}
        contentContainerStyle={{ paddingHorizontal: GRID_PADDING, paddingBottom: Math.max(insets.bottom, 20) + 20 }}
        columnWrapperStyle={{ gap: GRID_GAP, marginBottom: GRID_GAP }}
        showsVerticalScrollIndicator={false}
        ListEmptyComponent={<View style={styles.empty}>...</View>}  // 기존 empty state 를 옮김
      />
      ```
    - 부모 ScrollView (`styles.scrollView`) 는 제거. FlatList 가 루트 list 컴포넌트가 됨.
    - `styles.grid` (flexWrap row) 는 더 이상 필요 없지만 남겨둬도 무해.
    - 카드 JSX 안쪽: 공통 Recipe 의 3-way 분기 (Image → VideoPlayer + fallback). `videoPlayOverlay` 제거.
    - width/height: `width={CARD_WIDTH}`, `height={CARD_WIDTH * 4 / 3}` (기존 aspectRatio 3/4 → height = width * 4/3).

    #### 4. `app/src/screens/photographer/PhotographerProfileScreen.tsx`

    - 현재 `visiblePosts.map` inline 렌더 (lines 449-488). **FlatList 로 전환하되, 상위에 scrollable 부모 (ScrollView + Animated.ScrollView) 가 이미 있으므로** 내부 FlatList 는 `scrollEnabled={false}` + `numColumns={GRID_COLS}` 로 설정 — HomeScreen trending 과 동일 전략.
    - 컴포넌트 상단에 공통 Recipe state + callback 삽입.
    - `<View style={styles.postGrid}>{visiblePosts.map((post) => ...)}</View>` 를 다음으로 교체:
      ```typescript
      <FlatList
        data={visiblePosts}
        keyExtractor={(item) => item.id}
        numColumns={GRID_COLS}            // 3
        scrollEnabled={false}
        onViewableItemsChanged={onViewableItemsChanged}
        viewabilityConfig={viewabilityConfig}
        renderItem={({ item: post, index }) => {
          const previewUri = post.thumbnail_urls?.[0] ?? post.images[0];
          const hasVideo = (post.videos?.length ?? 0) > 0;
          const videoUri = post.videos?.[0];
          const isVisible = visibleIndices.has(index);
          return (
            <TouchableOpacity
              style={styles.postThumb}
              activeOpacity={0.85}
              onPress={() => navigation.navigate('PostDetail', { postId: post.id })}
            >
              {hasVideo && videoUri ? (
                <VideoPlayer
                  uri={videoUri}
                  mode="feed"
                  width={THUMB_SIZE}
                  height={THUMB_SIZE}          // 정사각형 그리드
                  isVisible={isVisible}
                />
              ) : previewUri ? (
                <Image source={{ uri: previewUri }} style={styles.postThumbImage} />
              ) : (
                <View style={[styles.postThumbImage, { backgroundColor: colors.surface }]} />
              )}
              {/* 기존 videoPlayOverlay (lines 465-469) 제거 */}
              {post.images.length > 1 && !hasVideo && (
                <View style={styles.multiIcon}>...</View>
              )}
              <View style={styles.thumbOverlay}>...</View>
            </TouchableOpacity>
          );
        }}
      />
      ```
    - **columnWrapperStyle** 은 필요 시 `{ gap: GRID_GAP }` 추가 (기존 styles.postGrid 가 flexWrap 이었으므로 FlatList 기본값과 차이 확인).
    - 부모 Animated.ScrollView 가 scrollY 를 추적하므로 `scrollEnabled={false}` 는 중복 스크롤 방지 필수.

    #### 5. `app/src/screens/archive/ArchiveScreen.tsx`

    - `renderPhotoGrid` (lines 173-207) 이 `<View style={styles.imageGrid}>{posts.map(...)}</View>` 반환. **FlatList 로 전환:**
      ```typescript
      const renderPhotoGrid = (posts: ReturnType<typeof getPhotoPost>[], allowLongPress = true) => {
        const valid = posts.filter((p): p is NonNullable<typeof p> => p !== null && p !== undefined);
        return (
          <FlatList
            data={valid}
            keyExtractor={(item) => item.id}
            numColumns={GRID_COLS}             // 3
            scrollEnabled={false}
            onViewableItemsChanged={onViewableItemsChanged}
            viewabilityConfig={viewabilityConfig}
            renderItem={({ item: post, index }) => {
              const previewUri = post.thumbnail_urls?.[0] ?? post.images[0];
              const hasVideo = (post.videos?.length ?? 0) > 0;
              const videoUri = post.videos?.[0];
              const isVisible = visibleIndices.has(index);
              return (
                <TouchableOpacity
                  style={styles.gridItem}
                  activeOpacity={0.8}
                  onPress={() => navigation.navigate('PostDetail', { postId: post.id })}
                  onLongPress={allowLongPress ? () => handlePostLongPress(post.id) : undefined}
                >
                  {hasVideo && videoUri ? (
                    <VideoPlayer
                      uri={videoUri}
                      mode="feed"
                      width={GRID_ITEM}
                      height={GRID_ITEM}
                      isVisible={isVisible}
                    />
                  ) : previewUri ? (
                    <Image source={{ uri: previewUri }} style={styles.gridImage} />
                  ) : (
                    <View style={[styles.gridImage, { backgroundColor: colors.surface }]} />
                  )}
                  {/* 기존 videoPlayOverlay 제거 */}
                  {post.images.length > 1 && !hasVideo && (
                    <View style={styles.gridMediaBadge}>...</View>
                  )}
                </TouchableOpacity>
              );
            }}
          />
        );
      };
      ```
    - 컴포넌트 상단에 공통 Recipe state + callback.
    - `posts.filter` 로 null 제거 (기존 `if (!post) return null;` 대응). 단일 FlatList 가 두 view (all tab / folder tab) 에서 모두 사용되므로 문제 없음.
    - **주의:** ArchiveScreen 은 all / folders 탭을 가지므로 visibleIndices 는 탭 전환 시 덮어쓰기 되어 자연스럽게 동작. 별도 분리 state 불필요.

    ### 수정 금지 (Task 2 범위 외)

    - `app/src/components/common/VideoPlayer.tsx` — 절대 변경 금지 (git diff 0 line 이어야 함)
    - `app/src/screens/explore/PostDetailScreen.tsx` — Test 13 pass, 이미 detail mode 마운트 완료 (Plan 04-09)
    - HomeScreen trending 그리드 (lines 363-447) — 이미 올바른 패턴, 재수정 금지
    - `app/src/screens/photographer/StudioScreen.tsx` / `CollectionDetailScreen.tsx` — Task 3 범위
    - Service layer / edge functions / migrations — 전부 변경 없음

    ### 공통 Pitfall 방지

    - **HomeScreen 의 trending `visibleIndices`** 와 featured 용 `featuredVisibleIds` 를 **같은 변수명으로 덮지 말 것**. trending 은 이미 작동 중.
    - FlatList `scrollEnabled={false}` 가 필요한 경우 (PhotographerProfileScreen, ArchiveScreen, 부모 ScrollView 가 있는 경우) 에만 설정. AllPostsScreen / FeaturedAllScreen 은 FlatList 가 루트 스크롤러이므로 기본 `true`.
    - `onViewableItemsChanged` 는 `useRef` 로 감싸야 React Native 가 "Changing onViewableItemsChanged on the fly is not supported" 워닝을 안 냄.
    - `ViewToken` 은 타입-only import (`import type { ViewToken } from 'react-native'`).
    - `VideoPlayer` 은 **default export** (`import VideoPlayer from '.../VideoPlayer'`) — HomeScreen 의 패턴과 동일.
    - Width/height 값은 **기존 styles 의 aspectRatio 에 맞춰 계산**. 정사각형이 아닌 경우 위 값 예시 참고.
  </action>
  <verify>
    <automated>
      cd app && ./node_modules/.bin/tsc --noEmit 2>&1 | tee /tmp/tsc-feed.txt && ! grep -E "error TS" /tmp/tsc-feed.txt
      # 5 스크린 모두 VideoPlayer import 추가 확인
      for f in app/src/screens/home/HomeScreen.tsx app/src/screens/home/AllPostsScreen.tsx app/src/screens/home/FeaturedAllScreen.tsx app/src/screens/photographer/PhotographerProfileScreen.tsx app/src/screens/archive/ArchiveScreen.tsx; do
        grep -q "import VideoPlayer" "$f" || { echo "MISSING VideoPlayer import in $f"; exit 1; }
        grep -q "onViewableItemsChanged" "$f" || { echo "MISSING onViewableItemsChanged in $f"; exit 1; }
        grep -q "mode=\"feed\"" "$f" || { echo "MISSING mode=feed in $f"; exit 1; }
        grep -q "isVisible={" "$f" || { echo "MISSING isVisible prop in $f"; exit 1; }
      done
      # VideoPlayer 컴포넌트 자체는 수정 금지 — diff 0
      test -z "$(git diff HEAD -- app/src/components/common/VideoPlayer.tsx)" || { echo "VideoPlayer.tsx must not be modified"; exit 1; }
      # trending grid 에 2번 손 안 댔는지 확인 — HomeScreen 의 기존 visibleIndices 심볼은 여전히 존재
      grep -n "const \[visibleIndices, setVisibleIndices\]" app/src/screens/home/HomeScreen.tsx | head -1
      grep -n "const \[featuredVisibleIds, setFeaturedVisibleIds\]" app/src/screens/home/HomeScreen.tsx
      # PostDetailScreen 수정 금지 — diff 0
      test -z "$(git diff HEAD -- app/src/screens/explore/PostDetailScreen.tsx)" || { echo "PostDetailScreen.tsx must not be modified"; exit 1; }
    </automated>
  </verify>
  <done>
    - `tsc --noEmit` 0 errors
    - 5 개 스크린 모두 `VideoPlayer`, `onViewableItemsChanged`, `mode="feed"`, `isVisible={` 문자열 포함
    - HomeScreen 에 trending 용 `visibleIndices` + featured 용 `featuredVisibleIds` 두 state 공존
    - `VideoPlayer.tsx` / `PostDetailScreen.tsx` diff 0 lines
    - 기존 정적 `videoPlayOverlay` View+Ionicons play 중복은 5 스크린 모두 제거 (VideoPlayer 가 시각 표현 담당)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: StudioScreen + CollectionDetailScreen — 영상-only fallback poster</name>
  <files>
    app/src/screens/photographer/StudioScreen.tsx,
    app/src/screens/photographer/CollectionDetailScreen.tsx
  </files>
  <behavior>
    - 영상-only 포스트 (images=[], videos=['url'], thumbnail_urls=[]) 가 StudioScreen 그리드 / CollectionDetailScreen 그리드에서 회색 빈 placeholder 대신 videos[0] 기반의 정지 첫 프레임 (expo-video native poster) 로 렌더된다.
    - 재생 (autoplay) 은 하지 않는다 — studio/collection 은 UI-SPEC 설계상 정적 썸네일 모드. VideoPlayer(mode='studio') 가 muted+no-autoplay+no-nativeControls 로 첫 프레임만 표시.
    - 기존 static play overlay (studio: 32x32, collection: 그리드용 오버레이) 는 유지 — VideoPlayer 위에 absolute position 으로 계속 얹힘.
    - 이미지-only / 혼합 / thumbnail_urls 존재 포스트는 기존 Image 렌더 그대로 보전 (회귀 금지).
    - 부모 FlatList / ScrollView 의 스크롤·페이지네이션·긴 눌림 동작 전부 보전.
  </behavior>
  <action>
    Task 2 와 달리 **videoPlayOverlay 는 유지** — 정적 썸네일 + play overlay 가 UI-SPEC 의도. VideoPlayer 는 `mode='studio'` 로 정지 poster 용도로만 사용.

    ### Edit 1 — `app/src/screens/photographer/StudioScreen.tsx` (lines 418-453 부근)

    imports 에 VideoPlayer 추가 (파일 내 기존 import 순서 따름):
    ```typescript
    import VideoPlayer from '../../components/common/VideoPlayer';
    ```

    현재 line 419-434 부근:
    ```typescript
    <View style={styles.postImageWrap}>
      <Image source={{ uri: previewUrl }} style={styles.postCardImage} />
      {hasVideo && (
        <View style={styles.videoPlayOverlay}>
          <Ionicons name="play" size={16} color="#FFFFFF" />
        </View>
      )}
      ...
    </View>
    ```

    다음으로 변경:
    ```typescript
    <View style={styles.postImageWrap}>
      {previewUrl ? (
        <Image source={{ uri: previewUrl }} style={styles.postCardImage} />
      ) : hasVideo && post.videos[0] ? (
        // Plan 04-10 Sub-issue B: 영상-only 포스트 fallback — expo-video native poster (첫 프레임). studio mode = autoplay 없음, nativeControls 없음, muted.
        <VideoPlayer
          uri={post.videos[0]}
          mode="studio"
          width={<styles.postCardImage 의 너비 — 기존 코드 스타일 상수에서 확인 필요. styles.postCardImage 가 width: '100%' 로 컨테이너 따라가므로, 부모 postImageWrap 의 명시적 width 가 없으면 FlatList 카드 width 를 사용. StudioScreen 의 기존 CARD_WIDTH 상수가 정의되어 있다면 그대로 사용. 없으면 SCREEN_WIDTH 파생값을 재사용.>}
          height={<postCardImage 의 height 또는 aspectRatio 파생값. 기존 styles.postImageWrap 이 aspectRatio 1/1 라면 width === height>}
        />
      ) : (
        <View style={[styles.postCardImage, { backgroundColor: colors.surface }]} />
      )}
      {hasVideo && (
        <View style={styles.videoPlayOverlay}>
          <Ionicons name="play" size={16} color="#FFFFFF" />
        </View>
      )}
      {post.is_featured && (
        <View style={styles.featuredBadge}>...</View>
      )}
      {post.status === 'pending' && (
        <View style={styles.pendingBadge}>...</View>
      )}
      <View style={styles.postOverlay}>...</View>
    </View>
    ```

    **width/height 계산:** `StudioScreen.tsx` 상단 또는 styles 에 grid card width 상수를 찾아 재사용. 없으면 다음 유틸 패턴 사용:
    ```typescript
    // 파일 상단, Dimensions import 옆에 추가
    const { width: SCREEN_WIDTH } = Dimensions.get('window');
    const STUDIO_GRID_GAP = 8;         // 실제 styles.postGrid 의 gap 값에 맞춤
    const STUDIO_CARD_WIDTH = (SCREEN_WIDTH - 32 - STUDIO_GRID_GAP) / 2;   // 2-col + 16 좌우 padding
    ```
    (실제 값은 기존 styles 확인 후 조정. Claude 재량.)

    ### Edit 2 — `app/src/screens/photographer/CollectionDetailScreen.tsx` (lines 129-148 부근)

    imports 에 VideoPlayer 추가:
    ```typescript
    import VideoPlayer from '../../components/common/VideoPlayer';
    ```

    현재 line 130-147:
    ```typescript
    renderItem={({ item }) => {
      const previewUrl = item.thumbnail_urls?.[0] ?? item.images[0];
      const hasVideo = (item.videos?.length ?? 0) > 0;
      return (
        <TouchableOpacity
          style={styles.gridItem}
          activeOpacity={0.85}
          onPress={() => navigation.navigate('PostDetail', { postId: item.id })}
        >
          <Image source={{ uri: previewUrl }} style={styles.gridImage} />
          {hasVideo && (
            <View style={styles.videoPlayOverlay}>
              <Ionicons name="play" size={14} color="#FFFFFF" />
            </View>
          )}
        </TouchableOpacity>
      );
    }}
    ```

    다음으로 변경:
    ```typescript
    renderItem={({ item }) => {
      const previewUrl = item.thumbnail_urls?.[0] ?? item.images[0];
      const hasVideo = (item.videos?.length ?? 0) > 0;
      return (
        <TouchableOpacity
          style={styles.gridItem}
          activeOpacity={0.85}
          onPress={() => navigation.navigate('PostDetail', { postId: item.id })}
        >
          {previewUrl ? (
            <Image source={{ uri: previewUrl }} style={styles.gridImage} />
          ) : hasVideo && item.videos[0] ? (
            // Plan 04-10 Sub-issue B: 영상-only fallback poster
            <VideoPlayer
              uri={item.videos[0]}
              mode="studio"
              width={GRID_ITEM_W}    // 기존 styles.gridItem width 또는 파생 상수
              height={GRID_ITEM_H}
            />
          ) : (
            <View style={[styles.gridImage, { backgroundColor: colors.surface }]} />
          )}
          {hasVideo && (
            <View style={styles.videoPlayOverlay}>
              <Ionicons name="play" size={14} color="#FFFFFF" />
            </View>
          )}
        </TouchableOpacity>
      );
    }}
    ```

    **width/height:** 파일 상단에 `GRID_COLS`, `GRID_GAP` 상수 또는 styles.gridItem width 를 확인해 같은 값을 사용. 없으면:
    ```typescript
    const { width: SCREEN_WIDTH } = Dimensions.get('window');
    const COL_GRID_COLS = 3;
    const COL_GRID_GAP = 2;    // styles 에서 실제 값 확인
    const GRID_ITEM_W = Math.floor((SCREEN_WIDTH - COL_GRID_GAP * (COL_GRID_COLS - 1)) / COL_GRID_COLS);
    const GRID_ITEM_H = GRID_ITEM_W;  // 정사각형
    ```

    ### 수정 금지

    - `VideoPlayer.tsx` — 재확인. `mode='studio'` 정책은 이미 구현됨 (autoplay 없음, nativeControls=false, muted=false 는 파일 line 35-38 확인). 만약 muted 여야 한다면 **절대 VideoPlayer 를 수정하지 말고** (mode='feed' 처럼 항상 muted) 대신 이 task 는 `mode='studio'` 를 그대로 사용. 정책상 무음이 요구되는 경우 future phase 에서 처리.
    - Service / DB / edge function — 변경 없음.
    - Task 2 에서 이미 수정한 5 스크린 — 재수정 금지.

    ### Pitfall 방지

    - `previewUrl` 가 undefined 일 수 있음. `Image` 에 `source={{ uri: undefined }}` 를 전달하면 안 됨 → 3-way 분기 (`previewUrl` / `hasVideo` / grey placeholder) 필수.
    - `mode='studio'` VideoPlayer 는 autoplay 안 함 → 여러 개 마운트되어도 배터리/데이터 비용 낮음.
    - 기존 `videoPlayOverlay` 는 **유지** — VideoPlayer 가 정지 poster 만 보여주므로 play 아이콘이 여전히 의미 있음.
  </action>
  <verify>
    <automated>
      cd app && ./node_modules/.bin/tsc --noEmit 2>&1 | tee /tmp/tsc-studio.txt && ! grep -E "error TS" /tmp/tsc-studio.txt
      grep -q "import VideoPlayer" app/src/screens/photographer/StudioScreen.tsx
      grep -q 'mode="studio"' app/src/screens/photographer/StudioScreen.tsx
      grep -q "import VideoPlayer" app/src/screens/photographer/CollectionDetailScreen.tsx
      grep -q 'mode="studio"' app/src/screens/photographer/CollectionDetailScreen.tsx
      # videoPlayOverlay 는 두 파일 모두 여전히 존재해야 함 (정적 썸네일 UI-SPEC 유지)
      grep -c "videoPlayOverlay" app/src/screens/photographer/StudioScreen.tsx | grep -E "^[1-9]"
      grep -c "videoPlayOverlay" app/src/screens/photographer/CollectionDetailScreen.tsx | grep -E "^[1-9]"
      # VideoPlayer.tsx diff 0
      test -z "$(git diff HEAD -- app/src/components/common/VideoPlayer.tsx)"
    </automated>
  </verify>
  <done>
    - `tsc --noEmit` 0 errors
    - StudioScreen / CollectionDetailScreen 모두 `import VideoPlayer` + `mode="studio"` 사용
    - videoPlayOverlay 스타일 + 사용처 모두 보존 (정적 썸네일 + play overlay 의도 유지)
    - VideoPlayer.tsx diff 0 lines
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    UAT Test 12 선행 조건 전수 복구:
    1. UploadPostScreen: 영상-only 포스트 작성 가능 (canPublish OR 조건 + doPublish image skip)
    2. 5 개 피드 surface (HomeScreen featured / AllPostsScreen / FeaturedAllScreen / PhotographerProfileScreen / ArchiveScreen) 에 trending 그리드와 동일한 viewport-aware VideoPlayer(mode='feed') 마운트
    3. StudioScreen / CollectionDetailScreen 영상-only fallback poster
  </what-built>
  <how-to-verify>
    **사전 준비:** EAS dev build 이미 설치된 실기기에서 `npx expo start --dev-client` 연결. 로그인 후 승인된 포토그래퍼 계정 (wecord.admin@gmail.com) 으로 진입.

    ### Step 1 — Sub-issue A 검증 (영상-only submit)
    1. Studio 탭 진입 → '+' 버튼 → UploadPostScreen 열기
    2. 제목 입력 ("테스트 영상-only")
    3. 팀 선택 (예: 두산)
    4. 저작권 체크박스 체크
    5. **이미지는 추가하지 않는다** → 영상 섹션에서 '동영상 추가' → MP4 20초 짜리 영상 선택 (미리 휴대폰 갤러리에 준비)
    6. 기대: 상단 '게시하기' 버튼이 **활성화된 파란색** 으로 표시됨 (이전: 회색 비활성)
    7. '게시하기' 탭 → 확인 Alert → '확인'
    8. 업로드 진행 → 성공 Toast
    9. Studio 화면으로 돌아가면 첫 카드에 새 포스트가 추가되어 있어야 함
    10. DB 확인 (선택): Supabase dashboard → photo_posts 새 row 의 `images: {}`, `videos: {https://....mp4}`

    ### Step 2 — Sub-issue A 부차 검증 (handleClose dirty-check)
    1. UploadPostScreen 다시 열기
    2. 영상만 1 개 추가 (다른 필드 비움)
    3. 좌상단 'X' 버튼 탭 → "취소하시겠습니까?" Alert 가 **표시되어야 함** (이전: 즉시 종료)

    ### Step 3 — Sub-issue B 검증: HomeScreen featured 캐러셀 autoplay
    1. 홈 탭 pull-to-refresh
    2. "이번 주 베스트" 가로 캐러셀까지 스크롤
    3. Step 1 에서 만든 영상 포스트가 featured 에 포함된다면 (is_featured 트리거 시점에 따라 즉시 노출 안 될 수 있음 — DB 로 확인 or 기존 영상 포함 featured 카드 사용) → 해당 카드가 viewport 안으로 들어오면 **영상 자동 재생 (muted + loop)**
    4. 가로로 스크롤해 viewport 밖으로 나가면 **pause**
    5. 카드 탭 → PostDetailScreen → detail mode native controls 확인 (Plan 04-09 pass 재확인)

    ### Step 4 — Sub-issue B 검증: AllPostsScreen / FeaturedAllScreen 그리드 autoplay
    1. 홈 trending 섹션의 '전체보기' 탭 → AllPostsScreen
    2. 영상 포스트 카드가 viewport 에 들어올 때까지 스크롤
    3. **Autoplay (muted+loop)** 확인, 스크롤 이탈 시 pause
    4. 뒤로가기 → '이번 주 베스트' 섹션 '전체보기' 탭 → FeaturedAllScreen 에서 동일 검증

    ### Step 5 — Sub-issue B 검증: PhotographerProfileScreen 그리드 autoplay
    1. Explore / 홈에서 포토그래퍼 프로필 (영상 포함 포스트가 있는) 진입
    2. 본인 프로필 (Studio 경유) 또는 타 포토그래퍼 프로필
    3. 포스트 그리드 스크롤 → 영상 카드 viewport 진입 시 autoplay, 이탈 시 pause

    ### Step 6 — Sub-issue B 검증: ArchiveScreen 그리드 autoplay
    1. 홈 피드에서 영상 포스트를 '북마크' 저장
    2. 프로필 → 보관함 탭 → 저장된 포스트 그리드에서 autoplay

    ### Step 7 — Sub-issue B 검증: Studio / CollectionDetail 영상-only fallback
    1. Step 1 에서 만든 영상-only 포스트를 Studio 그리드에서 확인 → **회색 placeholder 아닌 영상의 정지 첫 프레임 (expo-video poster)** 으로 표시됨. 우상단 play 아이콘 overlay 그대로 유지.
    2. 본인 컬렉션에 해당 포스트 추가 → CollectionDetailScreen 진입 → 동일하게 정지 poster + play overlay 표시

    ### Step 8 — 회귀 검증 (이미지 전용 포스트 동작 보전)
    1. 기존 이미지 전용 포스트가 모든 스크린에서 깨지지 않는지 확인
    2. PostDetailScreen (Test 13) 재생/일시정지/seek bar 정상 — Plan 04-09 결과 보전
    3. HomeScreen trending 그리드 (Plan 04-09 기구현) autoplay 정상 — Task 2 에서 trending 부분 건드리지 않았는지 확인

    ### 전체 판정
    - 모든 Step 이 pass → UAT Test 12 `result: issue` → `result: pass` 로 전환 가능
    - 일부 Step 실패 → 해당 Step 의 스크린 / 증상 기록 후 `resume-signal` 에 issue 로 기재
  </how-to-verify>
  <resume-signal>
    "approved" — 모든 Step 통과
    또는
    "issue: Step X — {증상}" — 특정 Step 실패 시 (예: "issue: Step 3 — featured 캐러셀에서 autoplay 되지 않음")
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (RN app) → R2 presigned PUT | 영상-only 포스트의 videos[] 배열이 R2 public URL 로 채워져 피드 렌더에서 expo-video 가 소비 |
| client → Supabase REST (createPhotoPost) | images=[] 수용 INSERT |
| client → Supabase Storage (photo_posts row read) | RLS 로 gated, 기존 policy 재사용 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10-01 | Tampering | UploadPostScreen canPublish | accept | 클라이언트 UI 게이팅 완화는 보안 경계 아님 — 서버 측 createPhotoPost 는 이미 images=[] + videos non-empty 를 구분 없이 수용 (07/029 migrations CHECK 는 NULL-pass 의미론). 우회 시나리오 없음. |
| T-04-10-02 | DoS | 5 피드 surface 의 동시 VideoPlayer 인스턴스 (expo-video 복수 mount) | mitigate | `viewabilityConfig.itemVisiblePercentThreshold=60` 로 viewport 밖 카드는 pause → 화면당 동시 재생 2~4개로 제한. 기존 HomeScreen trending (Plan 04-09) 에서 이미 검증된 비용 프로파일. mode='feed' 는 muted 이므로 오디오 스트림 비용 0. |
| T-04-10-03 | Information Disclosure | VideoPlayer(mode='studio') 가 R2 public URL 을 fetch | accept | R2 public URL 은 이미 공개 피드/상세 화면에서 expose 되는 동일 URL (pub-*.r2.dev). studio fallback 마운트가 새로운 노출 경로를 만들지 않음. custom domain 전환은 별도 phase (PROJECT Blockers 참조). |
| T-04-10-04 | Denial of Service (data plan) | 모바일 데이터 요금제 사용자에서 autoplay 비용 | accept | v1 결정 사항 (orchestrator context) — trending 과 동일한 60% viewport threshold 로 최소 마운트. 추후 phase 에서 settings 토글 도입 가능. |
</threat_model>

<verification>
## Automated gates (all tasks)

1. `cd app && ./node_modules/.bin/tsc --noEmit` → 0 errors
2. `git diff HEAD -- app/src/components/common/VideoPlayer.tsx` → 0 lines (컴포넌트 수정 금지 재확인)
3. `git diff HEAD -- app/src/screens/explore/PostDetailScreen.tsx` → 0 lines (Plan 04-09 결과 보전)
4. HomeScreen trending 그리드 코드 구조 보전: `grep -n "const \[visibleIndices, setVisibleIndices\]" app/src/screens/home/HomeScreen.tsx` → 매치
5. 8 개 files_modified 전체에 필요 import / 심볼 / 분기 존재:
   - UploadPostScreen.tsx: `images.length > 0 || videos.length > 0` (2곳), `let finalImages`, `if (images.length > 0)`, `imageUrls: finalImages`
   - 5 피드 스크린: `import VideoPlayer`, `onViewableItemsChanged`, `mode="feed"`, `isVisible={`
   - Studio/CollectionDetail: `import VideoPlayer`, `mode="studio"`, `videoPlayOverlay` 존재 유지

## Behavior gates (checkpoint Step 1-8, 실기기 UAT Test 12)

- Step 1 pass → Sub-issue A 해소
- Steps 3-6 pass → Sub-issue B autoplay 해소 (trending 과 동일 UX 5 곳 확산)
- Step 7 pass → Studio/CollectionDetail fallback 해소
- Step 8 pass → 회귀 없음 (trending, PostDetail, 이미지 포스트 전체 보전)

## UAT file 업데이트 지침 (SUMMARY 작성 시)

`.planning/phases/04-photographer/04-UAT.md` 에서:
- `### 12. VideoPlayer — Feed Mode Autoplay` 블록의 `result: issue` → `result: pass` + `verified_at: ...` + `resolved_by: "Plan 04-10 gap closure — Sub-issue A (영상-only canPublish OR 조건) + Sub-issue B (5 surface viewport-aware VideoPlayer + 2 fallback studio poster)"` 추가
- `## Gaps` 의 마지막 블록 (`status: diagnosed`) 을 `status: resolved` 로 flip + `resolved_by` 기입
- `## Summary` 의 `issues: 1` → `issues: 0`, `passed: 10` → `passed: 11`, `issue_list: [12]` → `issue_list: []`
</verification>

<success_criteria>
1. UAT Test 12 `result: pass` 로 전환 가능 (8-step 실기기 체크리스트 통과)
2. UploadPostScreen canPublish 조건이 `images.length > 0 || videos.length > 0` 로 변경되어 영상-only 포스트 작성 가능
3. doPublish 가 `finalImages` 변수를 경유하며 `images.length === 0` 시 uploadPostImages / generate-thumbnails 호출을 skip
4. 5 개 피드 surface 모두 `VideoPlayer(mode='feed', isVisible)` + `onViewableItemsChanged` + `viewabilityConfig {itemVisiblePercentThreshold: 60}` 패턴 적용 — trending 그리드와 동일 viewport autoplay
5. StudioScreen / CollectionDetailScreen 이 영상-only 포스트에서 VideoPlayer(mode='studio') 로 정지 poster 렌더 — 회색 placeholder 사라짐
6. 기존 trending 그리드 (HomeScreen 372-447), PostDetailScreen hero, 이미지 전용 포스트 동작 전부 보전 (diff 검증)
7. TypeScript strict 0 errors, VideoPlayer.tsx 수정 0 lines
8. 혼합 포스트 (images+videos) 는 v1 결정 사항대로 모든 피드 surface 에서 video-first 통일
</success_criteria>

<output>
완료 후 `.planning/phases/04-photographer/04-10-SUMMARY.md` 생성. 포함할 항목:

- frontmatter: phase, plan: 10, subsystem: mobile, tags, requirements, dependency-graph(requires: [04-04, 04-09], provides: ["UAT Test 12 재개 — 영상-only submit + 5 surface viewport autoplay + studio/collection fallback"]), key-files, decisions (video-first 통일 / poster fallback v1 결정 근거), metrics
- What Was Built: Task 1/2/3 별 변경 요약 + commit hash
- Deviations from Plan: 있다면 명시
- Tests / Verification: tsc 결과 + grep 결과 + Checkpoint Step 1-8 pass 기록
- Files Modified: 8 파일 +/- line 변화량
- Commits: 3 개 (task 별 1 commit 권장 — `feat(04-10): <설명>`)
- Known Stubs: 없음 (v1 scope 내 완결)
- Threat Flags: 신규 네트워크 endpoint 없음, RLS/auth 변경 없음, VideoPlayer 재사용
- Success Criteria Status: 1~8 ✅/❌
- Self-Check: SUMMARY 존재 / commits 존재 / tsc pass / VideoPlayer.tsx diff 0 / PostDetailScreen.tsx diff 0

UAT 파일 업데이트도 동일 커밋 또는 별도 `docs(04-10): UAT Test 12 resolved` 커밋으로 반영.
</output>
