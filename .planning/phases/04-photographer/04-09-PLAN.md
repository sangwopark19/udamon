---
phase: 04-photographer
plan: 09
type: execute
wave: 6
depends_on: [04-07]
files_modified:
  - app/src/screens/explore/PostDetailScreen.tsx
  - app/src/screens/home/HomeScreen.tsx
  - app/src/screens/home/AllPostsScreen.tsx
  - app/src/screens/home/FeaturedAllScreen.tsx
  - app/src/screens/photographer/PhotographerProfileScreen.tsx
  - app/src/screens/archive/ArchiveScreen.tsx
  - app/src/i18n/locales/ko.ts
autonomous: true
gap_closure: true
requirements: [PHOT-03, PHOT-04]

must_haves:
  truths:
    - "PostDetailScreen hero 가 images+videos 를 통합 media 배열로 구성하여 각 아이템을 type 에 따라 Image 또는 VideoPlayer(mode='detail') 로 분기 렌더한다"
    - "영상 전용 포스트 (images=[], videos=['url']) 가 PostDetailScreen 에서 hero slot 에 정상 표시된다 (gallery.length 체크가 videos 포함 길이를 본다)"
    - "PostDetailScreen thumbnail strip 이 video 아이템에 play 오버레이를 표시하며, 탭 시 hero 의 해당 슬라이드로 scrollToIdx 동작한다"
    - "HomeScreen trending/featured 카드, AllPostsScreen / FeaturedAllScreen / PhotographerProfileScreen / ArchiveScreen 그리드 카드가 video-bearing post 에 대해 play 오버레이를 표시한다"
    - "모든 피드 카드가 preview URL 을 `post.thumbnail_urls?.[0] ?? post.images[0]` 순서로 fallback 한다 — 영상 전용 포스트도 깨지지 않는다"
    - "HomeScreen trending 그리드가 FlatList 기반이 되어 onViewableItemsChanged 로 viewport-aware isVisible 을 VideoPlayer(mode='feed') 에 전달한다 (영상 포스트 한정)"
    - "Test 12 (Feed Mode Autoplay) / Test 13 (Detail Mode Native Controls) 이 blocked → re-testable 상태로 전환된다"
    - "VideoPlayer 컴포넌트는 수정 없이 재사용 (feed/detail/studio 3 모드 그대로)"
  artifacts:
    - path: "app/src/screens/explore/PostDetailScreen.tsx"
      provides: "hero = images+videos 분기 렌더 + video thumbnail 오버레이"
      contains: "VideoPlayer"
    - path: "app/src/screens/home/HomeScreen.tsx"
      provides: "trending grid = FlatList + viewport-aware VideoPlayer(mode='feed'), featured horizontal = play overlay"
      contains: "VideoPlayer"
    - path: "app/src/screens/home/AllPostsScreen.tsx"
      provides: "피드 카드 play overlay + thumbnail fallback"
      contains: "hasVideo"
    - path: "app/src/screens/home/FeaturedAllScreen.tsx"
      provides: "피드 카드 play overlay"
      contains: "hasVideo"
    - path: "app/src/screens/photographer/PhotographerProfileScreen.tsx"
      provides: "프로필 그리드 play overlay"
      contains: "hasVideo"
    - path: "app/src/screens/archive/ArchiveScreen.tsx"
      provides: "저장 그리드 play overlay"
      contains: "hasVideo"
    - path: "app/src/i18n/locales/ko.ts"
      provides: "a11y_video_preview 신규 i18n key"
      contains: "a11y_video_preview"
  key_links:
    - from: "app/src/screens/explore/PostDetailScreen.tsx"
      to: "app/src/components/common/VideoPlayer.tsx"
      via: "VideoPlayer mode='detail' 렌더링 — hero media 배열의 video 아이템"
      pattern: "VideoPlayer"
    - from: "app/src/screens/home/HomeScreen.tsx"
      to: "app/src/components/common/VideoPlayer.tsx"
      via: "trending grid FlatList renderItem 이 hasVideo 일 때 mode='feed' + isVisible prop"
      pattern: "VideoPlayer"
    - from: "app/src/screens/home/HomeScreen.tsx"
      to: "FlatList onViewableItemsChanged"
      via: "viewableItems 의 index Set 을 state 에 저장 → isVisible 계산"
      pattern: "onViewableItemsChanged"
---

<objective>
UAT Test 11 gap 해소 — "R2 업로드 완료된 영상이 피드/게시물에서 재생되지 않음". Data pipeline (R2 upload → createPhotoPost → PhotoPost.videos) 은 정상이고 VideoPlayer 컴포넌트도 완성되어 있으나 (Plan 04-04) **UploadPostScreen 1곳 외에는 어떤 화면도 VideoPlayer 를 import/mount 하지 않아** post.videos 가 무시되는 것이 원인.

Purpose:
1. PostDetailScreen hero 가 images+videos 를 통합 media array 로 구성하여 각 아이템을 Image / VideoPlayer(mode='detail') 로 분기 렌더 → Test 11 PASS + Test 13 (native controls) unblock.
2. HomeScreen trending 그리드는 FlatList + onViewableItemsChanged viewport-aware 재생으로 전환하여 Test 12 (Feed Mode Autoplay) 까지 해결. 나머지 피드 카드들 (AllPosts / FeaturedAll / PhotographerProfile / Archive) 는 play overlay + thumbnail fallback 만으로도 영상 전용 포스트가 깨지지 않도록 한다 (실제 인라인 autoplay 는 HomeScreen trending 에 국한 — 과도한 player 인스턴스 비용 방지).
3. 모든 피드 preview 가 `post.thumbnail_urls?.[0] ?? post.images[0]` fallback 순서로 통일되어 영상 전용 포스트 (images=[], videos=['url'], thumbnail_urls 가 EF 를 통해 채워짐) 가 safely preview 된다.

Output: VideoPlayer 컴포넌트가 PostDetailScreen (detail mode) 과 HomeScreen trending (feed mode) 에서 실제로 마운트되어 재생된다. 다른 피드는 play overlay + thumbnail fallback 만 추가. VideoPlayer 코드 자체는 변경 없음.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-photographer/04-UAT.md
@app/src/components/common/VideoPlayer.tsx
@app/src/screens/explore/PostDetailScreen.tsx
@app/src/screens/home/HomeScreen.tsx
@app/src/screens/home/AllPostsScreen.tsx
@app/src/screens/home/FeaturedAllScreen.tsx
@app/src/screens/photographer/StudioScreen.tsx
@app/src/screens/photographer/PhotographerProfileScreen.tsx
@app/src/screens/archive/ArchiveScreen.tsx
@app/src/types/photographer.ts

<interfaces>
<!-- Executor reference — do NOT rediscover via grep. -->

From app/src/components/common/VideoPlayer.tsx (existing — reuse as-is, DO NOT MODIFY):
```typescript
export interface VideoPlayerProps {
  uri: string;
  mode: 'feed' | 'detail' | 'studio';
  width: number;
  height: number;
  isVisible?: boolean;       // feed 모드 viewport-aware play/pause
  onError?: (e: Error) => void;
}
export default function VideoPlayer(props: VideoPlayerProps): JSX.Element;
```

Mode 정책:
- feed: muted + loop + viewport-aware autoplay (isVisible prop 필요). nativeControls 없음.
- detail: nativeControls 노출, PiP 허용. muted=false, loop=false.
- studio: thumbnail 미리보기 (autoplay/controls 모두 없음).

From app/src/types/photographer.ts (existing):
```typescript
export interface PhotoPost {
  id: string;
  photographer_id: string;
  team_id: string;
  title: string;
  description: string;
  images: string[];             // R2 이미지 URL 배열
  videos: string[];             // R2 public URL 배열, max 3
  thumbnail_urls: string[];     // generate-thumbnails EF 가 채움, 빈 경우 images[0] fallback
  like_count: number; view_count: number; comment_count: number;
  is_featured: boolean; featured_week?: string | null;
  status: 'pending' | 'approved' | 'rejected';
  rejection_reason?: string | null;
  created_at: string; updated_at: string;
  photographer: { display_name: string; avatar_url: string | null; is_verified: boolean };
  team: { name_ko: string };
  player?: { name_ko: string; number: number } | null;
  cheerleader?: { name_ko: string } | null;
}
```

Preview URL resolution pattern (reference StudioScreen.tsx:419 — 이미 증명된 기존 패턴):
```typescript
const previewUrl = post.thumbnail_urls?.[0] ?? post.images[0];
const hasVideo = (post.videos?.length ?? 0) > 0;
```

Video play overlay pattern (reference StudioScreen.tsx:791-801 — 기존 스타일 그대로 재사용):
```tsx
{hasVideo && (
  <View style={styles.videoPlayOverlay}>
    <Ionicons name="play" size={16} color="#FFFFFF" />
  </View>
)}
// styles.videoPlayOverlay (16~32 px round with rgba(0,0,0,0.55) bg):
videoPlayOverlay: {
  position: 'absolute', bottom: 6, right: 6, width: 32, height: 32, borderRadius: 16,
  backgroundColor: 'rgba(0,0,0,0.55)', justifyContent: 'center', alignItems: 'center',
},
```

Theme tokens (existing, use these — no new hex):
- colors.surface, colors.primary, colors.background from `app/src/styles/theme`
- radius.sm/md/lg/xl, spacing.sm/md/lg, fontSize.meta/body

Reference — UploadPostScreen 의 VideoPlayer import 라인 (파일 34):
```typescript
import VideoPlayer from '../../components/common/VideoPlayer';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PostDetailScreen hero 가 images+videos 통합 media 배열을 렌더</name>
  <files>app/src/screens/explore/PostDetailScreen.tsx, app/src/i18n/locales/ko.ts</files>
  <action>
PostDetailScreen hero FlatList 와 thumbnail strip 을 `gallery = post.images` (현재 line 185) 단일 배열에서 `media = [...images 태그, ...videos 태그]` 통합 배열로 전환.

1. Import 추가 (line 46 근처):
   ```typescript
   import VideoPlayer from '../../components/common/VideoPlayer';
   ```

2. Local type 정의 추가 (컴포넌트 상단, SpecItem 정의 근처):
   ```typescript
   type HeroMediaItem =
     | { kind: 'image'; uri: string }
     | { kind: 'video'; uri: string };
   ```

3. `const gallery = post.images;` (line 185) 를 다음으로 교체:
   ```typescript
   // Plan 04-09 gap closure — images + videos 통합 media 배열 렌더
   const gallery: HeroMediaItem[] = useMemo(
     () => [
       ...post.images.map<HeroMediaItem>((uri) => ({ kind: 'image', uri })),
       ...post.videos.map<HeroMediaItem>((uri) => ({ kind: 'video', uri })),
     ],
     [post.images, post.videos],
   );
   ```
   (주의: 파일 상단에 이미 `useMemo` import 존재 — line 1)

4. 기존 `gallery.length > 0` 체크는 그대로 유지. 이제 videos-only post 도 length>0 이 된다. Fullscreen modal `data={gallery}` 도 동일 배열을 쓰되 video 아이템은 fullscreen 에서도 VideoPlayer 로 렌더되어야 한다 (step 7 참조).

5. hero FlatList `renderItem` 교체 (line 358-384 근처):
   ```tsx
   renderItem={({ item, index }) => (
     item.kind === 'video' ? (
       <View style={styles.heroSlide}>
         <VideoPlayer
           uri={item.uri}
           mode="detail"
           width={SCREEN_WIDTH}
           height={(SCREEN_WIDTH * 4) / 3}
         />
         {/* Double-tap heart/fullscreen 은 영상에서는 적용 안 함 (native controls 우선) */}
       </View>
     ) : (
       <TouchableOpacity
         activeOpacity={0.9}
         onPress={() => {
           handleDoubleTap();
           setTimeout(() => {
             if (Date.now() - lastTapRef.current >= 300) {
               setFullscreenVisible(true);
             }
           }, 310);
         }}
         style={styles.heroSlide}
       >
         <ScrollView
           maximumZoomScale={2.5} minimumZoomScale={1}
           showsHorizontalScrollIndicator={false} showsVerticalScrollIndicator={false}
           bouncesZoom centerContent contentContainerStyle={{ flex: 1 }}
         >
           <Image source={{ uri: item.uri }} style={styles.heroImage} />
         </ScrollView>
       </TouchableOpacity>
     )
   )}
   ```

6. Thumbnail strip (line 419-431) — gallery 가 object 배열이 되었으므로 접근 방식 변경 + video 아이템에 play 오버레이:
   ```tsx
   {gallery.length > 1 && (
     <View style={styles.thumbnailRow}>
       {gallery.map((item, idx) => (
         <TouchableOpacity
           key={idx}
           style={[styles.thumbnail, idx === activeIdx && styles.thumbnailActive]}
           onPress={() => scrollToIdx(idx)}
           activeOpacity={0.8}
           accessibilityLabel={item.kind === 'video' ? t('a11y_video_preview') : undefined}
         >
           <Image
             source={{ uri: item.kind === 'video' ? (post.thumbnail_urls?.[0] ?? item.uri) : item.uri }}
             style={styles.thumbnailImage}
           />
           {item.kind === 'video' && (
             <View style={styles.thumbVideoOverlay}>
               <Ionicons name="play" size={14} color="#FFFFFF" />
             </View>
           )}
         </TouchableOpacity>
       ))}
     </View>
   )}
   ```
   참고: video 아이템의 thumbnail preview 는 thumbnail_urls 가 있으면 우선, 없으면 video URL 자체를 source 로 사용 (대부분의 경우 Image 가 fallback 표시 실패 시 grey placeholder 로 degrade — 기존 styles 배경 colors.surface 와 일치).

7. Fullscreen modal `FlatList data={gallery}` renderItem (line 841-854) 도 video 분기 추가:
   ```tsx
   renderItem={({ item }) => (
     <View style={styles.fullscreenSlide}>
       {item.kind === 'video' ? (
         <VideoPlayer uri={item.uri} mode="detail" width={SCREEN_WIDTH} height={SCREEN_HEIGHT} />
       ) : (
         <ScrollView
           maximumZoomScale={3} minimumZoomScale={1}
           showsHorizontalScrollIndicator={false} showsVerticalScrollIndicator={false}
           contentContainerStyle={styles.zoomContainer} bouncesZoom
         >
           <Image source={{ uri: item.uri }} style={styles.fullscreenImage} />
         </ScrollView>
       )}
     </View>
   )}
   ```

8. `specsGrid` 의 `<SpecItem value={`${gallery.length}`} icon="grid" />` (line 521) — 배열 길이로 그대로 작동. spec 은 이제 "미디어 총 개수" 의미가 되는데 본 plan 범위 밖이므로 수치만 변경. 정성적 레이블 변경 없이 유지.

9. 스타일 시트에 `thumbVideoOverlay` 추가 (기존 StudioScreen.tsx videoPlayOverlay 축소 버전 — thumbnail 72x72 기준):
   ```typescript
   thumbVideoOverlay: {
     position: 'absolute', bottom: 4, right: 4,
     width: 22, height: 22, borderRadius: 11,
     backgroundColor: 'rgba(0,0,0,0.55)',
     justifyContent: 'center', alignItems: 'center',
   },
   ```

10. **i18n** — `app/src/i18n/locales/ko.ts` 에 신규 key 추가:
    ```typescript
    a11y_video_preview: '영상 미리보기',
    ```
    (alphabetical 가까운 위치에 삽입. 기존 a11y_* 키들 근처에 두면 그룹 유지.)

### 주의사항
- VideoPlayer 컴포넌트 자체는 수정 금지 (Plan 04-04 산출물 — feed/detail/studio 3 모드 이미 완성).
- Double-tap like / 스와이프 dismiss / 리액션 팝업 등 기존 hero 동작은 image 아이템에만 적용 (video 는 nativeControls 우선). Test 11 는 "재생 가능" 이면 PASS, Test 13 이 native controls 검증.
- `gallery.length > 0` 가드는 그대로 유지 — 이제 videos-only post 도 조건 만족.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit 2>&1 | tee /tmp/tsc-task1-video.log && grep -q "VideoPlayer" src/screens/explore/PostDetailScreen.tsx && grep -q "HeroMediaItem" src/screens/explore/PostDetailScreen.tsx && grep -q "a11y_video_preview" src/i18n/locales/ko.ts</automated>
  </verify>
  <done>PostDetailScreen hero 와 fullscreen 이 images+videos 통합 배열을 렌더하며 video 아이템에 대해 VideoPlayer(mode='detail') 가 마운트된다. Thumbnail strip 에 play overlay 표시. TypeScript 타입체크 통과. i18n 키 1종 추가.</done>
</task>

<task type="auto">
  <name>Task 2: HomeScreen trending 그리드를 FlatList + viewport-aware VideoPlayer(mode='feed') 로 전환</name>
  <files>app/src/screens/home/HomeScreen.tsx</files>
  <action>
HomeScreen 의 trending 섹션 (line 336-398) 을 ScrollView 내 `<View style={styles.postGrid}>` + `.map()` 구조에서 `<FlatList numColumns={2}>` 로 전환하고 viewport-aware 영상 재생을 추가. featured 카드는 play overlay + thumbnail fallback 만 추가 (horizontal ScrollView 유지, autoplay 미적용 — 2개 이상 영상 동시 재생 비용 억제).

### 1. Imports
- 추가: `import { FlatList, ViewToken } from 'react-native';` (기존 `react-native` import line 에 병합)
- 추가: `import VideoPlayer from '../../components/common/VideoPlayer';`

### 2. Viewport tracking state (함수 상단, 기존 useState 블록 근처)
```typescript
const [visibleIndices, setVisibleIndices] = useState<Set<number>>(new Set());

const onViewableItemsChanged = useRef(({ viewableItems }: { viewableItems: ViewToken[] }) => {
  const indices = new Set<number>();
  viewableItems.forEach((vt) => {
    if (typeof vt.index === 'number') indices.add(vt.index);
  });
  setVisibleIndices(indices);
}).current;

const viewabilityConfig = useRef({ itemVisiblePercentThreshold: 60 }).current;
```
(`useRef` import 는 기존에 존재하지 않을 수 있으므로 react import 에 추가: `import React, { useState, useCallback, useMemo, useEffect, useRef } from 'react';`)

### 3. Featured horizontal section (line 195-225 의 map)
featured 각 카드에 hasVideo 로직 + thumbnail fallback:
```tsx
{featured.map((post) => {
  const previewUri = post.thumbnail_urls?.[0] ?? post.images[0];
  const hasVideo = (post.videos?.length ?? 0) > 0;
  return (
    <TouchableOpacity
      key={post.id}
      style={styles.featuredCard}
      activeOpacity={0.85}
      onPress={() => navigation.navigate('PostDetail', { postId: post.id })}
    >
      <View style={styles.featuredImageWrap}>
        {previewUri ? (
          <Image source={{ uri: previewUri }} style={styles.featuredImage} />
        ) : (
          <View style={[styles.featuredImage, { backgroundColor: colors.surface }]} />
        )}
        {hasVideo && (
          <View style={styles.featuredPlayOverlay}>
            <Ionicons name="play" size={16} color="#FFFFFF" />
          </View>
        )}
        <LinearGradient
          colors={['transparent', 'rgba(0,0,0,0.8)']}
          locations={[0.4, 1]}
          style={styles.featuredGradient}
        />
        <View style={styles.featuredTag}>
          <Text style={styles.featuredTagText}>📸 {t('home_featured_tag')}</Text>
        </View>
        <View style={styles.featuredBottom}>
          <Text style={styles.featuredPostName} numberOfLines={1}>{post.title}</Text>
          <View style={styles.featuredMetaRow}>
            <Text style={styles.featuredPhotographer}>@{post.photographer.display_name}</Text>
            <View style={styles.featuredLikes}>
              <Ionicons name="heart" size={10} color={colors.primary} />
              <Text style={styles.featuredLikesText}>{formatCount(post.like_count)}</Text>
            </View>
          </View>
        </View>
      </View>
    </TouchableOpacity>
  );
})}
```

### 4. Trending 그리드 → FlatList 전환 (line 336-398)
기존 `<FadeInView delay={100}><View style={styles.postGrid}>{trendingPosts.map(...)</View></FadeInView>` 를 다음으로 교체:

```tsx
<FadeInView delay={100}>
  <FlatList
    data={trendingPosts}
    keyExtractor={(item) => item.id}
    numColumns={2}
    scrollEnabled={false}
    columnWrapperStyle={{ gap: GRID_GAP, marginBottom: GRID_GAP }}
    contentContainerStyle={{ paddingHorizontal: GRID_PADDING }}
    onViewableItemsChanged={onViewableItemsChanged}
    viewabilityConfig={viewabilityConfig}
    renderItem={({ item: post, index }) => {
      const previewUri = post.thumbnail_urls?.[0] ?? post.images[0];
      const hasVideo = (post.videos?.length ?? 0) > 0;
      const videoUri = post.videos?.[0];
      const isVisible = visibleIndices.has(index);
      const td = KBO_TEAMS.find((t) => t.id === post.team_id);
      return (
        <PressableScale
          style={styles.postCard}
          scaleTo={0.97}
          onPress={() => navigation.navigate('PostDetail', { postId: post.id })}
        >
          <View style={styles.postImageWrap}>
            {hasVideo && videoUri ? (
              <VideoPlayer
                uri={videoUri}
                mode="feed"
                width={CARD_WIDTH}
                height={CARD_WIDTH * 1.25}
                isVisible={isVisible}
              />
            ) : previewUri ? (
              <Image source={{ uri: previewUri }} style={styles.postImage} />
            ) : (
              <View style={[styles.postImage, { backgroundColor: colors.surface }]} />
            )}
            <LinearGradient
              colors={['transparent', 'rgba(0,0,0,0.8)']}
              locations={[0.3, 1]}
              style={styles.postImageOverlay}
              pointerEvents="none"
            />
            <View style={styles.postTagRow}>
              <View style={[styles.teamTag, td && { borderColor: td.color }]}>
                <Text style={[styles.teamTagText, td && { color: td.color }]}>
                  {td?.shortName ?? post.team.name_ko}
                </Text>
              </View>
            </View>
            <View style={styles.postBottomInfo}>
              <View style={styles.engagementRow}>
                <Ionicons name="heart" size={10} color={colors.error} />
                <Text style={styles.engagementText}>{formatCount(post.like_count)}</Text>
                <Text style={styles.viewsText}>{t('home_views', { count: formatCount(post.view_count) })}</Text>
              </View>
              <Text style={styles.photographerName}>@{post.photographer.display_name}</Text>
            </View>
          </View>
          <View style={styles.postInfo}>
            <Text style={styles.postName} numberOfLines={2}>{post.title}</Text>
            <View style={styles.postFooter}>
              {post.player && (
                <View style={styles.playerBadge}>
                  <Text style={styles.playerBadgeText}>#{post.player.name_ko}</Text>
                </View>
              )}
              <View style={styles.formatBadge}>
                <Ionicons name={hasVideo ? 'videocam' : 'image'} size={10} color={colors.textSecondary} />
                <Text style={styles.formatText}>{hasVideo ? 'VIDEO' : 'PHOTO'}</Text>
              </View>
            </View>
          </View>
        </PressableScale>
      );
    }}
  />
</FadeInView>
```

중요 — 외부에 이미 `<ScrollView refreshControl=...>` 가 있어 본 FlatList 는 `scrollEnabled={false}` 로 렌더 목적만 사용. `onViewableItemsChanged` 는 FlatList 자체가 렌더링 대상 뷰를 계산하므로 부모 ScrollView 스크롤에서도 정상 작동 (FlatList 내부 viewport = 부모 viewport 와 동일).

Android 주의: `scrollEnabled={false}` + `onViewableItemsChanged` 조합이 Android 에서 viewability 이벤트를 트리거하지 않는 경우가 있다. 만약 실기기 QA 에서 Android autoplay 가 미동작 시 후속 gap 으로 다루되, 본 plan 에서는 iOS + Android emulator 기본 동작으로 구현 (isVisible=true fallback 은 mount 직후 useVideoPlayer 생성 단계에서 muted+loop 로 평가되므로 RED screen 은 발생하지 않음).

### 5. styles 추가
```typescript
featuredPlayOverlay: {
  position: 'absolute', top: 8, right: 8,
  width: 28, height: 28, borderRadius: 14,
  backgroundColor: 'rgba(0,0,0,0.55)',
  justifyContent: 'center', alignItems: 'center',
  zIndex: 2,
},
```
(기존 featuredTag 와 겹치지 않도록 top-right 에 배치. featuredTag 는 기존대로 bottom-left 계열이면 유지, 만약 top-right 였다면 featuredTag 를 top-left 로 옮기고 overlay 를 top-right 에 둔다 — 기존 스타일 확인 후 조정.)

`postGrid` 는 FlatList 가 대체했으므로 참조 없이 styles 에 남겨두어도 OK. 제거하지 않음.

### 6. `trendingPosts` useMemo 는 변경 없음. FlatList 는 trendingPosts 를 `data` 로 바로 소비.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit 2>&1 | tee /tmp/tsc-task2-video.log && grep -q "VideoPlayer" src/screens/home/HomeScreen.tsx && grep -q "onViewableItemsChanged" src/screens/home/HomeScreen.tsx && grep -q "thumbnail_urls" src/screens/home/HomeScreen.tsx</automated>
  </verify>
  <done>HomeScreen trending 그리드가 FlatList 로 전환되고 영상 포스트는 mode='feed' VideoPlayer 로 viewport-aware 재생된다. Featured 카드는 play overlay + thumbnail fallback. TypeScript 통과.</done>
</task>

<task type="auto">
  <name>Task 3: 나머지 피드 카드들 (AllPosts / FeaturedAll / PhotographerProfile / Archive) play overlay + thumbnail fallback 적용</name>
  <files>app/src/screens/home/AllPostsScreen.tsx, app/src/screens/home/FeaturedAllScreen.tsx, app/src/screens/photographer/PhotographerProfileScreen.tsx, app/src/screens/archive/ArchiveScreen.tsx</files>
  <action>
네 파일 모두 **영상 인라인 재생 없음** — play overlay + thumbnail fallback 만 적용한다 (과도한 player 인스턴스 비용 방지, 사용자는 해당 카드 탭 시 PostDetailScreen 으로 진입해 native controls 로 재생). StudioScreen.tsx:419-434 패턴을 모두 재사용.

### File 1: `app/src/screens/home/AllPostsScreen.tsx`

1. `renderPost` (line 55) 내부 `<Image source={{ uri: post.images[0] }} ...>` 를 대체:
   ```tsx
   const previewUri = post.thumbnail_urls?.[0] ?? post.images[0];
   const hasVideo = (post.videos?.length ?? 0) > 0;
   // ...
   <View style={styles.imageWrap}>
     {previewUri ? (
       <Image source={{ uri: previewUri }} style={styles.image} />
     ) : (
       <View style={[styles.image, { backgroundColor: colors.surface }]} />
     )}
     {hasVideo && (
       <View style={styles.videoPlayOverlay}>
         <Ionicons name="play" size={14} color="#FFFFFF" />
       </View>
     )}
     {/* 기존 LinearGradient + tagRow + bottomInfo 유지 */}
     ...
   </View>
   ```

2. styles 에 `videoPlayOverlay` 추가 (StudioScreen 패턴 복사):
   ```typescript
   videoPlayOverlay: {
     position: 'absolute', top: 8, right: 8,
     width: 28, height: 28, borderRadius: 14,
     backgroundColor: 'rgba(0,0,0,0.55)',
     justifyContent: 'center', alignItems: 'center',
     zIndex: 2,
   },
   ```

### File 2: `app/src/screens/home/FeaturedAllScreen.tsx`

1. `featured.map` 루프 내 (line 56-85) 동일 패턴 적용:
   ```tsx
   {featured.map((post) => {
     const previewUri = post.thumbnail_urls?.[0] ?? post.images[0];
     const hasVideo = (post.videos?.length ?? 0) > 0;
     return (
       <TouchableOpacity key={post.id} style={styles.card} ...>
         <View style={styles.imageWrap}>
           {previewUri ? (
             <Image source={{ uri: previewUri }} style={styles.image} />
           ) : (
             <View style={[styles.image, { backgroundColor: colors.surface }]} />
           )}
           {hasVideo && (
             <View style={styles.videoPlayOverlay}>
               <Ionicons name="play" size={14} color="#FFFFFF" />
             </View>
           )}
           {/* 기존 gradient + tag + bottom 유지 */}
           ...
         </View>
       </TouchableOpacity>
     );
   })}
   ```

2. styles 에 `videoPlayOverlay` 추가 (위와 동일).

### File 3: `app/src/screens/photographer/PhotographerProfileScreen.tsx`

1. `visiblePosts.map` (line 450) 내 `<Image source={{ uri: post.images[0] }} ...>` (line 457) 를 대체:
   ```tsx
   {visiblePosts.map((post) => {
     const previewUri = post.thumbnail_urls?.[0] ?? post.images[0];
     const hasVideo = (post.videos?.length ?? 0) > 0;
     return (
       <TouchableOpacity key={post.id} style={styles.postThumb} ...>
         {previewUri ? (
           <Image source={{ uri: previewUri }} style={styles.postThumbImage} />
         ) : (
           <View style={[styles.postThumbImage, { backgroundColor: colors.surface }]} />
         )}
         {hasVideo && (
           <View style={styles.videoPlayOverlay}>
             <Ionicons name="play" size={12} color="#FFFFFF" />
           </View>
         )}
         {post.images.length > 1 && !hasVideo && (
           <View style={styles.multiIcon}>
             <Ionicons name="copy-outline" size={12} color="#FFFFFF" />
           </View>
         )}
         {/* 기존 thumbOverlay + thumbStat + thumbPlayerTag 유지 */}
         ...
       </TouchableOpacity>
     );
   })}
   ```

2. styles 에 `videoPlayOverlay` 추가 (썸네일 작으므로 축소 버전):
   ```typescript
   videoPlayOverlay: {
     position: 'absolute', top: 6, right: 6,
     width: 22, height: 22, borderRadius: 11,
     backgroundColor: 'rgba(0,0,0,0.55)',
     justifyContent: 'center', alignItems: 'center',
     zIndex: 2,
   },
   ```

### File 4: `app/src/screens/archive/ArchiveScreen.tsx`

1. `renderPhotoGrid` 내 `<Image source={{ uri: post.images[0] }} ...>` (line 185) 를 대체:
   ```tsx
   {posts.map((post) => {
     if (!post) return null;
     const previewUri = post.thumbnail_urls?.[0] ?? post.images[0];
     const hasVideo = (post.videos?.length ?? 0) > 0;
     return (
       <TouchableOpacity key={post.id} style={styles.gridItem} ...>
         {previewUri ? (
           <Image source={{ uri: previewUri }} style={styles.gridImage} />
         ) : (
           <View style={[styles.gridImage, { backgroundColor: colors.surface }]} />
         )}
         {hasVideo && (
           <View style={styles.videoPlayOverlay}>
             <Ionicons name="play" size={12} color="#FFFFFF" />
           </View>
         )}
         {post.images.length > 1 && !hasVideo && (
           <View style={styles.gridMediaBadge}>
             <Ionicons name="copy-outline" size={10} color="#FFF" />
             <Text style={styles.gridMediaText}>{post.images.length}</Text>
           </View>
         )}
       </TouchableOpacity>
     );
   })}
   ```

2. styles 에 `videoPlayOverlay` 추가 (PhotographerProfileScreen 과 동일 축소 버전).

### 주의사항
- 네 파일 모두 VideoPlayer import 는 **불필요** (play overlay 만 추가 — 실제 재생은 PostDetailScreen 에서). 오직 HomeScreen trending 그리드만 인라인 재생 (Task 2).
- `colors.surface` 는 기존 theme import 에 이미 존재. 새 hex 도입 없음.
- `multiIcon` + `videoPlayOverlay` 둘 다 top-right 에 배치되므로, video 가 있으면 multiIcon 을 숨기는 조건을 추가 (위 코드에서 `!hasVideo` 가드 포함됨).

### `post.thumbnail_urls` 는 항상 배열이지만 비어있을 수 있음
- 타입: `thumbnail_urls: string[]` (optional chaining 은 undefined 대비 방어지만 런타임은 항상 array)
- `?.[0]` 이 undefined 일 때 `?? post.images[0]` fallback 으로 수렴
- `post.images[0]` 도 undefined 이면 (videos-only post) `previewUri` 가 undefined — 이 때 grey placeholder View 로 렌더.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit 2>&1 | tee /tmp/tsc-task3-video.log && grep -q "videoPlayOverlay" src/screens/home/AllPostsScreen.tsx && grep -q "videoPlayOverlay" src/screens/home/FeaturedAllScreen.tsx && grep -q "videoPlayOverlay" src/screens/photographer/PhotographerProfileScreen.tsx && grep -q "videoPlayOverlay" src/screens/archive/ArchiveScreen.tsx && grep -q "thumbnail_urls" src/screens/home/AllPostsScreen.tsx</automated>
  </verify>
  <done>네 개 피드/프로필/아카이브 스크린이 video-bearing post 에 대해 play overlay 를 표시하고 preview 는 thumbnail_urls→images 순서로 fallback 한다. 영상 전용 포스트가 깨지지 않는다. TypeScript 통과.</done>
</task>

</tasks>

<verification>
### Code-level 자동 검증 (실행 시 전체 통과 필수)
- `cd app && npx tsc --noEmit` — 타입 에러 0
- `grep -c "VideoPlayer" app/src/screens/explore/PostDetailScreen.tsx app/src/screens/home/HomeScreen.tsx app/src/screens/photographer/UploadPostScreen.tsx` — **3 파일 모두 ≥1** (기존 UploadPostScreen + 신규 2곳)
- `grep -l "hasVideo" app/src/screens/home/AllPostsScreen.tsx app/src/screens/home/FeaturedAllScreen.tsx app/src/screens/photographer/PhotographerProfileScreen.tsx app/src/screens/archive/ArchiveScreen.tsx app/src/screens/home/HomeScreen.tsx` — 5 파일 매치
- `grep -q "a11y_video_preview" app/src/i18n/locales/ko.ts` — 매치
- VideoPlayer.tsx 는 수정되지 않았음 (git diff 확인)

### Behavior-level 검증 (실기기 — UAT Tests 11/12/13 재실행)
- Test 11: 영상 업로드된 post 상세 진입 → hero carousel 의 video 슬라이드에서 VideoPlayer native controls 로 재생
- Test 12: 홈 trending 그리드 스크롤 → viewport 에 들어오는 영상 카드 autoplay (muted + loop), 벗어나면 pause
- Test 13: 상세 video hero — 재생/일시정지/seek bar 동작 확인
- 영상 전용 포스트 (images=[], videos=['url']): 피드 카드는 grey placeholder + play overlay, 상세는 video hero 로 정상 표시
- 이미지 전용 포스트: 기존 동작 보전 — 어떤 화면에서도 regressive 변화 없음 (double-tap like, pinch zoom, fullscreen 전부 유지)
</verification>

<success_criteria>
1. UAT Test 11 gap 이 `status: resolved` 로 flip 가능 — R2 업로드 영상이 상세 화면 hero 에서 VideoPlayer 로 재생됨
2. Test 12 (Feed Mode Autoplay) 가 blocked → pass-able 로 전환 (HomeScreen trending 그리드 viewport-aware autoplay)
3. Test 13 (Detail Mode Native Controls) 이 blocked → pass-able 로 전환
4. 영상 전용 포스트 (images=[], videos=['url']) 가 피드 카드 / 프로필 그리드 / 아카이브 / 상세 어디에서도 깨지지 않음
5. VideoPlayer 컴포넌트는 수정 없이 3곳에서 마운트 — feed mode(HomeScreen), detail mode(PostDetailScreen hero + fullscreen)
6. TypeScript strict 모드 통과 (no any)
7. 기존 이미지 전용 포스트 동작 (double-tap like, pinch zoom, fullscreen, thumbnail strip) 전부 보전
</success_criteria>

<output>
After completion, create `.planning/phases/04-photographer/04-09-SUMMARY.md`
</output>
